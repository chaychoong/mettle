# Task ledger (beads)

Lightweight, git-tracked, greppable task "beads" — no external tool dependency (see [ADR-0004](adr/0004-docs-and-task-system.md); we may adopt Steve Yegge's `bd`/beads tool later if the dependency graph warrants it).

**Bead format:** `mt-NNN` id · `status` · `rung/phase` · short title. Detail and dependencies below each. Statuses: `todo` `doing` `blocked` `done`. Reference beads by id from commits and ADRs.

**Legend:** ▢ todo · ◐ doing · ⛔ blocked · ✔ done

---

## Done (Pre-Rung-1)

- ✔ **mt-001** · P0 · Documentation & decision spine
  CLAUDE.md, docs index, STATE, ROADMAP, this ledger, ADRs 0001–0004, LIMITATIONS, Ledger scaffold.
- ✔ **mt-002** · P0 · Pin the conformance oracle (delegated → sonnet)
  Downloaded + verified Alloy 6.2.0 jar (SHA-256 pinned); proved headless verdict/count/`symmetryBreaking=0`/overflow-default/SAT4J, and found+documented a CLI bug (`-y`/`--ymmetry` is a no-op — use the `A4Options` API instead). → `docs/reference/alloy6-reference.md`; SHA/version folded into ADR-0002.
- ✔ **mt-003** · P0 · Draft steering rubrics (delegated → opus)
  STYLE.md + PORTING_RULES.md — tech-lead reviewed and accepted as binding rubrics.
- ✔ **mt-004** · P0 · Cargo workspace + `als-*` crate skeleton (delegated → sonnet)
  8 crates on the hand-designed DAG; binary crate = package `mettle` (fulfils the plan's `als-cli` role). CI green (build/fmt/clippy -D warnings/test), tech-lead re-verified. `Cargo.lock` committed.
- ✔ **mt-005** · P0 · Hand-designed core IR type skeleton (tech-lead-authored, NOT delegated)
  Done 2026-07-15: `Arena`/`ArenaId`/`define_id!` + `Span` in als-syntax; unified surface AST (temporal-first, spans required); three-sorted relational IR + `Universe`/`TupleSet`/`Bounds` in als-core; dependency-free `Var`/`Lit`/`Cnf`/`Solver` boundary in als-solve. Rationale: [ADR-0005](adr/0005-core-ir-type-skeleton.md).
- ✔ **mt-006** · P0 · Conformance harness (`als-conform`) v0 (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: `crates/als-conform/shim/OracleShim.java` drives the jar via the `A4Options` API (symmetry/noOverflow/solver always explicit; LEDGER-001 default); Rust side = typed outcomes, timeout-killed JVM per file, Net 0 expect-mining, deterministic scorecard (text+JSON), `conform` bin. 87/1129 enumeration facts pinned by integration tests (skip cleanly sans jar). Review fixes: shim source moved into the crate (was in git-ignored `oracle/`); unknown solver = hard error, no silent default.
- ✔ **mt-007** · P0 · Vendor corpora (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: alloytools-models (94 .als @ the jar's build commit), alloy4fun (Zenodo 10.5281/zenodo.17390557, CC-BY-4.0), portus-63 (63 models, licensing hot spot) vendored into git-ignored `corpus/`; kodkod investigated → no `.als`, not vendored. Provenance manifest committed: [reference/corpora.md](reference/corpora.md). Corpus files stay uncommitted until mt-008.
- ✔ **mt-008** · P0 · Resolve licensing posture (ADR)
  Done 2026-07-15, owner-decided → [ADR-0006](adr/0006-licensing-posture.md): mettle = **MPL-2.0** (root LICENSE + workspace manifest); stdlib `util/*.als` = **clean-room rewrite** (bead mt-015, never copy upstream text); corpora = local-only forever (git-ignored, reproducible via manifest + mt-009 script); jar stays ignored. PORTING_RULES legal-hygiene section updated per the ADR.
- ✔ **mt-009** · P0 · Reproducible corpus fetch script (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: `scripts/fetch-corpora.sh` (+ `corpora.sha256`, 192 files) reproduces `corpus/` byte-identically from the [reference/corpora.md](reference/corpora.md) pins in ~15s (alloy4fun optional via `--with-alloy4fun`); `--verify` re-checked independently: 192/192 pass, shellcheck clean. Bonus: surfaced and fixed an under-documented 4th upstream patch (`trace.als`) — manifest corrected.

## Done (Rung 1 — syntax) — owner touchpoint passed 2026-07-15
- ✔ **mt-010** · R1 · Lexer + spans (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: raw-token lexer per the pinned contract ([reference/alloy6-grammar.md](reference/alloy6-grammar.md) §1, [ADR-0007](adr/0007-rung1-lexer-parser-architecture.md)); typed `LexError` with caret-ready spans; **167/167 corpus files lex clean**. Review caught + jar-verified two divergences (number maximal-run rule: `1_000`/`0x123`/`0b12` illegal; string-follow class includes digits/quotes) — spec §1.5–1.6 corrected to match.
- ✔ **mt-011** · R1 · Parser + arena AST (delegated → opus, tech-lead reviewed)
  Done 2026-07-15: full surface grammar → arena AST; `cook.rs` (F1–F4) + `parser.rs` (recursive descent + Pratt, 21 levels, binder rule, min-BP-gated prefixes). **167/167 corpus parse rate.** Review caught 2 real bugs pre-merge (`;`-grouping semantics; prefix over-acceptance) + approved `Expect::Other(i32)`. Deferred: steps-scope build checks → resolve (LIMITATIONS).
- ✔ **mt-012** · R1 · Pretty-printer + parse→print→parse round-trip (delegated → opus, tech-lead reviewed)
  Done 2026-07-15: minimal-paren precedence-aware printer as `Display` (R9d) sharing one bp table with the parser (new `prec` module — no drift possible); span-free `dump` as round-trip witness; insta snapshots (U2, first dev-dep per P1); **corpus round-trip 167/167** (parse→print→reparse→dump-equal + print idempotence); `mettle parse <file> [--ast]` CLI (hand-rolled args; `file:line:col` errors to stderr per E3) = the Rung-1 human-testable build. Review trims: dropped gratuitous Vec clones, `prec` made pub(crate) (S4). No parser bugs surfaced.
- ✔ **mt-013** · R1 · Diagnostics (caret errors) + Alloy4Fun error-quality pass (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: rustc-style caret renderer in `crates/mettle/src/diagnostics.rs` (E3; multi-line/tab/EOF/UTF-8 edge cases unit-tested); alloy4fun differential pass (186k records → 150,891 unique) via new batch `ParseOnlyShim.java`. **2 real parser bugs found+fixed** w/ regression tests (closure-prefix binder exception; bare `disj`/`pred/totalOrder` over-acceptance). Zero jar-accepts+mettle-rejects; 99.79% exact position match; 2/1000 over-acceptance (binder composition) documented in LIMITATIONS → mt-014 bait. Evidence: [reference/alloy4fun-error-pass.md](reference/alloy4fun-error-pass.md).
- ✔ **mt-014** · R1 · Mutation fuzzer + binder-composition resolution (delegated → sonnet, tech-lead reviewed; binder rule independently re-verified against the jar 6/6, fuzzer determinism re-run, grammar-doc §3.1 corrected to the narrowed rule)
  Done 2026-07-15: zero-dep SplitMix64-seeded mutation harness (`tests/fuzz_mutations.rs`: byte/token/splice classes + deep-nesting/long-chain stressors; no-panic + sane-span + round-trip properties; default 4,248 mutants/~5s, `METTLE_FUZZ_ITERS` env override for offline runs, verified at 88,500 mutants/~127s). **Deep-nesting guard required** (measured, not assumed): unguarded `(`/`{`/binder-chain nesting SIGABRTs a debug build well within fuzz reach (worst-case ~212 levels safe on a 1 MiB thread) — `MAX_EXPR_DEPTH = 256` + new `ParseError::TooDeep`, verified safe to 100,000 adversarial levels; jar-probed as a deliberate, better-than-reference divergence. **Part 2 resolved:** ~220 jar probes mapped the exact binder-composition rule (one composition "hop", refreshed by `implies`, hard-blocked by comparisons/set-tests) → shared `crate::prec::child_binder_budget` threaded through both parser (`ParseError::BinderNeedsParens`) and printer (so the two can't drift); LIMITATIONS.md entry resolved. Fuzzer itself found and fixed one real bug (printer under-parenthesizing post-Part-2, since `needs_parens` only tracked `rightmost` not the new budget) and one documented-out-of-scope finding (printer/dumper recursion depth on long flat chains — candidate follow-up bead). 167/167 corpus preserved; full workspace gauntlet green. Evidence: [reference/fuzzing.md](reference/fuzzing.md). **Rung 1 complete → owner touchpoint.**

## Done (Rung 2 — names & types) — owner touchpoint passed 2026-07-16
Rung gauge: **same accept/reject decisions as the jar** on resolve/typecheck (ROADMAP rung 2). Home crate: `als-types` ("name resolution, sig hierarchy, and the relevance/type checker"). Filed 2026-07-15 after the Rung-1 owner touchpoint.

- ✔ **mt-016** · R2 · Pinned resolution & type-system contract (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: [reference/alloy6-resolution.md](reference/alloy6-resolution.md) pins the whole `resolveAll` behavior at commit `794226dd` — phase pipeline, module system (`open` search order, instance identity = filename+params, auto-aliasing), type system (union-of-products, **no `int`↔`Int` coercion in 6.2.0**, `ExprChoice` disambiguation ladder), the exact ~24-row reject taxonomy + warning catalog (**warnings never change the verdict** — the mt-020 gauge is binary), `util/*` interfaces (clean-room, signatures only), determinism notes. 48 probes, zero source↔jar divergences; tech lead independently re-verified 6 probes + the mode-1/no-ghost-sig entry-point claim against the sources. [ADR-0008](adr/0008-rung2-resolver-architecture.md) accepted (two-pass, typed-ID `ResolvedWorld`, render-free diagnostics, no `_` catch-alls in the taxonomy).
- ✔ **mt-017** · R2 · Module graph + `open` resolution (`als-types`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `FileTable`/`ModuleGraph` (typed-ID arenas), `ModuleLoader` abstraction (fs + injected-map), exact §2.1 search order incl. `.md` fallback + embedded-stdlib last, `computeModulePath` with the **prefix-cancel** reading (corpus-forced, then jar-verified on the mislocated zigbee modules — resolution-doc §9 residual closed), cycle detection, instance identity = (file, args) (probes 24/25), alias machinery + duplicate-alias reject (probe 26), private-open blocking, module-phase `ResolveError` variants. Review fixes: `seen_dollar` gate now scans **all** loaded files (reference threads its accumulator through every parse; root-only was a divergence); §9 note updated. 27 unit/integration tests + corpus load **167/167** (after stdlib wiring).
- ✔ **mt-015** · R2 · Clean-room `util/*` stdlib rewrite ([ADR-0006](adr/0006-licensing-posture.md)) (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: all 11 `util/*.als` written clean-room into `crates/als-types/stdlib/util/` (MPL-2.0, attested: author never opened corpus/upstream text) against the §7.1–§7.11 interface appendix (added mid-bead after the author's honest first-pass report exposed §7 underspecification; wrong guesses — `int2elem`/`elem2int` shapes, `util/relation` pred arities, `Seq.seqElems`, `util/time` macro family — all corrected). Parse + round-trip test (`tests/stdlib_parse.rs`); wired into `stdlib::MODULES` at merge. Body-level judgment calls (time macros, relation `complete`, rank arithmetic) recorded for mt-020 differential triage; `util/ordering` analyzer special-casing deferred to solve-time Ledger entries as planned.
- ✔ **mt-018** · R2 · Name resolution + type checker core (`als-types`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: full §1 phase pipeline over the mt-017 graph — builtins seed, sig hierarchy/cycles/subset sigs, enum desugar (+ synthetic `util/ordering`/`util/sequniv`/`util/integer` opens in the loader — the **jar auto-opens `util/integer` everywhere**, jar-probed twice), fields (implicit `this`, defined pass, name-clash), func/pred overload sets, facts/asserts/macros (20-unroll), commands + the two deferred scope checks; `Type` value type + fused bidirectional §4 checker (`resolve/expr.rs`) with the §4.4 candidate chain and **no int↔Int coercion**. All §5.1 rows have `ResolveError` variants. 37 probe-table unit tests + **corpus resolve 167/167 accept**; workspace green. Review: fused-pass deviation from ADR-0008 dec.4 + accept-lean ambiguity posture examined, jar-probed (mettle-accepts/jar-rejects on ambiguous 0-ary names confirmed), and **accepted as an explicit interim via [ADR-0009](adr/0009-fused-resolve-pass-accept-lean.md)**; divergences recorded in LIMITATIONS; mt-020 decides tightening.
- ✔ **mt-019** · R2 · `mettle check` CLI subcommand (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: `mettle check <file.als>` — load module graph → resolve/typecheck → rustc-style caret diagnostics via a label-parameterized renderer (`error:`/`warning:`, one code path); **multi-file aware** (span `FileId` → the right file's source/path from `graph.files`; load-phase fallbacks: disk re-read for `OpenedFileParse`, spanless one-liner when unrecoverable — never a caret into the wrong file). Warnings never fatal (§5.3); success prints `<path>: ok (N sigs, M funcs, K warnings)`. 11 spawn-the-binary tests + fixtures; 94/94 alloytools models exit 0; tech lead re-smoked good/submodule-error/missing-file paths by hand. Workspace green (266 tests).
- ✔ **mt-020** · R2 · Differential resolve/typecheck gauge vs the jar (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `resolve_gauge` bin (mettle batch driver + diff) + `ResolveGaugeShim.java` (real-path `parseEverything_fromFile` — the string shim spuriously fails multi-file models; equivalence-checked 0/3000 on single-file codes). **Scorecard: corpus 167/167 agree-ACCEPT; alloy4fun 150,891 codes → 0 jar-accepts/mettle-rejects (drop-in violations), 95.82% total agreement, 6,300 over-accepts (4.2%) measured + bucketed in LIMITATIONS.** 4 drop-in bugs found + fixed (subset-sig implicit-`this`; field-vs-`util/integer`-pred shadowing; call disambiguation by relevant type; higher-order macro), plus 4 over-acceptance closers (set-as-formula, formula-as-set, non-int comparison, non-binary `~`/`^`/`*`) — each with a jar-verified regression test. **ADR-0009 decision 3 fired and measured: tightening reverted** (28,402 false rejects vs ~1,478 gains — coarse types are the blocker, not choice logic); outcome recorded in the ADR; real fix filed as backlog bead mt-022. Evidence: [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md). **Rung 2 complete → owner touchpoint.**

## Done (Rung 2 extension — [ADR-0010](adr/0010-hundred-percent-before-signoff.md): ~100% similarity before the touchpoint)

- ✔ **mt-022** · R2 · Precise per-node relevant-type propagation (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: **root cause found** — the reference's `Type` keeps empty products *with their arity* (`NONE→…→NONE`); mettle collapsed them to the EMPTY sentinel, conflating "legal-but-empty" with "illegal" (exactly why ADR-0009's naive tightening false-rejected 28,402). `ty.rs` rewritten as a faithful behavioral port (verified vs sources @ `794226dd`); `expr.rs` gains bottom-up `infer`, a faithful `resolveHelper` pick on precise types with definite-vs-loose relevant positions, faithful weights/`applicable`/`@name` semantics. Every reverted tightening re-enabled and firing. **Gauge: agreement 95.82% → 98.36%, over-accepts 6,300 → 2,475, 0 jar-accepts/mettle-rejects throughout, corpus 167/167.** 6 new jar-verified regression tests; tech lead re-spot-checked illegal-join/legal-empty/ambiguous-name against the jar. Remainder root-caused → mt-025 (materialized typed tree; ~2,000 codes) + mt-026 (6 parser binder codes). Evidence: [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md) §9.
- ✔ **mt-025** · R2 · Materialized typed tree + full top-down relevant-type threading (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: the fused walk replaced by the reference's real structure — join-level `ExprChoice` materialization (`Context.process`, jar-instrumented to confirm), precise per-op top-down relevant slices (source-verified @`794226dd`), structural error-suppression (errors attach to the chosen path only); `Want`/`ambig`/`join_lenient`/loose-wants all removed. 6 real resolver bugs + 1 clean-room stdlib bug fixed. **Gauge: 98.36% → 99.79%** (over-accepts 2,475 → 320), **0 jar-accepts/mettle-rejects both directions, corpus 167/167 = 100%**; 8 jar-verified `_mt025` regression tests; ~45s mettle side. **Clean-room incident handled at review:** the agent read upstream ordering.als for its stdlib fix — honest file-exact accounting on challenge, line reverted, fix independently re-derived by a fresh clean-room agent (`~this/next`, different-but-equivalent formulation = evidence of independence), gauge re-verified identical; LESSONS updated. Residual 320 root-caused: ~112 compound-right-operand, ~94 deep left-of-join approximation, ~38 multiplicity flag, 6 parser codes (mt-026), ~10 tail. Evidence: [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md) §10.
- ✔ **mt-026** · R2 · Binder-composition rule vs the 6 jar-parse-reject codes (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: root cause — mt-014's rule let every ordinary prefix forward its ambient binder budget unchanged; the jar rejects a prefix-wrapped binder once an enclosing infix has spent its hop (`q and not some x: A | …`). 6 codes minimized + jar-verified, 460 fresh probes mapped the refined rule → new shared `prec::prefix_operand_budget` consumed by parser AND printer. Gauge: exactly the 6 flipped, nothing else moved — **over-accepts 320 → 314**, 0 false rejects, corpus round-trip + fuzzer green. Docs: fuzzing.md §2 (M2′), grammar §3.1, LIMITATIONS.
- ✔ **mt-023** · R2 · Warning parity with the jar ([LEDGER-002](../SEMANTICS_LEDGER.md) owner requirement; delegated → opus, tech-lead reviewed)
  Done 2026-07-16: **all 20 §5.2 warning classes implemented** on the mt-025 typed tree, firing conditions source-verified (incl. the `resolveClosure` reachability port); warning-capturing shim (additive) + `warn-diff` gauge matching by (class, line). **Parity: corpus 166/167 identical / 0 missing; alloy4fun 99.80% files identical, 192 missing (0.19% of files, root-caused: overload-collapse unused-var corner), 20 extra (temporal same-value suppression corner) — both directions' remainders individually root-caused, wrong-direction "fixes" measured and rejected.** `mettle check --strict` (warnings → exit 1) shipped with CLI tests; 16 jar-pinned warning regression tests. **Verdict byte-invariant** (tech lead re-ran the full gauge: 0 / 314 / 99.79% unchanged). Evidence: [reference/warning-parity.md](reference/warning-parity.md). **All ADR-0010 rung-gating beads done → owner touchpoint.**

- ✔ **mt-024** · harness · One-command conformance + speed benchmark (owner-requested; delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: `conform bench [<corpus-dir>]` — per-stage conformance (parse/resolve; solve slots in at Rung 3 without schema change) + honest speed (warm threaded mettle per stage; jar one-JVM batch with startup reported separately; size-spread cold sample; ratio only for the like-for-like pair, caveat printed). Text + `--json`, byte-stable except timings; `--skip-jar`. Tech lead ran it end-to-end: parse 167/167, resolve 167/167, mettle resolve ~4× jar batch total. Usage in `als-conform` module docs.

## Done (Rung 3 — relational core) — exit gauge green, owner touchpoint passed 2026-07-18

- ✔ **mt-028** · R3 · Pinned translation & solving contract (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: [reference/alloy6-translation.md](reference/alloy6-translation.md) pins scopes→universe→bounds (exact `Name$N` atom naming/order — resolves the numbering Ledger corner), the resolved-Expr→relational mapping + skolemization (`$cmd_var`), symmetry breaking (all Kodkod lex-leader; ADR-0002's SB-0 counting needs none of it), SAT boundary + outcome/enumeration semantics (87/1129 re-confirmed), self-verification story, and `util/ordering` exact-bounds pinning → LEDGER-004 (proposed). 9 jar probes; new oracle gotcha: **`expect 1` silently forces `symmetry 0`**. [ADR-0011](adr/0011-rung3-translation-solving-architecture.md) **Accepted** (owner approved writing our own CDCL solver, 2026-07-16).
- ✔ **mt-029** · R3 · Scopes → universe (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als-core::scope::compute_universe` — faithful `ScopeComputer` port (abstract-sum → overall → parent fixpoint, one/lone/some forcing, defaults, all scope errors as spanned `TranslateError`s), exact atom naming/order incl. module-qualified multi-instance names (`so/Ord$0`); ~30 jar probes; **universe spot-checks exactly match the jar** on real models. als-types widenings: `qualified_name`/`is_enum`, full `ResolvedCommand` (+`additional_exact` — the mt-035 seam). 27 §1-cited unit tests + corpus: **167/167 files, 564 root commands, 0 rejects, byte-identical twice**. Verdict gauge re-verified untouched (0/314/99.79%). Findings for mt-036: only root-module commands solve (jar `getAllCommands()` is root-only); String atoms deferred to Rung 4, steps expansion to Rung 6 (per ADR-0011).
- ✔ **mt-030** · R3 · Bounds builder (`als-core`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als_core::bounds_builder::compute_bounds` — faithful `BoundsComputer` port + the `A4Solution` builtin bounds: per-sig lower/upper atom bounds (bottom-up lowers, top-down floating-pool growth), leaf/`<Sig>_remainder`/abstract-union/subset relation allocation, field product bounds (incl. the `one`-sig owner-column strip), and quantified-over-atoms size/multiplicity/sibling-disjointness constraint formulas (never `#` cardinality — no bitwidth interaction); `Int`/`seq/Int` bound exact, `String` empty (Rung 4). Output includes the **denotation seam** for mt-031: one prebuilt `RelExprId` per sig/field. 18 jar probes (B1–B18, translation-ref §10.2) sharpened §1.4 → §1.4′: child growth takes the **whole** floating pool (cap is a formula), size formula iff `upper > scope`, disjointness unconditional, owner-strip is `one`-only. 22 jar-pinned goldens + 167/167 corpus (564 commands, ~14k relations, byte-identical twice). **Review found + fixed a real divergence (probe B19):** the reference's silent scope-raise (`n < Σ children-lower → n = lower`, exactness kept) was missing from mt-029's walk — `for exactly 2 P, exactly 3 C` panicked mettle's debug build where the jar solves SAT; ported into `scope.rs` with 3 regression tests; §1.2/§10.2 updated. Workspace gauntlet green (380 tests). `util/ordering` exact pinning deliberately excluded → mt-035.
- ✔ **mt-031** · R3 · IR lowering + goal assembly (`als-core` + additive `als-types` widening) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16, two parts. **Part A — choice recording:** `als_types::choice` — the mt-025 checker records every name/spine resolution (`NameChoice`/`SpineChoice`/`CallChoice`/`MacroChoice` with nested per-call-site tables) keyed by **(ModuleId, ExprId)** (parametric instances share a file), captured at the checker's own decision points, never re-derived; also exposed: module facts, sig appended facts, func bodies, field bounds, resolved command targets. **Verdict-neutrality proven**: alloy4fun gauge over all 150,891 codes byte-identical before/after (sha256 match, tech lead re-verified via `cmp`). **Part B — `als_core::lower::lower_command`** → `LoweredGoal` (goal + provenance-labeled conjuncts): full §2.1/§2.2 tables, §2.3 quantifier desugars (no skolemization per ADR-0011), int-literal promotion (`a.n = 1` → `= Int[1]`, jar-pinned), pred/fun inlining (recursion refused), synthesized field facts (mult/domain/defined — the §2.5 split pinned: mt-030 owns sig constraints, lower owns field facts, probes L1–L14 in translation-ref §10.3), `check` negation, `run`-pred existential params, disj guards/staged expansion. Deferred as **typed errors** (never wrong verdicts): temporal (Rung 6), String (Rung 4), `pred/totalOrder`/ordering (mt-035), nested-arrow field bounds + higher-order macros + misc (→ **mt-038**). 28 jar-quoted goldens + corpus: **167 files / 564 commands → 124 lower, 440 typed defers, 0 panics, deterministic**. Review fix: `a ; b` lowered as the pinned `a && after b` (was mis-pinned as `Until`; verdict-inert now, would have fossilized wrong into Rung 6). Gauntlet green (409 tests).
- ✔ **mt-032** · R3 · CDCL SAT solver (`als-solve`; [ADR-0011](adr/0011-rung3-translation-solving-architecture.md) owner decision) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als_solve::solver::CdclSolver` — hand-rolled, zero-dep (Cargo.lock untouched), deterministic by construction: two-watched-literal propagation, 1-UIP learning **with self-subsuming minimization**, non-chronological backjumping, Luby restarts (×100, no wall-clock), **integer VSIDS** (`u64` activities — bit-identical cross-platform, no floats) + phase saving, lowest-index tie-break; keep-all clause DB (sized against the 1129-re-solve SB-0 net; stable `ClauseRef`s). **Incremental enumeration seam for mt-033:** `new(&Cnf)`/`add_clause`/`solve` retaining learned clauses (sound: learned = resolvents of originals only) + `block(assignment, vars)`; `add_clause` re-picks watches among non-false lits so an already-falsified/unit blocking clause integrates correctly (the classic incremental bug, handled). One-shot `Cdcl` behind the `Solver` trait. **Correctness net (no jar needed):** brute-force reference + fixed-seed SplitMix64 fuzz — verdict parity, independent model check, **enumeration count = brute-force model count** (all-vars and random-subset projection), determinism per instance; PHP(≤8,7) UNSAT, colorings, parity; 1200 instances default (~0.85s), **30,000-instance release sweep re-run at review: green in 2.3s**. Perf smoke: PHP(8,7) 30ms, 200-var 3-SAT 28ms. Workspace 422 tests green.
- ✔ **mt-033** · R3 · CNF encoding + instance decode (`als-core`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16 — **mettle's first real SAT/UNSAT verdicts.** `als_core::{encode, solve}`: sparse boolean-matrix encoding (upper-bound tuples only, Tseitin circuit layer), ADR-0011 variable layout (primaries = every bounded relation's `upper∖lower` in `RelId`×lex order — unreferenced relations stay free, the jar's `r=r` equivalent; auxiliaries after), closure by iterated squaring, sequential-counter cardinality, two's-complement `int[·]` honoring the LEDGER-001 overflow switch (`SolveOptions`, forbid default); `Instance` decode + `InstanceEnumerator` (blocks primary vars via mt-032's seam). Int needs **measured** on the corpus (Const/Card/AtomToInt only — arithmetic/`sum` typed-defer to Rung 4). **Verification: 15 jar-verified verdict goldens all agree; SB-0 counts exact — probe T3 = 7, `#A=2` = 3, and `oracle/test1.als` show = 1129** (jar-pinned; the check command's 561-vs-464 count divergence is the documented no-skolemization effect — verdicts unaffected). Corpus end-to-end: 564 commands → 56 solved at the default 1s budget, 0 panics, deterministic. **Review found + fixed a second mt-029 scope bug (probe S1):** the reference's derivation rules run as *full passes* (re-run to exhaustion, restart on change); mettle's per-change restart let the abstract-difference rule back-derive an unscoped sibling to 0 — **11 wrong baseline UNSATs, all gone after the pass-discipline port** into `scope.rs` (+2 regression tests). Remaining baseline disagreement: **1** (`mediaAssets` `check PasteCut`, encoder under-constraint → mt-034 self-check/mt-037). Workspace 454 tests green.
- ✔ **mt-034** · R3 · Evaluator + self-check net (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als_core::eval` — direct three-sorted evaluation over a concrete `Instance` (translation-ref §6; the future REPL substrate): quantifier/comprehension grounding, closure fixpoint, integer slice with the §2.4 overflow semantics mirrored exactly (`goal ∧ ¬overflow` accept-set, so encoder and evaluator agree). Self-check wired into `solve_goal`/enumeration (`debug_assert!` every SAT instance; `self_check` checked mode names the failing conjunct via mt-031 provenance). **Strongest net: encoder↔evaluator differential — 15 models brute-force-enumerated through the evaluator, counts equal the solver's SB-0 enumeration on every one** (two independent semantics implementations agreeing). 5 corruption tests name the right conjunct; corpus sweep self-checks all solved SAT instances, 0 failures. **Headline: the mediaAssets baseline disagreement root-caused** — self-check *passes* → under-constrained goal, not encoder: **field-group `disj` is dropped** (`ResolvedField` never records it, no disjointness fact synthesized; minimal jar-verified repro; and the command is `[3] PasteNotAffectHidden` — earlier notes' "PasteCut" corrected). Fix needs an als-types widening → assigned to **mt-038** (gate before mt-037); tripwire test `field_disj_dropped_known_gap`. Workspace 482 tests green.
- ✔ **mt-035** · R3 · `util/ordering` exact bounds (LEDGER-004 **approved** 2026-07-16 — the amended two-part rule) (impl checkpointed by tech lead; verification delegated → opus, tech-lead reviewed)
  Done 2026-07-17, two sessions (impl OOM'd mid-verification — mt-039's harness leak, not the WIP; verification completed after mt-039). Per the approved rule: part (a) `exactly`-param exact scopes (general `additionalExactScopes` mechanism) + the `ResolvedWorld::ordering` seam; part (b) `pin_ordering` — `first`/`next` rebound to exact constants for childless/enum ordered sigs only; partition-choice case governed by the hand-built `pred/totalOrder` formula (count = n!); exact-remainder pinning (T14a) + exact-abstract-parent size witness (T14d); the ordering-name recording gap closed via a recording-only closure re-resolve (alloy4fun gauge byte-identity vs `9167036` verified — sha256 match, covers mt-039's join-base fix too). **10 jar-pinned probe-matrix tests** (pin-engages: T10/T12/T13/T16/T19; no-pin partition-choice: T14a/b/c/e counts 42/6/6/6 + T15=16, several freshly jar-probed at sym0). **All 8 ordering baseline disagreements resolved** — root cause was NOT the ordering surface but a pre-existing higher-order-quantifier mis-lowering (multiplicity-marked arrow bounds bound per-tuple; now typed defers → mt-038) + a budget artifact; corpus baseline overlap **114 agree / 1 disagree** (the known mt-038 field-`disj`). T14d-family SB-0 count divergence (verdict-neutral jar labeling-count artifact) documented in LIMITATIONS. Workspace gauntlet + clippy green; sweep 0 panics / 0 self-check failures.
- ✔ **mt-039** · R3 · Deterministic solve/encode effort budgets + corpus-solve harness hardening (unplanned; incident 2026-07-17) (impl tech-lead + completion → sonnet, resolver fix → opus, tech-lead reviewed)
  Done 2026-07-17. Final sweep at smoke defaults (500 conflicts / 1M effort units, env-scalable): 564 commands → 140 solved (86 SAT / 54 UNSAT), 88 too_large, 37 over_budget, 292 lower_defer, 7 encode_defer, **0 panics, 0 self-check failures**, deterministic; 2:49 wall, 155 MB peak (was: OOM'd the box). Full-depth sweep (2k/5M): 173 solved, 114 agree / 9 disagree — 1 known (mediaAssets → mt-038), **8 new all-ordering (→ mt-035 verification)**. Workspace gauntlet + clippy green.
  **Incident:** `tests/solve_corpus.rs` spawned a worker per command and abandoned it on `recv_timeout` — abandoned solvers kept grounding/learning forever (keep-all DB, no caps); with mt-035's WIP unlocking ~270 ordering commands, leaked threads accumulated and OOM'd the box (twice, killing the tmux session). Worse: a worker *panic* dropped the channel and was silently bucketed as `over_budget` — mt-033's "0 panics" never covered the encode/solve phase.
  **Fix (all deterministic, STYLE D1):** `CdclSolver::solve_within(conflict_limit)` (budget-exhausted ⇒ `None`, solver stays usable, effort actually stops); `SolveOptions::{conflict_budget, encode_budget}` + `SolveVerdict::Unknown`; encoder effort meter (gate requests incl. constant-folded, join pair-scans, clauses) failing typed `TranslateError::CapacityExceeded`; corpus test rewritten inline — no threads, env-scalable budgets, panics surface.
  **Surfaced + fixed (latent mt-031/033 bugs the swallowed panics hid):** quantifier vars minted arity-1 regardless of bound (`all R, r: univ->univ` panicked the encoder; now bound-arity, per-tuple = the jar's first-order reading, closure.als[0] jar-SAT); explicit `set`/`some`/`lone` quant-decl markers were silently stripped (wrong-verdict class → now typed HO defers); higher-arity run-pred params were per-tuple-bound (jar binds them as free skolem relations → typed defer, free-relation params filed under mt-038).
- ✔ **mt-036** · R3 · `mettle exec` CLI — the Rung-3 human-testable build (impl → sonnet, tech-lead spec/review/merge; commit `7d3a10d`)
  Done 2026-07-17. `mettle exec <file.als> [--command <name|index>] [--allow-overflow] [--conflicts N] [--encode-budget N]` drives every root-module command through the full pipeline and prints verdicts — `SAT`/`UNSAT (no instance)` for `run`, `COUNTEREXAMPLE`/`VALID (no counterexample)` for `check` — plus the decoded instance (sigs/fields/skolems in `RelId` order, Alloy arrow tuple syntax), `expect 0|1` checking (`ok`/`MISMATCH`), and typed `CANNOT EXECUTE: <reason>` lines for honest defers. **Named `exec` after the reference jar's own CLI verb** (the bead's working title "mettle run" would confusingly execute `check` commands; the Rung-2 `mettle check` static checker is untouched). Budgets default *unlimited* (interactive tool; the deterministic smoke budgets belong to the test harness). Exit 0 clean / 1 mismatch-defer-unknown-or-diagnostic / 2 usage / 141 pipe. 12 integration tests (`tests/exec.rs`); deterministic byte-identical output pinned; libraries untouched (E3 held — all rendering in the CLI crate; assert names recovered display-only from the AST). Smoke-verified on farmer.als (8-state ordering SAT instance + both expects ok), closure.als (counterexample), mediaAssets (VALID/COUNTEREXAMPLE/UNKNOWN at a bound), trash.als (temporal defer, exit 1). **→ owner touchpoint.**
- ✔ **mt-037** · R3 · Differential solve gauge + SB-0 counting net — the Rung-3 exit (impl → opus, docs pass → sonnet, tech-lead spec/review/triage/fixes)
  Done 2026-07-18. **Tool:** `solve-gauge` bin in `als-conform` (`crates/als-conform/src/solve_gauge/{mod,baseline,detect}.rs`, `src/bin/solve_gauge.rs`, `tests/solve_gauge_integration.rs`; `als-core` added as a crate dependency). Stage 1 runs the full mettle pipeline over every root command of both corpora and diffs verdicts against every `baselines/*-verdict.json`, partitioning every command into exactly one typed bucket (partition asserted). Stage 2 (`--count`) checks SB-0 instance-count parity against the jar, with typed skips for the documented divergence families (fo-skolem, ho-skolem, ordered-abstract partition, caps/timeouts). Deterministic text+JSON report, live progress via a render-free callback. Golden smoke: `oracle/test1.als` `run show` count_match 1129=1129, `check NoEmpty` skip_fo_skolem. **Defaults re-tuned after measurement:** conflicts 5000 / encode-budget 4,000,000 / primary-var-cap 20,000 — the originally spec'd 200k/50M defaults let single huge commands grind past 10h CPU; flags scale up for deeper runs.
  **Fresh jar baseline:** `baselines/portus-63-verdict.json` (158 commands / 63 files, 45/48 expect-matches, 3 stale-expect mismatches — vendored dijkstra/peterson copies — 10 file timeouts at 60s). Comparison set now **392 cached jar verdicts** (`baselines/README.md` already reflects this).
  **Three wrong-verdict conformance bugs found by the gauge, all fixed + regression-tested:**
  1. **Non-zero `String` scope silently solved against an empty String sig** (`fm2cfs.als`, 6 wrong UNSATs): the jar fills a non-zero String scope with the literal atoms plus synthetic padding atoms (recorded here as `unused%d` at the time; **mt-043 correction: the padding atoms are actually `"String%d"`** — `unused%d` is instance-display renaming, a different mechanism); mettle minted none and solved anyway. Fix: `als-core::scope` typed-defers (`TranslateError::StringUnsupported`) any `exactly N String` with N>0; `exactly 0 String` is unaffected. Tests: `tests/scope.rs` (`nonzero_string_scope_defers_typed`, `zero_string_scope_stays_solvable`).
  2. **Multiplicity-marked arrow on the RHS of `in` silently stripped to a plain product** (`hotel2.als[0]` wrong SAT: `Room<:keys in Room lone-> Key` lost its `lone`). Fix: routed through the jar-verified `arrow_value_constraint` conjuncts (membership + per-column mult quantifiers); a marked arrow anywhere *else* in an expression is now a typed `LoweringUnsupported` defer, never a silent strip, via a new `lower_rel_stripped` helper for the two callers that legitimately strip (membership products, skolem uppers). Tests in `tests/solve.rs`: `in_mult_arrow_rhs_enforces_columns`, `mult_arrow_outside_in_rhs_defers_typed`.
  3. **Clean-room stdlib `util/ordering` had `min` and `max` bodies swapped** (`min` returned the maximum) — surfaced because fixing bug 2 exposed `hotel1.als[0]`: mettle's old SAT agreement there was a *coincidence*, a bogus counterexample exploiting bug 2; with bug 2 fixed, mettle went wrong-UNSAT because `nextKey`'s `min` walked the ordering backwards and excluded the book's genuine counterexample. Fix: two-line body swap in `crates/als-types/stdlib/util/ordering.als` (`min[es] = es - es.^next`, `max[es] = es - es.^prev`), direction pinned by black-box jar differential probes (clean-room posture holds — the fix swaps our own existing text, never upstream's). Jar-pinned regression `ordering_min_max_pinned` in `tests/solve.rs` (three verdicts jar-verified). **alloy4fun byte-identity re-verified**: resolve-gauge over all 150,891 codes, sha256 identical before/after.
  **One count divergence root-caused and filed as mt-041** (leave that bead): `models/util/ordering.als` run as its own root module — the jar applies its exact-bounds optimization by recognizing the module directly (count 1), mettle's pinning keys on *opening* the module (counts n! = 2/6/24). Verdict-neutral.
  **Final gauge (2026-07-18, defaults 5000 conflicts / 4M encode-budget / 20k var cap):** 564 commands → verdict buckets **agree_sat 104, agree_unsat 83, DISAGREE 0, jar_nonverdict 2**; typed defers **capacity 71, encode 14, lower:higher_order 4, lower:lowering 217, lower:temporal 22, over_budget 37, scope 10** (the String defers); **0 panics, 0 self-check failures**. Counting net: count_match 15, COUNT_MISMATCH 3 (all the mt-041 family), skip_fo_skolem 55, skip_ho_skolem 7, skip_jar_timeout 1, skip_mettle_cap 23. **Baseline overlap: 187 agree / 0 disagree over 392 jar verdicts.** Run it: `cargo build --release -p als-conform && ./target/release/solve-gauge [--count]`. **Also filed this session:** mt-042 (machine portability, owner-directed). **Rung 3 exit gauge green → owner touchpoint.**
- ✔ **mt-038** · R3 · Close the mt-031 lowering defers + the field-`disj` wrong verdict (delegated four waves: disj → opus, nested arrows → sonnet, HO skolemization → opus, small buckets → sonnet; tech-lead specs/review/merge + the post-colon-`disj` negative-space guard)
  Done 2026-07-17, four commits (`f254979`, `b7cd70a`, `48d936e`, `2a6e950` + docs). **(1) Field-group `disj` synthesized** (the wrong-verdict class): `ResolvedSig::field_disj_groups` widening + the jar's staged `no(f0&f1) ∧ no((f0+f1)&f2)…` over full field relations (probes p1–p7, §10.3 L15–L19; `var` groups `always`-wrapped → temporal defer); tripwire flipped to `assert_unsat`; **`mediaAssets.als[3]` cleared — 0 baseline disagreements**. **(2) Nested multiplicity arrows** lower at any depth via the recursive per-column rule (probes n1–n7, L20–L26), factored as `arrow_value_constraint` over any relation value. **(3) HO skolemization** (§2.3/§10.6, T9a–T9g): polarity-threaded (`Pol`), HO decls + relational run-pred params become free relations (lower `{}` / sound `abstract_upper`) bound into augmented `Bounds` — encoder/enumeration/decode/self-check untouched; non-skolemizable HO → typed `HigherOrder` with the jar's exact error text; `ringlead`/`firewire` now lower. **(4) Small buckets:** int-sorted exprs in relation position cast via `IntToAtom` (jar probe: `+` stays relational union, §4.5); explicit-receiver `this` read from `args[0]` per `CallChoice::implicit_this` — a **latent wrong-verdict class** (args shifted left) fixed. **Plus:** post-colon `disj` (`f: disj e`) was a silent drop → now a typed defer everywhere (`bound_disj_unpinned`, zero corpus incidence, → mt-040). Final: corpus **321/564 lower** (was 124 at mt-031), 243 typed defers (root-caused per bucket), sweep **104 agree / 0 disagree**, 0 panics, 0 self-check failures, deterministic; alloy4fun gauge **byte-identical** over 150,891 codes for both als-types widenings (sha256 match vs `c57002d`). Remaining defers reclassified → mt-040 (resolver recording gaps), Rung 4 (integer-ordering builtins 39, seq), Rung 6 (temporal 22).

## Done (Rung 5 remainder — "it feels like a real tool"; all ✔ 2026-07-27 — [ADR-0014](adr/0014-rung5-repl-slice-before-rung6.md) sequence, architecture in [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md); next milestone = the combined feature-complete review)

- ✔ **mt-071** · R5 · **Instance-XML writer: jar-shape-exact `A4Solution.writeXML` port in `als-instance` + `mettle exec --xml`** ([ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Decision 1; delegated → opus, tech-lead reviewed with load-bearing differential re-runs)
  **Done 2026-07-27.** `als-instance` grew `xml.rs` (`write_instance_xml(&mut Ir, &XmlRequest) -> Result<String, TranslateError>`, `XmlSolution::{Static, Trace}` — the library seam mt-072's serve calls directly), implementing [reference/alloy6-instance-xml.md](reference/alloy6-instance-xml.md) in full: lazy touch-order IDs (`BTreeMap`-keyed, rebuilt per block like the reference's per-state ctor), fields interleaved after their owning `</sig>`, the **always-on `macros` mechanism** (`m<i>` fun-skolems; past-depth reachable funs ⇒ `tracelength + extra·(tracelength − loopState)` physical `<instance>` blocks), `String` atoms + pinned escaping, subset sigs/enums/both skolem namings, `<source>` embedding, `looplength` loop encoding. CLI: `mettle exec <file> --xml <PATH>` — single-command (multi-command files need `--command`), exclusive with `--repl`/`--eval`, prints the normal verdict then writes; **no instance ⇒ loud typed failure, no file** (mirrors the jar's writer throw). Zero new dependencies; determinism by construction (no hash iteration anywhere). **Verification: 13 jar-free shape tests + 10 goldens; differential round-trip 18/18 accepted by the jar's own `A4SolutionReader.read`** via the mt-070 XmlProbe harness — incl. the decisive 5-block past-depth case (reader re-derives `traceLength=5 loopState=3`, byte-consistent with the jar's own output) and a 6-block enum/temporal corpus trace (`leader_events#1`); evidence `scratchpad/probe/mt071/NOTES.md`. **Eight under-pinned doc corners closed from bytecode and folded back into the authority doc** (union-typed `<types>` via `Type.fold()` + sibling-collapse, writer section order, `command=` label reconstruction closing §1's flagged gap, macro-skolem `private="yes"`, `hasNoTuple` early-return, generalized escaping marked INFERRED, the never-firing `writeExpr` refinement loop knowingly omitted, `builddate` never read back). **LEDGER-013** (`approved`, Option B): ordinary-skolem `<types>` columns derived from the solver bound (least-common-prim-ancestor over-approximation — mettle's IR retains no skolem `Type`); reader-accepted, scorecard-invisible. Cosmetic divergences (m`<i>` index order, `<source filename=>` spellings) → LIMITATIONS. One additive als-types widening (`ResolvedFunc::qualified_name`); stage-1 sweep unchanged, full gauntlet green.
- ✔ **mt-072** · R5 · **`mettle serve`: the provider backend — Sterling wire protocol, evaluator hook, static enumeration verbs** ([ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Decision 2 as resolved + its mt-072 Amendments; delegated → opus, tech-lead reviewed incl. a dependency-trim send-back)
  **Done 2026-07-27.** One Rust process, `127.0.0.1` only, default port 4030: hand-rolled HTTP GET for the embedded assets (mt-075 stub page until the real frontend lands) + WS provider on the same port routed by the `Upgrade` header (so an external Sterling pointed at `ws://localhost:<port>` upgrades for free), speaking the pinned protocol — `data` (mt-071 `write_instance_xml`, byte-identical to `--xml`), `eval` (`{id,datumId,expression}`→`{id,result}` through the REPL's own `eval_line`; byte-identical to `--eval`, `:state N` works in the pane; **stale `datumId` refused by name** — deliberate divergence from Forge's answer-about-the-wrong-instance), `click` (static `next` via `InstanceEnumerator`; buttons vanish when exhausted; the four temporal verbs `next-trace`/`next-config`/`new-init`/`new-fork` + temporal `next` = typed `not-yet-supported` defers naming mt-076, temporal sessions offer **no buttons**), `meta`, literal `ping`/`pong`. **One pinned-shape extension, ADR-amended: a fifth outgoing type `error {code,message}`** (the protocol has no provider-refusal channel; verified ignored-safe by external Sterling's dispatcher). Seam: `als_sterling::ServeSession` trait (protocol testable without a solver); `mettle::serve::session::{StaticSession, TemporalSession}` implement it. CLI mirrors `--xml`'s conventions (single command, loud typed no-instance failure). **Deps (first runtime network dep, tech-lead decided + trimmed):** `tungstenite` **`default-features = false`** (frames only — the Sec-WebSocket-Accept echo is hand-rolled SHA-1+base64 in `handshake.rs`, pinned by 4 FIPS 180-4 + 8 RFC 4648 + the RFC 6455 §1.3 vectors) + `serde`/`serde_json` into the shipped binary; no async runtime, no HTTP framework, thread-per-connection; shipped tree 9→25 external crates (the trim dropped 11). Additive core widenings: `Arena`/`Ir` gain `Clone` (each appending consumer gets its own). **Tests: 18 als-sterling unit + 10 real-socket wire + 11 spawn-the-binary serve integration** (data/eval byte-parity vs exec, enumeration advance+exhaust, malformed-frame robustness ×11, two concurrent clients, temporal defers); workspace 944 pass. Full gauntlet + `cargo doc` clean; stage-1 sweep untouched (extraction-only exec/repl changes).
- ✔ **mt-075** · R5 · **The first-party frontend: graph + table views, trace stepper, evaluator pane, the modern design pass** ([ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Resolution 1 — owner-decided own frontend, browser-delivered; three slices, all landed 2026-07-27, one warm opus delegate throughout, tech-lead reviewed each incl. screenshot eyeballs)
  **Slice 3 (the design pass) done 2026-07-27.** Design thesis: **an instrument readout, not a web app** — silkscreened chrome band (10px wide-tracked uppercase mono zones: MODEL │ COMMAND │ ● CONNECTED), the semantic typography rule (**the solver speaks monospace, mettle speaks prose**), and the signature element: the **trace rail**, a tick scale with an amber arc drawn from the last state back to the loop target (closed curl for self-loops) — the lasso is drawn, not implied. Light = cool enamel paper; dark = tuned not inverted (separation by line, not glow); colour has three jobs only (teal = live/selected, amber = loops/refusals, per-sig hues inside the graph, retuned per theme); wordmark = mono + 3px teal rule, no drawn logo; one 4px spacing scale. Slice-2 gaps closed: **label lanes** in the pure layer (estimated 10px-mono metrics, never DOM-measured — determinism preserved, module-doc'd), **legend as key + control** (hover/click lights a sig), **subtractive focus** (hover/pin dims everything else; 14px hit twins on edges; Escape clears). Motion: 140ms, three uses, `prefers-reduced-motion` verified via CDP (durations collapse to 1e-05s). Empty/edge states unified (`.blank` component; exhaustion = a standing **bulletin band**, not a toast; refused clicks re-request `data` so retired buttons actually vanish). No webfont (justified: ~95% of the page is mono data; system stacks only). Gauntlet green, determinism script 5/5, offline test intact, zero new deps, servers cleaned up.
  **Slice 2 done 2026-07-27** (same warm opus delegate, tech-lead reviewed incl. screenshot + determinism-script re-runs): **the graph view is live and default** — `layout.js` (pure, 579 lines: atom nodes/tuple edges with middle-atom labels, DFS back-edge marking + longest-path layering, per-rank bend points so crossing reduction sees long edges, 4 barycenter sweeps with total-comparator ties, 4 distinguishable edge shapes incl. dashed back-edge bows and self-loops, label dealing) + `graph.js` (SVG + viewBox pan/zoom). **Determinism by construction** (document-order iteration, label-keyed never-iterated Map, 2dp boundary rounding — the guarantee is about the SVG *text*) checked three ways: the dev-only `tests/frontend/layout-determinism.mjs` (5/5 incl. permutation-sensitivity + stability), CDP byte-identical `outerHTML` after state/view round-trips, `node --check`. Witness marker corrected to the schema's `m<i>`-vs-numeric split (macro skolems draw faded, not "witness"). Offline test keeps its teeth (SVG xmlns carve-out documented). 2 browser-caught bugs fixed (CSS custom-property scoping; tall-drawing squeeze). Gauntlet green, zero new deps, smoke servers cleaned up.
  **Slice 1 done 2026-07-27** (delegated → opus, tech-lead reviewed incl. screenshot eyeball; browser-smoked by the delegate via headless Chrome, 2 real UI bugs found+fixed that way). **Zero-toolchain frontend** (tech-lead decision: hand-written ES modules + CSS, `include_str!`-embedded `ASSETS` table, no npm/bundler/CDN — an offline guarantee unit test asserts no off-origin reference in any asset): app shell + WS client with reconnect and toast-rendered `error` replies; **table view** (per-state sig/field/skolem cards, writer-order, `var` badges, builtins/private hidden behind a toggle); **trace stepper** (client-side over the multi-`<instance>` XML, first-`tracelength` blocks only per the §7 macro mechanism, loop chip marked, `normalizeState` = the server's clamp/wrap rule reimplemented so both agree); **evaluator pane** (history, `:state N` two-way sync with the stepper); server-driven buttons that retire/disable honestly. **Protocol amendment (d), ADR-0016 + sterling.md §2.3:** `click` gains optional `state` — `new-fork` forks at the client's displayed state (out-of-range = typed `state-out-of-range` refusal, never a guess), absent = mt-072's evaluator-pane fallback (external-Sterling compat). `instance.js` isolates the pure XML→model + loop math. Tests: 4 frontend-asset unit + wire/serve extensions (fork-at-sent-state incl. fallback pinning; app-shell + 5 assets served with right Content-Types). Gauntlet green, no new Rust deps. **Slice 2+: graph view (`meta` still advertises it), the distinctive design pass; deferred consciously: projection, persistence, transitive extents, read-side `backloop`, JS test harness (pure logic isolated for it).**
  Speaks the mt-072 protocol as its first-party client; consumes multi-`<instance>` temporal XML natively (per-state stepping with the loop marked, mirroring exec's pinned rendering); evaluator pane = the `eval` verb; deliberate visual design (typography/layout/dark mode), not templated defaults. A later Tauri desktop shell stays possible and additive, explicitly out of v1.
- ✔ **mt-076** · R5 · **Temporal trace enumeration: lasso `next()`/fork-family parity + temporal counting** ([ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Resolution 3; delegated → opus, probes-first per the bead cut; tech-lead reviewed, LEDGER-014 authored at review)
  **Done 2026-07-27.** **Probe wave first** (`scratchpad/probe/mt076/NOTES.md`, cells P-076-0..7, folded into [reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(g)/§(i)): the `fork(-1)` failure-mode "divergence" is a **rule, not discretion** (discriminator = free static primaries; both wave-2 observations explained); `fork(p≥0)` never moves the trace length and constrains state `p` itself; `fork(-2)` ≡ `fork(-3)` byte-for-byte; the duplicate unit is two-level (raw `(states, loop)` within a length, infinite trace across lengths); and the headline **correction: the jar's plain `next()` never leaves its first static configuration** (P-076-5, proven at symmetry 0 — refutes §(i)'s implied counting rule and closes the old MinLen T-17 anomaly). **Implementation:** `als_core::temporal_enum::TraceEnumerator` (`advance(TraceStep) -> TraceAdvance`, owns its `Ir` clone, one live stage per length; blocking over primaries **plus the lasso selector**, config held by unit-fixing static-relation primaries — **skolems excluded**: mettle skolemizes under temporal where the jar doesn't, a real config-lock bug caught and fixed mid-bead); typed budget/cap exhaustion reused, nothing hangs. **Counting: `skip_temporal_trace` retired** — fresh before/after nets at both settings: SB-0 `count_match` 53→56, SB-20 77→79, **`COUNT_MISMATCH` 0→0 both**, new typed `skip_temporal_config` (0/3) for the by-construction-unparityable many-config disagreements per **LEDGER-014** (temporal counts are configuration-relative; P-076-7 demonstrated it live on `leader.als[3]` — jar counts a three-node ring to the 10001 cap, mettle counts the empty model's single trace; three gauge policies measured, compare-but-soften shipped). leader.als's T-26 real jar baseline is now a live `count_match`. **Serve: all five temporal click verbs live** (`next`≡`next-trace` per P-076-3, `next-config`, `new-init`=fork(0), `new-fork`=fork(displayed+1) reading the display state from the evaluator pane — the reference's own arrangement; a protocol payload for a stepper is flagged to mt-075, not invented). One conservatively-chosen unprobed corner documented in code + doc: `NextPath` after a `Fork` stays inside the fork's length. exec/REPL surfacing skipped (no static-command enumeration surface exists either — symmetric). **Tests: 19 probe-cited enum conformance + serve rewrite (12) + the leader baseline gauge test** (slowest in the workspace, ~2–4 min debug). Gauntlet + `cargo doc` clean; **stage-1 sweep byte-identical before/after** (DISAGREE 0, 0 panics, 0 self-check failures).
- ✔ **mt-073** · R5 · **Release packaging: cargo-dist spine — GitHub Releases (4 native targets), shell installer, Homebrew tap, artifact attestations** ([ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Decision 4; delegated → sonnet, tech-lead reviewed; version gate decided in this bead's spec)
  **Done 2026-07-27.** **Version gate (tech-lead spec decision):** workspace `0.0.0 → 0.1.0` — the feature-complete candidate; pre-1.0 semver from here (minor = feature milestones, patch = fix-only), **`1.0.0` reserved for the drop-in bar** (blessed combined review + scorecard posture). Also fixed en route: `Cargo.toml`'s `repository` pointed at a nonexistent `mettle-lang/mettle` — load-bearing, cargo-dist derives installer/release URLs from it. **Lineage reversal vs the ADR's expectation:** `astral-sh/cargo-dist` (the fork mt-070's survey anticipated) upstreamed its work and was archived 2025-12-19; **`axodotdev/cargo-dist` is the maintained lineage again** — pinned at **v0.32.0** (2026-05-22) in `dist-workspace.toml` and installed via `cargo install --version 0.32.0 --locked`. Config: `ci=github`, the 4 native-runner targets (macOS arm64/x86_64 → macos-14/macos-15-intel, Linux arm64/x86_64 → ubuntu-22.04-arm/ubuntu-22.04 — no cross toolchain anywhere), `github-attestations = true` (`actions/attest@v4` in build-local), `tap = "chaychoong/homebrew-mettle"` + `publish-jobs = ["homebrew"]`, `[profile.dist]` = release + thin LTO. **One ADR deviation, reasoned in-file: no PowerShell installer** — with zero Windows targets dist itself warns and emits nothing ("skipping powershell installer"); shell + homebrew only, trivially restored the day a `windows-*` target lands (dist init's auto-added `x86_64-pc-windows-msvc` was explicitly removed to hold the ADR's 4 targets). `crates/als-conform` marked `dist = false` (the gauge instruments are not shipped product); `crates/mettle` gained `description`/`homepage` for a non-degenerate formula. **`mettle -V|--version` added** (hand-rolled dispatch idiom preserved — no clap; prints `mettle <CARGO_PKG_VERSION>` through the broken-pipe-safe writer; usage/module docs updated; 2 new e2e tests in `crates/mettle/tests/cli.rs`). **No crates.io job anywhere in the generated `release.yml`** (Decision 5 honored), tag-triggered `v*`. **Verified:** `dist plan` = exactly 4 targets, only the `mettle` package, expected artifact set (per-target `.tar.xz` + sha256s, `mettle-installer.sh`, `mettle.rb`, source tarball); local `dist build` for aarch64-apple-darwin → extracted archive runs `--version`/`parse` correctly; fmt/clippy/`cargo test -p mettle` green (61 tests). Homebrew publish needs a cross-repo PAT secret (`HOMEBREW_TAP_TOKEN`, `contents: write` on the tap repo) — the release itself proceeds without it (the `host` job doesn't depend on the homebrew job); framed to the owner at the combined review rather than storing a broad token unasked.
- ✔ **mt-074** · R5 · **Nix flake `packages`/`apps` output — `nix run github:chaychoong/mettle`, exact-pinned rustc 1.97.0** ([ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Decision 4; delegated → sonnet, tech-lead reviewed with a version-single-authority patch)
  **Done 2026-07-27.** `flake.nix` grew `packages.default` + `apps.default` and the dev shell **retired its documented rustc-1.95 compromise**: new pinned input `oxalica/rust-overlay` (`nixpkgs.follows`, one nixpkgs eval; resolved rev `c67ce00…` in `flake.lock`), toolchain built via **`rust-bin.fromRustupToolchainFile ./rust-toolchain.toml`** so `rust-toolchain.toml` stays the single version authority everywhere (rustup, CI, nix — the flake can no longer drift; chosen over fenix, which needs a separate hash-pinned manifest step). `packages.default` = `buildRustPackage` through `makeRustPlatform` on that toolchain, `cargoLock.lockFile` (zero git deps, zero C deps — no outputHashes, no linker fuss), `cargoBuildFlags = ["-p" "mettle"]` so only the product binary builds (als-conform's gauge bins excluded by not being a dependency); `doCheck = false` with the honest in-file reason (the real gate is the jar-backed conformance gauntlet this sandbox can't reproduce; implying package-green = gauge-green would mislead). Tech-lead review patch: the flake now **reads the version from `Cargo.toml`** (`builtins.fromTOML`) instead of hard-coding `0.1.0` — same single-authority rule as the toolchain. Honest determinism note added in-file (same pinned toolchain, no byte-identity claim vs a cargo build; the project's determinism guarantee is solver *output*, which the pin preserves). `/result` gitignored. **Verified:** `nix build` compiles exactly the 7 product crates; `./result/bin/mettle --help` + `nix run . -- --help` + `parse` on a corpus file all green; `nix develop -c rustc --version` = **1.97.0**; `nix flake check` green for aarch64-darwin (other systems evaluate clean but weren't built here — no Linux builder; the first tagged release's CI covers Linux for real).

- ✔ **mt-081** · North Star · **Conversion campaign chunk 3: encoder structural sharing lands — +19 net agreements, both count nets improve, zero wrong verdicts** ([ADR-0018](adr/0018-encoder-structural-sharing.md); delegated → the warm opus profiler, tech-lead reviewed the diff + ran the battery 2026-07-28)
  **Done 2026-07-28.** One file changed (`encode/mod.rs`, +723/−97): (1a) the collision-proof structural value cache — matrices content-interned to dense `MatrixId`s (`(arity, BTree-ordered (tuple, injective cell key))`, no lossy-hash equality anywhere), results cached under `(op, lhs id, rhs id)`, **caches hold ids, matrices live once** (the naive matrix-valued variant was built first and measured at 2.96 GB RSS with c11 still over budget; the id-plumbed version lands c11 at 0.55M spend/164 MB), interning metered into the budget; (2) support-bounded closure (`⌈log₂(support)⌉` rounds — Kodkod's own matrix-dimension bound; exhaustive brute-force-oracle equivalence to n=4 + seeded random to n=12); (3) the mt-049 strict-subset env rule deleted with its measured refutation documented in place. 11 new jar-free tests incl. an op-count regression bound verified to fail with the cache disabled, and run-to-run CNF byte-identity. **Subject rows: tf_minality_check[6] 33.6M→6.67M spend (SAT <1s), c11[7] 33.6M→0.55M — both beat the ≈7–9M projection.** **Battery (fresh full runs): agree 402→421 (242 SAT / 179 UNSAT), DISAGREE 0, 0 panics, 0 self-check failures; capacity 49→28; stage-1 wall 588s→478s; SB-0 count_match 56→65 / MISMATCH 0; SB-20 84→89 / MISMATCH 0.** Row diff vs the mt-079 re-baseline: +24 conversions (the TransForm pair 20, c11[7], firewire[3], both javatypes[0], elevator[32]), 2 neutral, **−5 disclosed: agree→over_budget on the reshaped CNF** (ringlead[2] + etl_scd[5] recover at 50k conflicts; OLAPUsagePrefs[0], elevator_spl_events[29], life.als[1] stay over_budget even at 50k — durable at probed budgets, plausibly lost per-copy aux diversity on SAT rows; all typed, never wrong). → **mt-082** re-pairs the conflicts default on the new shape. 1b (IR hash-consing) remains deferred per ADR-0018.

- ✔ **mt-082** · North Star · **Conversion campaign chunk 4: conflicts re-paired to 25k on the reshaped CNF — agree 428, both ADR-0018 recoverable rows back** ([ADR-0017 Amendment](adr/0017-gauge-default-budgets-paired-frontier.md); tech-lead-run grid + decision 2026-07-29)
  **Done 2026-07-29.** The ADR-0017 pairing rule applied to an encoder-shape change: structural sharing had moved five rows across the conflict boundary and roughly halved the wall cost of conflicts. Grid on the new encoder (encode 32M × conflicts 10k/25k/50k, sequential, row-diffed): **10k = 421/478s, 25k = 428 (+7) at 773s (1.62×), 50k = 435 (+14) at 1259s (2.63×) — zero regressions and DISAGREE 0 at every point.** ADR-0017's original 25k rejection (+6 at 3.7×) no longer described the instrument; **the default becomes conflicts 25k × encode 32M**, and its seven conversions include both recoverable ADR-0018 boundary rows (ringlead[2], etl_scd[5]). 50k stays a per-run flag (the further +7 pushes the three-net battery near an hour); the three durable ADR-0018 losses (OLAPUsagePrefs[0], elevator_spl_events[29], life[1]) remain over_budget even at 50k — family-D territory, deprioritized. **Re-baseline verified: fresh no-flag stage-1 row-identical to the 25k grid point (agree 428 = 242/186, capacity 28, over_budget 74, jar_nonverdict 21); SB-0 count_match 65 / SB-20 89, MISMATCH 0 both (count nets are enum-budget-bound — unmoved by conflicts, as expected); sweep baseline re-captured (header 25000/32M).** Pairing rule sharpened in the ADR amendment + the defaults comment: re-pair on any budget-default change OR encoder CNF-shape change. Gauntlet green. Campaign trail at defaults: 356 → 402 (ADR-0017) → 421 (ADR-0018) → **428**.

- ✔ **mt-083** · North Star · **Conversion campaign chunk 5: correctChord banked per-command — agree 428→444, and the first-ever comparisons expose 5 real wrong verdicts (→ mt-084)** (census attack-order step 3; tech-lead-run measurement campaign 2026-07-29)
  **Done 2026-07-29.** **Oracle-side banking:** the mt-069 `PerCommandProbe` pattern applied to `correctChord.als`'s 39 commands (one JVM per command, LEDGER-001 config, 900s tier-1 at 5 workers + 2700s tier-2 on the leftovers) — **36 of 39 banked** into `baselines/portus-63-slow-verdict.json` (33 tier-1 + 3 tier-2; only [13]/[22]/[25] resist 2700s). The mt-057 "the jar cannot sweep this file even at 7200s" finding is superseded: it was a serial-blotting artifact — **most commands answer in 2–75s**; a handful of pathological ones dominated every whole-file JVM. **Scorecard: agree 428→444 (253 SAT / 191 UNSAT), jar_nonverdict 21→0** — 16 of the 21 rows mettle already solved agree with the fresh jar verdicts… **and 5 DISAGREE, the gauge's first-ever wrong verdicts: [19] StabilizeDead / [20] StabilizeToNextStep / [21] StabilizeWithListChange / [24] ActiveStabilizeFromPredecessor (all `run … for 5 but 2 Time, 2 NetState, 6 Succs, 1 Base, 3 seq`; mettle UNSAT, jar SAT) and [30] `check IdealImpliesNoChangeEnabled for 9` (mettle SAT, jar UNSAT).** These were always wrong — invisible only because the file had no baseline. Triage done in-chunk: overflow-mode ruled out (jar SAT both modes on [19]; the model has no integer arithmetic — `util/ordering` ×2 + seq), mettle's verdict confirmed end-to-end via `mettle exec --command 19` (scopes echo correctly), harness clean (verify run row-identical to the full after-gauge: chord = 16 agree + 5 DISAGREE + 18 typed defers). **Diagnosis = mt-084, filed stop-the-line.** **Family-E probing (the census's 19 unprobed rows), all resolved:** leader_events[3] → **A** (converts at L1 100k conflicts); buffer[1]/[2]/[4] + leader[2] + leader_events[2] → **B** (all convert at L2 400k/128M — overturning mt-069's old-encoder finding that buffer's three were durable L2 holdouts; ADR-0018 moved the frontier); ertms_1A[9..13] → **D** (over_budget at L1 AND L2 — the census called them capacity, but the new encoder moved their bottleneck to search); chord's 8 E rows: 7 banked jar-side (6 UNSAT + [28] `LossOfPrincipal` SAT — the one satisfiable check), [25] unbanked; mettle-side they remain chord's own capacity story. **DISAGREE 0 in every famE probe run.** Instrument caveat recorded in `scratchpad/mt083/`: a mid-tier-1 system sleep window inflated several `timeout`-wrapped walls uniformly to ~2788s (alarms fired at wake) — verdicts unaffected (jar answers + budget-deterministic buckets); later runs caffeinate-protected, lesson filed. Artifacts: `scratchpad/mt083/` (before/after gauge JSONs, tier progress logs, famE L1/L2 JSONs, classification notes, runner + merge scripts).

- ✔ **mt-084** · North Star · **STOP-THE-LINE resolved same day: the 5 correctChord wrong verdicts were two clean-room stdlib defects in the seq shifting functions — fixed, probe-pinned, DISAGREE back to 0, agree 448** (delegated → opus diagnosis+fix, probes-first; tech-lead reviewed the diff + ran the battery 2026-07-29)
  **Done 2026-07-29.** **One mechanism, both polarities.** mettle's clean-room `util/sequniv` shifting functions bound their result-index comprehensions over bare `Int` instead of `seq/Int`, so `rest` emitted a tuple at index −1 and `insert`/`append` emitted one past the end — and since a `seq X` field is `seq/Int -> lone X`, such a result **can never equal a seq field**: [19] (`add[rest[list],…]`) and [20]/[21]/[24] (`list = insert[…,0,…]`) went spuriously UNSAT, while [30]'s `list != insert[…]` became a tautology ("a change is always enabled"), yielding the spurious counterexample. **The clamp is per-function negative space, jar-probed, not guessable:** `rest` drops the out-of-domain index but `delete` KEEPS it (decisive pair probed), `setAt` keeps too — documented in-file so nobody "fixes" them later. Two further defects caught while pinning: `insert`'s shift term is strictly `gt` not `gte` (latent in chord — it only inserts at 0), and **`util/seqrel`'s shifted lookup `j.(ord/prev) -> x in s` is vacuously true at the order boundary** (`none -> x in s`), putting every element at the last index — 6 of 11 jar probe rows diverged; respelled as an equality on the joined value, all 11 match. Minimal repro: `scratchpad/probe/mt084/p1_seqfuns.als` (8 rows, chord-free, all diverged before / agree after). **7 new probe-cited tests in `seq_conformance.rs`, each verified to FAIL against the pre-fix bodies** (revert-run-restore), covering both polarities + the negative space. **Battery (fresh full runs, tech-lead): agree 444→448 (257 SAT / 191 UNSAT), DISAGREE 5→0, 0 panics, 0 self-check failures; row diff vs the mt-083 baseline is EXACTLY the five rows** ([19]/[20]/[21]/[24]→agree_sat; [30]→typed over_budget — post-fix the correct UNSAT is genuinely harder than the pre-fix tautology: exhausts 25k conflicts, answers VALID at 100k; raising the default is an ADR-0017 paired-grid call, deliberately not made here); **SB-0 count_match 65 / SB-20 89, COUNT_MISMATCH 0 both — unmoved.** Package gauntlets green (als-types, als-core release incl. the whole-corpus suite). **Deliberately not fixed: `util/sequence.als` carries both defects in `pred rest`/`pred insert` but its Seq-atom interface diverges differently — needs its own probe round, filed as mt-085; LIMITATIONS updated.** Also noted (unrelated): the evaluator can't resolve module-qualified atom literals it prints. Delegate ops note: `cargo test -p als-core` in debug runs the whole-corpus solve suite inline (1h+); use `--release` (272s).

- ✔ **mt-086** · North Star · **(1b) IR hash-consing evaluated and REJECTED by measurement — the ADR-0018 deferral is closed** (delegated → opus instrumented profile, tech-lead decided 2026-07-29)
  **Done 2026-07-29.** The evaluation the ADR called for, run as an instrumented census of the **entire** current encode-bound worklist, not a sample. Fresh defaults sweep first (564 commands, agree 448, DISAGREE 0): `capacity` is down to **28 rows in two models** — TransForm tso ×22 + correctChord[0..5] — from the census's 117 (everything ADR-0018 targeted converted: all c11_perturbed, all ertms_1A, and 25 of 36 TransForm indices per file); `primary_var_cap` is empty, so capacity ≡ encode-bound by the `execute.rs:366` mapping. All 28 rows re-encoded with the budget lifted and per-bucket op accounting: true spends 51.3M / 140.5M / ~200.5M against the 32M ceiling, and **hash-consing would recover only 0.5–4.4%** — a value-cache hit is a free id-keyed `BTreeMap` probe charged zero ops (verified in code, `encode/mod.rs` `shared()`/`check_capacity`), so the only chargeable duplicate residue is Var-leaf interning + the four kinds `shared()` doesn't cover + formula/int gates. **Zero of 28 rows convert; post-1b overshoot is still 1.6×–6.2×.** The instrument is calibrated by a control (value cache disabled → 82.4% saveable on the same row) which also proves **1a and 1b are substitutes, not complements** — 1a strictly dominates because value identity is coarser than structural identity (c11: 151× arena duplication, 77% nominally 1b-saveable, already landed at 0.55M by 1a). Secondary wins immaterial (largest lowering wall 0.5 s). Tech-lead decision: **REJECT**, recorded in [ADR-0018](adr/0018-encoder-structural-sharing.md) Alternatives; instrumentation fully reverted, tree verified clean, artifacts in `scratchpad/probe/mt086/`. **Two pieces of campaign intelligence banked:** (1) the exact spend thresholds for any future ADR-0017 encode-grid point — 64M converts ≤8 TransForm rows, ~160M adds correctChord[0..5], ~224M the remaining 14; (2) the un-shared kinds cost a measured **4.6% of clauses on the correctChord family** (0.22% on TransForm) → spun off as mt-087, an encoder-local CNF-shape experiment aimed at the conflicts-bound bucket.

- ✔ **mt-088** · North Star · **The family-D census: the 67-row defer tail is fully costed — 33 more rows are budget-convertible, the durable tail is 23, and the whole capacity bucket clears at 256M encode** (coordinator-run 2026-07-29; full tables `scratchpad/mt088census/NOTES.md`)
  **Done 2026-07-29.** Three full sweeps vs the mt-087 100k×64M re-baseline (agree 484): **G1 250k×64M → 497** (+13; elevator_spl_events ×6 headline) at 1,556s; **G2 1M×64M → 507** (+10 more incl. ertms_1A ×3, lc-lenses ×3; plus **correctChord[13] → jar_nonverdict — at 1M conflicts mettle answers a row the jar's own mt-083 2700s banking tier could not**) at 5,666s; **G3 100k×256M → 503 with capacity 20 → 0** — every encode-bound row fits under 256M, confirming mt-086's true-spend table; 19 of 20 agree at once, `tso_minimize[23]` encodes but needs deeper conflicts too — at 5,116s. **Every move at every point is defer→agree; zero regressions; DISAGREE 0 / self-check 0 / panics 0 everywhere — no wrong verdicts at any budget ever probed.** The **durable tail at 1M is 23 rows**: correctChord ×8, handshake ×4, HotelVar/ertms_1A/lc-lenses ×2 each, 5 singletons (+tso_minimize[23] conditional) — deep UNSAT proofs on combinatorial protocol models; this list (23+1 rows) IS the mt-089 instrument worklist. **Defaults decision (tech lead): HOLD 100k×64M** — G1's +13 at 2.2× is near the ADR-0017 knee, but mt-089 and any family-D solver work both move the frontier again and would force consecutive re-pairs; deeper budgets stay per-run flags, census JSONs banked for the next paired grid. Full accounting at the measured frontier: deep-corner agree ≈ 527 of 564, with 13 typed-parity rows (lowering 7 / HO 4 / unbounded 2) and the 23-row tail the only distance left.

- ✔ **mt-091** · harness · **Speed up the debug `solve_corpus` gauntlet: per-package `opt-level = 2` on the solve-path crates, assertions provably still armed — measured 8.3× (3,802s → 460s)** (owner-approved 2026-07-30; tech-lead-authored — trivial-edit exception)
  The debug gauntlet's ~63 min was unoptimized encode/solve codegen, not assertion cost. Workspace `Cargo.toml` gains `[profile.dev.package.als-core]`/`[profile.dev.package.als-solve]` `opt-level = 2` (the canonical Cargo per-package override; integration-test deps build under `dev`, which is why the override sits there and not on `profile.test`). `debug-assertions`/`overflow-checks` are independent knobs and stay ON — guarded forever by the new canary (`als_core::DEBUG_ASSERTIONS_ARMED` const + `debug_assertions_are_armed_in_the_library_whenever_they_are_here` in the solve_corpus target: a test binary's own `cfg!` reflects only the test profile, so the const reports the library's actual setting). Deliberately NOT applied to als-syntax — the mt-014 deep-nesting depth guard is calibrated against debug frame sizes. Measured on the first post-change run (the mt-089 stage-2 commit gate): debug `solve_corpus` **3,801.8s → 460.3s (8.3×)**; both full workspace gauntlets (default + all-features, serial) now finish in ~18 min combined vs ~65 min for ONE before; compile delta unmeasurable in practice (the optimized als-core rebuild rode inside a 5.5s clippy pass). Canary green in both gauntlets.

- ✔ **mt-090** · North Star · **STOP-THE-LINE (latent) RESOLVED: the `implies`-antecedent overflow-guard polarity defect — fixed, probe-pinned, rule 4 retracted** (opus delegate probes-first, tech-lead reviewed + re-ran the load-bearing cells; 2026-07-30)
  Done 2026-07-30. Root cause: mettle implemented the *ideal* Milicevic/Jackson reading — `a => c` treated as `¬a ∨ c` for guard purposes, flipping the forbid-mode overflow-guard polarity across the antecedent — where the jar builds a genuine Kodkod `IMPLIES` node and toggles `Environment.negated` **only** in `visit(NotFormula)`. New pin **translation-ref §10.7f** (55 jar cells, `scratchpad/probe/mt090/`): polarity = the count of enclosing `not`s and nothing else; `DefCond.isUnivQuant` rules 0–3 are the **entire** classifier; **§10.7c rule 4 / §10.7e FACT 5 retracted in full** — mt-051's antecedent cells cancelled a wrong polarity against a wrong classification, and its "int-ITE" cells were relational ITEs over `Int[·]` casts (`plus[..]` returns the SET `Int`; a number literal is a set), i.e. FACT-2/FACT-4 machinery, not an escape. Probe matrix divergences **20 → 2**; the mt-089 minimized repro agrees **8/8** (was 6/8). Fix: `overflow_guard.rs` classifier now `isUnivQuant` verbatim (rule 4 + `contains_int_ite` deleted); `encode/mod.rs` + `eval.rs` antecedent polarity-flip removed (matched pair preserved). 10 probe-cited jar-free tests (`overflow_polarity_conformance.rs`), 9 revert-verified (the 10th is negative space, passes both sides by construction). LEDGER-005/LEDGER-010 amended (dated retractions, tech-lead no-fork delegation). **Verification: stage-1 sweep byte-identical to the mt-087 re-baseline (sha256-equal); tail re-run on the fixed encode 22 of 23 answered, DISAGREE 0, self-check 0 — `correctChord[28]` converts as agreeing SAT (573.8s CaDiCaL 1M×256M; pre-fix it answered the wrong UNSAT), `fullsub2[0]` stays the one genuinely-hard row; per-row CNF diff vs the pre-fix artifacts shows exactly ONE changed row (cc28), so all other mt-088/mt-089 numbers stand.** Disclosed residue (LIMITATIONS, zero corpus incidence): the int-position ITE with one int-literal branch (jar dispatches on the *then* branch; literal = set) — 2 cells went from accidentally-right to honestly-wrong, pinned by an `#[ignore]`d test pending its own probe round; and the unprobed cross-polarity translation-cache corner (g5). **mt-089 stage 2 is unblocked.**

- ✔ **mt-089** · North Star · **CaDiCaL backend behind the `Solver` trait — instrument (stage 1) + the `--solver` surface (stage 2) both DONE** ([ADR-0019](adr/0019-optional-cadical-backend.md), owner-decided 2026-07-29; stage 2 opus delegate 2026-07-30, tech-lead reviewed + re-ran the load-bearing cells)
  **Stage 2 done 2026-07-30 (dated addendum in ADR-0019).** Decisions: **`--solver mettle | cadical`** on exec (REPL rides it) and serve, default `mettle`, exact-match only, unknown name = hard exit listing this build's names, and a *compiled-out* name gets a distinct message naming the cargo feature (typo ≠ not-built-in). **Shipped builds keep the feature OFF** — ADR-0019's "all four targets build the C++" condition is undemonstrated until the new CI battery runs on a real tag; source builds opt in via `--features cadical` (host target verified end-to-end). **Budget mapping:** `--conflicts` binds natively (CaDiCaL's `limit("conflicts", n)` is per-solve-relative, same contract as `solve_within`; `u64::MAX` now passes the true unlimited sentinel), but the binding exposes **no conflict counter**, so `LiveSolver::effort()` is `Option` and the cumulative enumeration-effort budget is **refused** on an effort-less backend rather than silently charged zero. **Enumeration works under CaDiCaL** (exact under any sound backend; order/first-instance/locked-configuration are the backend's own, LEDGER-014); **counting stays structurally fenced** — `solve-gauge` deliberately has no `--solver` flag, so no scorecard/baseline number can exist off the yardstick (ADR-0019 §2). **Cross-backend arm** = `backend-instrument --cross` (one translate, both backends on the same CNF, non-zero exit on any verdict difference — the check that caught mt-090, now a fixture; `--rows -` pipes worklists from sweep reports). **FP discipline:** workspace `.cargo/config.toml` sets unforced `CXXFLAGS=-ffp-contract=off` (verified on the compile lines via `-vv`; inert on C++-free default builds); `.github/workflows/backend-determinism.yml` = the owner-requested cross-target battery (4-runner matrix, own-CDCL hash differences fail, cadical differences report as measurement). New seam: `als_solve::backend::{Backend, LiveSolver}` (enum, not `dyn` — the enumeration seam needs concrete incremental `add_clause`+`solve`). Verified: fmt/clippy clean both feature settings; **default-path stage-1 sweep byte-identical to the mt-087 re-baseline (sha256 re-checked by the tech lead)**; cross arm on a 60-row corpus sample: 56 comparable, **0 cross-disagree, 0 self-check failures, all 56 three-way agree with the jar**; feature-on test suites green; `cargo doc` clean; dep graph: default has zero `cadical`, feature adds `cadical → cc` only. Tech-lead re-ran: handshake[2] convert (CaDiCaL SAT 0.34s vs own-CDCL budget-exhaust ~21s), unknown-name + case-mismatch errors. **Disclosed limits:** everything cross-target (3 non-host builds, runner labels, artifact layout, Docker/flake claims about `g++`/libstdc++) is unverifiable until the next `v*` tag exercises the battery; the 47-row defer tail is NOT cross-checkable at default budgets (own arm exhausts on all 47 — a useful future cross-run needs the own arm at 1M+); instrument keeps its own two-arm dispatch (needs counters/wall-hooks the shipped seam withholds) and its stage-1 `cdcl` artifact vocabulary (documented). Pre-existing non-bead find: serve prints its verdict block before the listening banner (left alone). Stage 1 record below unchanged.
  **Stage 1 done 2026-07-29 (opus delegate, tech-lead reviewed: byte-identity sha-verified, wrong-verdict repro re-run by hand, calibration accepted).** Dependency: `cadical` crate **=0.1.16** (MIT) vendoring CaDiCaL 1.9.5 (MIT), safe API end-to-end (`unsafe_code = "forbid"` holds), one build-dep chain (`cc`); rejected `rustsat-cadical` (dep tree) and hand-FFI (unsafe layer); everything behind off-by-default features (`cadical` on als-solve, `cadical-instrument` on als-core/als-conform + a required-features instrument bin) — **default-features stage-1 sweep byte-identical to the mt-087 re-baseline (sha256-equal JSONs)**. Calibration: 11/11 three-way (CaDiCaL = own CDCL = jar), identical per-row CNF sizes — shared-encoding claim measured. **The tail table: 21 of 23 durable rows convert under CaDiCaL** (7 at 100k conflicts, +14 at 1M; all non-converts conflict-bound not wall-bound; `tso_minimize[23]` converts at 100k×256M — 8.6M vars/31.6M clauses, self-check passes; correctChord[22]/[25] answered where the jar's own banking could not). Still stuck: `fullsub2[0]` (genuinely hard) and `correctChord[28]` — which answered WRONG → **mt-090**, the instrument's oracle-independent check paying off on its first run. ~~**Family-D implication (measured, in order): clause-DB reduction (first-order — 40× on ertms_1A[9]), phase saving, restart policy; inprocessing NOT implicated; BCP micro-opts least.**~~ _Retracted 2026-07-30 (ADR-0020 revision note): the instrument measured the 40× gap, never its attribution; this sentence was recorded at wrap without artifact support, and the solver already has mt-049 clause-DB reduction and phase saving. Attribution is mt-092 stage 0._ Trait gaps for stage 2: binding exposes no conflict counter; enumeration seam honored + unit-tested but counting stays own-CDCL (ADR-0019 §2); packaging = first C++ dep (dist "zero-C-dep" comment + flake `nativeBuildInputs` both need stage-2 re-verification). **Stage 2 (surface) was blocked on mt-090 and is now unblocked (2026-07-30):** the fixed encode re-ran the tail at 1M×256M — **22 of 23 answered, DISAGREE 0, self-check 0; correctChord[28] converts as agreeing SAT (573.8s)**; per-row CNF diff vs the pre-fix artifacts shows cc28 is the ONLY changed row, so this table's other numbers and the family-D implication order stand. Remaining stage-2 spec unchanged below.
  Two stages, gate between them. **Stage 1 (instrument, dev-side only):** survey the binding path (existing `cadical` binding crates vs vendored C++ via `cc`) and write the dependency justification ADR-0019 requires (license MIT ✓; effect on the four cargo-dist targets + the nix flake measured, not guessed); implement `als_solve::Solver` for it, preserving the enumeration seam's `add_clause`/`block` semantics through IPASIR and mapping the conflict budget onto CaDiCaL's limit interface; then run the full defer tail (mt-088's worklist) under it at the ADR-0017 defaults and report the per-row table: converts-with-a-strong-backend vs still-stuck. Rows where CaDiCaL answers and the jar baseline exists MUST agree — a verdict difference between backends on the same encoding is a bug (encode or wiring), stop-the-line. That table prioritizes family-D upgrades to the own CDCL (clause-DB reduction, phase saving, BCP micro-opts — which of them buys which rows). **Stage 2 (surface):** `mettle exec/serve --solver <name>` (default = the own CDCL; naming decided in-bead), threaded through exec/REPL/serve; scorecard, counting nets, and sweep baselines stay own-solver-only (ADR-0019 §2); LIMITATIONS gains the honesty entry (deterministic-per-build, not cross-platform byte-pinned; temporal counts configuration-relative to CaDiCaL's own first solution, never compared to jar count baselines); the gauge gains a cross-backend arm (same encoding, two solvers, verdicts must match — a free oracle-independent check). Packaging: release matrix + flake must build it on all four targets or the surface ships behind a feature flag with the gap disclosed — measured in stage 1, decided at the gate. **Cross-target determinism battery (owner interest, 2026-07-29):** FP determinism is achievable engineering, not impossibility (basic IEEE ops are bit-specified; drift comes from FMA contraction/fast-math/vectorization) — so build CaDiCaL with disciplined flags (`-ffp-contract=off`, no fast-math), add a CI battery where every release target solves the same suite and output hashes are compared, and if it holds, a later ADR-0019 amendment can tighten "exempt from D1" to "verified reproducible at the pinned CaDiCaL version"; every CaDiCaL bump re-runs the battery. Delegation: stage 1 → opus (FFI + contract-preservation subtleties); stage 2 partly sonnet (CLI threading, packaging) under tech-lead review.

- ▢ **mt-099** · product · **Evaluator numeral rendering: bare `3` vs the jar's `{3}` — pin the full numeral-typing surface, then fix rendering** (next on "proceed"; cut at the 2026-08-20 wrap)
  Found at mt-098 (E-59, LIMITATIONS entry): mettle's evaluator renders a bare numeral literal as `3` where the jar renders `{3}` (same for negatives) — the mt-095 NUMBER-literal-is-a-set distinction surfacing at the rendering layer (the jar takes the Expression path; mettle types a numeral `int` and renders bare). LEDGER-012 pins the *tuple-order* divergence only; rendering *kind* (bare vs singleton tupleset) is the jar's own rule (`#e`/`sum` bare, Int-set-typed input as tupleset) and mettle matches it everywhere except this literal case. Scope: probe the jar's evaluator on the numeral-adjacent input surface (bare literal, negatives, `plus[3,4]` already pinned, arithmetic over literals, a literal in set position), then fix the evaluator's numeral typing/rendering to match; touches every numeral-valued evaluation, so the REPL suite is the acceptance net and any als-core eval change triggers the sweep byte-identity check. Delegate: opus (warm, has the mt-098 harness).

- ✔ **mt-098** · product · **The mt-084 evaluator residue FIXED: every atom name the evaluator prints is now legal input — the jar's exact-global-match rule newly pinned (E-50–E-59)** (warm opus delegate, tech-lead reviewed + re-ran the decisive jar cells; 2026-08-20)
  Done 2026-08-20. **The pinned rule (new — the evaluator contract was silent):** the jar registers each solved atom's label VERBATIM (`world.addGlobal(atom.label, atom)`) and evaluator input resolves it by **exact whole-string match** — alias and slash included, module-qualified lookup never involved. The tell, tech-lead re-run by hand: `so/Ord$0` → `{so/Ord$0}` while **`so/Ord` — the sig by its own qualified name — is "cannot be found"** (only atoms/skolems are ever `addGlobal`'d), as are `so/Ord$1`, `zz/Ord$0`, `A$9`. E-cells E-50–E-59 added to [reference/alloy6-evaluator.md](reference/alloy6-evaluator.md); probes banked at `scratchpad/probe/mt098/` (the mt-061 GUI-path harness — solve→writeXML→reparse→`addGlobal`→eval; `LiveEval` skips `addGlobal` and cannot see this). **The break was NOT the input layer:** mettle parsed `so/Ord$0` into a fine two-segment `QualName`, but the resolver's `env_get` is first-segment-only and gated `segs.len() == 1`, so the name fell through to the `$`-leniency at `expr.rs:1569`, which "resolved" it to bare `univ` WITH NO RECORDED CHOICE — the visible symptom was a lowering error and the leniency actively masked the cause since mt-084 (two instrumented false starts: `infer_name`/`spine_head` carry the same gate but are not the recording path; `resolve_name` is). **Fix:** `Cx::env_get_qualified` (whole-text env match, `ast_override`-gated to evaluator fragments, matches only registered globals — cannot resolve arbitrary qualified names, exactly the jar's negative space) consulted in `resolve_name` (recording `NameChoice::Var(joined)` — no lowering change needed) + `infer_name` + `spine_head`, ordered BEFORE the `$`-leniency (load-bearing). **Verification (tech-lead re-ran the jar cells + full gates):** round-trip 30/30 + 20/20 (every printed atom re-enters), jar parity 8/8 + 5/5 rejections, 4 new repl tests incl. the `so/first`/`so/next` model-name tripwire; **alloy4fun resolve gauge BYTE-IDENTICAL (sha `a037a931…` = the HEAD hash), full solve sweep BYTE-IDENTICAL (sha `feb57819…` = the mt-097 baseline) — agree 506, DISAGREE 0, self-check 0**. New divergence found and deliberately left, recorded (E-59 + LIMITATIONS, candidate bead): a bare numeral renders `3` in mettle's evaluator vs `{3}` in the jar — the mt-095 numeral-is-a-set distinction surfacing at the rendering layer; fixing it touches every numeral-valued evaluation, out of this bead's fenced scope.

- ✔ **mt-097** · North Star · **The 7-row mt-040 recording tail: 4 rows CONVERTED (agree 502→506), 3 pinned with probe evidence; ertms_1A[5] reclassified** (warm opus delegate probes-first, tech-lead reviewed + re-ran the decisive jar probe + full gates; 2026-08-20)
  Done 2026-08-20. The `lowering 7` bucket separated into four families by a temporary failing-name instrument (added, used, removed), which also corrected the standing classification. **Family B — a macro applied inside another macro's body (overlapping-ranges[0], philosophers[0]/[1]): FIXED in the lowerer.** Root cause: `bind_macro` lowered a nested application's arguments through a freshly-built module-level context, but the checker records those arguments' choices in the *enclosing macro's nested table* (`expand_macro` resolves them in its own `sub` context) — so the argument name (`s1`, the outer macro's parameter) had no visible resolution. Fix: `bind_macro` takes the caller's `Ctx` and lowers arguments in it (`replay_macro_rel`/`_formula` thread it from four sites); identical-by-construction for top-level uses (controls b5–b7). Probes `b_macro.als` 7/7. **Family C — `run` on a fun (INSLabel[1]): FIXED.** New pinned rule (translation-ref **§2.5(3a)**, 20 cells): `run f` existentially quantifies the fun's parameters over their declared bounds respecting multiplicities and **ignores the body** — decisive pred/fun asymmetry: `run p_false` (contradictory pred) UNSAT but `run f_empty` (always-empty fun) SAT; params really quantified (`for 0 A` UNSAT on `one`, SAT on `lone`/`set`). Tech lead re-ran `c_runfun.als` against the jar personally. Fix: drop the `!is_pred` defer, substitute `Const(true)` for a fun body in `run_pred`. **Family A — `Sig$` metamodel (hc7[0], einstein-wikipedia[0]): PINNED as a genuine feature, not a recording gap** — the jar's meta surface is exact (`#Vertex$.subfields = 2` SAT / `= 3` UNSAT; meta atoms in `univ`); a real implementation needs metamodel synthesis (meta-sigs + `subfields`/`value` in the universe, ground expansion of meta-quantifiers, resolver-side `$`-name handling) — requirements filed in LIMITATIONS/NOTES; mettle defers typed on all 7 probe cells. **Family D — ertms_1A[5]: RECLASSIFIED** — not the macro family; the unrecorded name is `next` under a triple `util/ordering` open, sitting as a join's compound-right operand — the exact position mettle deliberately does not re-resolve (the measured 28,400-false-reject tradeoff); closing it needs the join's left-slice threaded into compound resolution, an als-types change under the byte-identity rule — left pinned. **als-types: zero changed lines**, and the mt-040 byte-identity was *measured anyway* (resolve gauge over all 150,891 codes, sorted-and-path-normalized sha256 equal both sides). **Verification: sweep row-diff exactly the 4 predicted conversions — INSLabel[1]→agree_unsat, philosophers[0]/[1]→agree_sat, overlapping-ranges[0]→agree_unsat; agree 502→506 (274 SAT / 232 UNSAT), lowering 7→3, DISAGREE 0, self-check 0, panics 0** (tech lead read the row-diff off the delegate's banked JSONL and re-ran fmt/clippy/full suite). Tests: `recording_tail_conformance.rs` (7 jar-pinned). Banked: `scratchpad/probe/mt097/` (NOTES, 4 probe files with compare columns, `rowdiff.py`, both sweep reports). all four families triaged and pinned with measured blockers; the mt-095 ledger correction retracted** (warm opus delegate, tech-lead reviewed + re-ran the decisive jar pair + applied the ledger revert; 2026-08-20)
  Done 2026-08-20. **The bead's Part 1 could not be implemented honestly — the mt-095-amended LEDGER-010 layer-(2) wording ("any set former sheds the guard") is refuted by measurement.** The delegate ran a 36-cell former × reader matrix *before* touching code: mettle already matched the jar on 26 cells; the amended rule would have regressed 12 (the jar KEEPS the guard through intersection, difference, if-then-else, join, override and product — only the union and the comprehension shed, and mettle already sheds on comprehensions by refusing formula positions). Three successively narrower candidate rules each died to a cell: "any former" (f-rows, m14), "union always" (u11/v3 — a union of TWO capable casts is jar UNSAT), "union unless both capable" (implemented, measured 28→9 with no regressions — then refuted by t4c: `plus[F.v,7] + 1 in Int` with NO quantifier is jar UNSAT, the identical union shape that is jar SAT under a comprehension-∀ (u1/t4e); wave 4 re-probed mt-051's banked T1/T4 live and they HELD, which is what caught it; also v1 — a non-capable sibling sheds). The jar's mechanism is not IR-local (bytecode: `BooleanMatrix.and/or/difference/dot` all `mergeDefConds`, `choice` does not — opposite of observed on both counts; it lives in sparse-matrix folding). **Decision: PIN conservative — `collect_capable_casts` keeps descending through every former** (can turn jar SAT into mettle UNSAT, never the reverse); **LEDGER-010 layer (2) reverted to its pre-mt-095 "unions included" wording** by a dated mt-096 amendment (tech lead applied; the mt-095 ITE closure and layer-(3) narrowing stand). **Part 2 — all four families PINNED with measured blockers, none guessed:** R4 `let`-to-int sort (blocker is scoping — `ite_sort` runs before the binder is pushed — not the mt-056 machinery; needs a substitution env, a design choice); R3 `toInt` double-unwrap (measured: the peephole fixes n6/n7 but breaks n1/n2, net unchanged at 2 — mettle routes `#X = 1` through `lower_int` where the jar set-compares; needs the int-compare gate re-keyed on the jar's literal-`IntToExprCast` test first, its own bead); R2 `#`-guards-shrouded-cast (matrix shows `#` tracks the set-level readers — entangled with the union corner, not independently closable); R1 `Int[int[·]]` re-wrap (needs resolver-surface info mettle deliberately drops). New unexplained observation recorded, unbeaded: `lone` over an intersection sheds where `no`/`in` guard (u15). **No functional code ships**: `overflow_guard.rs` gains a doc comment; part E of `eq_typing_conformance.rs` (6 passing negative-space tests + 1 `#[ignore]`d 12-cell jar pin) holds the matrix; `eval_differential.rs` gains a 13-model both-modes encoder/evaluator brute-force making the shared-classifier claim a measured fact. Docs: new translation-ref §10.7h, §10.7g item 4 scoped to the `sum` reader, §10.7e FACT-3 note narrowed again, LIMITATIONS rewritten (all families PINNED with blockers). **Verification (tech-lead): re-ran the decisive t4c/t4e jar pair by hand (UNSAT/SAT confirmed), fmt/clippy clean, full workspace suite green, sweep byte-identical (sha `d8ca20a3…` = the mt-095/committed baseline exactly) — agree 502, DISAGREE 0.** Lesson filed in LESSONS.md: a rule generalized from one cell family is a hypothesis — probe the matrix and re-probe the banked cells a new rule would overturn before a contract amendment ships. Banked: `scratchpad/probe/mt096/` (NOTES, four probe files with jar/mettle/compare columns, `bm.disasm`, both sweep reports, the ledger draft).

- ✔ **mt-095** · North Star · **The mt-090 residual: int-position ITE with one int-literal branch — FIXED via the pinned then-branch dispatch rule; g5 probed clean; four new latent families disclosed** (opus delegate probes-first, tech-lead reviewed + re-ran gates + applied the LEDGER-010 amendment; 2026-08-20)
  Done 2026-08-20. **The pinned rule (new translation-ref §10.7g, from bytecode `tak.disasm:1394-1445` + 100 jar cells over six waves, both overflow modes):** `visit(ExprITE)` takes the **then** branch's translated Kodkod class alone — the else branch is coerced (`cform`/`cset`/`cint`) and never votes — and a bare NUMBER literal is an `IntToExprCast`, hence an `Expression`, hence **relational** (`instanceof Expression` tested before `instanceof IntExpression`; the ITE dispatch is the only observable position, since `toInt` unwraps the cast everywhere else). Decisive minimal pairs i1/i2 (same selected overflow, only the int-branch slot moves: SAT vs UNSAT) and i5/i2 (`0` for `#Node`: SAT vs UNSAT); i21 (`= 0` UNSAT) proves the empty-**set** denotation. **Fix: two lines at lowering** (`Lowerer::ite_sort` — then-branch dispatch, bare `Num` ⇒ `Sort::Rel`); the encoder/evaluator pair stays matched **by construction** (the change is upstream of both; the node becomes a `RelExprKind::IfThenElse` both back ends already handled). **The headline model correction: mt-090's predicted *dynamic* constant-escape does not exist** — wave 2 (j0–j7, fourteen cells all forbid SAT, including the exactly-bound/merely-constrained j6/j7 split that mettle's own `translation_constant` separates) proves the guard is shed by **structure** (a cast `toInt` must read with `.sum()` contributes no guard; one it UNWRAPS keeps it), so no escape work was needed. Probe matrix 28→9 divergences, revert-verified, no cell regressed. **g5 (the `FOL2BoolCache` cross-polarity corner): probed at 16 cells, 16 agreements** — the mechanism is real (LEM is UNSAT written out, SAT via `let`/shared-ITE-condition; correct `noOverflow` semantics, not a defect) and mettle shares it by construction (condition/`let`-RHS translated once). **The 9 residual cells = four NEW latent families, none implemented, all zero-corpus-incidence, recorded in LIMITATIONS + §10.7g and beaded as mt-096** — headlined by the LEDGER-010 layer-(2) correction ("unions included" is too strong; only a **bare** cast guards — k4/k16) and the `toInt` double-unwrap discarding `#` under an int reader (`#(plus[3,4]) >= 7`: jar reads 7, mettle 1 — n6/n7, no overflow involved). LEDGER-010 amended (dated, tech-lead no-fork delegation; the delegate's draft at `scratchpad/probe/mt095/LEDGER-010-amendment-draft.md`); §10.7e FACT 3 correction note added; §10.7f's two residuals marked closed. Tests: part D of `eq_typing_conformance.rs` (8 new jar-free probe-cited tests), the `#[ignore]` on `part_c_mixed_branch_ite_is_relational_in_the_jar` removed and passing, module header corrected. **Verification (tech-lead re-ran): fmt/clippy clean, workspace suite green; stage-1 sweep byte-identical before/after (sha256 `d8ca20a3…` both sides), scorecard unchanged — agree 502 (272/230), DISAGREE 0, panics 0.** Delegate's honest self-correction on record: its first reading of the `#`-discard family (from k10/k11 alone) was wrong, refuted by its own wave-6 n-cells and rewritten in all four places before reporting. Banked: `scratchpad/probe/mt095/` (NOTES.md, six probe files with before/after compare columns, `AstDump.java` — new resolved-AST printer that settled the wave-1 anomalies, three disassemblies, both sweep reports).

- ✔ **mt-094** · harness · **Live corpus-run dashboard: per-row JSONL telemetry + `conform watch` — DONE, acceptance sweep watched live end-to-end** (owner-requested 2026-08-19; sonnet delegate per the design sketch, tech-lead reviewed + ran all gates and the acceptance 2026-08-19)
  **Done 2026-08-19.** `solve-gauge --progress-jsonl PATH` emits one JSON object per line: `run_start` (the run's config + its sweep-baseline `mode_key` + the full ordered row list `{i, key}`, so the grid is read from the run, never hardcoded), `row_start` (worker-side, at the exact moment the existing start heartbeat already marks; `CmdItem` gained `pos`, its stable grid index), `row_done` (rides `parallel_fold_ordered`'s pre-existing `on_result` hook — a no-op until now — with bucket/secs and the disagreement/self-check/panic strings when present; completion order, readers key on `i`), `run_done`. Sink = `telemetry.rs`: Mutex'd `BufWriter`, every line flushed (a SIGKILL leaves at most a torn *last* line), best-effort emit that can never fail the sweep (the `status.rs` side-channel discipline). `conform watch <progress.jsonl> [--port N (default 4031)] [--baseline PATH]` = a fresh hand-rolled loopback GET server (no als-sterling dep — it would drag tungstenite for a server with no WS side; **zero new deps**): `/` serves ONE embedded hand-written page (`assets/watch.html`, inline CSS/JS, offline-tested like als-sterling's frontend), `/data` re-reads the JSONL fresh per poll (torn tail dropped, missing file ⇒ honest `waiting` payload so watch can start BEFORE the sweep) and joins historical wall from `baselines/corpus-sweep-sb20.json` strictly on the run's own `mode_key` (mt-059's lesson: a stage-1 time is no comparison for a counting run). **Determinism: untouched by construction and by test** — every emission is `if let Some(sink)` beside a hook that already existed; new integration test `telemetry_sink_does_not_move_the_report` (bare vs sink-attached at jobs 1/4, text + JSON byte-identical). 16 new tests; fmt / clippy `-D warnings` / 81 unit + 16 integration green (tech-lead re-ran, not just the delegate). **Acceptance, all tech-lead-run: (1)** watch started before any run answers `waiting`; **(2)** SIGKILL 25s into a jobs-8 sweep left 9/9 valid lines (1 `run_start` + 8 `row_start` — the LPT heavy front, correctly zero `row_done`) and the dashboard kept serving 8-running/556-pending off the dead file; **(3)** a full 564-row sweep watched live end-to-end — the mid-run check read the 8 running ertms_1A/correctChord rows' live elapsed (~270s) against their joined historical walls (380–537s), which is precisely the owner's "am I stuck?" use case — closing at 1,130/1,130 valid lines (1+564+564+1) and a final `/data` of all 564 done with buckets exactly the standing scorecard: **agree 502 (272/230), DISAGREE 0, over_budget 29, capacity 20, lowering 7, HO 4, unbounded_steps 2, 0 panics, 892s wall.** Scorecard unmoved (telemetry is not a semantics change). Deliberately out, per the bead's first-cut line: capture-sweep/backend-instrument telemetry, per-run history archives.

- ✔ **mt-093** · North Star · **The volume-lever campaign: trail saving → all-UIP shrink → tier retention — ALL THREE STAGES REJECTED BY MEASUREMENT (−2/−2/−6); campaign CLOSED at agree 502** ([ADR-0021](adr/0021-cdcl-volume-lever.md) + three dated addenda incl. the campaign close; stage 3 owner-authorized "once" 2026-08-19, rejected and reverted 2026-08-20)
  **Stage 3 (tier retention) done and REVERTED 2026-08-20** (warm opus delegate predictions-first + a second independent adversarial review — no correctness defect; tech-lead reviewed, re-ran gates, ran the verdict sweep): Oh core/tier2/local with LBD-refresh-on-use, 16-byte header preserved (compile-time-asserted, the mt-092 cache argument), one config pinned pre-measurement. **Full row-diff: net −6 (agree 502→496** — 2 conversions vs 8 regressions, and the regressions span SMALL rows (handshake ×2, hanoi, life) as well as big (ertms_1A[13], lc-lenses[7]), so the disclosed uncapped-core-ratchet flaw prices negative across the size spectrum; DISAGREE 0, zero verdict flips**)**. Delegate's disclosed fragility stands recorded: fixing its refresh-LBD convention defect *worsened* ertms 65% — coherence feeds the ratchet. Banked: `scratchpad/probe/mt093/stage3/` (633-line patch, A/B binaries, NOTES, adversarial-review record). **Campaign close (in the ADR): 0-for-3, "park at 502" reached by measurement; yield estimate retracted; what it bought = three measured model corrections + three banked implementations + the standing lesson that this corpus prices single-technique trajectory perturbations at ≤0. The tail's 22-of-24 answerability stays real — future routes (aged-core retention, vivification re-profile, or CaDiCaL-for-the-tail via the shipped `--solver cadical`) are new owner decisions, not scheduled.** Stage-1 and stage-2 records below/in the ADR.
  **Stage 2 (all-UIP shrink) done and REVERTED 2026-08-19** (opus delegate predictions-first + independent adversarial opus review — no correctness defect, ~16M fuzzed block attempts; tech-lead reviewed the pass, re-ran all gates, ran the verdict sweep): implementation correct and conservative-by-construction (monotone size improvement, asserting literal structurally untouched, level set/LBD/backjump invariant asserted; differential fuzz on verdicts AND enumeration counts, both arms), but the full row-diff came back **net −2 (agree 502→500**, 2 conversions vs 4 regressions, DISAGREE 0, zero verdict flips**)** and ADR-0021's pre-authorized revert fired — same net as stage 1, different mechanism. The measurements bought the campaign's sharpest correction yet, now in the ADR's stage-2 addendum: the pass's direct removal is only −0.5…−4.7% (predicted −10…−30%), the cross-arm −25…−37% mean-length drop is real compounding (not lottery), **and §(b)'s causal thesis — propagation volume tracks learned-clause length — is measured FALSE on the very rows it was derived from** (ertms: length −25%, props/conflict +25%). Banked: `scratchpad/probe/mt093/stage2/` (991-line patch, A/B stat-probe binaries, NOTES with scored predictions, the adversarial-review record, the disclosed un-budgeted block-walk MEDIUM). **The volume-lever framing is dead; what survives is the cross-run's 22-of-24 (the tail IS answerable at this budget by better search). Stage 3 (tier retention) targets search quality directly and does not rest on §(b) — but two −2s in a row make running it an owner decision, framed in STATE.**
  **Stage 1 (trail saving) done and REVERTED 2026-08-19** (opus delegate predictions-first, tech-lead reviewed + ran the sweep): implementation complete and correct (50 tests, clippy clean, DISAGREE/self-check/panics 0 everywhere), but the full row-diff came back **net −2 (agree 502→500**, 3 conversions vs 5 regressions**)** and ADR-0021 pre-authorized revert on exactly that. The delegate's replay-off control proved the win channel is not the ADR's mechanism: the algorithm never skips propagation of replayed literals, early conflict detection fired ~0 (6/2,333, 20/139,624), so the −73%/−18% calibration conflict drops were pure trajectory lottery — which the corpus then priced at −2. Two corrections recorded in the ADR addendum (mechanism model wrong; the survey's trajectory-neutrality claim disproven, as suspected). Banked: `scratchpad/probe/mt093/stage1-trail-saving.patch` (800 lines, with A/B JSONs + NOTES) and the `lits[0]`-across-backjumps hazard, binding on any future reason-holding technique. Sharpest lesson: **calibration rows cannot accept a search change — two rows both winning big (−73%!) still netted negative on the corpus; only the full row-diff decides.**
  The mechanism is ADR-0020 stage-0's §(b), sharpened by the mt-092 closing cross-run: re-propagation volume tracks post-minimization learned-clause length (477/1,932/13,429 pops/conflict at lengths 34/74/126), and CaDiCaL answers 22 of the 24 durable over_budget rows within the same 1M conflicts on identical CNFs — the tail's barrier is search quality. Three single-technique stages, each its own delegated implementation with its own full row-diffed sweep (never bundled): **(1) trail saving on backtrack** (Hickey & Bacchus SAT 2020 — keep the popped trail suffix, replay on redescent when consistent and the saved reason is live, invalidate on reduce_db tombstones; the survey's trajectory-neutrality claim is a *prediction to test*, not accepted — replayed reasons can differ and feed analyze; preferred acceptance = byte-identical sweep + identical conflict counts + lower wall, else reclassify as a disclosed search change and judge on the row-diff or revert); **(2) all-UIP shrinking** (Fleury & Biere SAT 2021 — a second pass after the existing recursive minimization: per decision-level block, highest first, resolve along the trail and replace the block with its level-local UIP when it collapses; bounded trajectory effect, disclosed from the start; predict post-min length −10–30%); **(3) tier retention** (Oh core/tier2/local replacing keep-better-half, LBD refreshed on use — today's LBD is static-at-learn-time; permanents/locked stay exempt; the biggest lottery, so last). Constraints inherited from ADR-0020: total integer orders everywhere, COUNT_MISMATCH 0/0 per stage, effort() = conflicts unchanged, instance re-pins disclosed, enumeration-seam correctness asserted per stage. **Close: ONE ADR-0017 paired-grid re-pair, then the mt-092 cross-run configuration as the final gate (any split = STOP THE LINE). Scored yield: base +5 / upside +12 at re-paired defaults; capacity 20 and fullsub2[0]/correctChord[13] explicitly out of scope.** Rejected in the ADR: chronological backtracking + lazy reimplication (most invasive, stage 1 gets the same goal safely), on-the-fly subsumption (overlaps existing minimization); vivification deferred to a post-stage-3 re-profile. Technique survey: sonnet delegate 2026-08-19 (parameter values machine-extracted — re-verify against primary sources at implementation).

- ✔ **mt-092** · North Star · **Family-D solver upgrade: attribute the own CDCL's wall gap, fix the measured terms, re-pair the defaults — DONE (agree 484→502)** ([ADR-0020](adr/0020-cdcl-clause-db-reduction.md) + its addenda; stages 0/1a/1b opus delegate 2026-07-30, tech-lead reviewed; re-pair + cross-run coordinator-run)
  **Bead closed 2026-08-19 — the deep-tail cross-run (ADR-0020 constraint 5, the last acceptance item) ran clean.** `backend-instrument --cross` over all 29 `over_budget` rows at the 250k×64M defaults, own arm 1M conflicts, CaDiCaL arm 900s wall, one translate per row: **exit 0, CROSS-DISAGREE 0, DISAGREE 0, self-check failures 0** (74 min at `--jobs 8`; artifacts `scratchpad/probe/mt092/cross-tail29-1M.{txt,json}` + NOTES). Own arm answered 5/29 — exactly the re-pair grid's five 1M conversions, so the instrument reproduces the sweep's trajectory through a separate harness — all five three-way agreements. CaDiCaL answered 22 of the 24 own-arm exhausts (unknown: fullsub2[0], correctChord[13]), every answer jar-matching where the jar has a verdict (correctChord[28] — the mt-090 row — answers SAT on the new trajectory; [22]/[25] answer where the jar's banking could not). Volume-lever case sharpened: 22 of 24 stuck rows are answerable within the same 1M conflicts on identical CNFs — the durable tail's barrier is search quality, not encoding.
  **Stage 0 done 2026-07-30** (`scratchpad/probe/mt092/NOTES.md`; predictions-first, accounting closed to 0.00% residue, probe overhead +0.7–0.8% at identical conflict counts). **Verdict: `propagate` is first-order, unanimous — 68.7/70.2/72.7% of wall** on ertms_1A[9]/elevator[0]/handshake[0]; memory-bound watch visits (12–24 ns each, 65–67% blocker-avoidable "other watch already true" arena round-trips). `pick_branch` second (10–19%; linear-scan model exact, decisions/conflict only 1.18–2.38); `analyze` third (11–17%); **reduce_db/restarts measured dead ≤0.2%**. Suspect 2 false as written — recursive minimization already exists (`minimize`/`lit_redundant`, 3.0× cut on ertms); its rationale survives as the volume lever (post-minimization lengths 34/74/126 drive re-propagation; visits ≈ trail pops × 3.3). **Blocker prototype measured: steady 1.13–1.46× per conflict, but it's a search change — conflict counts move 0.1×–1.5×** (handshake −90%, elevator +50%) and the budget is conflict-denominated. Prediction score 2/8 recorded (headline call — pick_branch — was wrong; third wrong attribution guess in the project, and the argument for the profile). **Stage 1a DONE 2026-07-30** (warm opus delegate, tech-lead reviewed the diff + independently re-ran the byte-identity sweep): two new modules — `als-solve/src/clause_arena.rs` (flat literal store + 16-byte headers, `ClauseRef` = header index so compaction moves literals never refs; `reduce_db` now closes with an order-preserving `compact` that keeps mt-049's "literals are freed" promise — never-reclaim was rejected on measurement, ~200 MB vs ~29 MB live on ertms; fused `watch_partner` = one header fetch for the read/swap/read that 65–67% of visits end at, swap kept unconditional because the watched pair's positions are load-bearing in `analyze`/`reduce_db`) and `als-solve/src/var_order.rs` (indexed max-heap on `(activity, Reverse(index))` — a no-ties total order, so the pop sequence is layout-independent and equals the old scan's argmax by construction; increase-key on bump, re-insert on unassign, full rebuild on the non-injective rescale). **Acceptance, all green: sweep sha256-identical to the mt-087 re-baseline (delegate + tech-lead re-runs, `211e7188…c266d4`); conflict counts EXACT on all three profiled rows (380,956 / 113,594 / 89,525); walls 1.54×/1.20×/1.15× (arena alone 1.15×/1.04×/1.04×, heap the rest); a pre-1a binary produces the same instance XML hash so constraint 6 never fires; 1,050 workspace tests pass incl. 16 new; clippy clean both feature settings.** Notable: the heap beat its 1.24× Amdahl bound on ertms (1.34×) — the O(vars) scan was also evicting `propagate`'s cache, so a per-decision sweep charges twice while a component-share profile bills it once (methodological lesson recorded in NOTES); an arena unit test caught a stale-offset tombstone panic pre-run; and an A/B measurement lesson joined mt-083's — this machine's absolute wall drifts ~1.5× over hours, so solver A/Bs interleave separate binaries with the old code as a live control. **Stage 1b DONE 2026-07-30** (same warm delegate; tech-lead reviewed + independently re-ran the sweep — byte-identical to the delegate's, `5d29605d…`, so determinism holds on the new trajectory): blocking literals in the watch scheme (`Watcher {cref, blocker}`; blocker = the other watched literal at install/refresh, a hint not an invariant — sound because "blocker true ⇒ clause satisfied" holds for any literal of the clause; new module-doc section separates determinism, intact and argued from first principles, from cross-version trajectory stability, never a contract and explicitly spent here). **Row diff at 100k×64M: net +2 (agree 484→486) — 5 defer→agree (firewire[4], both handshake[1]s, textbookSnapshotIsolation[0], HotelVar[0]) vs 3 disclosed agree→defer (firewire[1], elevator[4]/[22] — ALL recover at 250k with the jar's verdict); zero verdict flips, DISAGREE 0, self-check 0, panics 0. Count nets: count_match exactly 66/90, COUNT_MISMATCH 0/0** (the 5 skip-bucket moves all sit on rows whose verdict also moved — set-inclusion verified). A/B (1a as live control): handshake[0] **17×**, ertms_1A[9] 1.49× (cumulative mt-092: **2.29×**, top of the predicted 1.9–2.3× band), elevator[0] 0.73× — the stage-0 lottery, straight. Production blocker reproduced the stage-0 prototype's conflict counts to the digit (8,801/170,400/346,326). 1,053 tests, no goldens re-pinned (checked, not assumed). Sharpest finding: firewire[1] went 0.12s→over_budget — the lottery can defer a trivially easy row, the strongest argument for 1b's own row-diff. **Re-pair DONE 2026-07-30 (coordinator-run grid; [ADR-0017 third amendment](adr/0017-gauge-default-budgets-paired-frontier.md)): defaults → conflicts 250k × encode 64M — agree 502 (+16 over the 1b incumbent; +18 over ADR-0020's 484 start, beating the +13 base case), ZERO regressions, zero verdict flips, DISAGREE 0; wall 973s vs the 1,556s this exact point cost pre-mt-092 (mt-088's HOLD vindicated). 1M = 507 at 3,874s stays a per-run flag. All three stage-1b disclosed regressions recovered by the new default. Re-baseline verified: no-flag stage-1 row-identical to the grid point; SB-0 66 / SB-20 90, COUNT_MISMATCH 0 both (skip buckets shifted with the moved verdicts, count_match held exactly); sweep baseline recaptured at 250000/64M; pairing rule extended to solver search/wall changes. Remaining defer mass at the new defaults: over_budget 29 + capacity 20.** **The cross-backend deep-tail run ran clean 2026-08-19 (see the close-out paragraph above) and the bead is closed.** Future (not specced): the volume lever — learned-clause shrinking / tier retention — the only route to the deep tail per stage 0. **Then ONE ADR-0017 paired-grid re-pair** (mt-088 census JSONs banked; rescored prediction: +13 base rides on 1b; +23 retracted as a wall-side goal — the G2 band needs the volume lever, a separate future bead). Cross-backend deep-tail run (own arm 1M+, the mt-090-catching configuration) rides the grid chunk. Fallback if the grid disappoints: park per ADR-0020.

- ✔ **mt-087** · North Star · **Gate-level structural sharing + the remaining matrix kinds — the CNF-shape experiment that beat its forecast 17× and re-paired the defaults to 100k×64M** (delegated → opus implementation, tech-lead reviewed + ran the grid 2026-07-29; [ADR-0017 second amendment](adr/0017-gauge-default-budgets-paired-frontier.md))
  **Done 2026-07-29, two commits.** Implementation (`3cdad6b`): a **gate cache** in `circuit.rs` keys every conjunction by its canonical operand list (ascending dedup'd `Lit`s — ids only, `BTreeMap`), so a second request for a gate this encode already built reuses the auxiliary; disjunctions share the same namespace via De Morgan (clause-for-clause identical up to auxiliary polarity — the unification is free), and canonicalisation folds `x∧x`/`x∧¬x` en passant. An **`ExtKey` cache** extends matrix sharing to `Transpose`/`Ite`/`IntToAtom` (each keyed by exactly what its builder reads; the overflow-guard applicability is in the `IntToAtom` key so guarded/unguarded casts never share); `Comprehension` deliberately skipped — its only sound key is `(node id, env)`, which IS the existing memo. Budget honest (hit = free, miss unchanged). **The forecast was 4.6% clause reduction; the delivery was 79.9% on correctChord** ([0]: 64.0M→12.9M clauses; [30]: −62.3%, now VALID in 11 s at stock defaults; TransForm mc[15] −9.9%) — mt-086 scored only arena-duplicate residue, but the gate cache also collects every repeated gate a *single* traversal requests, the majority on seq/Int-heavy models. Immediate sweep effect at the old 25k×32M defaults: **+11 conversions / 1 disclosed recoverable regression (agree 448→458)**, DISAGREE 0, both count nets +1 and mismatch-free, sweep wall −70%. 4 new tests incl. whole-pipeline byte-identity; full gauntlets green. **Then the mandatory ADR-0017 re-pair, coordinator-run as a two-axis grid** (B=25k×32M incumbent 458 · A 10k: 450, 8 regressions · C 50k: 465 · D 100k: 476 · E 25k×64M: 466, exactly the 8 TransForm rows mt-086's thresholds predicted · **F 100k×64M: 484**, wall 699s): F's moves are exactly D∪E — the axes touch disjoint buckets, the corner is purely additive, zero regressions, DISAGREE 0 everywhere. D's 18 recover `ceilingsAndFloors[4]`, `life[1]`, `handshake[0]` (family-D rows now in reach of the cheaper conflicts). **Defaults → conflicts 100k × encode 64M** (`solve_gauge.rs`, second amendment): the corner costs the same absolute wall as the pre-sharing default (~650s) with +26 agreements. Re-baseline verified: no-flag stage-1 **row-identical to F**; **SB-0 count_match 66 / SB-20 90, COUNT_MISMATCH 0 both**; three-net battery ~37 min; sweep baseline artifact recaptured at 100000/64M via `--capture-sweep`. Campaign: **agree 448 → 484 (+128 since the census's 356)**; remaining defer mass over_budget 47 + capacity 20 (floor: correctChord[0..5] ~89M true spend, TransForm's 14 big rows ~190M+). Artifacts: `scratchpad/probe/mt087/` (implementation sweeps), `scratchpad/mt087grid/` (grid + re-baseline). **Probe round + fix for `util/sequence.als`'s copy of the mt-084 defect pair** (delegated → opus probes-first, tech-lead reviewed + re-ran load-bearing cells 2026-07-29)
  **Done 2026-07-29.** 145-row probe round (`scratchpad/probe/mt085/NOTES.md`, 7 probe files, predictions-before-runs) pinned `util/sequence`'s own semantics, then the fix — the module needed far more than the predicted pair. **Confirmed:** the vacuous-`in` boundary lookup (respelled as join equality in `rest`/`insert`) and `insert`'s `gte`→`gt` shift. **Refuted:** mt-084's `#elem = 1`-immune guess (the spurious boundary tuple breaks the contiguous-prefix sig fact at any element count) and the "other five preds should agree" assumption — all five diverged, none by index escape. **The unpredicted headline: the jar's `sequence` preds are PARTIAL** — `rest[empty]`, `add`/`insert` without room, overflowing `append`, out-of-range/inverted `subseq` are UNSAT where the clean-room bodies truncated/no-opped in the sequniv spirit (guards added: `some s.seqElems`, `some afterLastIdx`, end-to-end `append` fit, in-range ordered `subseq` window; `setAt` widened to accept the first free index; `append`'s offset fixed `#(ord/prevs[afterLastIdx])`→`#(inds[s1])` — the old form silently restarted `s2` at 0 on a full `s1`; `copy` is a ONE-WAY landing constraint, not an equality — two distinct dests satisfy one copy, jar-pinned). **Second surprise: `noDuplicates`/`allExist`/`allExistNoDuplicates` are 0-ary globals** (the jar type-rejects every receiver-style call; §7.5 already said 0-ary — the clean-room file had drifted): `allExist` = every sequence over (`SeqIdx`, `elem`) is realised by a `Seq` atom, pinned by exact-scope counting (SAT at the predicted count / UNSAT one below across three (k,n) shapes, e.g. exactly 15/14 Seq at 3 idx × 2 elem); `allExistNoDuplicates` carries `noDuplicates` as a real extra conjunct (the 5-dupfree+1-dup model is jar-UNSAT). Negative space documented in-file with probe rows: all 12 funcs already matched (36/36 values), sig fact + `canonicalizeSeqs` right as written, output uniqueness probed per-pred with `copy` the deliberate non-functional exception, and mt-084's index-escape defect is structurally impossible here (every binder is `j: SeqIdx`). 10 new probe-cited tests in `seq_conformance.rs`, each revert-verified (stash/run/restore: 10 fail pre-fix, 28 pass post-fix). Gates: als-types + als-core release gauntlets green, clippy/fmt clean; **full stage-1 sweep byte-identical before/after** — and the bead spec's "four corpus files open it" was wrong: **zero** corpus commands open `util/sequence` (the grep hit only the upstream module file itself, which has no commands), so the fix is fully latent. Tech lead re-ran the p5 exact-count discriminator pair and the p3 noDuplicates cell on both engines — all match. New latent findings filed in LIMITATIONS: mettle's resolver accepts receiver-style calls of 0-ary preds the jar rejects (→ mt-027 residual territory; the jar binds a receiver call to the immediately preceding atom); `fun/add` overflow exposure in `copy`/`append`/`subseq` index arithmetic at `SeqIdx` scope >~4 under bitwidth 4 (pre-existing, unprobed).

- ✔ **mt-080** · North Star · **Conversion campaign chunk 2: the family-C encode profile — duplication, not density, is the enemy; fix design decided** (delegated → opus profiler, tech-lead decided 2026-07-28)
  **Done 2026-07-28.** Instrumented encoder run to 32M exhaustion on `tso_transistency_perturbed_minimality_check.als[6]` (universe 71, closure = 65% of ops / 49% of spend) and `c11_perturbed.als[7]` (601,582 IR nodes for a 350-line model; product 70% of ops), each with a solving sibling for scale contrast; raw dumps + NOTES in `scratchpad/probe/mt080/` (gitignored). **Headline mechanism — the census's "re-grounds per binding" is confirmed but re-ranked: the memo is defeated because `lower.rs` re-lowers every pred/fun body per call site, minting fresh `RelExprId`s — identical *work* under distinct node ids, which the `(node id, env)` memo cannot see by construction.** TransForm: the same ~6 closure *values* are encoded 242 times (181 arena copies of `*co_pa` alone = 12.4M ops), every node `encodes==1`, zero env churn — a perfect per-node cache saves 3.3%, a structural *value* cache saves **71.3%**. c11: structural blindness is worth **78.7%** (scale-invariant — the solving sibling burns 20M of 32M the same way); the env-key strict-subset rule (free vars == env ⇒ uncacheable, inherited downstream) is real but secondary at 7.1%. Closure density: never decays because never sparse — every closure operand is 100% dense from round 0; per-closure cost is quadratic in sig scope (3,730 → 68,607 ops from scope 4→9) times a ~30× duplication multiplier. **Fix design (tech-lead decision): (1a) encoder-level structural value cache in `rel_uncached` — collision-proof key (full structural comparison or verify-on-hit; no lossy-hash equality), BTree-ordered, deterministic; (2) support-bounded closure iteration (`log2` of operand support, not universe — moves toward Kodkod's matrix-dimension bound; measured 28% of TransForm ops); (3) relax the memo's strict-subset env rule (very low risk, 7.1% on c11). Projected combined: both subject rows land ≈7–9M against the 32M ceiling. (1b) IR hash-consing deferred until 1a's sweep is green (span-merge diagnostics + DAG risks); level-variable acyclicity REJECTED (new primaries would break the SB-0 counting contract); dense matrices/mask-aware closure parked as measured non-payoffs.** Implementation = mt-081.

- ✔ **mt-079** · North Star · **Conversion campaign chunk 1: gauge defaults raised to encode 32M on the measured paired-knob frontier** ([ADR-0017](adr/0017-gauge-default-budgets-paired-frontier.md); delegated → sonnet for sweeps/analysis, tech-lead decided + authored docs 2026-07-28)
  **Done 2026-07-28.** The paired grid the mt-078 census called for: encode pinned 32M × conflicts {10k, 25k, 50k}, full corpus, `--jobs 8`, strictly sequential, each row-diffed against a same-day fresh defaults baseline (356 agree, 261s). **Frontier: A 10k/32M = 402 agree (+46) at 583s (2.2×); B 25k = +52 at 3.7×; C 50k = +60 at 7.3× — zero regressions and DISAGREE 0 everywhere; marginal conflicts buy +6/+14 agreements at superlinear wall.** The census's interaction flip does not reproduce with encode raised first (leader_events rows byte-identical across all four runs). **Decision (ADR-0017): defaults become `--encode-budget 32000000 --conflicts 10000`; standing pairing rule — never move one budget default without re-measuring the pair.** The 22 non-agree movers are bucket-neutral shuffle (correctChord + ertms_1A[9..13] + fullsub2[0] between capacity/over_budget/jar_nonverdict); correctChord stays blocked on deeper oracle-side banking as censused. Landed: `solve_gauge.rs` default + help + comment rewrite (the mt-049 "8M timed out past 40 min" finding predates the throughput campaign and no longer binds), sweep-baseline artifact re-captured under the new regime (header encode 32M). **Re-baseline verified: fresh no-flag stage-1 row-identical to grid A** (agree_sat 231 / agree_unsat 171, capacity 49, over_budget 81, jar_nonverdict 19, lowering 7, HO 4, unbounded_steps 2; 588s); **SB-0 count net: count_match 56, COUNT_MISMATCH 0** (skips: enum_budget 22, jar_timeout 94, mettle_cap 49, ho_skolem 9, ordered_abstract 1; 621s); **SB-20: count_match 79→84, COUNT_MISMATCH 0**, skip_temporal_config still 3 (619s). Full gauntlet green (fmt/clippy/test workspace-release, 0 failures). Grid + rebaseline artifacts: `scratchpad/mt078/grid-{A,B,C}.*`, `rebaseline-*.*` (gitignored). Next in census order: closure-encoding cost work (family C / TransForm + the c11 relation-param memo defeat).

- ✔ **mt-078** · North Star · **The capacity remainder, opening chunk: measured census of every capacity/over_budget defer row** (delegated → opus; tech-lead reviewed + filed 2026-07-28)
  **Done 2026-07-28.** Fresh default baseline: 564 commands in 261s at `--jobs 8`, agree 356, **DISAGREE 0**, capacity 117 + over_budget 62 = **179 defer rows across 35 files** (row-level table `scratchpad/mt078/mt078-census.txt`, gitignored). **Families:** **A 72** convert at L1 (tso_minimize 13, tso_minimality 12, ertms_1A 8, correctChord 7, c11 7…); **B 22** convert only at L2; **C 42** encode-bound even at 128M — the TransForm `tso_transistency_perturbed_*` pair alone is 36 (closure/acyclicity saturation over `Event` + ternary fields; every command re-grounds the whole memory model), correctChord 6 (seq-index arithmetic × triple-nested quantification × scope 10), with `c11_perturbed` pinning the sharpest mechanism: a **relation-valued predicate parameter defeats the mt-049 free-var memo**, so the axiom set re-grounds per binding; **D 20** search-bound even at 400k conflicts (handshake 8, lc-lenses 5); **D\* 4** probe-timeout-capped; **E 19** unprobed (>300s/command at L1 — breadth chosen over depth, unclassified-not-stuck). **Knob isolation (full-corpus sweeps):** encode-only 4M→32M = **+46 agreements at 2.2× wall (573s), zero regressions**; conflicts-only 10k→100k = +19 at 4.6× wall with ONE regression — leader_events[3] flips over_budget→capacity because more conflicts let the temporal step-sweep reach a longer length that blows the unraised encode ceiling: **the knobs interact; never move one without measuring the pair**. **Payoff caveat:** correctChord's 23 defer rows cannot become agreements at any mettle budget — the jar baseline for the file is a timeout marker; deeper oracle-side banking must come first. **DISAGREE 0 at every level probed, 0 panics, 0 self-check failures.** Instrument caveat recorded: 85 rows classified via per-command `mettle exec` against cached baselines (the six files that blew the L1 file cap), cross-checked against the gauge where both ran. **Attack order filed for the campaign:** (1) raise the default encode budget toward 32M after a paired-knob measurement, (2) closure-encoding cost work on the TransForm family + the relation-param memo defeat, (3) deeper jar baselines for correctChord and family-E probing, (4) family D deprioritized (solver-quality work, low yield).

- ✔ **mt-077** · R6 · **One-state temporal bounds answered: the `check … for 1 steps` fork decided B and implemented, jar-bug scope corrected** ([LEDGER-015](../SEMANTICS_LEDGER.md); owner decided "Do B" mid-review 2026-07-27; delegated → opus probes-first, tech-lead reviewed + docs authored at review)
  **Done 2026-07-27.** **Probe wave first** (`scratchpad/probe/mt077/NOTES.md`, cells P-077-1..5 + bisect B1–B6, predictions-before-runs): the deciding argument held — the jar solves the negated-`run` dual at 1 step, so the one-state semantics is jar-anchored — and the wave **refuted two wave-1 claims** in [reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(c): the NPE is *not* general to one-state `check`s (nine answered cleanly) and *not* isolated to `check` (three constant-folding `run`s NPE'd identically). Real trigger, empirically bisected: a temporal command at `maxtrace==1` whose translation constant-folds to a boolean (Pardinus dereferences a null non-`Whole` translation). **So the old blanket refusal was itself diverging on the majority subset — Option B increases conformance** and confines the deliberate divergence to inputs where the jar produces nothing. Implementation: the `Check && max == 1` early return deleted in `temporal_solve.rs` (+ the duplicate guard in `temporal_enum.rs`), `TranslateError::TemporalCheckAtOneStep` and its gauge bucket/classifier removed entirely; operator collapse at the self-loop verified both polarities (`always`≡`eventually`≡`after`≡`once`≡`historically`≡P, `until`≡rhs, `before`≡false, P-077-3). Tests 975→980 (probe-cited conformance incl. the jar-free negation-dual invariant over 5 goals; enum + exec end-to-end). Gauntlet green; **stage-1 sweep byte-identical** (zero corpus incidence). Cosmetic non-issue flagged honestly: at 2 states mettle's witness loops on state 0 where the jar's loops on state 1 — same verdict/length, STYLE-D1 self-consistency holds.

**The combined feature-complete review — DONE: BLESSED (owner, 2026-07-28; script + verdict record in [REVIEW-v0.1.0.md](REVIEW-v0.1.0.md)).** All parts passed: the Rung-5 install bar via the live brew channel, the Rung-6 temporal walk and the frontend on fresh-install-only demo models. Consequences: **ADR-0015 → Accepted, Rungs 5 and 6 exited** (ROADMAP status note), v0.1.1 cut with the mid-review fixes (favicon `d1d9047`, mt-077, the `-V` tracking string). The `check … for 1 steps` fork was decided mid-review (owner Option B, 2026-07-27 → mt-077 above); one finding (favicon 404) was fixed mid-review. Batched-gating cadence ends here — post-review work returns to ordinary chunk pacing; engineering gates unchanged throughout.

- ✔ **mt-070** · R5 · **Rung-5 remainder planned: instance-XML + Sterling + packaging contracts pinned, ADR-0016 Proposed, beads cut** (done 2026-07-27; three parallel sonnet recon/probe waves, tech-lead spec/review/load-bearing re-runs, ADR tech-lead-authored)
  **Done 2026-07-27.** **(1) The jar's instance-XML schema pinned** — new authority doc [reference/alloy6-instance-xml.md](reference/alloy6-instance-xml.md) (probe wave X-01..X-09b, `scratchpad/probe/mt070/`; harness disassembled `A4SolutionWriter` bytecode branch-exhaustively, then probed each branch live). Headline pins: `<instance>` block count ≠ `tracelength` when reachable zero-arg `fun`s have past-depth (`tracelength + extra·(tracelength − loopState)` blocks; `SimpleReporter.writeXML` **always** passes all reachable funs/preds as `macros` — the live path, which mt-064's own T-13/T-14 never saw because they passed an empty list); sig/field IDs lazy-memoized in touch order (byte-reconstructed); fields interleaved after their owning `</sig>`; `<types>` unconditional + `<tuple>`s only-when-nonempty; `String` the one builtin with `<atom>` children (quoted-spelling labels, XML-escaped); subset sigs top-level with per-parent `<type>` children; `enum` auto-injects a private `ordering/Ord` sig; skolem naming split anonymous `$var` vs named `$cmd_var`, macro skolems in a separate `m<i>` namespace; the flagged temporal×`sourceFiles` gap closed (no interaction; bonus: `util/integer` always in `sourceFiles`); round-trip + cross-process determinism confirmed. **Tech-lead verification: the six nominated load-bearing cells (X-01, X-03, X-04, X-06b, X-07, X-09) re-run fresh — all byte-identical.** **(2) Sterling's contract pinned** — new recon doc [reference/sterling.md](reference/sterling.md) (external-tool contract, NOT jar authority — the oracle jar ships zero Sterling assets, verified, so Sterling divergences are design choices, never conformance bugs). Live lineage = `sidprasad/sterling-ts` (what Forge ships; atdyer/alloy-js lineages stale since 2021-22). Protocol pinned from source: WS JSON `{type,version,payload}`, `data`/`click`/`eval`/`meta` + literal `ping`/`pong`; per-`<instance>`-element XML parsing (temporal traces natively understood); enumeration = provider-defined `click` strings (Forge convention `next-P`/`next-C`); eval = opaque `{id,datumId,expression}`→`{id,result}` (near-1:1 onto `ReplContext`). **Two load-bearing finds, both tech-lead re-verified against the clones:** Sterling's parser reads `backloop`/`loop`, never the jar's `looplength` (and Forge's `backloop` looks like the loop *index*, not the distance — pinned at mt-072's opening probe); and **no formal license exists anywhere in the Sterling lineage** (`package.json` MIT self-declaration only) → owner fork, ADR-0016 Decision 3 (rec: ask upstream, build meanwhile, embed on grant). Bundle: prebuilt release zips ~5.9 MB (mostly Monaco for a script view v1 doesn't need). **(3) Packaging surveyed** (memo; local claims verified: zero C deps workspace-wide, MPL-2.0, rustc pinned 1.97.0, flake dev-shell-only): recommendation = cargo-dist spine (Releases on 4 native-runner targets, shell/PS installers, tap, GitHub attestations) + flake `packages` output; Homebrew core out (notability bar), Windows deferred-not-foreclosed. **Time-sensitive find: crates.io `mettle` squatted 2026-07-26** by an unrelated crate (tech-lead re-verified against the crates.io API) → owner question, ADR-0016 Decision 5 (rec: skip crates.io for v1; channels don't need it). **ADR-0016 (Proposed)** routes all of it; beads mt-071–074 cut above. No implementation, no scorecard motion (planning chunk). Evidence: `scratchpad/probe/mt070/` (NOTES.md, XmlProbe harness + `rerun/` tech-lead reruns, sterling/ clones + README).

## Done (Rung 6 — temporal: "it does time"; implementation complete 2026-07-26/27, gate batched to the combined review)

Filed 2026-07-26, after the Rung-5 slice landed ([ADR-0014](adr/0014-rung5-repl-slice-before-rung6.md) sequencing). **Rung gauge (to be formalized in the Rung-6 ADR once the contract pins the shape):** the 22 `lower:temporal` defers become verdict agreements against fresh jar baselines, with every temporal construct either conformant or a typed defer. **Planning inventory (measured 2026-07-26):** the whole corpus temporal footprint is 4 files — `temporal/buffer.als` (5 cmds), `temporal/leader.als` (4), `temporal/leader_events.als` (4), `temporal/trash.als` (9) — and **none of the 22 has a banked jar verdict** (the mt-006/mt-037 captures excluded them), so a verdict-capture arm is part of the rung, not a given. Contract-first per the house pattern (mt-016/mt-043): no implementation until the authority doc pins the semantics.

- ✔ **mt-063** · R6 · **Temporal contract wave 1 PINNED — `docs/reference/alloy6-temporal.md`: discriminator, `steps` semantics, minimal-lasso verdicts, Pardinus architecture map, one genuine jar bug** (done 2026-07-26; probe → sonnet across a platform outage — the agent was killed by API 500s three times and resumed from its transcript each time; tech-lead spec/review + load-bearing-cell re-runs)
  **Done 2026-07-26.** The pinned facts a Rust implementer most needs: **(a)** the temporal discriminator is `isTemporalModel` = any reachable `var` sig/field OR any of the 11 temporal operators anywhere in facts+body (`CompUtil.java:189-201`); **(b)** `steps` semantics — default bound `[1,10]`; bare `for N steps` means the RANGE `[1,N]`, **not** exactly N (T-06: a model satisfiable at length 1 under `for 3 steps` returns a length-1 trace); `exactly N` desugars to `N..N`; the only legal open range is `1..` = unbounded, which the default bounded engine rejects with `ErrorAPI("Bounded engines do not support complete model checking.")` (T-08b — and two live `trash.als` commands hit exactly this, so the corpus exercises it); **(c)** verdicts — a bytecode-confirmed **incremental `for k in [min,max]` search returning the minimal SAT length immediately** (T-09/T-10b), so UNSAT honestly means "no counterexample within the bound"; **every** SAT temporal result is a lasso (`getLoopState()` always valid; even static instances are internally `TemporalInstance`); **skolemization is fully disabled under any temporal operator** (`Skolemizer.java:494-526`) and symmetry breaking runs per-state, skipping skolems (`SymmetryBreaker.java:213-232`); **(d)** the Pardinus translation map (static/variable relation partitioning, unrolling, loop encoding) deep enough to write the Rung-6 ADR without re-reading bytecode; **(e)** OracleShim drives all 4 corpus temporal files headlessly — 22 observed verdicts recorded in NOTES.md (buffer 2 SAT/3 slow UNSAT ~118s total; leader 3/1; leader_events 2/2 — diverging from leader on `liveness`, a real model difference; trash 7 verdicts + the 2 unbounded-rejection commands). **A genuine reproducible jar bug, pinned not guessed:** `check … for 1 steps` throws `ErrorFatal`/NPE (`Translation$Whole.cnf()` on null), isolated to `check` — `run … for 1 steps` is fine (T-10a/T-11). Filed in the Ledger's tracked-corners list as a genuine owner fork at implementation time (mettle cannot "match" a crash). **Tech-lead verification:** all six nominated load-bearing cells re-run under the nix JDK 21 — range-not-exact, unbounded rejection, minimality, bound-relativity, the NPE and its `check`-isolation all reproduce exactly. Evidence: `scratchpad/probe/mt063/NOTES.md` (710 lines, predictions-before-runs, `rerun_all.sh`/`rerun_corpus.sh`). **Waves 2+ filed as mt-064.**
  _Original filing:_ wave 1 pins **(a) the surface** — `var` sigs/fields, the full temporal operator set (`'` prime, `always`/`eventually`/`after`/`before`/`historically`/`once`/`until`/`releases`/`since`/`triggered`), which are already parsed (Rung 1 was temporal-first) but never lowered; **(b) `steps` scopes exactly** — `for N steps`, `for N..M steps`, the default when absent, what the bound *means* (trace length? unroll depth? searched incrementally or encoded once?), pinned by probes; **(c) verdict semantics** — SAT = a lasso trace within the steps bound (pin the lasso/back-loop rule and whether finite non-looping traces exist), `check` counterexample traces, `expect` interplay; **(d) translation architecture reconnaissance** — the jar bundles Pardinus (the temporal Kodkod fork): map `TemporalTranslator`'s unrolling (static vs variable relations, loop variables, prime at the loop edge, skolemization and symmetry under time) from source/bytecode the way mt-061 did for the GUI layer, far enough to shape the Rung-6 ADR's mettle architecture; **(e) the oracle harness check** — confirm `OracleShim` can drive temporal commands headlessly with pinned steps/options (the 4 corpus files are the probe subjects; their verdicts get banked in a later arm). Later waves (separate beads once wave 1 lands): trace instance rendering + XML, enumeration operators (New Config/Init/Fork/Trace), the REPL's per-state story (evaluator contract §4), counting semantics if any.
- ✔ **mt-064** · R6 · **Temporal contract wave 2 PINNED (T-13..T-28) + ADR-0015 Proposed — the contract phase is complete, implementation beads cut** (done 2026-07-26; probe → sonnet, tech-lead spec/review + load-bearing re-runs, ADR tech-lead-authored)
  **Done 2026-07-26.** Wave 2 extends `alloy6-temporal.md` with §(f)–(j): **(trace/XML)** one `<instance>` element PER STATE (not per trace); the loop encoded as `looplength = tracelength − loopState`, not a direct index; rigid sigs re-emitted every state; `A4Solution.toString(state)` jar-verified byte-for-byte against its source. **(REPL per-state — the finding that shapes mt-068):** `eval(expr, state)` NEVER throws on out-of-range state — `state ≥ traceLength` wraps through the loop, negatives clamp to state 0 (verified identically on the lenient Sig path, the strict Formula/Expression path, and the real GUI XML-round-trip path); all 11 temporal operators are legal direct evaluator input, evaluated relative to the given state, no special-casing. **(Enumeration — the decisive cell, T-19):** plain `next()` is NOT confined to the minimal trace length — it exhausts each length's raw solutions before advancing through `[min,max]` (why leader.als's count explodes for wide ranges); `fork(p)` holds states `0..p−1` fixed — New Init = `fork(0)`, New Fork = `fork(current+1)` (GUI wiring bytecode-traced). **(Counting):** the recorded leader.als SB-0 baseline reproduced LIVE (T-26: `count:1` ×2 + `unsat` ×1, exact match). **(Loose ends):** electrod solver id pinned (its solving honesty is NOT — reports a "possibly unsound" UNSAT; out of scope until unbounded matters). Two Pardinus-internal curiosities recorded open, not guessed (a MinLen single-solution anomaly; `fork(-1)` divergence across fixtures). **Tech-lead verification:** T-19 (length crossing) and T-22 (wrap/clamp) re-run under the nix JDK 21 — both reproduce exactly. **ADR-0015 (Proposed) written from the two waves:** bounded lasso solving as per-length unrolling over the existing CDCL (one deterministic encode+solve per k, first SAT wins, no cross-length incrementality — ADR-0013's measure-first logic), skolemization-blocked temporal lowering, per-state SB, typed defers for `1..`-unbounded and the check-at-length-1 jar bug (owner fork pending), unbounded/electrod out of scope disclosed. Implementation cut: **mt-065** (IR/bounds unrolling) → **mt-066** (LTL-on-lasso lowering) → **mt-067** (solve driver + verdicts) → **mt-068** (exec trace rendering + REPL state index) → **mt-069** (conformance arm: bank the 22 verdicts, gauge temporal bucket, jar-free probe-cell tests).
- ✔ **mt-065** · R6 · **Temporal IR & bounds: the static/variable relation partition + k-state unrolling + the lasso selector** ([ADR-0015](adr/0015-rung6-temporal-architecture.md) §1) (done 2026-07-26; opus delegate, tech-lead spec/review)
  **Done 2026-07-26.** `Relation` gains a closed `Mutability { Static, Variable }` enum (not a bool — R2a/R6), set at all six bounds-builder allocation sites from the resolved `var` flags; the flagged corner (remainder-of-var-parent) turned out **settled by jar source**, not an assumption — `BoundsComputer.java:194` passes the parent's own flag, and the jar pins the union rigid with `always (sum' = sum)` (`:206-207`, mt-066's business); fields follow the field's flag (`:448`), `util/ordering`/builtins/string-singletons/skolems static. New `als-core::temporal`: `unroll(ir, bounds, k)` → `UnrolledBounds` (relation-major RelId allocation — copies contiguous per original, order documented because RelId order IS variable numbering; per-state copies named `name@s`, collision-proof since `@` can't occur inside an identifier; originals **dropped** from the unrolled bounds, the bridge map `states: BTreeMap<RelId, Vec<RelId>>` the only handle) + `LassoSelector::mint` (k fresh vars, ALO + pairwise AMO matching the encoder's shape). New `als-types::is_temporal_model` — the pinned §(a) discriminator with the two reachability scopes separated (var half whole-world, operator half per-command free-facts+body), explicit-worklist walk, no dispatch wiring (mt-067). **Inert by construction and by measure: full stage-1 solve-gauge sweep before/after = BYTE-IDENTICAL** (fresh both sides). 37 new tests (13 unit + 24 jar-free conformance incl. the inertness property itself); workspace gauntlet green (fmt/clippy -D warnings/795 tests). Six source-cited-but-unprobed corners recorded in [reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(k) for **mt-069** (macro-body visibility is the one genuine divergence risk needing a probe).
- ✔ **mt-066** · R6 · **Temporal lowering: LTL-on-lasso translation of the 11 operators + prime** ([ADR-0015](adr/0015-rung6-temporal-architecture.md) §2) (done 2026-07-26; opus delegate, tech-lead spec/review; 8-fixture/45-cell probe wave, every prediction held)
  **Done 2026-07-26.** The headline: **ADR-0015's "honest prefix" shorthand for past operators was refuted by live probe P-D2** — the jar evaluates past against the true lasso history — so mettle implements the jar: lowering happens over a **logical timeline** (`[0..k) ++ [l..k) × d`, `d` = past-nesting depth, sufficiency argued in the module doc; forcing `d=0` fails exactly the three past-under-future tests), with the loop index split **once above the whole goal** (`⋁_l (LoopIs(l) ∧ Goal_l)`) since the timeline's shape depends on `l`; temporal-free subtrees memoised per physical state across branches. New `als-core::temporal_lower::lower_temporal_command` (reuses the entire static walk via `lower_command_keeping_temporal` — one flag, no duplicated walk, agreement asserted by test) + `FormulaKind::LoopIs` (encoder maps it to the mt-065 `LassoSelector`; typed error in eval — temporal self-check is mt-067's shape) + `solve_temporal_goal` (one fixed length; selector minted after primaries so numbering is stable) + the `var`-hierarchy facts the static bounds builder can't emit (union rigidity `always (sum'=sum)`, the `[electrum]` subsig-migration ban) + the probe-confirmed per-conjunct `always` seam (top-level facts/body = state 0 only; field-decl/appended facts always-wrapped iff temporal). All 12 expansion equations + the conjunct seam + the structural constraints pinned in [reference/alloy6-temporal.md](reference/alloy6-temporal.md) **§(l)** (45 cells; also closes §(k) item 6 — temporal skolems exist and are rigid). **Inert: stage-1 sweep byte-identical before/after.** 36 new tests (31 conformance + 5 unit), every constant citing its probe id; workspace gauntlet green. Handed to mt-067: the k-sweep driver, per-state SB, steps-range widening, temporal self-check (needs the solved loop state), encode-size measurement at large k.
- ✔ **mt-067** · R6 · **Solve driver & verdicts: incremental `for k in [min,max]`, minimal-SAT-wins, typed defers** ([ADR-0015](adr/0015-rung6-temporal-architecture.md) §3) (done 2026-07-26; opus delegate, tech-lead spec/review; the dispatch bead — the sweep delta was the acceptance and every row is accounted)
  **Done 2026-07-26.** `als-core::temporal_solve::solve_temporal_command` — the one shared driver (exec + gauge): ascending k sweep, unroll → `lower_temporal_command` → encode+solve per length, first SAT = the minimal k-state lasso (`TemporalTrace`: per-state `Instance`s keyed by **original** RelIds + `loop_state`, statics/skolems verbatim per state); range exhausted = UNSAT-within-bound; a budget/capacity outcome at ANY length is the whole command's outcome (never skip an inconclusive length). `steps` widened faithfully (`StepsScope{min,max}` = the jar's `minprefix`/`maxprefix` with `resolved()` reproducing `ScopeComputer.java:474-493` literally, incl. the inverted-range clamp); jar-exact rejects: `N..` at N≠1 (resolver), `steps`-on-static (scope phase, T-03 text), `1..` → "Bounded engines do not support complete model checking.", `check … for 1 steps` → typed defer naming the pinned jar NPE (**ledgered owner fork now DUE — surfaced to owner 2026-07-26**). Per-state SB falls out of the unrolled representation (partition refinement-neutral by test; skolems dropped under temporal per `SymmetryBreaker.java:231-232`; relation-major lex bit order = disclosed verdict-neutral divergence from the jar's state-major). Temporal self-check wired (`Evaluator::with_loop_state`). **STOP-THE-LINE side catch: a pre-existing STATIC wrong-verdict bug** — subset-sig `one`/`some`/`lone` multiplicities were never emitted (`emit_prim_constraints` is prim-gated); 10-cell jar probe wave (scratchpad/probe/mt067/, all predictions held) pinned it incl. the `=`-subset case → `emit_subset_multiplicities` runs off the denotation (`BoundsComputer.java:473/477/479`); zero corpus rows moved (no static corpus command exercised it), 5 regression tests. **Gauge delta (tech-lead re-run, fresh both sides): exactly the 22 temporal rows moved — agree_sat 195→201, agree_unsat 148→153, jar_nonverdict 16→18 (buffer, the jar-timeout file), over_budget 55→62, `temporal:unbounded_steps` 2 — DISAGREE 0, 0 panics, 0 self-check failures; every solved verdict matches its wave-1 jar observation.** 32 new tests; gauntlet green. Correction banked for mt-069: `baselines/alloytools-models-verdict.json` already holds real jar verdicts for leader/leader_events/trash — only buffer.als needs fresh banking. Handed to mt-068: pinned trace rendering (exec prints a one-line trace note today), REPL per-state (typed refusal today), exec default budgets (a wide unbudgeted `steps` range grinds).
- ✔ **mt-068** · R6 · **Surface: exec trace rendering + REPL per-state** ([ADR-0015](adr/0015-rung6-temporal-architecture.md) §4) (done 2026-07-26; opus delegate, tech-lead spec/review; one refuting probe — the eval-state contract corrected in place)
  **Done 2026-07-26.** `mettle exec` renders temporal SAT as the jar's pinned `toString(state<0)` shape — `---Trace---`, one `------State N-------` block per state with `(loop)` on the back-loop target, rigid content re-emitted whole per block (§(f), T-13/T-14); inside a block the lines are the unchanged mt-036 static renderer, so static output stays byte-identical and the two paths cannot drift (LEDGER-012 posture: shapes exact, tuple order mettle's live order; label spelling divergences recorded in LIMITATIONS). REPL/`--eval` on a temporal command now evaluates per-state (`:state N` meta-command — colon namespace because `state` is a legal sig name; `--state N` one-shot; banner names k and the loop target once). **Probe P-068-1 refuted the pre-normalization reading of §(h):** the jar's `eval(expr, state)` evaluates at **logical time on the infinite lasso** — present values look wrapped (T-22 is the consequence), futures are pass-invariant, **pasts accumulate across loop passes**; only the GUI's arrows normalize first. Same true-lasso-history family as P-D2. mettle implements the jar: `eliminate_fragment_at_state` reuses the mt-066 timeline machinery verbatim at the CONCRETE solved loop target (no LoopIs split, no second LTL semantics), pass capped at the fragment's exact past-depth (bounds `:state 10^12` to instant); contract §(h) extended in place + probe-table row. `TemporalTrace` carries `TraceArtifacts` (unrolled view + flat instance + goal — irreproducible post-solve: re-lowering would mint unassigned skolems). exec stays deliberately unbudgeted (jar drop-in). **Inert: stage-1 sweep byte-identical.** 24 new tests (incl. all nine P-068-1 cells jar-free); gauntlet green (884 passed). Handed to mt-069: temporal operators at a *static* command's prompt stay a typed defer (jar behavior unprobed — needs a cell).
- ✔ **mt-069** · R6 · **Conformance arm: bank the 22 temporal verdicts, gauge temporal bucket → comparisons, probe cells → jar-free tests** ([ADR-0015](adr/0015-rung6-temporal-architecture.md) §5) (done 2026-07-26; sonnet delegate, tech-lead spec; two STOP-THE-LINE finds — **both FIXED 2026-07-27** in the same-session follow-up below)
  **Follow-up fixes (2026-07-27, tech-lead decision: fix now; executed by the warm mt-065/mt-067 delegates in parallel, disjoint files).** **(K4) Macro-body visibility:** `als_types::temporal` now follows used macros through the recorded `ChoiceTable` (`MacroChoice` — the same seam `lower::replay_macro_*` expands through, so resolution is never re-derived; frame-carrying worklist, each body walked in its own module under its own choice table, `BTreeSet<MacroId>` dedupe = cycle guard + linear bound, called preds still non-descended per K1). K4a/K4b repro flips to jar behavior end-to-end; 4 new tests (nested macro-in-macro, args-position operator, unused-body = marked assumption). **(L6) `exactly N..M steps`:** one match arm collapses to `N..N` discarding `M` (the jar's Command-construction behavior); the wrong-verdict class demonstrated closed by a solve-level pair test (SAT at `2..4 steps` vs UNSAT-within-bound at `exactly 2..4 steps`); `exactly N..` open-range left unpinned-and-unchanged, assumption-commented. LIMITATIONS/Ledger/§(k)/§(m) flipped open→FIXED with probe ids; **sweep byte-identical vs the post-banking baseline (both zero corpus incidence); full workspace gauntlet green (55 binaries)**.
  **Done 2026-07-26.** **(1) `buffer.als` banked per-command**: `scratchpad/probe/mt069/PerCommandProbe.java` reruns `OracleShim.runOne`'s exact schema for ONE command index per JVM launch (`OracleShim` itself always runs a whole file under one timeout — per-command banking means a slow command can never blot out the verdicts before it), 1800s cap each (none needed more than ~90s); all 5 verdicts matched the mt-063 wave-1 observation exactly, merged into `baselines/alloytools-models-verdict.json` byte-shape-preserving (totals `commands` 234→239, `timeouts` 1→0). Stage-1 sweep delta, fresh both sides: `agree_sat` 201→203, `jar_nonverdict` 18→16, nothing else moved, `DISAGREE` 0 before and after. **(2) The probe debt, all nine cells live-probed** (`scratchpad/probe/mt069/NOTES.md`): K1 (non-descent into a called pred), K2 (appended sig fact excluded), K3 (`;` is temporal) all **CONFIRM** exactly as source-cited (K1/K2/K3 already had jar-free tests in `temporal_conformance.rs` — comments updated from "flagged, unprobed" to probe-confirmed citations); L7 (static `disj` group on a `var` sig) and L8 (`BoundsComputer.size`'s var-sig witness shape) both **CONFIRM** — attempted constructions, no divergence found, matching the doc's "narrow"/"masked" characterization now on the strength of a real attempt; M068 (a temporal operator at a STATIC command's evaluator prompt) — the jar answers fine over its degenerate length-1 self-loop trace, no throw; mettle's typed refusal there is a conservative capability gap, not a wrong answer, so **not** an escalation. **Two cells REFUTE mettle's shipped behavior — STOP-THE-LINE, left unfixed per this bead's ground rules:** K4a/K4b — the jar expands a `let` macro's body *before* `CompUtil.isTemporalModel`'s scan, so a macro whose body has a temporal operator (used from a fact or a command body) makes the command temporal; mettle's `als_types::temporal::expr_has_temporal` walks the surface AST, where a used macro is an opaque `Name` (the same non-descent rule K1 confirmed for called preds/funs), so mettle misclassifies such a command as static, rejecting a `steps` scope the jar accepts and solves — verified against the built binary, zero corpus incidence. L6/L6b — the jar silently collapses `for exactly N..M steps` to `N..N` at parse time (`Command.toString()`/`getMinTrace`/`getMaxTrace` all confirm), discarding the written `M`; mettle's `als_types::resolve::members::steps_scope` keeps the full `[N,M]` range regardless of `is_exact`, searching *wider* than the jar — a genuine wrong-verdict-class risk (a model SAT only at the wider lengths would flip mettle's answer), zero corpus incidence. Both filed in [LIMITATIONS.md](../LIMITATIONS.md), [SEMANTICS_LEDGER.md](../SEMANTICS_LEDGER.md)'s "Corners that NEED entries", and [reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(m) — awaiting a tech-lead decision (fix now vs. ledger-and-defer), neither `als_types::temporal` nor `als_types::resolve::members::steps_scope` touched. **(3) Temporal counting posture verified, not relitigated:** every SAT temporal command stays typed `skip_temporal_trace` unconditionally, confirmed live against `leader.als` (whose SB-0 baseline holds a real jar count, T-26) — `agree_sat 3` / `skip_temporal_trace 3` / `COUNT_MISMATCH 0`; new jar-free regression `crates/als-conform/tests/solve_gauge_integration.rs::leader_als_stays_skip_temporal_trace_despite_its_real_count_baseline`. **(4) Deep-budget spot-run** on the 7 temporal `over_budget` rows at mt-050's L1 (100k/32M/200k) and L2 (400k/128M/500k) levels, `--only temporal`, both runs completed naturally within the 3600s cap, **`DISAGREE 0` at every depth**: **4 of 7 converted to real agreements** — `leader_events.als[3]`/`trash.als[1]` at L1, `leader.als[2]`/`leader_events.als[2]` at L2 — **3 remain over_budget even at L2** (`buffer.als`'s three commands, the file ADR-0015 itself flags as the temporal encode-cost outlier); every converted row matches its wave-1 jar-observed UNSAT. Full per-row table in `scratchpad/probe/mt069/NOTES.md`. The default-budget scorecard's `over_budget 62` is unaffected (a diagnostic spot-check, reported separately per mt-050's precedent). **(5) Test audit:** every load-bearing probe-table cell has a jar-free test except the deliberately-unimplemented enumeration operators (T-15–T-21, ADR-0015 consequence 4), the out-of-scope electrod cell (T-27), and the two STOP-THE-LINE cells (deliberately untested, per the escalation rule). 2 new tests (`expect` on a temporal command; `leader.als`'s counting posture) + 3 test-comment updates; **886 workspace tests, 0 failures**; gauntlet green (`fmt --check`, `clippy -D warnings`, `cargo test --workspace`).
  _mt-064's original filing:_ The second (and intended last) contract wave before implementation beads are cut, picking up `alloy6-temporal.md`'s "Unpinned / next waves" list: **(a)** how a trace instance renders — the XML shape for temporal solutions (states, the back-loop index), what `A4Solution.writeXML` emits per state, and what exec's text rendering should show (the jar GUI shows state-by-state with the loop marked); **(b)** the enumeration operators (New Config / New Init / New Fork / New Trace — which the gauge and `exec` need to understand even if only to defer typed); **(c)** the evaluator's per-state story (evaluator contract §4: `setCurrentState` call sites, `state` validity bounds, whether temporal operators are legal evaluator input) — this is what makes the mt-062 REPL a trace debugger; **(d)** counting semantics under temporal scopes, if the jar defines any for the SB-0 net (leader.als HAS an SB-0 count-baseline entry — pin what the jar counted); **(e)** loose ends wave 1 flagged: the electrod solver-name cell, the check-at-length-1 bug's root cause (only if cheap — the behavior is pinned, the mechanism is curiosity), `minprefix=-1` vs explicit-1 equivalence. **Deliverable:** wave-2 sections in the authority doc + **ADR-0015 (Proposed): the Rung-6 mettle architecture** — trace-unrolled encoding on the existing CDCL/enumeration machinery, the steps-scope surface, verdict/defer taxonomy, and the implementation bead cut. After this, implementation starts.

## Done (Rung 5 slice — the evaluator REPL; [ADR-0014](adr/0014-rung5-repl-slice-before-rung6.md)) — landed 2026-07-26

- ✔ **mt-061** · R5 · **The jar evaluator's contract is pinned — `docs/reference/alloy6-evaluator.md`, 49 probe cells + bytecode/source citations** (filed + done 2026-07-25; probe → sonnet, tech-lead spec/review + full-battery re-verification)
  **Done 2026-07-25.** The GUI console's **actual** code path traced end-to-end (`OurConsole.do_command` → `SimpleGUI$6.compute`, via `javap` bytecode — the GUI layer ships no source): the evaluator **re-parses the module from the solve's written XML** (frozen embedded sources, not disk), reconstructs a fresh `A4Solution` via `A4SolutionReader`, registers **every atom and skolem name as an ordinary module global** (that is the whole mechanism behind `A$0`/`$foo_x` resolving — no special syntax), wraps user input as **`run { <input> }` through the ordinary pred-body grammar** (one slot for formulas/expressions/comprehensions/`let`/arithmetic), and evals with a state index. **Load-bearing facts for mt-062:** `plus[3,4]` is `Int`-**set**-typed and renders `{7}` — only `#`/`sum` return a bare numeral (and as a Java *String*: the source's own javadoc claiming Integer/Boolean is stale, caught against bytecode); bitwidth/maxseq inherit from the solved command (proven by a boundary-crossing cell under two `Int` scopes); **noOverflow is a no-op in eval position — arithmetic always wraps silently** (the source's `" (OF)"` suffix could not be triggered in three overflow shapes; filed unpinned, prescription = silent wrap until a real model surfaces it); UNSAT never reaches eval through the GUI (`writeXML` refuses; `resultUNSAT` bytecode never calls it). **The universe-order divergence is flagged as mt-062's one explicit choice point:** the XML round-trip reorders `univ` (atoms then `-1..-8, 0..7` ints) vs the live solve order — semantically irrelevant, scorecard-invisible, to be decided and ledgered at implementation, not silently inherited. §5 records the port shape (register atoms as globals directly against the live instance; do NOT replicate the XML round-trip). §7 records five honest unpinned corners. **Tech-lead verification:** the full 49-cell battery recompiled + re-run inside the nix shell's JDK 21 (the agent's box had only JDK 26) — all 25 distinct output lines byte-identical across JDKs; the nominated load-bearing cells (E-13/14, E-26/27, E-34/35, E-36, E-03/45/46) individually checked against the doc's claims. Evidence + re-run log: `scratchpad/probe/mt061/NOTES.md` (`probes.sh` reruns everything in one shot).
- ✔ **mt-062** · R5 · **The evaluator REPL shipped — `mettle exec --repl` / `--eval`, implemented from the pinned contract; the Rung-5 slice is complete** (done 2026-07-26; impl → opus, tech-lead spec/review + sweep-hash and hands-on verification)
  **Done 2026-07-26.** `mettle exec <file> --repl [--command N]` solves one command and drops into a `> ` prompt evaluating any Alloy expression against the instance; `--eval <EXPR>` (repeatable) is the non-interactive form that makes the acceptance battery honest. **Five additive seams, one per crate boundary:** `als-syntax::parse_fragment` (the contract's single-grammar-slot trick — input wrapped as a `run { … }` body through the ordinary parser, so formulas/expressions/comprehensions/`let`/arithmetic share one slot and declarations reject as plain parse errors); `als-types::resolve_session`/`ResolvedSession::resolve_fragment` (same `Resolver::run` pipeline and verdict, but keeps the symbol tables `finish()` discards — `param_sigs` is not reconstructable from a `ResolvedWorld` — with fragment `ExprId`s in their own arena + `ChoiceTable` so nothing collides); `als-core::lower_fragment` (lowers with skolemization blocked outright — the instance is fixed, and an HO decl becomes exactly the jar's "Higher-order quantification is not allowed in the evaluator."); `crates/mettle/src/repl.rs` (`ReplContext`/`eval_input`/render — render-agnostic core, loop and `--eval` as thin drivers); exec split so solve artifacts survive into the REPL. Atom names (`A$0`) are fresh exactly-bound singleton relations appended to `Ir`/`Bounds`/`Instance` — the `Evaluator` reads them like any relation; skolems bind to the relations the solve minted; eval runs `allow_overflow: true` unconditionally (contract: noOverflow is a NO-OP in eval position — verified live: farmer.als `#State` at 8 States/4-bit prints `-8`, the jar's own silent wrap). **LEDGER-012 approved at review (tech lead, Option B, flagged as a deliberate fork not a no-fork):** tuple order = mettle's live solve order, not the GUI console's XML-round-trip artifact — both orders are jar-observed from two jar code paths, sets identical, scorecard-invisible, contract §5 recommends it; reversible if the owner ever wants console-exact bytes. Honest edges in LIMITATIONS (E-22's weaker message; universe atoms named even when sig-unclaimed; scope-outs: enumeration, temporal state indices, readline, Sterling). **Verification:** 33-cell probe battery in `crates/mettle/tests/repl.rs` reproduces the mt-061 cells (E-37..E-42 byte-identical to the jar; univ cells pinned in live order cross-referencing LEDGER-012); 758 workspace tests + fmt + clippy green; **stage-1 sweep hash `c77ef8ce…` reproduced by the tech lead on the final tree** — the shared-crate seams are provably inert; hands-on farmer.als session exercised ordering-module names (`first.near`), quantified formulas, and the caret-diagnostic error path.

## Done (Rung 4 — integers, coverage & counting) — exit gate blessed 2026-07-25 ([ADR-0012](adr/0012-rung4-integers-strings-counting.md) §7)

Filed 2026-07-18 after the Rung-3 owner touchpoint. **Rung gauge (proposed; ADR-0012 at mt-043 formalizes it, owner blesses the gate):** solve-gauge **0 verdict disagreements with every defer bucket accounted for** — the only remaining typed defers are temporal (Rung 6), the jar's own errors (higher-order parity, already holding), and an honest small capacity/budget remainder; the counting net loses its `skip_fo_skolem` family (mt-047) and the mt-041 mismatches. Scorecard motion: baseline overlap climbs from 187/392 agree toward "everything non-temporal agrees." Per mt-027's standing rule, any bead touching type machinery notes its effect on the 314 alloy4fun over-accepts.

**Planning inventory** (from the mt-037 gauge over 564 corpus commands): `lower:lowering` 217 (≈140 = mt-040 recording gaps, 39 = integer-ordering builtins → mt-044, remainder seq/arithmetic/small buckets → mt-044/046), `capacity` 71 + `over_budget` 37 + `encode` 14 (→ mt-049), `temporal` 22 (Rung 6), `scope` 10 (String → mt-045), `higher_order` 4 (jar errors — already at parity). Counting skips: `fo_skolem` 55 (→ mt-047), `mettle_cap` 23 (→ mt-049), `COUNT_MISMATCH` 3 (→ mt-041).

- ✔ **mt-043** · R4 · Pinned Rung-4 semantics contract + ADR-0012 (delegated → opus, tech-lead reviewed; probes I11/S2/K4 independently re-run against the jar at review — all confirmed)
  Done 2026-07-18: [reference/alloy6-translation.md](reference/alloy6-translation.md) **§11–§16** + probe matrices **§10.7–§10.12** (~50 jar probes, sources at `794226dd`, both overflow settings): integer arithmetic at bitwidth (div toward zero, rem sign-of-dividend, `<<`/`>>`/`>>>` = logical-left/arithmetic-right/logical-right; **forbid mode is the Milicevic/Jackson polarity rule, NOT flat `∧ ¬overflow`** — decisive probe I11: `all n: Int | plus[n,7] >= n` is UNSAT allow / SAT forbid); `Int/next`/`Int/zero` are the only builtin Int relations translation actually references (`min`/`max` lower as int constants — simplifies mt-044); String = referenced-literal collection + **`"String%d"` padding** (the `unused%d` claim in earlier docs was wrong — that's instance-display renaming of sig-unclaimed atoms; LIMITATIONS/STATE corrected at review) + `max(N,#referenced)` expansion + non-exact-scope reject; `seq X` = `seq/Int -> lone X` + exactly one contiguity fact `dom(f)−dom(f).(Int/next) ⊆ Int/zero`; FO skolemization = depth-0 top-level effective-existentials, `$<cmdLabel>_<var>` naming, count effect pinned (`some x:A|x=x` for 3 = jar 12 vs 7); SB=20 = lex-leader length cap, never the verdict. **Ledger: LEDGER-006 (`#`) / LEDGER-007 (String) / LEDGER-008 (seq) at `verified` → to owner; LEDGER-005 (integers) `proposed`** with two named residuals (allow-mode `div[x,0]` sweep for zero/negative dividends; deep mixed-polarity forbid coverage) — closed at mt-044 start, before arithmetic ships. **[ADR-0012](adr/0012-rung4-integers-strings-counting.md) Proposed** (integer ops extend mt-033's encode layer with the mt-034 evaluator matched pair binding; String minting in scope; seq split bounds/lower; FO skolem extends mt-038's HO machinery, superseding ADR-0011's FO stance for Rung 4; SB posture; exit gate awaiting owner blessing).
- ✔ **mt-040** · R4 · Resolver recording gaps + `disj`-bound semantics (delegated → opus, tech-lead reviewed: gauntlet + solve-gauge + byte-identity all independently re-run, one misleading comment fixed, probe rows L27–L29 added to §10.3 at review)
  Done 2026-07-18. **(a) 0-param relation-valued `let` macros as spine bases** (`adjacent[s]`/`s.adjacent`) now record via `CandOrigin::Macro` + `flush_rec::record_zero_macro`; the same fix records **compound/call sub-exprs buried as a join's compound-right operand** (`fr.(w.*co_pa)`, `rf_pa.(Ghost[g])` — `RecNode::Sub(ExprId)` resolved standalone, recording-only) — together the dominant unlock (tso's 77-command `co_pa` cluster, ptcris's 20-command `similar`). **(b) callable-by-name HO-macro args**: `MacroChoice::callables` records the `FuncId` (conservative: unambiguous single overload only); lowerer binds `Binding::Callable` and inlines `param[args]` as the real call. **(c) `run`-target own-module priority: PINNED, not shipped** — 7 decisive probe pairs + sources confirm own-module-first (`getRawQS` before `getRawNQS`); **LEDGER-009 filed at `verified`** (awaiting owner approval), jar-pinned test `run_target_own_module_first` `#[ignore]`d ready to flip; the 2 commands stay typed defers. **(d) post-colon `disj` field bounds lower**: jar-pinned fact `all this,that: owner | this!=that implies no (this.f & that.f)` (§10.3 L27–L28, DumpK2 + decisive SAT/UNSAT pair, uniform across arity/mult); the **quant-decl `x: disj e` turned out to be a jar resolve error** (L29) — kept an honest typed defer instead of synthesizing a fact the reference forbids. **Gauge:** corpus lowering **311 → 443** (defers 243 → 111); solve-gauge **agree_sat 104→114, agree_unsat 83→86, DISAGREE 0**, `lower:lowering` 217→85, capacity 71→187 (big unlocked models hitting solver budgets — mt-049's business), 0 panics, 0 self-check failures. **Byte-identity: sha256 `00eb950a…` identical** over all 150,891 alloy4fun codes (agent twice, tech lead once) — the 314 mt-027 over-accepts unchanged by construction. Honest tail documented in LIMITATIONS: `$`-meta names, nested param-macro receivers, ordering `next`.
- ✔ **mt-044** · R4 · Integer fidelity end-to-end (delegated → opus impl + sonnet probe agent ×4 rounds, tech-lead reviewed with two mid-flight corrections + independent gauntlet re-run and 5 hand-probe verifications)
  Done 2026-07-19. **Semantics first:** LEDGER-005's residuals closed and then some — full 16×16 div/rem sweep (`div[x,0]=−sign(x)`; **`div[MIN,−1]=−8`, correcting mt-043's I8** — `(0-1)` was a probe-spelling artifact, only `(0-8)` hits the MINUS peephole), and a three-round forbid-mode investigation that first "found" two guard defects, then **retracted one**: `negate[8]` carries a permanent forbid-mode overflow flag (it computes `minus[0,8]`) that silently emptied every conjunction-shaped probe domain, mimicking a nesting-position defect. Final pinned rule is a single per-variable classification (universal-rescue iff the binder's own domain is literally bare `Int`/`seq/Int` — a sig-/comprehension-bound ∀ is *excluded*, `DefCond.isInt` defect, so the common `all p: Sig | #…` shape never rescues); shift semantics pinned (§10.7d: `⌈log2 w⌉` amount mask, `>>`/`>>>` never self-overflow, `<<`'s junk-bit spurious-overflow circuit). **LEDGER-005 `approved`** (tech lead, no-fork) with the jar-unpinned ITE/`implies` sliver carved out as a standing typed defer. **Implementation:** encoder circuits (incl. structural ports of Kodkod's non-restoring division and `shl` overflow loop) + evaluator matched pair, shared `overflow_guard.rs` classifier, `Int/next`/`Int/zero` bounds, `fun/min|max|next|prev` lowering, **LEDGER-009 shipped** (own-module-first, test live), `int_conformance.rs` (jar-pinned constants, CI-safe). **Gauge: agreements 243 → 248 (151 SAT / 97 UNSAT), DISAGREE 0**, 0 panics, 0 self-check failures, byte-identical ×2 + tech-lead re-run; the integer-builtin/arithmetic defer sites are gone (net bucket movement is small — formerly-deferring commands mostly still defer on other constructs or budgets; buckets: lowering 83, capacity 98, encode 15, over_budget 68). Honest new defers: 10 eq-typing commands (→ mt-051) + the ITE/`implies` sliver.
- ✔ **mt-051** · R4 · Int-equality typing rule pinned + implemented; the 10 eq-typing defers AND the ITE/`implies` sliver retired (probe → sonnet; impl → opus; two tech-lead verification probe rounds)
  Done 2026-07-21. **Semantics first (LEDGER-010, §10.7e):** the "jar int-compares one-sided `=`" hypothesis in this bead's own text was WRONG — the pinned truth is **set equality always** (int-compare only when both translated sides are `IntToExprCast`; `#priority = #pid + 1` is the union pitfall, forcing `#pid=#priority=1` as sets — peterson's UNSATs are bug-compatible set collapse, not arithmetic). Forbid mode has three pinned layers: **(A)** an overflowed capable cast denotes the **empty set** (jar cells are env-less `Int.eq` → per-cell `∧¬of`; polarity/context-independent), **(B)** the §10.7c rules-0–3 guard threads through the compared sides' set structure (`=`/`in`/mult-tests, unions included; bare-Int-∀ RESCUES — first jar-confirmed set-eq rescue; closed operands EXCLUDE), **(C)** a translation-constant cast contributes no (B) guard (the R-cardun/T5/T6 constant escape; int-compare path keeps constant flags). **Rule 4 pinned** (ITE either-branch + `implies`-antecedent escape = correctly-classified rescue; bare `!` is NOT an escape, probe V-not) — LEDGER-005's carve-out closed. **Review drama, filed in LESSONS:** the probe agent's 27 cells all reproduced but its proposed rule ("no dependency ⇒ no guard") was refuted by the existing mt-044 pinned tests + a closed-but-variable-circuit discriminating cell; the tech lead's own first cells burned on the `(0-2)` spelling artifact (the mt-044 `(0-1)` family), which surfaced that **mettle lacks the `0-(max+1)` MINUS peephole entirely → filed mt-052**; the R-cardun mystery bottomed out via `javap` bytecode checks + the `Int.java` env-less-`eq` discovery. **Implementation (opus, tech-lead reviewed; encoder/evaluator matched pair):** shared `collect_capable_casts` + `translation_constant` in `overflow_guard.rs` (both back ends use the same predicates by construction), cast-emptying at `IntToAtom`, guard threading at `RelCompare`/`MultTest`, `eq_typing_defer` + `GuardDecision::Defer` deleted; delegate caught the probe-spelling issue in the test models and respelled via `min` — tech lead re-pinned all six respelled cells against the jar before accepting. **Tests:** new `eq_typing_conformance.rs` (14 fns: Q1 value cells, rescue/defectA, closed D4/T1/T4/D6/D3a, constant-escape trio, T7 MultTest, full Part C family incl. V-not); `int_conformance.rs` defer row → rule-derived verdicts. **Gauge:** agree 276 → **289 (166 SAT / 123 UNSAT), DISAGREE 0** over 392 cached verdicts, **`encode` defer bucket eliminated** (14 → 0; capacity 113, over_budget 62), 0 panics, 0 self-check failures, byte-identical ×2; full workspace gauntlet green. money.als solves SEND+MORE=MONEY end-to-end (`mettle exec` SAT).
- ✔ **mt-045** · R4 · String support (delegated → opus, tech-lead reviewed: gauntlet + gauge independently re-run, worktree HEAD-baseline diff, fm2cfs exec smoke test)
  Done 2026-07-19. **New `als-core::strings`** ports `Command.getAllStringConstants`: referenced-literal collection over the command goal + all module facts + sig appended facts + field-decl bounds, recursing into **called** funcs/preds only (probe S7) via the same `als_types::choice` table the lowerer inlines from — collection and lowering can never drift. Scope/universe phase (`scope.rs::mint_string_atoms`) appends the quoted literal atoms **last** (after the ascending int atoms, §1.3), pads an exact `for … but N String` scope with **`"String%d"`** atoms (LEDGER-007; never `unused%d`), expands to `max(N, #referenced)` (probe S4); non-exact scope stays the pre-solve `StringScopeNotExact` reject (S1). Bounds phase binds `String` **exactly** to them + one exact singleton per referenced literal (the jar's `s2k` map, `BoundsResult::string_denote`); `ExprKind::Str` lowers to that singleton — **`TranslateError::StringUnsupported` deleted entirely**. Encoder + evaluator gained an explicit `int_end` (int atoms no longer run to the universe's end — string atoms trail them), a latent-assumption fix the matched pair needed. Deterministic literal order = lexicographic (jar's is HashSet-nondeterministic; string atoms are symmetric, verdict/SB-0-count neutral — the LEDGER-007 micro-choice). **Tests:** `string_conformance.rs` pins S1–S7 + solve-level (distinct-literal singletons, `#String` against exact scope, String-typed field columns); LEDGER-007's Test field filled. **Gauge (HEAD-worktree before/after at identical defaults): agree 248 → 257 (+6 SAT incl. fm2cfs's 6 formerly-wrong-UNSATs, +3 UNSAT), `scope` 10 → 0 (9 → agreements, 1 → over_budget), every other bucket identical, DISAGREE 0, 0 panics, 0 self-check failures, byte-identical ×2.** Note: gauge `jar_nonverdict` is 16 at current budgets (STATE's old "2" was the mt-037-era figure) — pre-existing, untouched by mt-045.
- ✔ **mt-046** · R4 · `seq` support (delegated → opus impl + sonnet enum-budget follow-up, tech-lead reviewed with two independent probe rounds and one clean-room body correction)
  Done 2026-07-19. **Implementation:** `seq X` field = `seq/Int -> lone X` (arity-3, index column upper-bounded by the `seq/Int` atoms) via `lower.rs::seq_arrow_constraints`, plus the **per-owner** contiguity fact `dom(this.f) − dom(this.f).(Int/next) ⊆ Int/zero` emitted inside the `all this: owner` wrapper — per-owner-vs-global pinned by decisive probe mt046-contig (§10.10a: owner with {1} and no {0} → jar UNSAT); expression-position `seq` keeps the marked-arrow typed-defer posture. **Clean-room stdlib verified differentially and corrected twice:** the agent found `idxOf`/`lastIdxOf` had their min/max closures swapped in all three seq modules (jar: `idxOf` = first occurrence); tech-lead review then re-probed the corner the agent's probes missed — **`afterLastIdx` is the smallest UNUSED index, not `lastIdx.next`** (gapped `{1->e}` → 0; full sequence → none; probes mt046-noncontig/-full/-seqrel-gap) — and rewrote the three bodies to the min-unused form (no empty-case hack). **Counting-net hardening (follow-up, sonnet):** `correctChord.als` (39 commands, 22 capacity + 13 jar-nonverdict-now-solving + 4 over_budget — it alone dominates sweep time) ground the SB-0 net for an hour via unbudgeted per-instance enumeration solves; added `SolveOptions::enum_conflict_budget` (cumulative, deterministic, conflict-counted) + `solve-gauge --enum-budget` (default 2M) ending in a typed `skip_enum_budget`, never a truncated count. **Tests:** `seq_conformance.rs` (15: Q1–Q4, per-owner contiguity regression, lone-value, stdlib direction + min-unused + add-to-empty regressions) + enumerator budget-exhaustion tests in `tests/solve.rs`; LEDGER-008 Test field filled. **Gauge:** agree 257 → **258, DISAGREE 0**, `lowering` 83 → 43 (the seq family gone: +1 agree, +13 jar_nonverdict, +22 capacity, +4 over_budget — big models hitting throughput budgets, mt-050's business), byte-identical ×2, 46/46 suites. **Counting net terminates:** count_match 15 → 27, COUNT_MISMATCH still exactly the 3 filed mt-041 rows, skips typed (fo_skolem 90, cap 28, ho 9, jar_timeout 1). Sweep wall-time is now ~25 min (chord-dominated) — parallelization is mt-050.
- ✔ **mt-047** · R4 · First-order skolemization (delegated → opus, tech-lead reviewed with two independent fixes + the runtime-observability campaign)
  Done 2026-07-20. **Implementation:** a top-level effective-existential FO decl (positive `some`, or a `check`'s negated `all`; never under a universal — K3) now mints a skolem **constant** relation via the shared `alloc_skolem` core (extends mt-038's HO machinery/`Pol` thread): lower `{}`, upper = `abstract_upper`, membership + `one` conjoined, `un.make`-style name uniquification; run-pred receivers/params skolemize the same way. **Naming jar-exact incl. inlined funcs** (tech-lead fix: `inline_stack.last()` supplies the §15 funcName prefix — `run foo { q }` with `some x` in `q` mints `$q_x`, pinned by `called_pred_existential_uses_func_label`). **Seam bug found at review:** `abstract_upper` only consulted the bounds builder's table, so a decl bounded by an *earlier skolem* (`check { all b: Book, n: b.names | … }`) silently lost its skolem — an SB-0 under-count (addressBook2e[3] 6744 vs jar; addressBook3b[3] 4920 vs 6024) and 5 spurious `higher_order` defers; one fix (also read `skolem_bounds`) cured all seven, regression `dependent_decl_skolemizes_through_earlier_skolem`. **Gauge:** agree 258 → **276 (163 SAT / 113 UNSAT), DISAGREE 0**, `higher_order` back to exactly the 4 jar-error-parity commands, over_budget 73 → 59 (skolemized goals solve easier), byte-identical sweeps, 47/47 suites. **Counting net:** `skip_fo_skolem` (90) **retired**; count_match 27 → **48**; COUNT_MISMATCH = exactly the 3 filed mt-041 rows; jar-pinned K1–K4 + NoEmpty=561 in `fo_skolem_conformance.rs` (12 tests). **Runtime-observability campaign (owner-directed, same chunk):** the enumeration budget went through two wrong denominations (conflicts, then +decisions) that each ground 3h before the right one — **effort = conflicts + decisions + propagation clause-visits** (new deterministic solver counters), default 250M (≈≤80s/command, `--enum-budget` to deepen; 51 typed `skip_enum_budget`); the gauge now emits a stderr liveness heartbeat + slowest-10 table; every long run carries a hard `timeout`. Full `--count` completes in ~85 min. Lessons filed (budget-denomination entry); mt-050 inherits the ptcris/chord slow-command table as its perf worklist.
- ✔ **mt-048** · R4 · Symmetry breaking (delegated → opus, tech-lead reviewed with three review probes + two review fixes; posture per ADR-0012 decision 6)
  Done 2026-07-21. **Contract-first:** `SymmetryDetector`/`SymmetryBreaker` at `794226dd` pinned in translation-ref §16.1–§16.4 (fetched to `scratchpad/src794/`): coarsest-partition detection, the per-`(class, adjacent-pair)` lex-leader with the **soft cap at relation boundaries**, `(arity, name)` relation order (`$`-skolems eat slots first), mirror/identity filters, trivial-circuit escape, `expect 1 ⇒ SB 0`. **Implementation (opus):** `encode/symmetry.rs` (signature-grouping partition — proven equal to Kodkod's on every bounds shape mettle emits, diagonal caveat documented; `SbpPlan`), `generate_sbp`/`lex_leq` in the encoder (Tseitin-only — primary vars untouched), `SolveOptions::symmetry` default **20**, expect-forcing in exec + both gauge stages, gauge `--symmetry`/`--count-symmetry`, `sb_conformance.rs` (9 jar-pinned probes incl. cap-sensitivity at SB=2/1). **Review was the story again:** (1) the pinned §16.1 "retainAll/usesInts" gating from the *static* source is a **dead letter on the jar's solve path** — probes Y6/uf1-SB20/fmrun show nothing is mention-gated and every int **and string** atom (padding included!) is always a singleton class (per-int exact bounds; per-atom `s2k` singletons, A4Solution.java:391–400); the missing string singletons were the root cause of the fm2cfs SB-20 family (6 mismatches → 5 matches + 1 typed cap-skip), and the dead `usesInts` walker was deleted; (2) the unmentioned-sig probe pinned full-bounds participation (SB-0 56 / SB-20 12); (3) a `univ`-field probe surfaced the **pre-existing mt-053** live-universe divergence (nothing to do with symmetry). **Final gauge (both sweeps byte-identical ×2, 0 panics, 0 self-check failures):** verdict net at SB-20 default — **agree 300 (166 SAT / 134 UNSAT), DISAGREE 0** (SBP resolved 18 formerly over-budget commands); SB-0 counting net unchanged (**49 matches**, mismatches = mt-041 ×3); new **SB-20 count net: 71 matches, 6 mismatches** = mt-041 ×3 + the filed mt-055 residual (tso ×2, wetdry ×1).
- ✔ **mt-049** · R4 · Throughput: solver `reduce_db` + env-cached grounding memoization + budget re-tune (delegated → opus, tech-lead reviewed + independently re-ran gauntlet; tech lead ran the re-tune measurements as tracked jobs after two detached agent runs died)
  Done 2026-07-19 (commit `20d0560`). **Solver:** deterministic learned-clause reduction — integer LBD ranking (lowest-`ClauseRef` ties), least-useful half tombstoned (ClauseRef-stable, unwatched, literals freed) on a cumulative-conflict schedule (first 2k, +300/round; `set_reduce_schedule` override); only solver-learned resolvents deletable — original/blocking clauses permanent, locked clauses kept; new `fuzz_with_forced_reduction` (reduce on ~every conflict) over the 30k-instance brute-force net: verdicts, enumeration counts, determinism all invariant. **Encoder:** memo caches re-keyed `(node, bindings of exactly its free vars)` via new `encode/freevars.rs` (per-node free-var sets, BTree-ordered); sub-exprs not mentioning the innermost binder encode once, gates shared across bindings; full-env nodes deliberately uncached; primary-var model set unchanged (encoder↔evaluator differential + self-check green). **Budgets (measured):** conflict default 5k→10k (sweep 15:45 wall); **encode ceiling stays 4M** — 8M timed out >40 min, 24/50M grinds single commands for 8.5+ CPU-hours (the mt-037 grind mode; a 30k/24M probe was killed at 8.5 CPU-hours with zero output). **Gauge at new defaults: agree_sat 142, agree_unsat 101 (= 243 agreements, was 200 at mt-040), DISAGREE 0, capacity 187→99, over_budget 69, 0 panics, 0 self-check failures, byte-identical twice.** Note: the gauge's `capacity` bucket = the *encode-budget* family (`CapacityExceeded`), not the 20k primary-var cap (that bucket is 0 corpus-wide). Honest remainder in LIMITATIONS: capacity 99 + over_budget 69, each solvable at larger per-run budgets via flags.
- ▢ **mt-052** · R4 · MINUS peephole: `0-(max+1)` in expression position is `IntConstant(min)` in the jar (filed at mt-051 review; live wrong-verdict candidate)
  `TranslateAlloyToKodkod.java:1239-1248` (commit `794226dd`): `case MINUS` special-cases a literal-`0` LHS with a literal-`max+1` RHS to `IntConstant(min)` — everything else stays set difference. mettle treats `0-8` (bw 4) as `{0}−{…} = {0}`, so `fact { F.v = 0-8 } run { F.v = min }` is mettle-UNSAT vs jar-SAT (verified at mt-051 review, `scratchpad/probe/`). Small fix in lowering's `-` handling; needs the command bitwidth at that point plus a jar-pinned test pair (in-range `0-2` must STAY set difference — the mt-044 `(0-1)` artifact family). Zero corpus incidence at current gauge (DISAGREE 0), so opportunistic.
- ✔ **mt-054** · ops · **Throughput & feedback-loop campaign, parts (a)–(d)** (owner-directed 2026-07-21; impl delegated → opus in one wave, tech-lead review + all slow verification; part (e) = the mt-042(b–d)→migration chunk, tracked there)
  Done 2026-07-21 (commits `b3bd80b` + baselines/docs). **(a) Parallel deterministic fan-out:** `solve-gauge --jobs N` — `parallel.rs::parallel_fold` (scoped threads, atomic work counter, mpsc progress replayed on the coordinator so the library's `FnMut(&str)` sink needs no `Send`; results fold position-ordered, never completion-ordered; fetched items always run to completion per mt-039). **Verified: full 564-command stage-1 sweep sha256-identical (stdout AND json) at `-j1`/`-j2`, mt-048 numbers exact (agree 300, DISAGREE 0); 12:09 vs 18:47 wall (~1.55× on 2 cores, tail-dominated — multiplies with the bigger box).** **(b) Cached jar count baselines:** `baselines/{alloytools-models,portus-63}-count-sb{0,20}.json` (config header: symmetry/cap/overflow/solver hard-error on mismatch, `jar_timeout` warn-only; per-command typed outcomes; miss = `skip_no_count_baseline`), captured via `--refresh-counts` (parallel JVMs, atomic rewrite per file, `--resume`; ~2h40m one-time). Live-vs-cache delta triaged: 4 files at the 300s JVM boundary → re-captured at 900s into `*-slow-count-sb0.json` supplements (merge = sorted-name, later wins); chordbugmodel ×2 converted, ceilingsAndFloors/life = the net's 3 honest `skip_jar_timeout`. **Both nets reproduce the live-era results exactly, JVM-free: SB-0 count_match 49 / mt-041 ×3 in 27:39 (was ≈78 min); SB-20 71 / 6 in 26:20 (was ≈56 min).** `--live-jar` keeps the live path (integration tests + out-of-corpus smokes). **(c)** `--fail-fast` (dispatch-stop only, in-flight finishes; PARTIAL-marked report, exit 1), `--only` substring filters, `--from-report`/`--from-buckets` delta re-runs over the report's new `per_command` array. **(d)** `status.rs` `StatusFile` — long runs atomically rewrite git-ignored `status/<tool>.txt` (~2s rate limit; phase, k/N, current file, last-10 heartbeats, DONE summary) tee'd from the same progress stream as the stderr heartbeat (library stays E3-clean); `scripts/watch-status.sh`. Review: delegate's 9-item divergence list adjudicated (all sound — notably the untagged-enum deserialization workaround and extending the config guard to `--resume`); one review fix (restored the measured budget-rationale comments); tech lead re-ran all fast gates (51 tests, fmt, clippy) + owned every slow sweep (deadlined, status-observed).
- ✔ **mt-055** · R4 · SB-20 count-net residual — **BOTH HALVES CLOSED** (wetdry via the `this/` naming migration 2026-07-22; tso root-caused + fixed 2026-07-25: root cause → opus, adversarial review → codex-backed triage, review fixes → same opus agent on warm context, tech-lead adjudication + all slow verification)
  **Done 2026-07-25 (tso half).** The cause was **not** in the model's memory-model relations: mettle skolemized existentials Kodkod refuses to. Kodkod's `Skolemizer` sets `skolemDepth = -1` for the **whole subtree** under a positive `or`/`implies`, a negated `and`, and `iff` (`Skolemizer.java:471`, `:541-542` — re-read by the tech lead before acceptance). Trigger: `tso_transistency_perturbed.als:526-530`, a positive `or` whose right disjunct is `some a: pte.map.VirtualAddress | …` — mettle minted `$sc_per_loc_a`, the jar mints nothing. **Doubly observable:** the extra free relation multiplies instances *and*, because `$`-names sort first in the §16.3 SBP relation order, it ate the whole lex-leader budget for the `VirtualAddress` class — which is what reconciles this bead's own old finding ("zero symmetric duplicates among the 121") with an over-count: the SBP was correctly excluded as *cause* but was the *amplifier*. Skolemizing there is sound; the jar is conservative, so conformance copies the conservatism. The coordinator's `rf_p` lead (correlating exactly with the two divergent commands) was a **coincidence — dead**, recorded in `scratchpad/probe/mt055-tso/matrix/NOTES.md` along with the stood-down measurement campaign's partial matrix. **Review found a live hole of the fix's own class:** formula-valued `if/else` branches bypassed the rule — the jar's `visit(ExprITE)` emits `c.implies(t) and c.not().implies(e)` (`TranslateAlloyToKodkod.java:776-783`), two positive `IMPLIES` at positive polarity / a negated `AND` at negative, blocking both branches; mettle's own `(c∧t)∨(¬c∧e)` IR shape is *not* what the jar sees. Jar-pinned + fixed (positive 16 → 4, negative 24 → 6). Review also corrected the `exprs.len() >= 2` guard's **stated reason** (right in substance; the carve-out is `getSingleFormula`'s binary heap of `BinaryFormula` ANDs short-circuiting at `n == 1`, `:1035-1058` — Alloy never builds a Kodkod `NaryFormula` for a block) and two §10.6 table rows mis-spelled with `;` (Alloy 6 sequencing → jar count 1, a different model). Second hole — formula-valued `let` freezes polarity at the binding site — jar-pinned but **deliberately not fixed** (the conservative block trades over- for under-skolemization; the faithful fix is a design change) → **mt-056** + LIMITATIONS. **Verification, before/after on the identical tree/binary/box** (the tech lead reverted `lower.rs` to HEAD and re-ran rather than trusting STATE.md's prose — correctly: the recorded SB-0 `enum_budget 52` was stale, true pre-fix 79, so the post-fix 78 had looked like a regression): **SB-0 52 → 53 matches, MISMATCH 0 → 0; SB-20 74 → 77 matches, MISMATCH 2 → 0** (the 2 being exactly `tso[0] 121 vs 11` and `tso[4] 96 vs 8`). Stage-1 unchanged, byte-identical ×2 — agree 343 (195/148), DISAGREE 0, 0 panics/self-checks; defers a wash (capacity 116 → 117, over_budget 56 → 55), so the MIGRATION sweep hash is re-recorded `e7a6c1a8…` → `c77ef8ce…` with no verdict moved. New `crates/als-core/tests/skolem_polarity_conformance.rs` (11 jar-pinned; SB-20 **and** SB-0 for 9, the 2 ITE rows SB-20 only). Gauntlet + fmt + clippy green. **Process note:** the delegate self-reported having written one *guessed* number into a doc table, caught it, reverted the arm and measured — the honest catch is why the table is now trustworthy.
  _Original filing:_ SB-20 count-net residual: **wetdry ROOT-CAUSED; tso re-scoped as an upstream semantic divergence** (filed at mt-048; investigated 2026-07-22, two parallel worktree agents + jar-bytecode differential)
  **wetdry.als[0] (888 vs 784) — root-caused and jar-confirmed:** the jar names root-module relations `this/X` (verified via `A4Solution.debugExtractKInput` dumps); mettle's were bare, so the §16.3 `(arity, name)` SBP relation sort interleaved root-vs-opened-module relations in the WRONG order and the soft cap burned lex bits on non-discriminating relations (mettle's 888→784 transition at N=32→33 vs the jar's 8→9). Two-line fix (root-module qualified-name prefix `this/` + atom-label strip) validated bit-for-bit convergent; productionization = the crate-wide naming migration (~70 mechanical test-string updates; tech-lead call: adopt the jar's convention, no local sort-key hack). Minimal two-module probe pinned (`scratchpad/probe/mt055-wetdry/`, jar 1750@sb5 / 1652@sb20 vs buggy 2550/2028). **tso[0]/[4] (121 vs 11, 96 vs 8) — the SBP is conclusively EXCLUDED** (the strongest evidence chain yet: the pinned jar's own `SymmetryBreaker` bytecode replayed via reflection over tuple-verified production bounds → bit-identical relation order/tuple order/cap point; partition detection identical; isomorphism check over mettle's 121 instances → zero symmetric duplicates; counts N-invariant at 50/100). The `p: PTag->univ` params are INLINED by substitution, never skolemized (one unrelated `$sc_per_loc_a` skolem, bounds verified correct). **Therefore mettle's raw post-SBP solution space genuinely exceeds the jar's — an upstream bounds/facts/encoding divergence, wrong-verdict-adjacent in principle.** **Record correction (tech-lead error, LESSONS):** earlier notes claimed "SB-0 counts clean" for these commands — in fact tso's SB-0 baseline is a file-level `timeout` and wetdry's SB-0 is the `10001` cap sentinel: NO SB-0 comparison was ever verified for either; the constraint was mis-inferred from a mismatch-list's silence. Next for tso: shrink the model (multi-level abstract sig hierarchy + shared parent atom pool + the `fr`/`fr_pa`/`fr_va` derived-relation facts) until raw counts are small enough to diff instance-by-instance on both sides; harness artifacts in `scratchpad/probe/mt055-tso/` (jar-bytecode SBP replay `Probe.java`, `isomorphism_check.py`). The row8a plain-product HO decl reading (`univ_conformance.rs::row8a_jar_ho_reading`, `#[ignore]`d) remains a separate latent axis — no longer tso's suspected cause.
- ✔ **mt-060** · docs · **STATE.md pruned to current-state-only: 105 lines/72.7KB → 83 lines/29.9KB; cold-pickup acceptance PASSED** (filed 2026-07-25 by a cold-pickup handoff test; done 2026-07-26, prune → sonnet with verify-before-delete, tech-lead review + acceptance run)
  **Done 2026-07-26.** The ~33KB nested-parenthetical `Last updated:` mega-sentence (mt-041→mt-062 history), both "Superseded plan" sections, "Key syntax facts", and both "Recent decisions" sections deleted — each block first verified to exist equal-or-more-detailed in its TASKS.md bead / ADR / MIGRATION.md / grammar reference (zero relocations proved necessary; the delegate checked owner quotes, verification figures, and the as-executed migration log individually). STATE.md now: one-paragraph latest-chunk header + pointer to the beads, current rung, trimmed scorecard, builds, what-exists inventory, next chunk, open questions, standing constraints. Tech-lead review caught two stale lines the prune preserved too faithfully — the Current-rung tail still said "We are now at the Rung-3 exit" and What-exists still said "not yet published to a remote" — both corrected. **Acceptance (the bead's own criterion): a fresh read-only haiku agent given the repo + "proceed" named mt-064 as the next action, with CLAUDE.md and STATE.md each read in ONE call, no pagination** (TASKS.md still takes two reads — it is the append-only ledger, pagination there is by design).
  _Original filing:_ (filed 2026-07-25)
  STATE.md is **66 KB / ~100 lines**, and its `Last updated:` field is now a single sentence carrying the entire mt-041 → mt-059 history in nested parentheticals. A fresh read-only agent given only the repo and "proceed" needed three paginated tool calls to get through it and named this as the thing that most slowed cold pickup. That is a live tension with [CLAUDE.md](../CLAUDE.md)'s own stated philosophy — *"intentionally lean (progressive disclosure)… links to everything; it does not duplicate it"* — and STATE.md is precisely the file that principle exists to protect, because it is the one every session reads first. **Shape to aim for:** STATE.md keeps only *current* state (rung, headline numbers, what exists, In flight, Next chunk, open questions) plus links; the per-chunk narrative moves to the beads in TASKS.md, which already carry it in full and are the natural home. **Acceptance:** re-run the cold-pickup test (a fresh read-only agent, repo + "proceed" only) and have it name the right next action without pagination. Note the completed-bead narratives must not be *deleted* — they are the referenceable handoff — only relocated. Not blocking anything, but it compounds: every chunk adds to the top of that sentence.
- ✔ **mt-058** · ops · **Split `solve_gauge/mod.rs` into concern submodules — pure mechanical, canonical hash unchanged** (filed 2026-07-25 out of mt-057; done 2026-07-25; impl → sonnet, tech-lead spec/review + acceptance sweep)
  **Done 2026-07-25.** Split exactly along the filed seams: `execute.rs` (410 lines — phase-A/phase-B executor: `resolve_phase`/`resolve_file`/`command_items`/`compute_command` plus `classify_command`/`compare_verdict` and the item/result types), `report.rs` (216 — `SolveGaugeReport`/`PerCommand`, both impl blocks, `render_list`; `pub use` re-export from `mod.rs` preserves external paths), `count.rs` (272 — `classify_count`/`resolve_count`/`presettled_count_bucket`, `CountOutcome`/`CountDisp`, the live-jar `run_jar_stage` path), leaving `mod.rs` at **711** = config (`GaugeConfig`), file selection/filtering, `run_gauge` orchestration, and the in-file test module. All moves verbatim (verified: 17 insertions / 860 deletions in `mod.rs` — mod decls, imports, doc-comment adjustments only); visibility widened to `pub(super)` at the narrowest set that compiles; `mod count/execute/report` stay private. `mod.rs` remains over S2's soft cap and carries a fresh one-line justification (doc-heavy config + orchestration + tests; further splitting only widens the `pub(super)` surface — tech-lead call to stop at the filed seams rather than force the number). **Acceptance: the canonical stage-1 stdout hash reproduced exactly — `c77ef8ce…` at `--jobs 10`** — plus 66+5+9 als-conform tests, fmt, clippy `-D warnings` all green.
  _Original filing:_ `crates/als-conform/src/solve_gauge/mod.rs` is **~1550 lines** (measured 2026-07-25, post-mt-059) against S2's ~500-line soft cap. It was already over (1124) before mt-057 and grew with the artifact wiring plus the two-phase command-parallel executor; restructuring mid-bead would have obscured a diff whose whole value was reviewability, so it carries the S2 justification comment instead. Natural seams, in the order they'd peel off cleanly: **(a)** the phase-A/phase-B executor (`resolve_file`, `command_items`, the resolve-cost reporting) → `execute.rs`; **(b)** report types + the text renderer (`SolveGaugeReport`, `PerCommand`, `render_*`) → `report.rs`; **(c)** stage-2 classification (`classify_count`, the jar-todo path) → `count.rs`, leaving `mod.rs` as config, orchestration and the public entry point. Pure mechanical move — **the acceptance test is that the canonical stage-1 stdout hash is unchanged** (currently `c77ef8ce…`), which makes this a safe bead to hand to a cheap model with the hash as the gate. Not blocking anything.
- ✔ **mt-059** · ops · **Check the count baseline BEFORE enumerating — SB-0 counting net 14m34s → 3m45s (3.9×), zero coverage traded** (filed + done 2026-07-25; the owner's question — "mesh.als[0], what does that run? a single SAT solve?" — is what exposed it; impl → opus, tech-lead spec/review + all slow verification)
  **Done 2026-07-25.** **No: a counting-net command is an exhaustive enumeration, not one solve** — solve → record → block → solve again, until UNSAT or the budget runs out, walking the raw solution space at symmetry 0. That distinction was the whole bead. `mesh.als[0]` cost **753.1s of the SB-0 net's 882s wall**, while the *same command in stage 1*, where it genuinely is one solve, does not even cross the slow-log threshold.
  **The defect was ordering, not speed:** `CountOutcome::JarTodo(n)` carried a count mettle had **already computed**, and only then was the baseline consulted. For roughly three-quarters of the corpus no possible enumeration result could change the command's bucket.
  **The rule, stated once** (`count_baseline.rs::CountLookup::resolution`, consumed by `disposition` so the pre- and post-enumeration decisions are the same rule read twice — drift impossible by construction): skip the enumeration iff the disposition is identical for **every** possible `n`. `CountResolution::NeedsCount(j)` (a real jar count) is the sole count-dependent case and still enumerates; `Fixed(bucket)` covers every marker. **The tech lead's open question — does `unsat` compare against `n == 0`? — answered NO** by the delegate: it folds into the count-independent `Marker(_) → skip_jar_error`, and separately **0** corpus commands have an SB-0 `unsat` marker without a verdict-baseline entry, so no verdict signal is swallowed.
  **Two tech-lead data errors the delegate overturned, both the same mis-read of the baseline format:** (a) "`mesh` has no jar count baseline at all" — **wrong**, it has a file-level `"timeout"`, so `mesh.als[0]` moves to `skip_jar_timeout`, not `skip_no_count_baseline` (the conclusion that the enumeration could never yield a comparison survives intact); (b) "267 of 564 commands have no SB-0 entry" — **wrong**, 267 is the file-level-timeout count and true `Miss` is **0**. Actual resolvable-without-a-count: **430 of 564 at SB-0**, 409 at SB-20 — a *bigger* win than specced. Consequence: `skip_no_count_baseline` did not grow at all; `skip_jar_timeout` and `skip_jar_error` did.
  **Also shipped:** **mode-keyed scheduling costs** (`SweepEntry.ms_by_mode`, additive — the legacy scalar `ms` migrates under its own header's mode, so the committed artifact needed no re-capture); captures carry forward modes they did not measure, so a stage-1 capture never erases counting-net times. **Caveat to respect when capturing:** a capture from a *counting* run rewrites the header to `count_enabled:true, count_symmetry:N`, which makes the artifact config-mismatch for the other symmetry — so **capture from the stage-1 run last** (this battery did). **`--enumerate-all`** is the deliberate escape hatch: it forces enumeration where no comparison is possible, so the *incremental* enumerator (`block()` + retained learned clauses, mt-032) keeps being exercised on those models rather than lapsing silently — the one genuine coverage cost of this bead, now opt-in rather than lost. Pinned in the artifact header (it changes which `skip_*` bucket a non-comparable command lands in, so a `--delta` across the switch would be fabricated).
  **Verification (full corpus, tech-lead-run):** **SB-0 `count_match` 53 / `COUNT_MISMATCH` 0** and **SB-20 `77 / 0` — both exactly unchanged**, which is the criterion that proves the reordering changed no comparison (and holds by construction: those two buckets can only arise from `NeedsCount`). Stage-1 canonical **`c77ef8ce…` at `--jobs 10` and `--jobs 6`**, agree 343 (195/148), DISAGREE 0, 0 panics / 0 self-check. Bucket re-partition as predicted: SB-0 `skip_enum_budget` 78 → **15**, `skip_jar_timeout` 3 → **69**, `skip_mettle_cap` 51 → 48. **Wall: SB-0 net 14m34s → 3m45s (3.9×); SB-20 net 5m10s → 4m33s; stage-1 unchanged 3m36s. Battery ~23 min → ~12 min** — and ~37 min → ~12 min across mt-057 + mt-059 together, with **zero coverage traded**. Mode-keyed hints alone were worth 4m41s → 3m45s on SB-0. 51 suites + fmt + clippy green.
  **Remaining floor:** SB-20 is now the slowest leg (4m33s) and stage-1 is bounded by `correctChord.als[23]` at ~236s, which *is* a single solve. If speed is ever revisited, that is where the encode-vs-solve split (instrumentation, not profiling) applies — mostly encode ⇒ suspect an accidentally-quadratic translation and profile *that one deterministic command*; mostly solve ⇒ a hard instance, record it and stop. Constraint: the determinism contract (fixed build → byte-identical output; no floats, no hashing near numbering) rules out much standard SAT tuning.
- ✔ **mt-057** · ops · **Feedback-loop speed, round 2 — delivered as command-level parallelism, NOT as skipping** (owner-directed 2026-07-25; impl → opus across three waves, tech-lead spec/review + all slow verification)
  **Done 2026-07-25. The bead's original premise was wrong and the measurements killed it.** Items (1)+(2) shipped first exactly as specced — a committed sweep artifact with a config header, known-capped skipping, LPT scheduling — and on the full corpus delivered **8% of wall time**: run A (no artifact) 10m09s / 17m47s CPU, run B (`--full`, LPT) 9m51s / 18m11s, run C (skipping 172 commands) 9m21s / **11m02s CPU**. Real CPU saving, near-zero wall saving. **Root cause of the miss:** `correctChord.als` alone summed to **556.5s of a 560s run** (next slowest file: 15.4s) because the unit of parallelism was the **file**, so one pathological file was a serial critical path — **1.18× effective parallelism on ten cores**. Skipping *other* files' commands and reordering files cannot touch that. (The delegate's honest 3.7× was measured on the alloytools subset, which does not contain the pathology — a tech-lead spec error: I told them not to run the full sweep.)
  **What actually shipped, after the owner's directive ("don't compromise on correctness, but do everything we can to speed things up"):** **command-level parallelism** — phase A resolves every file once in parallel into an `Arc<ResolvedFile>`, phase B takes the **command** as the work unit over that shared resolve. Resolve-hoist overhead **measured, not assumed: 0.4s summed across 167 files / 564 commands** (0.07%), and printed every run so it stays measured. Byte-neutrality is structural, not tested-into-existence: `command_items` flattens file-sorted / index-ascending so a command's queue position *is* its report position, and `parallel_fold_ordered` returns results position-indexed — execution order cannot reach the caller's fold. The counting nets needed **no separate change** (they run inside stage 1) and got it for free.
  **Then the skip lane was deleted.** Re-measured after parallelism, `--fast` bought **6%** (3m23s vs 3m35s) while carrying two blind spots — a previously-capped command that becomes newly solvable *and wrong*, and panics/self-check failures on the largest models. Defensible at 10 minutes, not at 12 seconds; owner approved removal. Replaced by the strictly stronger test **`an_artifact_never_changes_the_report`**: capture an artifact, rewrite *every* command in it to `mettle_defer:capacity` (the most hostile claim it can make), assert the report is unchanged in text and JSON. **Kept** (all cost no coverage): the artifact, LPT, `--delta`, `--capture-sweep`, the partial/filtered capture refusals, two-phase execution. `--full`/`--recheck-capacity` remain accepted no-ops.
  **Delegate correction worth recording (tech-lead error, LESSONS):** the spec's anti-rot claim — "skipping cannot hide a DISAGREE, only a lost gain" — was **wrong**, and the delegate demonstrated the counterexample live: a code change can make a previously-capped command newly solvable *and answered wrongly*; nothing regresses in the scorecard, but mettle is newly wrong and the instrument stays silent. A second delegate correction killed a strictness rule the tech lead had already approved: hard-erroring on *any* artifact header mismatch would have failed **every deep-budget sweep** the moment the artifact was committed. Rule re-derived — strict exactly when the artifact's *content* reaches the answer (now `--delta` alone), warn-and-ignore when it can only supply scheduling hints.
  **Final verification (full corpus, tech-lead-run):** canonical stage-1 stdout **`c77ef8ce…` reproduced three ways** — `--jobs 10` without an artifact, `--jobs 10` with one present and LPT live, and `--jobs 3` — so determinism survives command-level parallelism across job counts. Results **identical to the pre-mt-057 tree**: agree 343 (195 SAT / 148 UNSAT), DISAGREE 0, 0 panics, 0 self-check failures; SB-0 **53 / 0**; SB-20 **77 / 0**. **Wall: stage-1 10m09s → 3m38s (2.8×), SB-20 net 12m53s → 5m10s (2.5×), SB-0 net 14m06s → 14m34s (flat — its critical path is one command, see mt-059); battery ~37 min → ~23 min.** 51 suites + fmt + clippy green. Follow-ups filed: **mt-058** (S2 split), **mt-059** (the SB-0 critical path — the real remaining prize).
  _Original filing:_ **Feedback-loop speed, round 2: make the fast lane the default** (owner-directed 2026-07-25 — "let's do it in the very next step after mt-055 is done"; scheduled ahead of any Rung 5/6 content work)
  **The problem, measured on the Mac at `--jobs 10` during mt-055:** stage-1 sweep **10 min**, SB-0 counting net **14 min**, SB-20 counting net **13 min**, a full battery (stage-1 ×2 + both nets) **47 min**, plus a **28 min** baseline re-measure — ≈85 min of verification wall time for a one-file lowering fix. Three structural causes: **(a) we pay the most for the commands that tell us the least** — `correctChord[23]`/`[26]` alone are 183s + 175s of a 600s sweep and both terminate in a *typed defer*; 172 commands (capacity 117 + over_budget 55) burn full budget every run to re-report "still too big"; **(b) one instrument answers two different questions** — "did my change do what I intended" and "is mettle still a drop-in" both get the 47-minute hammer, while mt-054's `--only`/`--from-report`/`--from-buckets`/`--fail-fast` went unused all session; **(c) there is no persisted "before"** — headline numbers live as English prose in STATE.md, so every delta re-derives the baseline (and STATE.md's SB-0 `enum_budget 52` was found *stale* at mt-055: the real pre-fix value was 79, which briefly looked like a regression until it was measured).
  **Scope (items 1–3 are the bead; 4 is free discipline, 5 is opportunistic).** **(1) Committed known-capped list** — capacity/over_budget commands recorded with the budget they were measured under; default runs skip them and report "N skipped, unchanged", re-validated only at rung gates or under an explicit `--recheck-capacity`. Estimated stage-1 **10 min → ~2–3 min**. **(2) Longest-processing-time-first scheduling** — the report already carries per-command wall times; feed them back as a descending sort so the tail starts first instead of last (~1.5–2× on a tail-dominated sweep, composes with (1)). **(3) Commit the sweep report as a baseline artifact** (`baselines/mettle-sweep-*.json`) so any run diffs in seconds and the *default output is what changed*, not absolute numbers. **(4)** Name the tiers (T0 unit + jar-pinned tests / T1 smoke subset / T2 full stage-1 / T3 counting nets / T4 determinism ×2 + deep budgets), one command each, with an explicit rule for which tier a chunk owes — and stop charging determinism ×2 per chunk, it is an engine property and belongs at rung gates. **(5)** A first-class one-shot probe tool (`.als` snippet → mettle count + jar count at SB-N): three separate agents hand-rolled their own `Probe.java` during mt-055; this shortens *investigations*, which is where the human-visible latency actually lives.
  **Design constraint (the failure mode to engineer against):** (1) and (3) both introduce committed state that can rot into a lie — a stale capped-list would silently hide a regression. Mitigation, mirroring mt-054(b)'s count baselines: every artifact carries the config header + tree hash it was captured under and **hard-errors on mismatch**, and rung gates always run the unabridged sweep.
- ▢ **mt-056** · R4 · backlog · A **formula-valued `let`** freezes skolemization polarity at the binding site (filed 2026-07-25 out of the mt-055 review; count-only, verdict-neutral, **zero corpus incidence**)
  **The divergence.** mettle's `push_let_bindings` (`crates/als-core/src/lower.rs`) lowers a formula-valued `let` RHS **eagerly, at the `let`'s own polarity**, and binds the resulting `FormulaId` for every use, so the mt-055 connective rule (translation-ref §10.6) is evaluated at the **binding** site. The jar translates the RHS once too (`TranslateAlloyToKodkod.visit(ExprLet)`, `.java:796-800`) but *places that Kodkod node at each use*, and skolemization is a **separate later pass over the assembled Kodkod tree** (Kodkod `Skolemizer`, inside `Translator.translate`) — so the rule is decided in the **use site's** connective context. **Jar-pinned repro** (`scratchpad/probe/mt055-tso/review/let_or.als`): `run { let p = (some b: B | b in A) | p or some A } for 2` — the use sits under a positive `or`, the jar mints **no** `$b` and counts **6**; mettle skolemizes at the `let` and mints `$b`.
  **Two candidate fixes.** **(1) Conservative — block skolemization while lowering a formula-`let` RHS.** One-line, kills the over-skolemization, but trades it for potential **under**-skolemization whenever a formula-`let` is used in a genuinely skolemizable position (a fresh divergence in the opposite direction, and a *capacity* regression if the RHS carries a higher-order decl that then has to defer). **(2) Faithful — lower the RHS lazily, per use** (bind the *unlowered* `ExprId` + its `Ctx` and lower at each use under the ambient `Pol`), which reproduces the jar's placement semantics exactly. (2) is the right answer and is a design change to `Binding::Formula` and its consumers, not a chunk-tail edit — which is why mt-055 shipped neither.
  **Status/priority.** Pre-existing (not introduced by mt-055), verdict-neutral (skolemization is sound and complete for satisfiability), and **no corpus command hits it** — the post-mt-055 full battery's SB-20 counting net is clean (77 matches, 0 mismatches). Backlog, not blocking the Rung-4 gate. Recorded in LIMITATIONS.md. **Unprobed corner to settle first:** the jar shares one Kodkod node across uses and its `Skolemizer` memoises per node, so a formula-`let` used at **two different** connective contexts has a first-visit-wins behaviour neither side has been probed on — probe that before choosing (2)'s exact shape.
- ✔ **mt-053** · R4 · `univ`/`iden` lower as the **live** universe (probe → sonnet, impl → opus, tech-lead review + verification; LEDGER-011 approved under Option B)
  Done 2026-07-22. **Semantics first (probe matrix, `scratchpad/probe/mt053/NOTES.md`, 9 rows + controls, tech lead independently re-ran the 2 load-bearing cells):** the jar's `univ` = `Int ∪ String(padding incl.) ∪ ⋃(top-level sig live populations)` — **genuinely dynamic per candidate instance** (a `no B`-forced-empty sig's allocated atom is unreachable via `univ`: UNSAT), Int/String unconditional (never mention-gated), subset/extends idempotent (7/7/7), `iden` = the live diagonal, skolem/pred-param domains observably live too (22→68 scaling — which **disproved this bead's own suspected mt-055 mechanism**; the bound-vs-membership-constraint layering reconciles it with the source's true-UNIV internals). **Implementation (opus):** `lower.rs::live_univ`/`live_iden` at the single `ExprKind::Const(Univ|Iden)` seam — SigId-ordered union of the mt-030 sig denotes (review hardening: a missing denote is an invariant panic, never a silent skip); `iden ∩ (live→live)`; every internal/bounds-level all-atoms use untouched (sound over-approximations; liveness rides the lowered membership constraints). Evaluator needed no change (fix is upstream of both back ends; self-check + eval-differential nets green). **Tests:** new `univ_conformance.rs` (15 live jar-pinned tests + `row8a_jar_ho_reading` `#[ignore]`d for mt-055); `sb_conformance` univ probe re-pinned to the jar's 65549/32902; translation-ref §2.1 + new §10.8; LIMITATIONS entry closed; **LEDGER-011 `approved`** (tech lead, no-fork). **Verification (full battery, byte-identical ×2 at `-j1`/`-j2`):** agree 300 → **301 (166 SAT / 135 UNSAT), DISAGREE 0**, 0 panics/self-checks; honest movement: jar_nonverdict 38→35 + over_budget 44→43 vs capacity 113→116 (live union costs encode budget on univ-heavy models — mt-050 recovers at depth); SB-0 net **unchanged 49 / mt-041 ×3**; SB-20 70 matches (one → typed enum_budget skip) / mismatches unchanged = mt-041 ×3 + mt-055 (as the probes predicted). MIGRATION hash re-recorded (`72ad3b33…`).
- ✔ **mt-041** · R4 · Ordering exact-bound detection re-keyed on the jar's real trigger; root-module count family retired (probe → sonnet, impl → opus, codex adversarial review, tech-lead review/adjudication; LEDGER-004 amended under Option B)
  Done 2026-07-24. **The filed question ("module path? sig label? field names?") had a decisive answer: NONE of them.** The probe agent's source-read + 7-row discriminating matrix (translation-ref §10.1b, `scratchpad/probe/mt041/`) pinned the jar's trigger as the reserved `pred/totalOrder` keyword itself — a context-sensitive grammar token (`Alloy.cup:146`/`383–390`, string-matched pre-resolution) that becomes Kodkod's native TotalOrdering predicate (`TranslateAlloyToKodkod.java:1069–1077`); renaming the module/file/sig/fields all keep count 1 (rows 2–5), the keywordless equivalent order counts n! (row 6, decisive negative control). Separately pinned: a parametric module run as **root** materializes header params as fresh top sigs with `[exactly X]` forcing exactness **at header level** (`CompModule.addModelName:1270–1298`; row 7 — plain `for N elem` still counts 1). **Implementation (opus, two review rounds):** `resolve_ordering` part (b) re-keyed structurally — replace, not union — on the recorded `BuiltinCall::TotalOrder` choice (the single shared recognition), matched as a top-level conjunct (block/`&&` descent) of a `one` sig's own appended fact over its **own** fields (review fix: own-field requirement closes a subsig-of-`one`-parent over-match); root header `exactly` → `root_exact_sigs` → `additional_exact` (general, not ordering-specific); `pin_ordering` gains the `lower == upper` gate (unprobed non-exact corner stays hand-built, count-conservative) + a release-safe bounds-shape guard. **Adversarial review (codex → triage) flagged an unsound-rebind HIGH for `in`-subset-typed fields; adjudicated a FALSE POSITIVE by delegate pushback + jar probe row 8**: both implementations bound subset columns parent-wide with the `in B` narrowing as a formula, the pin soundly forces `B = A`, and the jar counts **1** (`mt041_subset_field_pin_is_sound`, jar-pinned). 7 `mt041_*` tests in `tests/solve.rs`; LESSONS filed (proxy-of-a-pinned-mechanism). **Gauge:** SB-0 counting net **count_match 49 → 52, COUNT_MISMATCH 3 → 0** — the exit gate's counting criterion is now met outright; SB-20 mismatches drop to the mt-055 tso ×2; stage-1 sweep byte-identical to pre-mt-041 (hash `72ad3b33…` unchanged — verdict-invisible by construction); 50 suites green.
- ✔ **mt-042** · ops · Machine portability: git remote, one-command asset bootstrap, pinned toolchains (owner-directed 2026-07-18; (b–d) delegated → sonnet 2026-07-22, tech-lead reviewed)
  **(a) Git remote — DONE 2026-07-18:** owner created **public** `git@github.com:chaychoong/mettle.git`; standing workflow: work on `main`, push after each chunk; public hosting safe per ADR-0006. **(b) `scripts/bootstrap.sh` — DONE 2026-07-22:** fresh clone → one command fetches the jar (pinned URL, **SHA-256 verified on download**, refuses + never overwrites on mismatch, idempotent) then delegates to `fetch-corpora.sh` (`--with-alloy4fun` passthrough); `--verify` re-checks everything downloadless. Shellcheck clean; gated on a real fresh-clone test incl. the corrupt-jar refusal path; tech lead re-ran `--verify` on the live checkout (192/192). **(c) Toolchain pinning — DONE 2026-07-22:** `rust-toolchain.toml` pins 1.97.0 + rustfmt/clippy (the exact authority; CI aligned via explicit `toolchain: 1.97.0`); `flake.nix` dev shell (JDK 21, git/curl/python3/shellcheck, rustc via `rustPackages_1_95` — nixpkgs `nixos-26.05` @ `fd146203`, rev-pinned in the input URL; **no stable nixpkgs branch ships 1.97.x yet**, delta documented in the flake header and MIGRATION's toolchain caveat). **Honest caveat: the flake is authored-but-unverified — no nix in this VM; first `nix develop` on the destination box generates + commits `flake.lock` (MIGRATION Phase 3 step 1).** **(d) `docs/MIGRATION.md` — DONE 2026-07-22:** three-phase checklist — wrap + recorded sweep-hash (`c4f7f8ca…`, 2026-07-21, re-record after any code change) → clone to the same absolute path + rsync `~/.claude` once (`~/.ssh`/`.gitconfig` separate) → arrival: `nix develop`/lock, `bootstrap.sh`, full gauntlet (the 5 jar-integration tests catch a wrong jar/JDK first), **cross-machine determinism hash compare (mismatch = stop the line)**, cached SB-0 net (expect 49/mt-041×3). Linked from docs/README.md.
- ✔ **mt-050** · R4 · Rung-4 exit: deep-budget sweeps vs refreshed baselines; ADR-0012 gate measured met (defer classification → sonnet, let-formula fix → opus, jar campaign + sweeps coordinator-owned, tech-lead review; owner touchpoint = the 2026-07-25 report)
  Done 2026-07-25, on the Mac as planned. **Three arms.** **(1) Jar verdict campaign:** the 10 portus 60s-timeout files re-captured at 1800s/7200s → **152 new command verdicts** in `baselines/portus-63-slow-verdict.json` (supplement pattern, command entries override file-level timeouts; triage in baselines/README — elevator `I3a` = stale upstream expect, jar-UNSAT both overflow modes). ertms_1A converted at 7200s (14/14 expect-matches); **correctChord is the one file the jar itself cannot sweep even at 7200s** — permanently non-comparable, no further retries. **(2) The `lowering 43` decomposition** (sonnet, per-command re-runs + minimal repros): 36/43 were ONE bug, not a temporal family — a formula-valued `let` binding (`let extra = … => … else some none`) unconditionally lowered via `lower_rel` (elevator_spl_events, one shared fact → all 36 commands). **Fixed** (opus): `Binding::Formula` + sort-aware `push_let_bindings` shared by all three `Let` arms, `lookup_binder_formula` consumption, `sort_of` formula-ITE recognition; relation-valued lets byte-identical; unreachable cross-sort uses defer typed. Elevator command 0 → **VALID matching `expect 0`**; 10 new tests (lower + solve). Residual bucket = the known typed recording tail, exactly 7. **(3) Deep-budget sweeps** (budgets trial-validated on chord/tso/ertms first, per owner-runtime-deadlines): chord+ertms at L1 (100k/32M/200k), the other 30 worklist files at L2 (400k/128M/500k) — **DISAGREE 0 everywhere, 0 panics/self-checks**; deep-ertms +8 agreements vs its fresh verdicts; deep-B 180 agreements incl. the TransForm minimality scripts' first-ever verdict comparisons (mt-055's model family — all agree); chord solves 23/39 at depth (jar: none); ~76 commands remain capped even at L2 (honest capacity). **Default battery (final tree): agree 301 → 343 (195/148), DISAGREE 0, jar_nonverdict 35 → 16 (= chord), lowering 43 → 7; SB-0 52/0 ×2 byte-identical; SB-20 74/2; hash re-recorded `e7a6c1a8…`; gauntlet+fmt+clippy green.** Gate state vs ADR-0012: every criterion met; one named disclosure (the 7-command tail) for the owner's blessing.

**Sequencing:** mt-043 (contract) first — the rung's authority document, pattern-matching mt-016/mt-028; mt-040 interleaves freely (no contract dependency, biggest unlock). Then mt-044→045→046 (shared encoder/evaluator surface, integers first — largest defer family), mt-047/mt-048 behind the contract, mt-049 opportunistic between coverage beads, mt-050 last.

## Backlog (later rungs)
Tracked at rung granularity in [ROADMAP.md](ROADMAP.md); expanded into beads when a rung becomes "Next".
- ▢ **mt-027** · R? · Close the residual diagnostic-parity gap (owner-accepted-and-deferred 2026-07-16)
  The measured remainder after mt-022/025/026/023, all documented in [LIMITATIONS](../LIMITATIONS.md) and root-caused in [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md) §10.4 + [reference/warning-parity.md](reference/warning-parity.md): **verdicts** — 314/150,891 over-accepts (~112 compound-right-operand re-checking, ~94 deep-join type precision, ~38 multiplicity-flag tracking, ~10 tail); **warnings** — 192 missing (overload-collapse unused-var) + 20 extra (temporal same-value suppression). Owner decision: move to Rung 3 with the gap filed; buckets 1–2 sit on type machinery Rungs 3–4 must deepen anyway — **revisit this bead whenever that machinery changes** (each Rung-3/4 typing bead should note its effect on these counts), or schedule it standalone if the counts stall. Exit: 100% or a proven jar-bug floor, re-gauged.
- ▢ **mt-021** · R? · Printer/dumper recursion-depth safety on pathologically long flat operator chains
  mt-014's second fuzzer finding (see [reference/fuzzing.md](reference/fuzzing.md) §1): `Ast::pretty`/`dump` recurse per operand and can overflow on adversarial flat chains far beyond any real model. Fixing properly touches the public, currently-infallible `pretty`/`pretty_to_string` signatures — needs a small ADR first. Not rung-gating; schedule opportunistically.

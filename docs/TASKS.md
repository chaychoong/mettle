# Task ledger (beads)

Lightweight, git-tracked, greppable task "beads" — no external tool dependency (see [ADR-0004](adr/0004-docs-and-task-system.md); we may adopt Steve Yegge's `bd`/beads tool later if the dependency graph warrants it).

**Bead format:** `mt-NNN` id · `status` · `rung/phase` · short title. Detail and dependencies below each. Statuses: `todo` `doing` `blocked` `done`. Reference beads by id from commits and ADRs.

**Legend:** ▢ todo · ◐ doing · ⛔ blocked · ✔ done

---

## Done (Pre-Rung-1)

- ✔ **mt-001** · P0 · Documentation & decision spine
  CLAUDE.md, docs index, STATE, ROADMAP, this ledger, ADRs 0001–0004, LIMITATIONS, Ledger scaffold.
- ✔ **mt-002** · P0 · Pin the conformance oracle (delegated → sonnet)
  Downloaded + verified Alloy 6.2.0 jar (SHA-256 pinned); proved headless verdict/count/`symmetryBreaking=0`/overflow-default/SAT4J, and found+documented a CLI bug (`-y`/`--ymmetry` is a no-op — use the `A4Options` API instead). → `docs/reference/alloy6-reference.md`; SHA/version folded into ADR-0002.
- ✔ **mt-003** · P0 · Draft steering rubrics (delegated → opus)
  STYLE.md + PORTING_RULES.md — tech-lead reviewed and accepted as binding rubrics.
- ✔ **mt-004** · P0 · Cargo workspace + `als-*` crate skeleton (delegated → sonnet)
  8 crates on the hand-designed DAG; binary crate = package `mettle` (fulfils the plan's `als-cli` role). CI green (build/fmt/clippy -D warnings/test), tech-lead re-verified. `Cargo.lock` committed.
- ✔ **mt-005** · P0 · Hand-designed core IR type skeleton (tech-lead-authored, NOT delegated)
  Done 2026-07-15: `Arena`/`ArenaId`/`define_id!` + `Span` in als-syntax; unified surface AST (temporal-first, spans required); three-sorted relational IR + `Universe`/`TupleSet`/`Bounds` in als-core; dependency-free `Var`/`Lit`/`Cnf`/`Solver` boundary in als-solve. Rationale: [ADR-0005](adr/0005-core-ir-type-skeleton.md).
- ✔ **mt-006** · P0 · Conformance harness (`als-conform`) v0 (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: `crates/als-conform/shim/OracleShim.java` drives the jar via the `A4Options` API (symmetry/noOverflow/solver always explicit; LEDGER-001 default); Rust side = typed outcomes, timeout-killed JVM per file, Net 0 expect-mining, deterministic scorecard (text+JSON), `conform` bin. 87/1129 enumeration facts pinned by integration tests (skip cleanly sans jar). Review fixes: shim source moved into the crate (was in git-ignored `oracle/`); unknown solver = hard error, no silent default.
- ✔ **mt-007** · P0 · Vendor corpora (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: alloytools-models (94 .als @ the jar's build commit), alloy4fun (Zenodo 10.5281/zenodo.17390557, CC-BY-4.0), portus-63 (63 models, licensing hot spot) vendored into git-ignored `corpus/`; kodkod investigated → no `.als`, not vendored. Provenance manifest committed: [reference/corpora.md](reference/corpora.md). Corpus files stay uncommitted until mt-008.
- ✔ **mt-008** · P0 · Resolve licensing posture (ADR)
  Done 2026-07-15, owner-decided → [ADR-0006](adr/0006-licensing-posture.md): mettle = **MPL-2.0** (root LICENSE + workspace manifest); stdlib `util/*.als` = **clean-room rewrite** (bead mt-015, never copy upstream text); corpora = local-only forever (git-ignored, reproducible via manifest + mt-009 script); jar stays ignored. PORTING_RULES legal-hygiene section updated per the ADR.
- ✔ **mt-009** · P0 · Reproducible corpus fetch script (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: `scripts/fetch-corpora.sh` (+ `corpora.sha256`, 192 files) reproduces `corpus/` byte-identically from the [reference/corpora.md](reference/corpora.md) pins in ~15s (alloy4fun optional via `--with-alloy4fun`); `--verify` re-checked independently: 192/192 pass, shellcheck clean. Bonus: surfaced and fixed an under-documented 4th upstream patch (`trace.als`) — manifest corrected.

## Done (Rung 1 — syntax) — owner touchpoint passed 2026-07-15
- ✔ **mt-010** · R1 · Lexer + spans (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: raw-token lexer per the pinned contract ([reference/alloy6-grammar.md](reference/alloy6-grammar.md) §1, [ADR-0007](adr/0007-rung1-lexer-parser-architecture.md)); typed `LexError` with caret-ready spans; **167/167 corpus files lex clean**. Review caught + jar-verified two divergences (number maximal-run rule: `1_000`/`0x123`/`0b12` illegal; string-follow class includes digits/quotes) — spec §1.5–1.6 corrected to match.
- ✔ **mt-011** · R1 · Parser + arena AST (delegated → opus, tech-lead reviewed)
  Done 2026-07-15: full surface grammar → arena AST; `cook.rs` (F1–F4) + `parser.rs` (recursive descent + Pratt, 21 levels, binder rule, min-BP-gated prefixes). **167/167 corpus parse rate.** Review caught 2 real bugs pre-merge (`;`-grouping semantics; prefix over-acceptance) + approved `Expect::Other(i32)`. Deferred: steps-scope build checks → resolve (LIMITATIONS).
- ✔ **mt-012** · R1 · Pretty-printer + parse→print→parse round-trip (delegated → opus, tech-lead reviewed)
  Done 2026-07-15: minimal-paren precedence-aware printer as `Display` (R9d) sharing one bp table with the parser (new `prec` module — no drift possible); span-free `dump` as round-trip witness; insta snapshots (U2, first dev-dep per P1); **corpus round-trip 167/167** (parse→print→reparse→dump-equal + print idempotence); `mettle parse <file> [--ast]` CLI (hand-rolled args; `file:line:col` errors to stderr per E3) = the Rung-1 human-testable build. Review trims: dropped gratuitous Vec clones, `prec` made pub(crate) (S4). No parser bugs surfaced.
- ✔ **mt-013** · R1 · Diagnostics (caret errors) + Alloy4Fun error-quality pass (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-15: rustc-style caret renderer in `crates/mettle/src/diagnostics.rs` (E3; multi-line/tab/EOF/UTF-8 edge cases unit-tested); alloy4fun differential pass (186k records → 150,891 unique) via new batch `ParseOnlyShim.java`. **2 real parser bugs found+fixed** w/ regression tests (closure-prefix binder exception; bare `disj`/`pred/totalOrder` over-acceptance). Zero jar-accepts+mettle-rejects; 99.79% exact position match; 2/1000 over-acceptance (binder composition) documented in LIMITATIONS → mt-014 bait. Evidence: [reference/alloy4fun-error-pass.md](reference/alloy4fun-error-pass.md).
- ✔ **mt-014** · R1 · Mutation fuzzer + binder-composition resolution (delegated → sonnet, tech-lead reviewed; binder rule independently re-verified against the jar 6/6, fuzzer determinism re-run, grammar-doc §3.1 corrected to the narrowed rule)
  Done 2026-07-15: zero-dep SplitMix64-seeded mutation harness (`tests/fuzz_mutations.rs`: byte/token/splice classes + deep-nesting/long-chain stressors; no-panic + sane-span + round-trip properties; default 4,248 mutants/~5s, `METTLE_FUZZ_ITERS` env override for offline runs, verified at 88,500 mutants/~127s). **Deep-nesting guard required** (measured, not assumed): unguarded `(`/`{`/binder-chain nesting SIGABRTs a debug build well within fuzz reach (worst-case ~212 levels safe on a 1 MiB thread) — `MAX_EXPR_DEPTH = 256` + new `ParseError::TooDeep`, verified safe to 100,000 adversarial levels; jar-probed as a deliberate, better-than-reference divergence. **Part 2 resolved:** ~220 jar probes mapped the exact binder-composition rule (one composition "hop", refreshed by `implies`, hard-blocked by comparisons/set-tests) → shared `crate::prec::child_binder_budget` threaded through both parser (`ParseError::BinderNeedsParens`) and printer (so the two can't drift); LIMITATIONS.md entry resolved. Fuzzer itself found and fixed one real bug (printer under-parenthesizing post-Part-2, since `needs_parens` only tracked `rightmost` not the new budget) and one documented-out-of-scope finding (printer/dumper recursion depth on long flat chains — candidate follow-up bead). 167/167 corpus preserved; full workspace gauntlet green. Evidence: [reference/fuzzing.md](reference/fuzzing.md). **Rung 1 complete → owner touchpoint.**

## Done (Rung 2 — names & types) — owner touchpoint passed 2026-07-16
Rung gauge: **same accept/reject decisions as the jar** on resolve/typecheck (ROADMAP rung 2). Home crate: `als-types` ("name resolution, sig hierarchy, and the relevance/type checker"). Filed 2026-07-15 after the Rung-1 owner touchpoint.

- ✔ **mt-016** · R2 · Pinned resolution & type-system contract (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: [reference/alloy6-resolution.md](reference/alloy6-resolution.md) pins the whole `resolveAll` behavior at commit `794226dd` — phase pipeline, module system (`open` search order, instance identity = filename+params, auto-aliasing), type system (union-of-products, **no `int`↔`Int` coercion in 6.2.0**, `ExprChoice` disambiguation ladder), the exact ~24-row reject taxonomy + warning catalog (**warnings never change the verdict** — the mt-020 gauge is binary), `util/*` interfaces (clean-room, signatures only), determinism notes. 48 probes, zero source↔jar divergences; tech lead independently re-verified 6 probes + the mode-1/no-ghost-sig entry-point claim against the sources. [ADR-0008](adr/0008-rung2-resolver-architecture.md) accepted (two-pass, typed-ID `ResolvedWorld`, render-free diagnostics, no `_` catch-alls in the taxonomy).
- ✔ **mt-017** · R2 · Module graph + `open` resolution (`als-types`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `FileTable`/`ModuleGraph` (typed-ID arenas), `ModuleLoader` abstraction (fs + injected-map), exact §2.1 search order incl. `.md` fallback + embedded-stdlib last, `computeModulePath` with the **prefix-cancel** reading (corpus-forced, then jar-verified on the mislocated zigbee modules — resolution-doc §9 residual closed), cycle detection, instance identity = (file, args) (probes 24/25), alias machinery + duplicate-alias reject (probe 26), private-open blocking, module-phase `ResolveError` variants. Review fixes: `seen_dollar` gate now scans **all** loaded files (reference threads its accumulator through every parse; root-only was a divergence); §9 note updated. 27 unit/integration tests + corpus load **167/167** (after stdlib wiring).
- ✔ **mt-015** · R2 · Clean-room `util/*` stdlib rewrite ([ADR-0006](adr/0006-licensing-posture.md)) (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: all 11 `util/*.als` written clean-room into `crates/als-types/stdlib/util/` (MPL-2.0, attested: author never opened corpus/upstream text) against the §7.1–§7.11 interface appendix (added mid-bead after the author's honest first-pass report exposed §7 underspecification; wrong guesses — `int2elem`/`elem2int` shapes, `util/relation` pred arities, `Seq.seqElems`, `util/time` macro family — all corrected). Parse + round-trip test (`tests/stdlib_parse.rs`); wired into `stdlib::MODULES` at merge. Body-level judgment calls (time macros, relation `complete`, rank arithmetic) recorded for mt-020 differential triage; `util/ordering` analyzer special-casing deferred to solve-time Ledger entries as planned.
- ✔ **mt-018** · R2 · Name resolution + type checker core (`als-types`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: full §1 phase pipeline over the mt-017 graph — builtins seed, sig hierarchy/cycles/subset sigs, enum desugar (+ synthetic `util/ordering`/`util/sequniv`/`util/integer` opens in the loader — the **jar auto-opens `util/integer` everywhere**, jar-probed twice), fields (implicit `this`, defined pass, name-clash), func/pred overload sets, facts/asserts/macros (20-unroll), commands + the two deferred scope checks; `Type` value type + fused bidirectional §4 checker (`resolve/expr.rs`) with the §4.4 candidate chain and **no int↔Int coercion**. All §5.1 rows have `ResolveError` variants. 37 probe-table unit tests + **corpus resolve 167/167 accept**; workspace green. Review: fused-pass deviation from ADR-0008 dec.4 + accept-lean ambiguity posture examined, jar-probed (mettle-accepts/jar-rejects on ambiguous 0-ary names confirmed), and **accepted as an explicit interim via [ADR-0009](adr/0009-fused-resolve-pass-accept-lean.md)**; divergences recorded in LIMITATIONS; mt-020 decides tightening.
- ✔ **mt-019** · R2 · `mettle check` CLI subcommand (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: `mettle check <file.als>` — load module graph → resolve/typecheck → rustc-style caret diagnostics via a label-parameterized renderer (`error:`/`warning:`, one code path); **multi-file aware** (span `FileId` → the right file's source/path from `graph.files`; load-phase fallbacks: disk re-read for `OpenedFileParse`, spanless one-liner when unrecoverable — never a caret into the wrong file). Warnings never fatal (§5.3); success prints `<path>: ok (N sigs, M funcs, K warnings)`. 11 spawn-the-binary tests + fixtures; 94/94 alloytools models exit 0; tech lead re-smoked good/submodule-error/missing-file paths by hand. Workspace green (266 tests).
- ✔ **mt-020** · R2 · Differential resolve/typecheck gauge vs the jar (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `resolve_gauge` bin (mettle batch driver + diff) + `ResolveGaugeShim.java` (real-path `parseEverything_fromFile` — the string shim spuriously fails multi-file models; equivalence-checked 0/3000 on single-file codes). **Scorecard: corpus 167/167 agree-ACCEPT; alloy4fun 150,891 codes → 0 jar-accepts/mettle-rejects (drop-in violations), 95.82% total agreement, 6,300 over-accepts (4.2%) measured + bucketed in LIMITATIONS.** 4 drop-in bugs found + fixed (subset-sig implicit-`this`; field-vs-`util/integer`-pred shadowing; call disambiguation by relevant type; higher-order macro), plus 4 over-acceptance closers (set-as-formula, formula-as-set, non-int comparison, non-binary `~`/`^`/`*`) — each with a jar-verified regression test. **ADR-0009 decision 3 fired and measured: tightening reverted** (28,402 false rejects vs ~1,478 gains — coarse types are the blocker, not choice logic); outcome recorded in the ADR; real fix filed as backlog bead mt-022. Evidence: [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md). **Rung 2 complete → owner touchpoint.**

## Done (Rung 2 extension — [ADR-0010](adr/0010-hundred-percent-before-signoff.md): ~100% similarity before the touchpoint)

- ✔ **mt-022** · R2 · Precise per-node relevant-type propagation (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: **root cause found** — the reference's `Type` keeps empty products *with their arity* (`NONE→…→NONE`); mettle collapsed them to the EMPTY sentinel, conflating "legal-but-empty" with "illegal" (exactly why ADR-0009's naive tightening false-rejected 28,402). `ty.rs` rewritten as a faithful behavioral port (verified vs sources @ `794226dd`); `expr.rs` gains bottom-up `infer`, a faithful `resolveHelper` pick on precise types with definite-vs-loose relevant positions, faithful weights/`applicable`/`@name` semantics. Every reverted tightening re-enabled and firing. **Gauge: agreement 95.82% → 98.36%, over-accepts 6,300 → 2,475, 0 jar-accepts/mettle-rejects throughout, corpus 167/167.** 6 new jar-verified regression tests; tech lead re-spot-checked illegal-join/legal-empty/ambiguous-name against the jar. Remainder root-caused → mt-025 (materialized typed tree; ~2,000 codes) + mt-026 (6 parser binder codes). Evidence: [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md) §9.
- ✔ **mt-025** · R2 · Materialized typed tree + full top-down relevant-type threading (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: the fused walk replaced by the reference's real structure — join-level `ExprChoice` materialization (`Context.process`, jar-instrumented to confirm), precise per-op top-down relevant slices (source-verified @`794226dd`), structural error-suppression (errors attach to the chosen path only); `Want`/`ambig`/`join_lenient`/loose-wants all removed. 6 real resolver bugs + 1 clean-room stdlib bug fixed. **Gauge: 98.36% → 99.79%** (over-accepts 2,475 → 320), **0 jar-accepts/mettle-rejects both directions, corpus 167/167 = 100%**; 8 jar-verified `_mt025` regression tests; ~45s mettle side. **Clean-room incident handled at review:** the agent read upstream ordering.als for its stdlib fix — honest file-exact accounting on challenge, line reverted, fix independently re-derived by a fresh clean-room agent (`~this/next`, different-but-equivalent formulation = evidence of independence), gauge re-verified identical; LESSONS updated. Residual 320 root-caused: ~112 compound-right-operand, ~94 deep left-of-join approximation, ~38 multiplicity flag, 6 parser codes (mt-026), ~10 tail. Evidence: [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md) §10.
- ✔ **mt-026** · R2 · Binder-composition rule vs the 6 jar-parse-reject codes (delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: root cause — mt-014's rule let every ordinary prefix forward its ambient binder budget unchanged; the jar rejects a prefix-wrapped binder once an enclosing infix has spent its hop (`q and not some x: A | …`). 6 codes minimized + jar-verified, 460 fresh probes mapped the refined rule → new shared `prec::prefix_operand_budget` consumed by parser AND printer. Gauge: exactly the 6 flipped, nothing else moved — **over-accepts 320 → 314**, 0 false rejects, corpus round-trip + fuzzer green. Docs: fuzzing.md §2 (M2′), grammar §3.1, LIMITATIONS.
- ✔ **mt-023** · R2 · Warning parity with the jar ([LEDGER-002](../SEMANTICS_LEDGER.md) owner requirement; delegated → opus, tech-lead reviewed)
  Done 2026-07-16: **all 20 §5.2 warning classes implemented** on the mt-025 typed tree, firing conditions source-verified (incl. the `resolveClosure` reachability port); warning-capturing shim (additive) + `warn-diff` gauge matching by (class, line). **Parity: corpus 166/167 identical / 0 missing; alloy4fun 99.80% files identical, 192 missing (0.19% of files, root-caused: overload-collapse unused-var corner), 20 extra (temporal same-value suppression corner) — both directions' remainders individually root-caused, wrong-direction "fixes" measured and rejected.** `mettle check --strict` (warnings → exit 1) shipped with CLI tests; 16 jar-pinned warning regression tests. **Verdict byte-invariant** (tech lead re-ran the full gauge: 0 / 314 / 99.79% unchanged). Evidence: [reference/warning-parity.md](reference/warning-parity.md). **All ADR-0010 rung-gating beads done → owner touchpoint.**

- ✔ **mt-024** · harness · One-command conformance + speed benchmark (owner-requested; delegated → sonnet, tech-lead reviewed)
  Done 2026-07-16: `conform bench [<corpus-dir>]` — per-stage conformance (parse/resolve; solve slots in at Rung 3 without schema change) + honest speed (warm threaded mettle per stage; jar one-JVM batch with startup reported separately; size-spread cold sample; ratio only for the like-for-like pair, caveat printed). Text + `--json`, byte-stable except timings; `--skip-jar`. Tech lead ran it end-to-end: parse 167/167, resolve 167/167, mettle resolve ~4× jar batch total. Usage in `als-conform` module docs.

## Done (Rung 3 — relational core) — exit gauge green, owner touchpoint passed 2026-07-18

- ✔ **mt-028** · R3 · Pinned translation & solving contract (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: [reference/alloy6-translation.md](reference/alloy6-translation.md) pins scopes→universe→bounds (exact `Name$N` atom naming/order — resolves the numbering Ledger corner), the resolved-Expr→relational mapping + skolemization (`$cmd_var`), symmetry breaking (all Kodkod lex-leader; ADR-0002's SB-0 counting needs none of it), SAT boundary + outcome/enumeration semantics (87/1129 re-confirmed), self-verification story, and `util/ordering` exact-bounds pinning → LEDGER-004 (proposed). 9 jar probes; new oracle gotcha: **`expect 1` silently forces `symmetry 0`**. [ADR-0011](adr/0011-rung3-translation-solving-architecture.md) **Accepted** (owner approved writing our own CDCL solver, 2026-07-16).
- ✔ **mt-029** · R3 · Scopes → universe (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als-core::scope::compute_universe` — faithful `ScopeComputer` port (abstract-sum → overall → parent fixpoint, one/lone/some forcing, defaults, all scope errors as spanned `TranslateError`s), exact atom naming/order incl. module-qualified multi-instance names (`so/Ord$0`); ~30 jar probes; **universe spot-checks exactly match the jar** on real models. als-types widenings: `qualified_name`/`is_enum`, full `ResolvedCommand` (+`additional_exact` — the mt-035 seam). 27 §1-cited unit tests + corpus: **167/167 files, 564 root commands, 0 rejects, byte-identical twice**. Verdict gauge re-verified untouched (0/314/99.79%). Findings for mt-036: only root-module commands solve (jar `getAllCommands()` is root-only); String atoms deferred to Rung 4, steps expansion to Rung 6 (per ADR-0011).
- ✔ **mt-030** · R3 · Bounds builder (`als-core`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als_core::bounds_builder::compute_bounds` — faithful `BoundsComputer` port + the `A4Solution` builtin bounds: per-sig lower/upper atom bounds (bottom-up lowers, top-down floating-pool growth), leaf/`<Sig>_remainder`/abstract-union/subset relation allocation, field product bounds (incl. the `one`-sig owner-column strip), and quantified-over-atoms size/multiplicity/sibling-disjointness constraint formulas (never `#` cardinality — no bitwidth interaction); `Int`/`seq/Int` bound exact, `String` empty (Rung 4). Output includes the **denotation seam** for mt-031: one prebuilt `RelExprId` per sig/field. 18 jar probes (B1–B18, translation-ref §10.2) sharpened §1.4 → §1.4′: child growth takes the **whole** floating pool (cap is a formula), size formula iff `upper > scope`, disjointness unconditional, owner-strip is `one`-only. 22 jar-pinned goldens + 167/167 corpus (564 commands, ~14k relations, byte-identical twice). **Review found + fixed a real divergence (probe B19):** the reference's silent scope-raise (`n < Σ children-lower → n = lower`, exactness kept) was missing from mt-029's walk — `for exactly 2 P, exactly 3 C` panicked mettle's debug build where the jar solves SAT; ported into `scope.rs` with 3 regression tests; §1.2/§10.2 updated. Workspace gauntlet green (380 tests). `util/ordering` exact pinning deliberately excluded → mt-035.
- ✔ **mt-031** · R3 · IR lowering + goal assembly (`als-core` + additive `als-types` widening) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16, two parts. **Part A — choice recording:** `als_types::choice` — the mt-025 checker records every name/spine resolution (`NameChoice`/`SpineChoice`/`CallChoice`/`MacroChoice` with nested per-call-site tables) keyed by **(ModuleId, ExprId)** (parametric instances share a file), captured at the checker's own decision points, never re-derived; also exposed: module facts, sig appended facts, func bodies, field bounds, resolved command targets. **Verdict-neutrality proven**: alloy4fun gauge over all 150,891 codes byte-identical before/after (sha256 match, tech lead re-verified via `cmp`). **Part B — `als_core::lower::lower_command`** → `LoweredGoal` (goal + provenance-labeled conjuncts): full §2.1/§2.2 tables, §2.3 quantifier desugars (no skolemization per ADR-0011), int-literal promotion (`a.n = 1` → `= Int[1]`, jar-pinned), pred/fun inlining (recursion refused), synthesized field facts (mult/domain/defined — the §2.5 split pinned: mt-030 owns sig constraints, lower owns field facts, probes L1–L14 in translation-ref §10.3), `check` negation, `run`-pred existential params, disj guards/staged expansion. Deferred as **typed errors** (never wrong verdicts): temporal (Rung 6), String (Rung 4), `pred/totalOrder`/ordering (mt-035), nested-arrow field bounds + higher-order macros + misc (→ **mt-038**). 28 jar-quoted goldens + corpus: **167 files / 564 commands → 124 lower, 440 typed defers, 0 panics, deterministic**. Review fix: `a ; b` lowered as the pinned `a && after b` (was mis-pinned as `Until`; verdict-inert now, would have fossilized wrong into Rung 6). Gauntlet green (409 tests).
- ✔ **mt-032** · R3 · CDCL SAT solver (`als-solve`; [ADR-0011](adr/0011-rung3-translation-solving-architecture.md) owner decision) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als_solve::solver::CdclSolver` — hand-rolled, zero-dep (Cargo.lock untouched), deterministic by construction: two-watched-literal propagation, 1-UIP learning **with self-subsuming minimization**, non-chronological backjumping, Luby restarts (×100, no wall-clock), **integer VSIDS** (`u64` activities — bit-identical cross-platform, no floats) + phase saving, lowest-index tie-break; keep-all clause DB (sized against the 1129-re-solve SB-0 net; stable `ClauseRef`s). **Incremental enumeration seam for mt-033:** `new(&Cnf)`/`add_clause`/`solve` retaining learned clauses (sound: learned = resolvents of originals only) + `block(assignment, vars)`; `add_clause` re-picks watches among non-false lits so an already-falsified/unit blocking clause integrates correctly (the classic incremental bug, handled). One-shot `Cdcl` behind the `Solver` trait. **Correctness net (no jar needed):** brute-force reference + fixed-seed SplitMix64 fuzz — verdict parity, independent model check, **enumeration count = brute-force model count** (all-vars and random-subset projection), determinism per instance; PHP(≤8,7) UNSAT, colorings, parity; 1200 instances default (~0.85s), **30,000-instance release sweep re-run at review: green in 2.3s**. Perf smoke: PHP(8,7) 30ms, 200-var 3-SAT 28ms. Workspace 422 tests green.
- ✔ **mt-033** · R3 · CNF encoding + instance decode (`als-core`) (delegated → opus, tech-lead reviewed)
  Done 2026-07-16 — **mettle's first real SAT/UNSAT verdicts.** `als_core::{encode, solve}`: sparse boolean-matrix encoding (upper-bound tuples only, Tseitin circuit layer), ADR-0011 variable layout (primaries = every bounded relation's `upper∖lower` in `RelId`×lex order — unreferenced relations stay free, the jar's `r=r` equivalent; auxiliaries after), closure by iterated squaring, sequential-counter cardinality, two's-complement `int[·]` honoring the LEDGER-001 overflow switch (`SolveOptions`, forbid default); `Instance` decode + `InstanceEnumerator` (blocks primary vars via mt-032's seam). Int needs **measured** on the corpus (Const/Card/AtomToInt only — arithmetic/`sum` typed-defer to Rung 4). **Verification: 15 jar-verified verdict goldens all agree; SB-0 counts exact — probe T3 = 7, `#A=2` = 3, and `oracle/test1.als` show = 1129** (jar-pinned; the check command's 561-vs-464 count divergence is the documented no-skolemization effect — verdicts unaffected). Corpus end-to-end: 564 commands → 56 solved at the default 1s budget, 0 panics, deterministic. **Review found + fixed a second mt-029 scope bug (probe S1):** the reference's derivation rules run as *full passes* (re-run to exhaustion, restart on change); mettle's per-change restart let the abstract-difference rule back-derive an unscoped sibling to 0 — **11 wrong baseline UNSATs, all gone after the pass-discipline port** into `scope.rs` (+2 regression tests). Remaining baseline disagreement: **1** (`mediaAssets` `check PasteCut`, encoder under-constraint → mt-034 self-check/mt-037). Workspace 454 tests green.
- ✔ **mt-034** · R3 · Evaluator + self-check net (delegated → opus, tech-lead reviewed)
  Done 2026-07-16: `als_core::eval` — direct three-sorted evaluation over a concrete `Instance` (translation-ref §6; the future REPL substrate): quantifier/comprehension grounding, closure fixpoint, integer slice with the §2.4 overflow semantics mirrored exactly (`goal ∧ ¬overflow` accept-set, so encoder and evaluator agree). Self-check wired into `solve_goal`/enumeration (`debug_assert!` every SAT instance; `self_check` checked mode names the failing conjunct via mt-031 provenance). **Strongest net: encoder↔evaluator differential — 15 models brute-force-enumerated through the evaluator, counts equal the solver's SB-0 enumeration on every one** (two independent semantics implementations agreeing). 5 corruption tests name the right conjunct; corpus sweep self-checks all solved SAT instances, 0 failures. **Headline: the mediaAssets baseline disagreement root-caused** — self-check *passes* → under-constrained goal, not encoder: **field-group `disj` is dropped** (`ResolvedField` never records it, no disjointness fact synthesized; minimal jar-verified repro; and the command is `[3] PasteNotAffectHidden` — earlier notes' "PasteCut" corrected). Fix needs an als-types widening → assigned to **mt-038** (gate before mt-037); tripwire test `field_disj_dropped_known_gap`. Workspace 482 tests green.
- ✔ **mt-035** · R3 · `util/ordering` exact bounds (LEDGER-004 **approved** 2026-07-16 — the amended two-part rule) (impl checkpointed by tech lead; verification delegated → opus, tech-lead reviewed)
  Done 2026-07-17, two sessions (impl OOM'd mid-verification — mt-039's harness leak, not the WIP; verification completed after mt-039). Per the approved rule: part (a) `exactly`-param exact scopes (general `additionalExactScopes` mechanism) + the `ResolvedWorld::ordering` seam; part (b) `pin_ordering` — `first`/`next` rebound to exact constants for childless/enum ordered sigs only; partition-choice case governed by the hand-built `pred/totalOrder` formula (count = n!); exact-remainder pinning (T14a) + exact-abstract-parent size witness (T14d); the ordering-name recording gap closed via a recording-only closure re-resolve (alloy4fun gauge byte-identity vs `9167036` verified — sha256 match, covers mt-039's join-base fix too). **10 jar-pinned probe-matrix tests** (pin-engages: T10/T12/T13/T16/T19; no-pin partition-choice: T14a/b/c/e counts 42/6/6/6 + T15=16, several freshly jar-probed at sym0). **All 8 ordering baseline disagreements resolved** — root cause was NOT the ordering surface but a pre-existing higher-order-quantifier mis-lowering (multiplicity-marked arrow bounds bound per-tuple; now typed defers → mt-038) + a budget artifact; corpus baseline overlap **114 agree / 1 disagree** (the known mt-038 field-`disj`). T14d-family SB-0 count divergence (verdict-neutral jar labeling-count artifact) documented in LIMITATIONS. Workspace gauntlet + clippy green; sweep 0 panics / 0 self-check failures.
- ✔ **mt-039** · R3 · Deterministic solve/encode effort budgets + corpus-solve harness hardening (unplanned; incident 2026-07-17) (impl tech-lead + completion → sonnet, resolver fix → opus, tech-lead reviewed)
  Done 2026-07-17. Final sweep at smoke defaults (500 conflicts / 1M effort units, env-scalable): 564 commands → 140 solved (86 SAT / 54 UNSAT), 88 too_large, 37 over_budget, 292 lower_defer, 7 encode_defer, **0 panics, 0 self-check failures**, deterministic; 2:49 wall, 155 MB peak (was: OOM'd the box). Full-depth sweep (2k/5M): 173 solved, 114 agree / 9 disagree — 1 known (mediaAssets → mt-038), **8 new all-ordering (→ mt-035 verification)**. Workspace gauntlet + clippy green.
  **Incident:** `tests/solve_corpus.rs` spawned a worker per command and abandoned it on `recv_timeout` — abandoned solvers kept grounding/learning forever (keep-all DB, no caps); with mt-035's WIP unlocking ~270 ordering commands, leaked threads accumulated and OOM'd the box (twice, killing the tmux session). Worse: a worker *panic* dropped the channel and was silently bucketed as `over_budget` — mt-033's "0 panics" never covered the encode/solve phase.
  **Fix (all deterministic, STYLE D1):** `CdclSolver::solve_within(conflict_limit)` (budget-exhausted ⇒ `None`, solver stays usable, effort actually stops); `SolveOptions::{conflict_budget, encode_budget}` + `SolveVerdict::Unknown`; encoder effort meter (gate requests incl. constant-folded, join pair-scans, clauses) failing typed `TranslateError::CapacityExceeded`; corpus test rewritten inline — no threads, env-scalable budgets, panics surface.
  **Surfaced + fixed (latent mt-031/033 bugs the swallowed panics hid):** quantifier vars minted arity-1 regardless of bound (`all R, r: univ->univ` panicked the encoder; now bound-arity, per-tuple = the jar's first-order reading, closure.als[0] jar-SAT); explicit `set`/`some`/`lone` quant-decl markers were silently stripped (wrong-verdict class → now typed HO defers); higher-arity run-pred params were per-tuple-bound (jar binds them as free skolem relations → typed defer, free-relation params filed under mt-038).
- ✔ **mt-036** · R3 · `mettle exec` CLI — the Rung-3 human-testable build (impl → sonnet, tech-lead spec/review/merge; commit `7d3a10d`)
  Done 2026-07-17. `mettle exec <file.als> [--command <name|index>] [--allow-overflow] [--conflicts N] [--encode-budget N]` drives every root-module command through the full pipeline and prints verdicts — `SAT`/`UNSAT (no instance)` for `run`, `COUNTEREXAMPLE`/`VALID (no counterexample)` for `check` — plus the decoded instance (sigs/fields/skolems in `RelId` order, Alloy arrow tuple syntax), `expect 0|1` checking (`ok`/`MISMATCH`), and typed `CANNOT EXECUTE: <reason>` lines for honest defers. **Named `exec` after the reference jar's own CLI verb** (the bead's working title "mettle run" would confusingly execute `check` commands; the Rung-2 `mettle check` static checker is untouched). Budgets default *unlimited* (interactive tool; the deterministic smoke budgets belong to the test harness). Exit 0 clean / 1 mismatch-defer-unknown-or-diagnostic / 2 usage / 141 pipe. 12 integration tests (`tests/exec.rs`); deterministic byte-identical output pinned; libraries untouched (E3 held — all rendering in the CLI crate; assert names recovered display-only from the AST). Smoke-verified on farmer.als (8-state ordering SAT instance + both expects ok), closure.als (counterexample), mediaAssets (VALID/COUNTEREXAMPLE/UNKNOWN at a bound), trash.als (temporal defer, exit 1). **→ owner touchpoint.**
- ✔ **mt-037** · R3 · Differential solve gauge + SB-0 counting net — the Rung-3 exit (impl → opus, docs pass → sonnet, tech-lead spec/review/triage/fixes)
  Done 2026-07-18. **Tool:** `solve-gauge` bin in `als-conform` (`crates/als-conform/src/solve_gauge/{mod,baseline,detect}.rs`, `src/bin/solve_gauge.rs`, `tests/solve_gauge_integration.rs`; `als-core` added as a crate dependency). Stage 1 runs the full mettle pipeline over every root command of both corpora and diffs verdicts against every `baselines/*-verdict.json`, partitioning every command into exactly one typed bucket (partition asserted). Stage 2 (`--count`) checks SB-0 instance-count parity against the jar, with typed skips for the documented divergence families (fo-skolem, ho-skolem, ordered-abstract partition, caps/timeouts). Deterministic text+JSON report, live progress via a render-free callback. Golden smoke: `oracle/test1.als` `run show` count_match 1129=1129, `check NoEmpty` skip_fo_skolem. **Defaults re-tuned after measurement:** conflicts 5000 / encode-budget 4,000,000 / primary-var-cap 20,000 — the originally spec'd 200k/50M defaults let single huge commands grind past 10h CPU; flags scale up for deeper runs.
  **Fresh jar baseline:** `baselines/portus-63-verdict.json` (158 commands / 63 files, 45/48 expect-matches, 3 stale-expect mismatches — vendored dijkstra/peterson copies — 10 file timeouts at 60s). Comparison set now **392 cached jar verdicts** (`baselines/README.md` already reflects this).
  **Three wrong-verdict conformance bugs found by the gauge, all fixed + regression-tested:**
  1. **Non-zero `String` scope silently solved against an empty String sig** (`fm2cfs.als`, 6 wrong UNSATs): the jar fills a non-zero String scope with the literal atoms plus synthetic padding atoms (recorded here as `unused%d` at the time; **mt-043 correction: the padding atoms are actually `"String%d"`** — `unused%d` is instance-display renaming, a different mechanism); mettle minted none and solved anyway. Fix: `als-core::scope` typed-defers (`TranslateError::StringUnsupported`) any `exactly N String` with N>0; `exactly 0 String` is unaffected. Tests: `tests/scope.rs` (`nonzero_string_scope_defers_typed`, `zero_string_scope_stays_solvable`).
  2. **Multiplicity-marked arrow on the RHS of `in` silently stripped to a plain product** (`hotel2.als[0]` wrong SAT: `Room<:keys in Room lone-> Key` lost its `lone`). Fix: routed through the jar-verified `arrow_value_constraint` conjuncts (membership + per-column mult quantifiers); a marked arrow anywhere *else* in an expression is now a typed `LoweringUnsupported` defer, never a silent strip, via a new `lower_rel_stripped` helper for the two callers that legitimately strip (membership products, skolem uppers). Tests in `tests/solve.rs`: `in_mult_arrow_rhs_enforces_columns`, `mult_arrow_outside_in_rhs_defers_typed`.
  3. **Clean-room stdlib `util/ordering` had `min` and `max` bodies swapped** (`min` returned the maximum) — surfaced because fixing bug 2 exposed `hotel1.als[0]`: mettle's old SAT agreement there was a *coincidence*, a bogus counterexample exploiting bug 2; with bug 2 fixed, mettle went wrong-UNSAT because `nextKey`'s `min` walked the ordering backwards and excluded the book's genuine counterexample. Fix: two-line body swap in `crates/als-types/stdlib/util/ordering.als` (`min[es] = es - es.^next`, `max[es] = es - es.^prev`), direction pinned by black-box jar differential probes (clean-room posture holds — the fix swaps our own existing text, never upstream's). Jar-pinned regression `ordering_min_max_pinned` in `tests/solve.rs` (three verdicts jar-verified). **alloy4fun byte-identity re-verified**: resolve-gauge over all 150,891 codes, sha256 identical before/after.
  **One count divergence root-caused and filed as mt-041** (leave that bead): `models/util/ordering.als` run as its own root module — the jar applies its exact-bounds optimization by recognizing the module directly (count 1), mettle's pinning keys on *opening* the module (counts n! = 2/6/24). Verdict-neutral.
  **Final gauge (2026-07-18, defaults 5000 conflicts / 4M encode-budget / 20k var cap):** 564 commands → verdict buckets **agree_sat 104, agree_unsat 83, DISAGREE 0, jar_nonverdict 2**; typed defers **capacity 71, encode 14, lower:higher_order 4, lower:lowering 217, lower:temporal 22, over_budget 37, scope 10** (the String defers); **0 panics, 0 self-check failures**. Counting net: count_match 15, COUNT_MISMATCH 3 (all the mt-041 family), skip_fo_skolem 55, skip_ho_skolem 7, skip_jar_timeout 1, skip_mettle_cap 23. **Baseline overlap: 187 agree / 0 disagree over 392 jar verdicts.** Run it: `cargo build --release -p als-conform && ./target/release/solve-gauge [--count]`. **Also filed this session:** mt-042 (machine portability, owner-directed). **Rung 3 exit gauge green → owner touchpoint.**
- ✔ **mt-038** · R3 · Close the mt-031 lowering defers + the field-`disj` wrong verdict (delegated four waves: disj → opus, nested arrows → sonnet, HO skolemization → opus, small buckets → sonnet; tech-lead specs/review/merge + the post-colon-`disj` negative-space guard)
  Done 2026-07-17, four commits (`f254979`, `b7cd70a`, `48d936e`, `2a6e950` + docs). **(1) Field-group `disj` synthesized** (the wrong-verdict class): `ResolvedSig::field_disj_groups` widening + the jar's staged `no(f0&f1) ∧ no((f0+f1)&f2)…` over full field relations (probes p1–p7, §10.3 L15–L19; `var` groups `always`-wrapped → temporal defer); tripwire flipped to `assert_unsat`; **`mediaAssets.als[3]` cleared — 0 baseline disagreements**. **(2) Nested multiplicity arrows** lower at any depth via the recursive per-column rule (probes n1–n7, L20–L26), factored as `arrow_value_constraint` over any relation value. **(3) HO skolemization** (§2.3/§10.6, T9a–T9g): polarity-threaded (`Pol`), HO decls + relational run-pred params become free relations (lower `{}` / sound `abstract_upper`) bound into augmented `Bounds` — encoder/enumeration/decode/self-check untouched; non-skolemizable HO → typed `HigherOrder` with the jar's exact error text; `ringlead`/`firewire` now lower. **(4) Small buckets:** int-sorted exprs in relation position cast via `IntToAtom` (jar probe: `+` stays relational union, §4.5); explicit-receiver `this` read from `args[0]` per `CallChoice::implicit_this` — a **latent wrong-verdict class** (args shifted left) fixed. **Plus:** post-colon `disj` (`f: disj e`) was a silent drop → now a typed defer everywhere (`bound_disj_unpinned`, zero corpus incidence, → mt-040). Final: corpus **321/564 lower** (was 124 at mt-031), 243 typed defers (root-caused per bucket), sweep **104 agree / 0 disagree**, 0 panics, 0 self-check failures, deterministic; alloy4fun gauge **byte-identical** over 150,891 codes for both als-types widenings (sha256 match vs `c57002d`). Remaining defers reclassified → mt-040 (resolver recording gaps), Rung 4 (integer-ordering builtins 39, seq), Rung 6 (temporal 22).

## Now (Rung 4 — integers, coverage & counting: "it agrees with Alloy across everything I have")

Filed 2026-07-18 after the Rung-3 owner touchpoint. **Rung gauge (proposed; ADR-0012 at mt-043 formalizes it, owner blesses the gate):** solve-gauge **0 verdict disagreements with every defer bucket accounted for** — the only remaining typed defers are temporal (Rung 6), the jar's own errors (higher-order parity, already holding), and an honest small capacity/budget remainder; the counting net loses its `skip_fo_skolem` family (mt-047) and the mt-041 mismatches. Scorecard motion: baseline overlap climbs from 187/392 agree toward "everything non-temporal agrees." Per mt-027's standing rule, any bead touching type machinery notes its effect on the 314 alloy4fun over-accepts.

**Planning inventory** (from the mt-037 gauge over 564 corpus commands): `lower:lowering` 217 (≈140 = mt-040 recording gaps, 39 = integer-ordering builtins → mt-044, remainder seq/arithmetic/small buckets → mt-044/046), `capacity` 71 + `over_budget` 37 + `encode` 14 (→ mt-049), `temporal` 22 (Rung 6), `scope` 10 (String → mt-045), `higher_order` 4 (jar errors — already at parity). Counting skips: `fo_skolem` 55 (→ mt-047), `mettle_cap` 23 (→ mt-049), `COUNT_MISMATCH` 3 (→ mt-041).

- ✔ **mt-043** · R4 · Pinned Rung-4 semantics contract + ADR-0012 (delegated → opus, tech-lead reviewed; probes I11/S2/K4 independently re-run against the jar at review — all confirmed)
  Done 2026-07-18: [reference/alloy6-translation.md](reference/alloy6-translation.md) **§11–§16** + probe matrices **§10.7–§10.12** (~50 jar probes, sources at `794226dd`, both overflow settings): integer arithmetic at bitwidth (div toward zero, rem sign-of-dividend, `<<`/`>>`/`>>>` = logical-left/arithmetic-right/logical-right; **forbid mode is the Milicevic/Jackson polarity rule, NOT flat `∧ ¬overflow`** — decisive probe I11: `all n: Int | plus[n,7] >= n` is UNSAT allow / SAT forbid); `Int/next`/`Int/zero` are the only builtin Int relations translation actually references (`min`/`max` lower as int constants — simplifies mt-044); String = referenced-literal collection + **`"String%d"` padding** (the `unused%d` claim in earlier docs was wrong — that's instance-display renaming of sig-unclaimed atoms; LIMITATIONS/STATE corrected at review) + `max(N,#referenced)` expansion + non-exact-scope reject; `seq X` = `seq/Int -> lone X` + exactly one contiguity fact `dom(f)−dom(f).(Int/next) ⊆ Int/zero`; FO skolemization = depth-0 top-level effective-existentials, `$<cmdLabel>_<var>` naming, count effect pinned (`some x:A|x=x` for 3 = jar 12 vs 7); SB=20 = lex-leader length cap, never the verdict. **Ledger: LEDGER-006 (`#`) / LEDGER-007 (String) / LEDGER-008 (seq) at `verified` → to owner; LEDGER-005 (integers) `proposed`** with two named residuals (allow-mode `div[x,0]` sweep for zero/negative dividends; deep mixed-polarity forbid coverage) — closed at mt-044 start, before arithmetic ships. **[ADR-0012](adr/0012-rung4-integers-strings-counting.md) Proposed** (integer ops extend mt-033's encode layer with the mt-034 evaluator matched pair binding; String minting in scope; seq split bounds/lower; FO skolem extends mt-038's HO machinery, superseding ADR-0011's FO stance for Rung 4; SB posture; exit gate awaiting owner blessing).
- ✔ **mt-040** · R4 · Resolver recording gaps + `disj`-bound semantics (delegated → opus, tech-lead reviewed: gauntlet + solve-gauge + byte-identity all independently re-run, one misleading comment fixed, probe rows L27–L29 added to §10.3 at review)
  Done 2026-07-18. **(a) 0-param relation-valued `let` macros as spine bases** (`adjacent[s]`/`s.adjacent`) now record via `CandOrigin::Macro` + `flush_rec::record_zero_macro`; the same fix records **compound/call sub-exprs buried as a join's compound-right operand** (`fr.(w.*co_pa)`, `rf_pa.(Ghost[g])` — `RecNode::Sub(ExprId)` resolved standalone, recording-only) — together the dominant unlock (tso's 77-command `co_pa` cluster, ptcris's 20-command `similar`). **(b) callable-by-name HO-macro args**: `MacroChoice::callables` records the `FuncId` (conservative: unambiguous single overload only); lowerer binds `Binding::Callable` and inlines `param[args]` as the real call. **(c) `run`-target own-module priority: PINNED, not shipped** — 7 decisive probe pairs + sources confirm own-module-first (`getRawQS` before `getRawNQS`); **LEDGER-009 filed at `verified`** (awaiting owner approval), jar-pinned test `run_target_own_module_first` `#[ignore]`d ready to flip; the 2 commands stay typed defers. **(d) post-colon `disj` field bounds lower**: jar-pinned fact `all this,that: owner | this!=that implies no (this.f & that.f)` (§10.3 L27–L28, DumpK2 + decisive SAT/UNSAT pair, uniform across arity/mult); the **quant-decl `x: disj e` turned out to be a jar resolve error** (L29) — kept an honest typed defer instead of synthesizing a fact the reference forbids. **Gauge:** corpus lowering **311 → 443** (defers 243 → 111); solve-gauge **agree_sat 104→114, agree_unsat 83→86, DISAGREE 0**, `lower:lowering` 217→85, capacity 71→187 (big unlocked models hitting solver budgets — mt-049's business), 0 panics, 0 self-check failures. **Byte-identity: sha256 `00eb950a…` identical** over all 150,891 alloy4fun codes (agent twice, tech lead once) — the 314 mt-027 over-accepts unchanged by construction. Honest tail documented in LIMITATIONS: `$`-meta names, nested param-macro receivers, ordering `next`.
- ✔ **mt-044** · R4 · Integer fidelity end-to-end (delegated → opus impl + sonnet probe agent ×4 rounds, tech-lead reviewed with two mid-flight corrections + independent gauntlet re-run and 5 hand-probe verifications)
  Done 2026-07-19. **Semantics first:** LEDGER-005's residuals closed and then some — full 16×16 div/rem sweep (`div[x,0]=−sign(x)`; **`div[MIN,−1]=−8`, correcting mt-043's I8** — `(0-1)` was a probe-spelling artifact, only `(0-8)` hits the MINUS peephole), and a three-round forbid-mode investigation that first "found" two guard defects, then **retracted one**: `negate[8]` carries a permanent forbid-mode overflow flag (it computes `minus[0,8]`) that silently emptied every conjunction-shaped probe domain, mimicking a nesting-position defect. Final pinned rule is a single per-variable classification (universal-rescue iff the binder's own domain is literally bare `Int`/`seq/Int` — a sig-/comprehension-bound ∀ is *excluded*, `DefCond.isInt` defect, so the common `all p: Sig | #…` shape never rescues); shift semantics pinned (§10.7d: `⌈log2 w⌉` amount mask, `>>`/`>>>` never self-overflow, `<<`'s junk-bit spurious-overflow circuit). **LEDGER-005 `approved`** (tech lead, no-fork) with the jar-unpinned ITE/`implies` sliver carved out as a standing typed defer. **Implementation:** encoder circuits (incl. structural ports of Kodkod's non-restoring division and `shl` overflow loop) + evaluator matched pair, shared `overflow_guard.rs` classifier, `Int/next`/`Int/zero` bounds, `fun/min|max|next|prev` lowering, **LEDGER-009 shipped** (own-module-first, test live), `int_conformance.rs` (jar-pinned constants, CI-safe). **Gauge: agreements 243 → 248 (151 SAT / 97 UNSAT), DISAGREE 0**, 0 panics, 0 self-check failures, byte-identical ×2 + tech-lead re-run; the integer-builtin/arithmetic defer sites are gone (net bucket movement is small — formerly-deferring commands mostly still defer on other constructs or budgets; buckets: lowering 83, capacity 98, encode 15, over_budget 68). Honest new defers: 10 eq-typing commands (→ mt-051) + the ITE/`implies` sliver.
- ✔ **mt-051** · R4 · Int-equality typing rule pinned + implemented; the 10 eq-typing defers AND the ITE/`implies` sliver retired (probe → sonnet; impl → opus; two tech-lead verification probe rounds)
  Done 2026-07-21. **Semantics first (LEDGER-010, §10.7e):** the "jar int-compares one-sided `=`" hypothesis in this bead's own text was WRONG — the pinned truth is **set equality always** (int-compare only when both translated sides are `IntToExprCast`; `#priority = #pid + 1` is the union pitfall, forcing `#pid=#priority=1` as sets — peterson's UNSATs are bug-compatible set collapse, not arithmetic). Forbid mode has three pinned layers: **(A)** an overflowed capable cast denotes the **empty set** (jar cells are env-less `Int.eq` → per-cell `∧¬of`; polarity/context-independent), **(B)** the §10.7c rules-0–3 guard threads through the compared sides' set structure (`=`/`in`/mult-tests, unions included; bare-Int-∀ RESCUES — first jar-confirmed set-eq rescue; closed operands EXCLUDE), **(C)** a translation-constant cast contributes no (B) guard (the R-cardun/T5/T6 constant escape; int-compare path keeps constant flags). **Rule 4 pinned** (ITE either-branch + `implies`-antecedent escape = correctly-classified rescue; bare `!` is NOT an escape, probe V-not) — LEDGER-005's carve-out closed. **Review drama, filed in LESSONS:** the probe agent's 27 cells all reproduced but its proposed rule ("no dependency ⇒ no guard") was refuted by the existing mt-044 pinned tests + a closed-but-variable-circuit discriminating cell; the tech lead's own first cells burned on the `(0-2)` spelling artifact (the mt-044 `(0-1)` family), which surfaced that **mettle lacks the `0-(max+1)` MINUS peephole entirely → filed mt-052**; the R-cardun mystery bottomed out via `javap` bytecode checks + the `Int.java` env-less-`eq` discovery. **Implementation (opus, tech-lead reviewed; encoder/evaluator matched pair):** shared `collect_capable_casts` + `translation_constant` in `overflow_guard.rs` (both back ends use the same predicates by construction), cast-emptying at `IntToAtom`, guard threading at `RelCompare`/`MultTest`, `eq_typing_defer` + `GuardDecision::Defer` deleted; delegate caught the probe-spelling issue in the test models and respelled via `min` — tech lead re-pinned all six respelled cells against the jar before accepting. **Tests:** new `eq_typing_conformance.rs` (14 fns: Q1 value cells, rescue/defectA, closed D4/T1/T4/D6/D3a, constant-escape trio, T7 MultTest, full Part C family incl. V-not); `int_conformance.rs` defer row → rule-derived verdicts. **Gauge:** agree 276 → **289 (166 SAT / 123 UNSAT), DISAGREE 0** over 392 cached verdicts, **`encode` defer bucket eliminated** (14 → 0; capacity 113, over_budget 62), 0 panics, 0 self-check failures, byte-identical ×2; full workspace gauntlet green. money.als solves SEND+MORE=MONEY end-to-end (`mettle exec` SAT).
- ✔ **mt-045** · R4 · String support (delegated → opus, tech-lead reviewed: gauntlet + gauge independently re-run, worktree HEAD-baseline diff, fm2cfs exec smoke test)
  Done 2026-07-19. **New `als-core::strings`** ports `Command.getAllStringConstants`: referenced-literal collection over the command goal + all module facts + sig appended facts + field-decl bounds, recursing into **called** funcs/preds only (probe S7) via the same `als_types::choice` table the lowerer inlines from — collection and lowering can never drift. Scope/universe phase (`scope.rs::mint_string_atoms`) appends the quoted literal atoms **last** (after the ascending int atoms, §1.3), pads an exact `for … but N String` scope with **`"String%d"`** atoms (LEDGER-007; never `unused%d`), expands to `max(N, #referenced)` (probe S4); non-exact scope stays the pre-solve `StringScopeNotExact` reject (S1). Bounds phase binds `String` **exactly** to them + one exact singleton per referenced literal (the jar's `s2k` map, `BoundsResult::string_denote`); `ExprKind::Str` lowers to that singleton — **`TranslateError::StringUnsupported` deleted entirely**. Encoder + evaluator gained an explicit `int_end` (int atoms no longer run to the universe's end — string atoms trail them), a latent-assumption fix the matched pair needed. Deterministic literal order = lexicographic (jar's is HashSet-nondeterministic; string atoms are symmetric, verdict/SB-0-count neutral — the LEDGER-007 micro-choice). **Tests:** `string_conformance.rs` pins S1–S7 + solve-level (distinct-literal singletons, `#String` against exact scope, String-typed field columns); LEDGER-007's Test field filled. **Gauge (HEAD-worktree before/after at identical defaults): agree 248 → 257 (+6 SAT incl. fm2cfs's 6 formerly-wrong-UNSATs, +3 UNSAT), `scope` 10 → 0 (9 → agreements, 1 → over_budget), every other bucket identical, DISAGREE 0, 0 panics, 0 self-check failures, byte-identical ×2.** Note: gauge `jar_nonverdict` is 16 at current budgets (STATE's old "2" was the mt-037-era figure) — pre-existing, untouched by mt-045.
- ✔ **mt-046** · R4 · `seq` support (delegated → opus impl + sonnet enum-budget follow-up, tech-lead reviewed with two independent probe rounds and one clean-room body correction)
  Done 2026-07-19. **Implementation:** `seq X` field = `seq/Int -> lone X` (arity-3, index column upper-bounded by the `seq/Int` atoms) via `lower.rs::seq_arrow_constraints`, plus the **per-owner** contiguity fact `dom(this.f) − dom(this.f).(Int/next) ⊆ Int/zero` emitted inside the `all this: owner` wrapper — per-owner-vs-global pinned by decisive probe mt046-contig (§10.10a: owner with {1} and no {0} → jar UNSAT); expression-position `seq` keeps the marked-arrow typed-defer posture. **Clean-room stdlib verified differentially and corrected twice:** the agent found `idxOf`/`lastIdxOf` had their min/max closures swapped in all three seq modules (jar: `idxOf` = first occurrence); tech-lead review then re-probed the corner the agent's probes missed — **`afterLastIdx` is the smallest UNUSED index, not `lastIdx.next`** (gapped `{1->e}` → 0; full sequence → none; probes mt046-noncontig/-full/-seqrel-gap) — and rewrote the three bodies to the min-unused form (no empty-case hack). **Counting-net hardening (follow-up, sonnet):** `correctChord.als` (39 commands, 22 capacity + 13 jar-nonverdict-now-solving + 4 over_budget — it alone dominates sweep time) ground the SB-0 net for an hour via unbudgeted per-instance enumeration solves; added `SolveOptions::enum_conflict_budget` (cumulative, deterministic, conflict-counted) + `solve-gauge --enum-budget` (default 2M) ending in a typed `skip_enum_budget`, never a truncated count. **Tests:** `seq_conformance.rs` (15: Q1–Q4, per-owner contiguity regression, lone-value, stdlib direction + min-unused + add-to-empty regressions) + enumerator budget-exhaustion tests in `tests/solve.rs`; LEDGER-008 Test field filled. **Gauge:** agree 257 → **258, DISAGREE 0**, `lowering` 83 → 43 (the seq family gone: +1 agree, +13 jar_nonverdict, +22 capacity, +4 over_budget — big models hitting throughput budgets, mt-050's business), byte-identical ×2, 46/46 suites. **Counting net terminates:** count_match 15 → 27, COUNT_MISMATCH still exactly the 3 filed mt-041 rows, skips typed (fo_skolem 90, cap 28, ho 9, jar_timeout 1). Sweep wall-time is now ~25 min (chord-dominated) — parallelization is mt-050.
- ✔ **mt-047** · R4 · First-order skolemization (delegated → opus, tech-lead reviewed with two independent fixes + the runtime-observability campaign)
  Done 2026-07-20. **Implementation:** a top-level effective-existential FO decl (positive `some`, or a `check`'s negated `all`; never under a universal — K3) now mints a skolem **constant** relation via the shared `alloc_skolem` core (extends mt-038's HO machinery/`Pol` thread): lower `{}`, upper = `abstract_upper`, membership + `one` conjoined, `un.make`-style name uniquification; run-pred receivers/params skolemize the same way. **Naming jar-exact incl. inlined funcs** (tech-lead fix: `inline_stack.last()` supplies the §15 funcName prefix — `run foo { q }` with `some x` in `q` mints `$q_x`, pinned by `called_pred_existential_uses_func_label`). **Seam bug found at review:** `abstract_upper` only consulted the bounds builder's table, so a decl bounded by an *earlier skolem* (`check { all b: Book, n: b.names | … }`) silently lost its skolem — an SB-0 under-count (addressBook2e[3] 6744 vs jar; addressBook3b[3] 4920 vs 6024) and 5 spurious `higher_order` defers; one fix (also read `skolem_bounds`) cured all seven, regression `dependent_decl_skolemizes_through_earlier_skolem`. **Gauge:** agree 258 → **276 (163 SAT / 113 UNSAT), DISAGREE 0**, `higher_order` back to exactly the 4 jar-error-parity commands, over_budget 73 → 59 (skolemized goals solve easier), byte-identical sweeps, 47/47 suites. **Counting net:** `skip_fo_skolem` (90) **retired**; count_match 27 → **48**; COUNT_MISMATCH = exactly the 3 filed mt-041 rows; jar-pinned K1–K4 + NoEmpty=561 in `fo_skolem_conformance.rs` (12 tests). **Runtime-observability campaign (owner-directed, same chunk):** the enumeration budget went through two wrong denominations (conflicts, then +decisions) that each ground 3h before the right one — **effort = conflicts + decisions + propagation clause-visits** (new deterministic solver counters), default 250M (≈≤80s/command, `--enum-budget` to deepen; 51 typed `skip_enum_budget`); the gauge now emits a stderr liveness heartbeat + slowest-10 table; every long run carries a hard `timeout`. Full `--count` completes in ~85 min. Lessons filed (budget-denomination entry); mt-050 inherits the ptcris/chord slow-command table as its perf worklist.
- ✔ **mt-048** · R4 · Symmetry breaking (delegated → opus, tech-lead reviewed with three review probes + two review fixes; posture per ADR-0012 decision 6)
  Done 2026-07-21. **Contract-first:** `SymmetryDetector`/`SymmetryBreaker` at `794226dd` pinned in translation-ref §16.1–§16.4 (fetched to `scratchpad/src794/`): coarsest-partition detection, the per-`(class, adjacent-pair)` lex-leader with the **soft cap at relation boundaries**, `(arity, name)` relation order (`$`-skolems eat slots first), mirror/identity filters, trivial-circuit escape, `expect 1 ⇒ SB 0`. **Implementation (opus):** `encode/symmetry.rs` (signature-grouping partition — proven equal to Kodkod's on every bounds shape mettle emits, diagonal caveat documented; `SbpPlan`), `generate_sbp`/`lex_leq` in the encoder (Tseitin-only — primary vars untouched), `SolveOptions::symmetry` default **20**, expect-forcing in exec + both gauge stages, gauge `--symmetry`/`--count-symmetry`, `sb_conformance.rs` (9 jar-pinned probes incl. cap-sensitivity at SB=2/1). **Review was the story again:** (1) the pinned §16.1 "retainAll/usesInts" gating from the *static* source is a **dead letter on the jar's solve path** — probes Y6/uf1-SB20/fmrun show nothing is mention-gated and every int **and string** atom (padding included!) is always a singleton class (per-int exact bounds; per-atom `s2k` singletons, A4Solution.java:391–400); the missing string singletons were the root cause of the fm2cfs SB-20 family (6 mismatches → 5 matches + 1 typed cap-skip), and the dead `usesInts` walker was deleted; (2) the unmentioned-sig probe pinned full-bounds participation (SB-0 56 / SB-20 12); (3) a `univ`-field probe surfaced the **pre-existing mt-053** live-universe divergence (nothing to do with symmetry). **Final gauge (both sweeps byte-identical ×2, 0 panics, 0 self-check failures):** verdict net at SB-20 default — **agree 300 (166 SAT / 134 UNSAT), DISAGREE 0** (SBP resolved 18 formerly over-budget commands); SB-0 counting net unchanged (**49 matches**, mismatches = mt-041 ×3); new **SB-20 count net: 71 matches, 6 mismatches** = mt-041 ×3 + the filed mt-055 residual (tso ×2, wetdry ×1).
- ✔ **mt-049** · R4 · Throughput: solver `reduce_db` + env-cached grounding memoization + budget re-tune (delegated → opus, tech-lead reviewed + independently re-ran gauntlet; tech lead ran the re-tune measurements as tracked jobs after two detached agent runs died)
  Done 2026-07-19 (commit `20d0560`). **Solver:** deterministic learned-clause reduction — integer LBD ranking (lowest-`ClauseRef` ties), least-useful half tombstoned (ClauseRef-stable, unwatched, literals freed) on a cumulative-conflict schedule (first 2k, +300/round; `set_reduce_schedule` override); only solver-learned resolvents deletable — original/blocking clauses permanent, locked clauses kept; new `fuzz_with_forced_reduction` (reduce on ~every conflict) over the 30k-instance brute-force net: verdicts, enumeration counts, determinism all invariant. **Encoder:** memo caches re-keyed `(node, bindings of exactly its free vars)` via new `encode/freevars.rs` (per-node free-var sets, BTree-ordered); sub-exprs not mentioning the innermost binder encode once, gates shared across bindings; full-env nodes deliberately uncached; primary-var model set unchanged (encoder↔evaluator differential + self-check green). **Budgets (measured):** conflict default 5k→10k (sweep 15:45 wall); **encode ceiling stays 4M** — 8M timed out >40 min, 24/50M grinds single commands for 8.5+ CPU-hours (the mt-037 grind mode; a 30k/24M probe was killed at 8.5 CPU-hours with zero output). **Gauge at new defaults: agree_sat 142, agree_unsat 101 (= 243 agreements, was 200 at mt-040), DISAGREE 0, capacity 187→99, over_budget 69, 0 panics, 0 self-check failures, byte-identical twice.** Note: the gauge's `capacity` bucket = the *encode-budget* family (`CapacityExceeded`), not the 20k primary-var cap (that bucket is 0 corpus-wide). Honest remainder in LIMITATIONS: capacity 99 + over_budget 69, each solvable at larger per-run budgets via flags.
- ▢ **mt-052** · R4 · MINUS peephole: `0-(max+1)` in expression position is `IntConstant(min)` in the jar (filed at mt-051 review; live wrong-verdict candidate)
  `TranslateAlloyToKodkod.java:1239-1248` (commit `794226dd`): `case MINUS` special-cases a literal-`0` LHS with a literal-`max+1` RHS to `IntConstant(min)` — everything else stays set difference. mettle treats `0-8` (bw 4) as `{0}−{…} = {0}`, so `fact { F.v = 0-8 } run { F.v = min }` is mettle-UNSAT vs jar-SAT (verified at mt-051 review, `scratchpad/probe/`). Small fix in lowering's `-` handling; needs the command bitwidth at that point plus a jar-pinned test pair (in-range `0-2` must STAY set difference — the mt-044 `(0-1)` artifact family). Zero corpus incidence at current gauge (DISAGREE 0), so opportunistic.
- ✔ **mt-054** · ops · **Throughput & feedback-loop campaign, parts (a)–(d)** (owner-directed 2026-07-21; impl delegated → opus in one wave, tech-lead review + all slow verification; part (e) = the mt-042(b–d)→migration chunk, tracked there)
  Done 2026-07-21 (commits `b3bd80b` + baselines/docs). **(a) Parallel deterministic fan-out:** `solve-gauge --jobs N` — `parallel.rs::parallel_fold` (scoped threads, atomic work counter, mpsc progress replayed on the coordinator so the library's `FnMut(&str)` sink needs no `Send`; results fold position-ordered, never completion-ordered; fetched items always run to completion per mt-039). **Verified: full 564-command stage-1 sweep sha256-identical (stdout AND json) at `-j1`/`-j2`, mt-048 numbers exact (agree 300, DISAGREE 0); 12:09 vs 18:47 wall (~1.55× on 2 cores, tail-dominated — multiplies with the bigger box).** **(b) Cached jar count baselines:** `baselines/{alloytools-models,portus-63}-count-sb{0,20}.json` (config header: symmetry/cap/overflow/solver hard-error on mismatch, `jar_timeout` warn-only; per-command typed outcomes; miss = `skip_no_count_baseline`), captured via `--refresh-counts` (parallel JVMs, atomic rewrite per file, `--resume`; ~2h40m one-time). Live-vs-cache delta triaged: 4 files at the 300s JVM boundary → re-captured at 900s into `*-slow-count-sb0.json` supplements (merge = sorted-name, later wins); chordbugmodel ×2 converted, ceilingsAndFloors/life = the net's 3 honest `skip_jar_timeout`. **Both nets reproduce the live-era results exactly, JVM-free: SB-0 count_match 49 / mt-041 ×3 in 27:39 (was ≈78 min); SB-20 71 / 6 in 26:20 (was ≈56 min).** `--live-jar` keeps the live path (integration tests + out-of-corpus smokes). **(c)** `--fail-fast` (dispatch-stop only, in-flight finishes; PARTIAL-marked report, exit 1), `--only` substring filters, `--from-report`/`--from-buckets` delta re-runs over the report's new `per_command` array. **(d)** `status.rs` `StatusFile` — long runs atomically rewrite git-ignored `status/<tool>.txt` (~2s rate limit; phase, k/N, current file, last-10 heartbeats, DONE summary) tee'd from the same progress stream as the stderr heartbeat (library stays E3-clean); `scripts/watch-status.sh`. Review: delegate's 9-item divergence list adjudicated (all sound — notably the untagged-enum deserialization workaround and extending the config guard to `--resume`); one review fix (restored the measured budget-rationale comments); tech lead re-ran all fast gates (51 tests, fmt, clippy) + owned every slow sweep (deadlined, status-observed).
- ▢ **mt-055** · R4 · SB-20 count-net residual: tso_transistency ×2 + wetdry ×1 (filed at mt-048 wrap; count-only, verdicts agree, SB-0 counts unaffected)
  The SB-20 net's only non-mt-041 mismatches after the string-singleton fix: `tso_transistency_perturbed.als[0]` mettle=121 jar=11, `[4]` mettle=96 jar=8 (mettle **under-breaks** ~11–12×; fingerprint: `univ`-typed relation-valued pred params `p: PTag->univ` → HO skolem uppers built from mettle's all-atoms `univ` — likely entangled with **mt-053**, so probe again *after* that fix); `wetdry.als[0]` mettle=888 jar=784 (mettle under-breaks; no String/seq/univ in the model — un-root-caused, needs shrinking). Not rung-gating: ADR-0012's exit-gate counting criterion is SB-0-based, where all three commands are clean. Approach: shrink each to a minimal probe (the mt-053 lesson: test the *deployed* shape), pin the jar mechanism, extend §16 + `sb_conformance.rs`.
- ▢ **mt-053** · R4 · `univ`/`iden` are the **live** universe in the jar, not the all-atoms constant (filed at mt-048 review; live wrong-count, wrong-verdict candidate)
  `A4Solution.java:336–338` + `:699` (commit `794226dd`): the jar's `univ` starts as `Expression.INTS.union(KK_STRING)` and each **top-level** sig's *live expression* is unioned in — so `univ` = Int ∪ String ∪ (union of top sigs' actual sets), excluding allocated-but-dead sig atoms; `iden` = `Expression.IDEN ∩ (a2k(UNIV) × UNIV)` (TranslateAlloyToKodkod.java:824/893), the live-restricted diagonal. mettle lowers `RelConst::Univ`/`Iden` as the all-atoms constant/diagonal. Pinned probe: `sig A { f: A -> univ } run { some f } for 2 A, 1 Int` — SB-0 jar **65549** = 65535 + 2·(2³−1) (a live-`univ` row has 3 columns when `A` = one atom) vs mettle **65565** = 65535 + 2·(2⁴−1); jar SB=20 = **32902** (`scratchpad/src794/sbprobes/review/`). Wrong-verdict shape: `run { some u: univ | u !in Int and u !in A … }`-style goals. Zero corpus incidence (solve-gauge DISAGREE 0; SB-0 counting net mismatches = mt-041 only). Fix in lowering's `univ`/`iden` denotations (a union RelExpr over top-sig denotes + `Int` + `String`), NOT in the encoder's internal `RelConst::Univ` (the jar itself keeps Kodkod's true-UNIV for internal uses, e.g. skolem decl domains); needs a probe matrix (subset sigs, strings present/absent, `iden` in expressions) and the sb_conformance univ probe re-pinned. When fixed, `goal_uses_ints` starts seeing the `Int` reference inside `univ` — jar-faithful (`usesInts` is true for `univ`-typed fields in the jar, since `INTS` is inside the expansion).
- ▢ **mt-041** · R4 · Ordering exact-bound detection for `util/ordering` run as the **root module** (filed at mt-037 triage; verdict-neutral; schedule opportunistically)
  The alloytools corpus vendors the ordering module's own test file (`models/util/ordering.als`, `module util/ordering[exactly elem]`) and runs it directly. The jar applies its exact-bounds ordering optimization there too — `Ord.First`/`Ord.Next` pinned to constants, SB-0 count **1** at every `exactly N elem` — while mettle's LEDGER-004 pinning (mt-035) keys on *opening* `util/ordering`, so the root-module case leaves `Ord.Next` free under `pred/totalOrder` and counts **n!** (gauge `COUNT_MISMATCH`: 2/6/24 vs 1 at n=2/3/4). Verdicts and expects all agree — count-only divergence. Fix needs the jar's actual detection rule pinned first (by module path? sig label `Ord`? field names?) via probes (e.g. a renamed copy of the module), then the pinning extended to whatever rule the jar uses.
- ◐ **mt-042** · ops · Machine portability: git remote, one-command asset bootstrap, pinned toolchains (owner-directed 2026-07-18)
  **(a) Git remote — DONE 2026-07-18:** owner created **public** `git@github.com:chaychoong/mettle.git`; `origin` added, `main` pushed. Standing workflow (owner-directed): work on `main`, push to `origin main` after each chunk. Public hosting is safe per ADR-0006 (corpora/jar git-ignored, MPL-2.0 code, clean-room stdlib) — keep it that way. Parts (b)–(d) remain. **(b) One-command asset bootstrap** — extend `scripts/` so a fresh clone runs one script to fetch everything git-ignored: corpora (exists: `fetch-corpora.sh`, SHA-pinned) + the reference jar into `oracle/` **with the ADR-0002 SHA-256 verified on download** (pin in `docs/reference/alloy6-reference.md`); probe scratch files stay recreatable-from-docs. **(c) Toolchain pinning — nix flake (owner-decided 2026-07-18: nix already on all owner machines)** — `flake.nix` + lock pinning rustc and JDK 21 (`nix develop` = the whole toolchain; the hermetic JDK also de-flakes the jar harness); keep a `rust-toolchain.toml` alongside so non-nix environments (CI) still resolve the right rustc via plain rustup. **(d) Migration checklist** in `docs/` — commit in-flight work first; keep the same absolute repo path (`/home/exedev/repos/mettle` — the agent memory dir is keyed to it); rsync `~/.claude` once; `~/.ssh`/`.gitconfig` move separately (not under `~/.claude`); on arrival run `cargo test --workspace` + the 5 jar-integration tests (they pin the 87/1129 enumeration facts and catch a wrong jar/JDK immediately).
- ▢ **mt-050** · R4 · Rung-4 exit: differential gauge re-run vs refreshed baselines (needs mt-042(b–d) + migration first — tech-lead rec 2026-07-21: run the exit sweeps on the bigger box)
  Re-run solve-gauge + both count nets, refresh/extend `baselines/` where new coverage needs fresh jar verdicts (the parallel fan-out and cached count baselines shipped at mt-054 — this bead now = the deep-budget exit sweeps over the capacity/over_budget remainder + the ADR-0012 gate → owner touchpoint). The heartbeat's slowest-command table (ptcris, chord, etl_scd, HotelVar) is the perf worklist. **Hardware plan (owner-acked 2026-07-19):** bigger box 8–16 cores, 32–64GB (RAM is what makes deeper encode budgets safe); **mt-042(b–d) jumps the queue before any migration.**

**Sequencing:** mt-043 (contract) first — the rung's authority document, pattern-matching mt-016/mt-028; mt-040 interleaves freely (no contract dependency, biggest unlock). Then mt-044→045→046 (shared encoder/evaluator surface, integers first — largest defer family), mt-047/mt-048 behind the contract, mt-049 opportunistic between coverage beads, mt-050 last.

## Backlog (later rungs)
Tracked at rung granularity in [ROADMAP.md](ROADMAP.md); expanded into beads when a rung becomes "Next".
- ▢ **mt-027** · R? · Close the residual diagnostic-parity gap (owner-accepted-and-deferred 2026-07-16)
  The measured remainder after mt-022/025/026/023, all documented in [LIMITATIONS](../LIMITATIONS.md) and root-caused in [reference/alloy4fun-resolve-pass.md](reference/alloy4fun-resolve-pass.md) §10.4 + [reference/warning-parity.md](reference/warning-parity.md): **verdicts** — 314/150,891 over-accepts (~112 compound-right-operand re-checking, ~94 deep-join type precision, ~38 multiplicity-flag tracking, ~10 tail); **warnings** — 192 missing (overload-collapse unused-var) + 20 extra (temporal same-value suppression). Owner decision: move to Rung 3 with the gap filed; buckets 1–2 sit on type machinery Rungs 3–4 must deepen anyway — **revisit this bead whenever that machinery changes** (each Rung-3/4 typing bead should note its effect on these counts), or schedule it standalone if the counts stall. Exit: 100% or a proven jar-bug floor, re-gauged.
- ▢ **mt-021** · R? · Printer/dumper recursion-depth safety on pathologically long flat operator chains
  mt-014's second fuzzer finding (see [reference/fuzzing.md](reference/fuzzing.md) §1): `Ast::pretty`/`dump` recurse per operand and can overflow on adversarial flat chains far beyond any real model. Fixing properly touches the public, currently-infallible `pretty`/`pretty_to_string` signatures — needs a small ADR first. Not rung-gating; schedule opportunistically.

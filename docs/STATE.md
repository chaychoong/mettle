# Current state

> The live "where are we" doc. Update this at the end of every work chunk. On pickup, read this first.

**Last updated:** 2026-07-29 (fourth entry today) — **mt-085 DONE: the last carrier of the mt-084 seq-stdlib defect pair is closed — `util/sequence.als` probe-pinned and fixed; the fix is fully latent (zero corpus commands open the module, full sweep byte-identical before/after).** Opus delegate probes-first (145 rows, 7 probe files in `scratchpad/probe/mt085/`); tech lead reviewed and re-ran the load-bearing cells. Both predicted defects confirmed (vacuous-`in` boundary lookup → join equality; `insert` `gte`→`gt`), but the round's real finding was unpredicted: **the jar's `sequence` preds are partial** — `rest[empty]`, no-room `add`/`insert`, overflowing `append`, out-of-range/inverted `subseq` are UNSAT where the clean-room bodies truncated in the sequniv spirit (sequniv clamps; sequence refuses — per-module negative space, now probe-pinned both ways) — and **`noDuplicates`/`allExist`/`allExistNoDuplicates` are 0-ary globals** (jar type-rejects receiver calls; §7.5 said so all along, the clean-room file had drifted; `allExist` = every sequence is realised, pinned by exact-scope counting SAT/UNSAT-one-below at three (k,n) shapes). Also fixed en route: `append`'s offset restarted `s2` at index 0 on a full `s1`; `copy` is a one-way landing constraint, not an equality; `setAt` extends at the first free index. 10 revert-verified probe-cited tests (seq_conformance 28 total); scorecard unmoved by construction (agree 448, DISAGREE 0). The bead spec's "four corpus files open it" was wrong — zero do. New latent residue in LIMITATIONS: mettle accepts receiver-style calls of 0-ary preds the jar rejects (mt-027 territory); `fun/add` wrap exposure in `copy`/`append`/`subseq` at `SeqIdx` scope >~4, bitwidth 4. Prior entry (same day, third): **mt-084 DONE: the STOP-THE-LINE is resolved same day — all 5 correctChord wrong verdicts were one clean-room stdlib mechanism, fixed and probe-pinned; agree 444→448, DISAGREE 5→0, both count nets unmoved and mismatch-free.** Root cause (opus diagnosis delegate, probes-first; tech-lead reviewed + ran the battery): `util/sequniv`'s shifting functions (`rest`/`insert`/`append`/`subseq`) bound their result-index comprehensions over bare `Int` instead of `seq/Int` — a shifted result could then never equal a `seq X` field (which is `seq/Int -> lone X`), making [19]/[20]/[21]/[24] spuriously UNSAT and [30]'s `list != insert[…]` a tautology (the spurious counterexample). The clamp is **per-function jar-probed negative space** (`delete`/`setAt` deliberately do NOT clamp — decisive probe pair documented in-file); two further defects fixed en route (`insert` shifts on strict `gt`, and `util/seqrel`'s boundary-vacuous `none -> x in s` lookup respelled as joined-value equality). Row diff vs mt-083 is EXACTLY the five rows — zero collateral movement; [30] is now a typed over_budget at the 25k default (the correct UNSAT is harder than the old tautology; answers VALID at 100k — any default raise is an ADR-0017 paired-grid call, not made here). 7 revert-verified probe-cited tests; minimal repro chord-free. **`util/sequence.als` carries the same defect pair, latent (zero live incidence) — filed as mt-085 with the probe-round spec; LIMITATIONS flipped.** Prior entry (same day, second): **mt-083 DONE: correctChord is banked per-command and the first-ever comparisons land — agree 428→444, jar_nonverdict 21→0, and STOP THE LINE: 5 real wrong verdicts exposed (→ mt-084, the next chunk, outranking the census order).** The mt-069 `PerCommandProbe` pattern (one JVM per command, 900s/2700s tiers) banked **36 of 39** commands into the slow supplement — the mt-057 "jar can't sweep this file at 7200s" fact was a serial-blotting artifact; most commands answer in 2–75s. Of the 21 rows mettle already solved: 16 agree, **5 DISAGREE — the gauge's first wrong verdicts ever: [19]/[20]/[21]/[24] (the `run Stabilize…` family, mettle UNSAT / jar SAT, shared scope shape `for 5 but … 3 seq`) and [30] (`check IdealImpliesNoChangeEnabled for 9`, mettle SAT / jar UNSAT)**. Triage in-chunk: overflow ruled out, mettle confirmed end-to-end via exec, harness verified row-identical; suspect surface = seq-index arithmetic (the census's own family-C mechanism for chord). These were always wrong — the file just had nothing to compare against. **Family-E fully resolved** (the census's 19 unprobed rows): leader_events[3]→A; buffer[1,2,4]+leader[2]+leader_events[2]→**B (all convert at L2 on the ADR-0018 encoder — mt-069's "buffer's three are durable holdouts" is overturned)**; ertms_1A[9..13]→D (search-bound at L2; the census's capacity call predated the encoder change); chord's 8 E rows: 7 banked jar-side (incl. [28] `LossOfPrincipal` SAT, the one satisfiable check). DISAGREE 0 in every famE probe. Ops lesson filed: a mid-run laptop sleep inflated `timeout`-wrapped walls (alarms fire at wake) — verdicts unaffected, long runs are caffeinate-protected now. Artifacts in `scratchpad/mt083/`; bead + mt-084 spec in [TASKS.md](TASKS.md). Prior entry (same day, first): **mt-082 DONE: the conflict default is re-paired to 25k on the reshaped CNF ([ADR-0017 Amendment](adr/0017-gauge-default-budgets-paired-frontier.md)) — agree 428, both recoverable ADR-0018 boundary rows recovered, DISAGREE 0.** The pairing rule bound through the encoder, not a knob: structural sharing halved the wall cost of conflicts, so the re-measured frontier (10k = 421/478s, 25k = 428/773s, 50k = 435/1259s, zero regressions everywhere) overturned ADR-0017's original 25k rejection. New defaults **conflicts 25k × encode 32M**; 50k stays a per-run flag; the three durable ADR-0018 losses (OLAPUsagePrefs[0], elevator_spl_events[29], life[1]) are family-D territory, still over_budget at 50k. Re-baseline verified (stage-1 row-identical to the grid point; SB-0 65 / SB-20 89, MISMATCH 0 — count nets are enum-budget-bound, unmoved by conflicts; sweep baseline re-captured at 25000/32M). Rule sharpened: **re-pair on any budget-default change OR encoder CNF-shape change.** Campaign trail at defaults: 356 → 402 → 421 → **428**; the remaining defer mass is capacity 28 + over_budget 74, headlined by correctChord (oracle-side banking needed) and family D/E. Prior entry (2026-07-28, fifth): **mt-081 DONE: encoder structural sharing landed ([ADR-0018](adr/0018-encoder-structural-sharing.md)) — agree 402→421, DISAGREE 0, both count nets improve and stay mismatch-free, the sweep got faster (588s→478s).** One file (`encode/mod.rs`): the collision-proof structural value cache (content-interned matrices, ids in the caches, matrices stored once — the naive variant was measured at 2.96 GB RSS and rebuilt), support-bounded closure squaring (Kodkod's matrix-dimension bound, brute-force-oracle-verified), and the mt-049 strict-subset env rule deleted with its refutation documented in place. Subject rows crushed the projection: TransForm[6] 33.6M→6.67M spend, c11[7] 33.6M→0.55M. Row diff vs mt-079: **+24 conversions** (the TransForm pair is 20 of them) / 2 neutral / **−5 disclosed** — agree→over_budget on the reshaped CNF; ringlead[2]+etl_scd[5] recover at 50k conflicts, OLAPUsagePrefs[0]/elevator_spl_events[29]/life[1] are durable losses at probed budgets (typed defers, never wrong verdicts). SB-0 count_match 56→65, SB-20 84→89, MISMATCH 0 both. **→ mt-082 filed (next chunk): re-pair the conflicts default on the new CNF shape per the ADR-0017 pairing rule.** 1b (IR hash-consing) stays deferred. Prior entry (same day, fourth): **mt-080 DONE: the family-C encode profile is in — duplication, not density, is the enemy, and the fix design is decided.** Opus profiler, instrumented encoder to 32M exhaustion on one TransForm row + one c11 row (solving siblings for scale contrast; artifacts `scratchpad/probe/mt080/`). The census's mechanism confirmed but re-ranked: **`lower.rs` re-lowers every pred/fun body per call site, so identical work arrives under fresh node ids and the `(node id, env)` memo is blind by construction** — TransForm encodes the same ~6 closure values 242 times (structural value cache saves 71.3% of ops), c11's structural blindness is worth 78.7% (scale-invariant), the env-key strict-subset rule is real but secondary (7.1%). Closure matrices are 100% dense from round 0; per-closure cost is quadratic in sig scope. **Decided (tech lead): implement (1a) a collision-proof structural value cache in the encoder + (2) support-bounded closure iteration (Kodkod-style matrix-dimension bound) + (3) the env-rule relaxation — projected to land both subject rows at ≈7–9M against the 32M ceiling; IR hash-consing (1b) deferred until 1a's sweep is green; level-variable acyclicity rejected (would break the SB-0 counting contract).** Implementation = mt-081, in flight. Prior entry (same day, third): **mt-079 DONE: the gauge defaults are raised on the measured paired-knob frontier — encode 4M→32M, conflicts held at 10k: +46 agreements (356→402) at 2.2× wall, zero regressions, DISAGREE 0 ([ADR-0017](adr/0017-gauge-default-budgets-paired-frontier.md)).** The grid the census called for (encode pinned 32M × conflicts 10k/25k/50k, full corpus, strictly sequential at `--jobs 8`, every point row-diffed against a same-day fresh defaults baseline): point A (10k) wins decisively — it captures 46 of the 60 agreements available anywhere on the grid at 583s, while 25k/50k buy only +6/+14 more at 3.7×/7.3× wall (search-bound family-D territory, deliberately deprioritized). The census's temporal interaction flip does **not** reproduce with encode raised first — the pairing rule ("never move one budget default without re-measuring the pair") is now standing in ADR-0017 and the rewritten `solve_gauge.rs` comment (mt-049's "8M timed out past 40 min" predated the throughput campaign and no longer binds). Sweep-baseline artifact re-captured under the new regime; **scorecard re-baselined below from fresh runs** — stage-1 (no flags) row-identical to grid A; SB-0 count_match 56 / MISMATCH 0; **SB-20 count_match 79→84** / MISMATCH 0 (five more enumerations complete under the deeper ceiling); full gauntlet green. Also: the stale ADR-index line for 0015 fixed (file said Accepted since the review; index said Proposed). Next in census order: closure-encoding cost work (TransForm family + the c11 relation-param memo defeat). Prior entry (same day, second; session wrapped at the context threshold): **mt-078 DONE: the capacity-remainder census is measured, classified, and filed — the conversion campaign now has a costed worklist.** Opus delegate, tech-lead reviewed; full findings in the TASKS bead. The load-bearing numbers: 179 defer rows → families A 72 (convert at L1) / B 22 (L2) / C 42 (encode-bound; TransForm tso pair = 36, mechanism incl. the c11 relation-param memo defeat) / D 20 (search-bound) / D* 4 / E 19 (unprobed); **encode-only 4M→32M = +46 agreements, 2.2× wall, zero regressions** vs conflicts-only +19 at 4.6× with one instructive regression (the knobs interact through the temporal step-sweep — pair any default change); **correctChord's 23 rows need oracle-side deeper banking before any mettle budget pays off**; DISAGREE 0 everywhere. Also this entry: **the one-state NPE was filed upstream by the owner — [AlloyTools#350](https://github.com/AlloyTools/org.alloytools.alloy/issues/350)** — links recorded in temporal-ref/LIMITATIONS/LEDGER-015 (`7fac6aa`); and **zero-versioning decided (owner):** 0.x by intention, no 1.0.0 promise — README/ROADMAP/CLI comments updated (`433d380`), the scorecard carries the maturity signal. Prior entry (same day): **THE COMBINED FEATURE-COMPLETE REVIEW IS BLESSED (owner: "all pass") — Rungs 5 and 6 are exited, ADR-0015 is Accepted, and v0.1.1 ships the mid-review fixes.** The owner ran all parts on the fresh brew install alone: part 1 (install → visualized instance) passed earlier; parts 2–3 passed on self-contained demo models (`wastebasket.als` — SAT trace + VALID + a real 3-state COUNTEREXAMPLE + the unbounded rejection; `toggle.als` for the REPL walk, every expected answer machine-verified via `--eval` first — one correction found in the tech lead's own script, not mettle: `after` is formula-only, the expression form is the prime `Raised'`). Mid-review the owner also: decided the fork (→ mt-077, prior entry), got the favicon finding fixed (`d1d9047`), chose the version posture (option C, prior entry), and redirected the README provenance section to dogfooding-honesty framing (`e4b2dd7` — "whether you should use it", no safety assertion). Two expected-behavior questions answered in-review, not findings: serve's multi-command disambiguation listing, and the counterexample's genuinely-empty final state ("nothing to draw" is the designed empty-state treatment; table + evaluator agree). **Verdict processing: [REVIEW-v0.1.0.md](REVIEW-v0.1.0.md) marked DONE-BLESSED with the verdict record; ADR-0015 → Accepted; ROADMAP gains the all-six-rungs-exited status note; batched-gating cadence ends — back to ordinary chunk pacing. v0.1.1 cut on this commit (version bump; tag push; the release pipeline is fully hands-free since the PAT landed).** Upstream facts from mid-review research (sourced, in the 07-28 follow-on below): Alloy 6.2.0 still latest; the one-state NPE unreported upstream — **offering to file it upstream remains an open owner call.** The North Star is now the whole remaining game: 1.0.0 = measured drop-in parity; the honest remainder is capacity/budget defers (mt-050 throughput) + LIMITATIONS. Prior entry (2026-07-27, fourteenth): **mt-077: the `check … for 1 steps` fork is DECIDED (owner: Option B, mid-review) and IMPLEMENTED — one-state temporal bounds are answered; [LEDGER-015](../SEMANTICS_LEDGER.md) filed; the jar bug's pinned scope CORRECTED.** The owner asked for the fork explained, then decided "Do B"; the deciding argument (a `check P` is the negated `run {not P}`, and the jar solves `run … for 1 steps`, so the one-state semantics is jar-anchored through the dual) was validated by an opus probes-first delegate (`scratchpad/probe/mt077/`, P-077-1..5 + bisect B1–B6) — which also **refuted two wave-1 claims** in the temporal contract: the NPE is *not* general to one-state `check`s (nine answered cleanly) and *not* isolated to `check` (three constant-folding `run`s NPE identically); the real trigger is a `maxtrace==1` command whose translation constant-folds to a boolean. **So the old blanket refusal was itself diverging from the jar on the majority (answering) subset — Option B increases conformance** and confines the deliberate divergence to the crashing subset, where mettle returns the verdict the crash withholds. Code: the early return deleted in `temporal_solve.rs` + the duplicate guard in `temporal_enum.rs`; `TranslateError::TemporalCheckAtOneStep` and its gauge bucket removed entirely; tests 975→980 (probe-cited + the jar-free negation-dual invariant); gauntlet green, **stage-1 sweep byte-identical** (zero corpus incidence). Docs: LEDGER-015 (approved, genuine-fork owner decision), LIMITATIONS flipped, temporal-ref §(c)/probe-log/unpinned-tail corrected in place, ADR-0015 §3 + ADR-0016 Resolution 3 annotated, TASKS bead, review doc §4 marked decided (parts 1–3 remain). Also this session, pre-mt-077: **review finding #1 fixed** — the app shell now declares an inline SVG data-URI favicon (the lasso mark), so the browser never requests `/favicon.ico` (`d1d9047`; the offline test's `http://` scan gained the shell-side SVG-namespace carve-out). Both fixes are on `main` but NOT in the v0.1.0 binaries — batch into v0.1.1 after the review. **Follow-on (2026-07-28): two owner questions answered mid-review.** (1) Upstream check (researched, sourced): **Alloy 6.2.0 (2025-01-09) is still the latest release** — the pin is current; the one-state NPE appears **unreported anywhere** (AlloyTools/Pardinus/Kodkod trackers + discourse all swept) — mt-077's find looks genuinely novel; offering to file it upstream is pending the owner (outward-facing). Cadence ~19 months/release. (2) **Version posture decided (owner: option C):** mettle keeps independent semver (1.0.0 = the drop-in bar) and *states* the tracked Alloy version instead of mirroring it — `mettle -V` now prints `mettle 0.1.0 (tracking Alloy 6.2.0)` (`TRACKED_ALLOY_VERSION` in `main.rs`, updated when the pin moves; cli tests pin the string) and the README's opening states the target + the 1.0.0 reservation. Mirroring was rejected for number-collision risk with future upstream releases and loss of mettle's own maturity signal (the PyPy own-version+stated-target pattern won). Prior entry (same day): **v0.1.0 is released and every install channel is live or one owner click away; the combined feature-complete review script is written and handed over ([REVIEW-v0.1.0.md](REVIEW-v0.1.0.md)).** The owner cut and pushed the `v0.1.0` tag: Release workflow — all four platform builds + `host` green, the GitHub Release shipped complete (14 assets incl. `mettle-installer.sh` and `mettle.rb`); Docker workflow green (image on GHCR); tag CI green. Two follow-ups handled this chunk: **(1) Homebrew tap repointed** — the owner's real tap is the existing shared `chaychoong/homebrew-tap` (NOT the planned `homebrew-mettle`, which was never created); `dist-workspace.toml` + regenerated `release.yml` now point there (`52396a3`; install spelling **`brew install chaychoong/tap/mettle`**), and since the v0.1.0 run predated the repointing (its homebrew job failed red as long predicted — wrong repo AND no `HOMEBREW_TAP_TOKEN`, `announce` skipped, release unaffected), the tech lead **hand-published the formula**: `mettle.rb` from the release assets → tap `Formula/`, checksum cross-verified, pushed (tap commit "mettle 0.1.0"), then **verified end-to-end: `brew install chaychoong/tap/mettle` → `mettle 0.1.0` → solves a model → uninstalled clean** (left uninstalled so the owner's review is a true fresh install). **(2) GHCR image is private** — first `GITHUB_TOKEN` publish defaults private, anonymous pull denied; making it public is owner chore 2 in the review doc (the tech lead's gh token lacks `read:packages`). **Mid-chunk owner direction: the tag was deleted and re-cut on the repointed commit** (release+tag deleted, `v0.1.0` re-tagged at `b51a093`, all workflows re-ran green except the homebrew job's predicted `Input required and not supplied: token`; the rebuilt tarballs got new sha256s, so the tap formula was hand-refreshed and `brew install` re-verified) — the payoff is that the workflow frozen at the tag now targets the right tap, so once the secret exists, **Re-run failed jobs** on release run 30266081200 publishes automatically. README's Install section now leads with the brew one-liner (owner ask). **Both owner chores subsequently closed same-day: the owner minted the `HOMEBREW_TAP_TOKEN` PAT and re-ran the failed job — the v0.1.0 release run is now green end-to-end incl. `publish-homebrew-formula` (axo-bot formula commit on the tap, install-verified) and `announce`; and the GHCR image was public all along — the earlier `denied` was a stale local `docker login ghcr.io` credential (`docker logout ghcr.io` fixed it; gotcha recorded in the review doc). Every future `v*` tag publishes fully automatically. No release plumbing remains.** **Next: the owner runs [REVIEW-v0.1.0.md](REVIEW-v0.1.0.md)** — the batched human tests (Rung-5 install bar via the live channels, Rung-6 gate incl. the trash.als/REPL walk, the frontend, the `check … for 1 steps` A/B fork). Prior entry (same day; session wrapped at the context threshold): **Owner-directed pre-tag hardening on top of the finished ADR-0016 cut:** (1) full docs staleness sweep (3 parallel auditors; LIMITATIONS.md header/serve-section and the ADR-index fork status were the load-bearing fixes; all folded in, commit `f47a490`); (2) contribution story — README "Found a difference from Alloy?" + `.github` divergence issue template + `CONTRIBUTING.md` (nix = the fast path, prerequisites listed for the rest, `bootstrap.sh` kept only for SHA-verified jar/corpus fetch); (3) **Docker channel**: root `Dockerfile` (rust:1.97.0-slim → distroless/cc, 37 MB, `-p mettle` only), `.dockerignore`, `.github/workflows/docker.yml` publishing multi-arch (native amd64+arm64 runners, digest-then-merge, `GITHUB_TOKEN`-only) to **ghcr.io/chaychoong/mettle** on `v*` tags, README "Try it" docker one-liners up top; (4) **`mettle serve --bind <addr>`** (default stays loopback — the security posture holds; containers opt into `0.0.0.0`; 3 new tests) — plus a same-day lesson: the delegate's banner split raced consumers closing stdout (5 serve tests reset on Linux CI only; macOS always won the race) — fixed by restoring the **single-write banner invariant**, now documented in `serve.rs` (`3593e77`). CI green end-to-end. **Still owner-gated: `git tag v0.1.0` + push (triggers release.yml AND docker.yml), tap repo + `HOMEBREW_TAP_TOKEN`.** Prior entry (same day): **mt-073 + mt-074 DONE: the ADR-0016 cut is fully implemented — release packaging and the flake output landed, the workspace is version 0.1.0, and the v0.1.0 tag is owner-gated.** mt-073 (sonnet delegate, tech-lead reviewed): cargo-dist **v0.32.0** — lineage reversal vs the survey: the astral-sh fork was archived 2025-12-19 and **axodotdev/cargo-dist is maintained again** — `dist-workspace.toml` + generated tag-triggered `release.yml`, 4 native-runner targets, shell installer + Homebrew formula (**no PowerShell** — zero Windows targets, dist warns it would be a no-op), GitHub attestations, `[profile.dist]` thin-LTO, als-conform `dist = false`, **`mettle -V|--version`** added in the hand-rolled idiom (+2 e2e tests). **Version gate decided in-bead (tech-lead):** `0.0.0 → 0.1.0` = the feature-complete candidate; pre-1.0 semver (minor = feature milestones, patch = fixes); **1.0.0 reserved for the drop-in bar**; also fixed the stale `repository` URL (`mettle-lang` → `chaychoong`, load-bearing for installer URLs). mt-074 (sonnet delegate, tech-lead reviewed + one patch): flake `packages.default`/`apps.default` on the **exact rustc 1.97.0** via pinned `oxalica/rust-overlay` reading `rust-toolchain.toml` (single authority; the dev shell's documented 1.95 compromise retired); review patch makes the flake read the version from `Cargo.toml` too; `nix run github:chaychoong/mettle` works; `/result` gitignored. **CI was red from the mt-076 push until today** — the new leader.als counting test lacked the standard corpus-absent skip guard (corpus/ is git-ignored, CI never has it); fixed (`8731016`), CI green since, verified twice. **Owner pre-tag gates delivered:** root README rewritten (benchmarking section with same-day re-measured figures — headline correction: parse+resolve is **~21×** the jar's warm batch, not STATE's stale ~4×; the standing diff-a-fresh-run rule vindicated again — plus 4 frontend screenshots in `docs/screenshots/`, real install channels, MPL-2.0 license truth pass). **Blocked on the owner:** the permission classifier denies `git tag`/`gh repo create` — the owner cuts `v0.1.0` (tag push triggers the release workflow; the homebrew publish job will fail red until `chaychoong/homebrew-mettle` + a `HOMEBREW_TAP_TOKEN` PAT secret exist — the GitHub Release itself ships complete regardless, `host` does not depend on it) — and a full docs staleness audit ran (3 parallel auditors; findings folded in). **Next: the combined feature-complete review.** Prior entry (same day; session wrapped): **mt-075 COMPLETE: slice 3, the distinctive design pass, landed — the frontend bead is done** (same warm opus delegate; tech-lead reviewed with both-theme screenshots). Design thesis: an instrument readout, not a web app — silkscreened mono chrome, "the solver speaks monospace, mettle speaks prose", and the signature **trace rail** that draws the lasso's loop as an amber arc. Slice-2 gaps closed in the pure layer (label lanes with estimated metrics — determinism preserved, 5/5), legend-as-control, subtractive focus, 140ms restrained motion with `prefers-reduced-motion` verified. Empty/edge states unified; exhaustion is a bulletin band; refused clicks re-request data. No webfont, zero new deps, offline test intact, gauntlet green. **This session shipped six chunks: mt-071 (XML writer) → mt-072 (serve backend) → mt-076 (temporal enumeration + LEDGER-014) → mt-075 slices 1–3 (the complete first-party frontend). Remaining in the ADR-0016 cut: mt-073/074 (packaging, version-gated) → the combined feature-complete review.** Prior entry (same day): **mt-075 slice 2 done: the graph view is live and default** (same warm opus delegate; tech-lead re-ran the determinism script 5/5 and eyeballed the screenshots — file_system.als renders layered with per-sig hues, dashed back-edge bows, the skolem witness marked). Pure `layout.js` (deterministic by construction — document-order iteration, total comparators, 2dp boundary rounding; the SVG text is byte-stable, CDP-verified) + `graph.js` SVG rendering with viewBox pan/zoom; view switch graph⇄table, one state source of truth; `meta`'s `views` claim now true. Zero new deps either language. **Next: mt-075 slice 3 — the distinctive design pass** (+ slice 2's flagged gaps: label measure-then-place, hue legend, hover affordance). Prior entry (same day): **mt-075 slice 1 done: the first-party frontend's working core is live** (opus delegate, tech-lead reviewed; delegate browser-smoked it via headless Chrome — 2 real UI bugs found+fixed that way — and the tech lead eyeballed the screenshots). **Zero-toolchain** (tech-lead decision): hand-written ES modules + CSS embedded via `include_str!`, no npm/bundler/CDN, offline guarantee unit-tested. Live at `mettle serve`: app shell + reconnecting WS client with toast-rendered `error`s, the table view (writer-order sig/field/skolem cards, `var` badges, builtins toggle), the temporal trace stepper (loop chip ↺, first-`tracelength` blocks only, client `normalizeState` = server rule), evaluator pane with two-way `:state N` sync, server-driven buttons. **Protocol amendment (d)** (ADR-0016 + sterling.md §2.3): `click` gains optional `state`; `new-fork` forks at the client's displayed state, typed `state-out-of-range` refusal, absent = evaluator-pane fallback. Gauntlet green, no new Rust deps. **Next: mt-075 slice 2 — the graph view; then slice 3, the distinctive design pass.** Prior entry (same day): **mt-076 done: temporal trace enumeration is live and temporal counting joined the nets** (opus delegate, probes-first; tech-lead reviewed, LEDGER-014 authored at review). Probe wave P-076-0..7 closed §(g)'s unpinned corners — the `fork(-1)` failure mode is a **rule** (free-static-primaries discriminator), `fork(-2)` ≡ `fork(-3)`, the duplicate unit is two-level — and delivered a headline **§(i) correction: the jar's `next()` never leaves its first static configuration** (P-076-5), making temporal counts **configuration-relative by construction** ([LEDGER-014](../SEMANTICS_LEDGER.md), approved: gauge compares-but-softens via the new typed `skip_temporal_config`; P-076-7 demonstrated it live on `leader.als[3]`). `als_core::temporal_enum::TraceEnumerator` implements `next`/`fork(p)`/`next-config` with the pinned semantics (config lock unit-fixed — skolems excluded, a real bug caught mid-bead since mettle skolemizes under temporal where the jar doesn't); **`skip_temporal_trace` retired**: fresh nets SB-0 `count_match` 53→56, SB-20 77→79, **COUNT_MISMATCH 0→0 at both settings**, leader.als's T-26 baseline now a live match. All five serve temporal click verbs live. 19 probe-cited conformance tests + serve rewrite; gauntlet clean; stage-1 sweep byte-identical. **Next: mt-075, the first-party frontend (2–3 chunks; spec decides the display-state protocol amendment).** Prior entry (same day): **mt-072 done: `mettle serve` is live** (opus delegate, tech-lead reviewed with a dependency-trim send-back). One localhost process, one port (default 4030): hand-rolled HTTP for the embedded stub page + WS provider speaking the pinned Sterling protocol (`data` = mt-071 XML byte-identical to `--xml`; `eval` byte-identical to `--eval` incl. `:state N`; static `next` enumeration with honest button retirement; temporal fork verbs = typed defers naming mt-076; `meta`/`ping`/`pong`). One ADR-amended pinned-shape extension: outgoing `error {code,message}` (the protocol has no provider-refusal channel; verified ignored-safe by external Sterling). First runtime network dep decided + trimmed: `tungstenite` `default-features = false` (accept-echo hand-rolled, FIPS/RFC-vector-pinned) + serde/serde_json; no async runtime; shipped tree 9→25 crates. Seam: `als_sterling::ServeSession` trait / `StaticSession`+`TemporalSession` impls. 39 new tests (18 unit + 10 real-socket wire + 11 spawn-the-binary integration); workspace 944 pass, gauntlet + `cargo doc` clean; sweep untouched (extraction-only exec/repl changes; additive `Arena`/`Ir` `Clone`). **Next: mt-076 (temporal trace enumeration) — tech-lead sequencing: solver-side parity before the mt-075 frontend, since it unlocks the temporal click verbs and moves the scorecard.** Prior entry (same day): **mt-071 done: the jar-shape-exact instance-XML writer is live** (`als-instance::write_instance_xml` + `mettle exec --xml <PATH>`; opus delegate, tech-lead reviewed). Implements [reference/alloy6-instance-xml.md](reference/alloy6-instance-xml.md) in full — lazy touch-order IDs, interleaved fields, the always-on `macros` mechanism incl. past-depth extra-instance blocks, String atoms/escaping, both skolem namings, `<source>`, `looplength`. **Differential verification: 18/18 mettle-written files accepted by the jar's own `A4SolutionReader.read`** (XmlProbe round-trip; the 5-block `MacroPastDepth` case reads back identically to the jar's own output; tech lead re-ran 2 load-bearing cells), + 13 jar-free shape tests/10 goldens; zero new deps; full gauntlet green, stage-1 sweep unchanged (one additive als-types widening: `ResolvedFunc::qualified_name`). Eight bytecode-pinned corners folded back into the authority doc (§1.1 writer order, §1.2 `command=` reconstruction — the flagged gap, `Type.fold()` union `<types>`, generalized escaping marked INFERRED, etc.); **LEDGER-013** filed+approved (ordinary-skolem `<types>` from the solver bound — sound over-approximation, reader-accepted, scorecard-invisible); two cosmetic divergences (m`<i>` index order, `<source filename=>` spellings) → LIMITATIONS. **Next: mt-072, the `mettle serve` provider backend.** Prior entry (same day): **pacing convention added to [CLAUDE.md](../CLAUDE.md) "Start here": owner's "keep going [until N% context]" = a multi-chunk grant to N% (default 30%), measured by a new machine-level context-usage hook, then wrap and stop.** Prior entry: **the owner resolved both mt-070 forks and reset the gating cadence** (recorded in [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) "Resolutions"): **(1)** Sterling assets → **option C, browser-first** — mettle writes its **own minimal frontend** (modern design pass included; NOT Tauri — a desktop shell stays a possible later additive bead), served by `mettle serve` in the browser, keeping the pinned Sterling wire protocol (external Sterling = free interop; license question evaporates — nothing upstream embedded); **(2)** crates.io → **skipped outright** (binary product, internal crates; no rename, no dispute); **(3)** **all human tests batch into ONE combined feature-complete review** (per-rung gating superseded until then; the Rung-6 gate and the `check … for 1 steps` fork ride along; engineering gates unchanged every chunk), and **the feature-complete bar includes temporal trace enumeration**. Beads re-cut: mt-071 (XML writer) → mt-072 (serve backend) → **mt-075 (first-party frontend, NEW)** + **mt-076 (temporal trace enumeration, NEW — retires `skip_temporal_trace`, temporal counting joins the nets)** → mt-073/074 (packaging). Distance to the combined review: ~6–9 chunks. Prior entry (same day): **mt-070 done: the Rung-5 remainder is planned — contracts pinned, [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Proposed, beads mt-071–074 cut.** Three parallel recon/probe waves (all sonnet delegates, tech-lead reviewed with load-bearing re-runs): **(1)** the jar's complete instance-XML schema is pinned in the new authority doc [reference/alloy6-instance-xml.md](reference/alloy6-instance-xml.md) (X-01..X-09b; headline: `<instance>` block count ≠ `tracelength` under the always-on `macros` mechanism, lazy touch-order IDs, `String` the one builtin with atoms; six load-bearing cells re-run byte-identical); **(2)** Sterling's integration contract is pinned in the new recon doc [reference/sterling.md](reference/sterling.md) (external-tool contract, NOT jar authority — the jar ships no Sterling; live lineage `sidprasad/sterling-ts`; WS `data`/`click`/`eval`/`meta` protocol; two load-bearing finds re-verified: the `backloop`-vs-`looplength` XML mismatch, and **no formal license anywhere in the Sterling lineage** → owner fork); **(3)** packaging surveyed (cargo-dist spine + flake `packages` output recommended; **crates.io `mettle` squatted 2026-07-26** by an unrelated crate → owner question). No implementation, no scorecard motion. **Two NEW owner decisions filed in "Open questions" (Sterling assets/license, crates.io name), joining the two standing ones (Rung-6 gate, `check … for 1 steps`).** Prior entry (same day): **BOTH mt-069 STOP-THE-LINE finds are FIXED (tech-lead decision: fix now, executed same-session by the warm mt-065/mt-067 delegates) — Rung 6's implementation run is complete and clean; the gate is with the owner.** Fix 1 (macro-body visibility): the discriminator now follows used macros through the recorded `ChoiceTable` — the same seam the lowerer's macro replay reads, so resolution is never re-derived — cycle-guarded, worklist-based; the K4a/K4b repro now solves temporally like the jar while K1's called-pred non-descent is preserved structurally (4 new tests; the unused-macro-body case is a marked assumption, not a pinned fact). Fix 2 (`exactly N..M steps`): one match arm collapses to `N..N` discarding `M`, matching the jar's Command-construction behavior (L6/L6b); the wrong-verdict class is demonstrated closed by an end-to-end pair test (SAT at `2..4 steps` vs UNSAT-within-bound at `exactly 2..4 steps`); `exactly N..` (open range) deliberately left unpinned-and-unchanged. LIMITATIONS/Ledger/reference §(k)/§(m) all flipped open→FIXED with probe ids. Sweep byte-identical vs the post-banking baseline (both fixes have zero corpus incidence); full gauntlet green. **Rung-6 exit ask to the owner is in "Open questions"; the `check … for 1 steps` fork also still awaits the owner (options framed there).** Prior entry (mt-069 itself): buffer.als banked per-command (agree 356, DISAGREE 0), 9 probe cells closed (7 confirm), counting posture pinned (typed `skip_temporal_trace` until a trace-enumeration bead), deep-budget spot-run converted 4 of 7 temporal over_budget rows with DISAGREE 0 at every depth. `buffer.als`'s 5 commands banked per-command against the jar (`baselines/alloytools-models-verdict.json`; the file-level timeout entry is gone) — stage-1 sweep delta exactly as predicted, `agree_sat` 201→203, `jar_nonverdict` 18→16, `DISAGREE` 0 before and after. All nine outstanding §(k)/§(l)/mt-068 probe cells live-probed (`scratchpad/probe/mt069/`): seven **CONFIRM** mettle's shipped behavior (three of them — K1/K2/K3 — already had jar-free tests, now updated from "flagged, unprobed" to probe-confirmed comments); one (M068, a temporal operator at a static command's evaluator prompt) is a pinned capability gap, not a divergence. **Two REFUTE mettle's shipped behavior — real, corpus-reachable (zero current incidence) bugs, left unfixed per this bead's escalation rule:** (1) a macro whose body contains a temporal operator is invisible to `als_types::temporal::expr_has_temporal`'s surface-AST scan, so mettle misclassifies such a command as static where the jar solves it temporally (probes K4a/K4b); (2) `for exactly N..M steps` — the jar silently collapses this to `N..N` at parse time, mettle keeps the full `[N,M]` range and searches wider than the jar, a genuine wrong-verdict-class risk (probes L6/L6b). Both filed in [LIMITATIONS.md](../LIMITATIONS.md), [SEMANTICS_LEDGER.md](../SEMANTICS_LEDGER.md)'s "Corners that NEED entries", and [reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(m) — **awaiting a tech-lead decision, not fixed by this bead.** Temporal counting posture verified: every SAT temporal command stays typed `skip_temporal_trace` even where a real count baseline exists (`leader.als`, T-26) — new jar-free regression test. Deep-budget spot-run on the 7 temporal `over_budget` rows (both L1 100k/32M/200k and L2 400k/128M/500k completed naturally, well inside the 3600s cap): **4 of 7 converted to `agree_unsat`** (`leader_events.als[3]`/`trash.als[1]` at L1, `leader.als[2]`/`leader_events.als[2]` at L2) — **3 remain over_budget even at L2** (`buffer.als`'s three commands), `DISAGREE` 0 at every depth. 2 new tests + 3 test-comment updates; full gauntlet green (886 tests, 0 failures). **Next: the tech lead reviews the two escalations (fix now vs. ledger-and-defer) — Rung 6's gate goes to the owner once that's settled, alongside the still-open `check … for 1 steps` fork.**

Per-chunk history: the ✔ beads in [TASKS.md](TASKS.md), newest first. Decisions and rationale: [adr/](adr/).

## Current rung
**ALL SIX RUNGS EXITED (2026-07-28): Rungs 5 and 6 blessed at the combined feature-complete review ([REVIEW-v0.1.0.md](REVIEW-v0.1.0.md)); ADR-0015 Accepted. What follows is the North-Star remainder, not a rung.** Rung-6 history: implementation run complete and clean (mt-065–mt-069 all ✔, both mt-069 escalations fixed 2026-07-27). The contract is pinned ([reference/alloy6-temporal.md](reference/alloy6-temporal.md), waves 1+2+the mt-069 closing wave §(m)) and [ADR-0015](adr/0015-rung6-temporal-architecture.md) (Proposed) fixes the architecture: per-length lasso unrolling on the existing CDCL. Bead order: **mt-065 ✔ mt-066 ✔ mt-067 ✔ mt-068 ✔ mt-069 ✔** (unrolling + lowering + solve driver + surface + conformance arm, all landed 2026-07-26 — **temporal models solve, render, debug, and are now measured against the jar end-to-end**). Note: ADR-0015 §2 was amended at mt-066 — past operators follow the true lasso history (probe P-D2), not the "honest prefix" the ADR originally sketched. The 22 `lower:temporal` defers are now real agreements (mt-067's banking + mt-069's buffer.als completion — 0 remain unbanked). **Gate status:** the tech lead decided both mt-069 finds — **fix now — and both are fixed and verified** (2026-07-27, warm-delegate follow-ups; sweep byte-identical, wrong-verdict class demonstrated closed by test). The `check … for 1 steps` fork attached to this rung is **resolved** (owner Option B → mt-077/LEDGER-015, 2026-07-27; one-state bounds answered) — no owner decision remains on the rung besides the batched gate blessing itself. **The Rung-5 remainder is now planned AND owner-resolved (mt-070, 2026-07-27, [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) + its Resolutions): mt-071 (instance-XML writer) → mt-072 (serve backend) → mt-075 (own frontend, browser-first) + mt-076 (temporal trace enumeration) → mt-073/074 (packaging), ending at the combined feature-complete review where all human tests now batch — and as of 2026-07-27 every bead in that cut is ✔ done; the review (plus the owner-side v0.1.0 tag) is all that remains.** Rung-5 slice complete 2026-07-26 (`mettle exec --repl`); the rest of Rung 5 waits until after Rung 6 ([ADR-0014](adr/0014-rung5-repl-slice-before-rung6.md)). Rung 4 CLOSED 2026-07-25 — gate blessed under owner delegation with the 7-command recording-tail disclosure named (ADR-0012 §7, Accepted); at closure both counting nets were mismatch-free at both symmetry settings and DISAGREE was 0 at default and deep budgets. Earlier rung exits: Rung 3 2026-07-18, Rung 2 2026-07-16, Rung 1 2026-07-15 — histories in [TASKS.md](TASKS.md)'s per-rung Done sections and the ADR index.

## Scorecard
**Verdicts** — corpus 167/167 = 100% both stages; alloy4fun 150,891 codes → **0 jar-accepts/mettle-rejects**, **99.79% agreement**, 314 over-accepts. **Solve (fresh runs 2026-07-29 post-mt-083, at the amended [ADR-0017](adr/0017-gauge-default-budgets-paired-frontier.md) defaults — 25k conflicts / 32M encode / 20k var cap / 250M enum-effort, stage-1 symmetry 20, [ADR-0018](adr/0018-encoder-structural-sharing.md) encoder, correctChord banked per-command at mt-083):** 564 commands → **agree 448 (257 SAT / 191 UNSAT), DISAGREE 0** (the five mt-083 wrong verdicts fixed at mt-084 — clean-room seq-stdlib shifting-function defects), jar_nonverdict 0 (the correctChord timeout-marker class is gone), 0 panics, 0 self-check failures; remaining typed defers at defaults: capacity 28 + over_budget 75 (census families B–D incl. the three durable ADR-0018 boundary rows and correctChord[30], whose now-correct UNSAT exhausts 25k conflicts but answers at 100k; family E is resolved — see the mt-083 bead), **lowering 7 (= the mt-040 recording tail exactly)**, HO 4 (= jar-error parity), `temporal:unbounded_steps` 2 (the jar's own rejection, text-matched). Stage-1 wall ~650s at `--jobs 8`. Campaign trail at defaults: 356 (census baseline) → 402 (ADR-0017 encode raise) → 421 (ADR-0018 structural sharing) → 428 (mt-082 conflicts re-pair) → 444 + 5 wrong verdicts exposed (mt-083 banking) → **448, DISAGREE 0 restored (mt-084 stdlib fix)**; rows and grids in TASKS mt-078/079/081/082/083/084. **SB-0 counting net (`--count`; JVM-free from cached baselines since mt-054), post-mt-082 re-baseline (unmoved by the conflicts re-pair — enum-budget-bound):** count_match **65** (56→65 under structural sharing), **COUNT_MISMATCH 0**, all skips typed (enum_budget 10, jar_timeout 104, mettle_cap 53, ho_skolem 9, ordered_abstract 1; `skip_temporal_config` 0 at SB-0 — LEDGER-014's by-construction bucket). **SB-20 count net (`--count-symmetry 20`), post-mt-082:** count_match **89** (79→84 at the ADR-0017 defaults, →89 under ADR-0018), **COUNT_MISMATCH 0**, `skip_temporal_config` 3 (buffer.als[0]/[3], leader.als[3] — configuration-relative, LEDGER-014). **Both nets remain mismatch-free at both symmetry settings; temporal SAT commands now count** (leader.als's T-26 real jar baseline is a live match). Full per-bucket figures: diff a fresh run (standing rule), the mt-076 report table in [TASKS.md](TASKS.md) is the last measured pair. _Caution learned at mt-055: these bucket figures go stale silently — the previously-recorded SB-0 `enum_budget 52` was wrong by 27 against a same-tree re-measure, so **diff a fresh before/after run, never this prose** (that is exactly what mt-057 item (3) exists to fix)._ Warnings (mt-023) — all 20 classes; corpus 0 missing; alloy4fun **99.80% files identical** (192 missing / 20 extra, root-caused in [reference/warning-parity.md](reference/warning-parity.md)). Speed (mt-024 `conform bench`, re-measured 2026-07-27) — whole-corpus parse+resolve: mettle 61.5 ms vs jar batch 1,305 ms (**~21×**, thread-parallel-vs-single-thread caveat printed by the tool; median per file 1.1 ms vs 5.4 ms in-JVM / 160 ms cold JVM).

**Conformance scorecard (Rung 1/3 reference figures):** Rung-1 gauge: **corpus lex, parse, AND round-trip rate 167/167** (alloytools-models + portus-63), plus mt-014's mutation fuzzer (default 4,248 mutants/~5s in CI, verified to 88,500 mutants offline) — zero panics, sane spans, round-trip holds. Rung-3 solve gauge (mt-037): **187 agree / 0 disagree over 392 cached jar verdicts** (`baselines/`, alloytools-models + the refreshed portus-63 baseline, triaged); SB-0 counting net: 15 exact count matches, 3 count-only divergences (mt-041, verdict-neutral), typed skips for the FO/HO-skolem and cap/timeout families.

## Builds
`cargo build/fmt/clippy/test` all green workspace-wide. **Human-testable now (mt-036 + mt-062 + mt-067):** `mettle exec <file.als>` executes every command — verdicts, instances/counterexamples, `expect` checks, typed `CANNOT EXECUTE` for honest defers — **including Alloy 6 temporal models**: try `./target/release/mettle exec corpus/alloytools-models/models/examples/temporal/trash.als` (lasso search over the `steps` range, minimal SAT wins, **full state-by-state trace rendering in the jar's pinned shape with the loop marked**, `VALID (no counterexample within N steps)` wording, jar-exact `1..` rejection). **REPL (mt-062 + mt-068):** `mettle exec <file.als> --repl [--command N]` / `--eval <EXPR> [--state N]` — evaluator over the solved instance; **on a temporal command it is a trace debugger**: `:state N` moves the evaluation point (jar semantics — wraps through the loop above the trace, clamps below 0, pasts accumulate across passes), all 11 temporal operators are legal input. **XML export (mt-071):** `mettle exec <file.als> --xml <PATH>` writes the solved command as jar-shape instance XML (temporal commands: the whole lasso incl. the past-depth extra blocks); no instance ⇒ typed failure, no file. **Serve (mt-072 + mt-076):** `mettle serve <file.als> [--command <sel>] [--port N]` — browser page (stub until mt-075) + Sterling-protocol WS provider on one localhost port; try `./target/release/mettle serve corpus/…/tutorial/farmer.als --command 0`. Temporal commands enumerate live: New Trace / New Config / New Init / New Fork all answer (mt-076). **The browser app is complete (mt-075, all three slices):** graph view (default; deterministic layered layout, legend, subtractive focus) + table view, the trace rail drawing the lasso's loop, evaluator pane, server-driven buttons, the full design pass — dark/light, fully offline, zero JS toolchain. `mettle parse`/`mettle check` unchanged. Caveat: exec applies no solve budgets, so a wide `steps` range on a big model can grind (leader.als ~11 min); the gauge's budgets bound the same command fine — exec defaults are an mt-068 consideration.

## What exists
- Repo at `/Users/choong/repos/chaychoong/mettle` (primary MacBook Pro since the 2026-07-24 migration); public remote `github.com/chaychoong/mettle` — work on `main`, push after each chunk.
- Documentation spine: `CLAUDE.md`, `docs/` (index, STATE, ROADMAP, TASKS, ADRs 0001–0005), `LIMITATIONS.md`, `SEMANTICS_LEDGER.md`. Session routines: pickup in `CLAUDE.md` → "Start here"; close via `docs/SESSION_WRAP.md`; lessons in `docs/LESSONS.md`.
- **Binding steering rubrics:** `STYLE.md` + `PORTING_RULES.md` (numbered rules D#/I#/E#/R# citable in review).
- **Pinned conformance oracle:** Alloy **6.2.0** (ADR-0002, [reference/alloy6-reference.md](reference/alloy6-reference.md)); jar in git-ignored `oracle/`.
- **Cargo workspace (mt-004):** 8 crates on the hand-designed DAG; CI at `.github/workflows/ci.yml`.
- **Core type skeleton (mt-005, [ADR-0005](adr/0005-core-ir-type-skeleton.md)):** `als-syntax` = `Arena<I,T>`/`ArenaId`/`define_id!` + `Span`/`FileId` + the full Alloy 6 surface AST (unified `Expr`, temporal first-class, spans required); `als-core` = three-sorted relational IR (`Formula`/`RelExpr`/`IntExpr`) + `Universe`/`TupleSet`/`Bounds` (BTree-ordered, invariants asserted); `als-solve` = dependency-free `Var`/`Lit`/`Cnf`/`Assignment`/`Outcome` + `Solver` trait. Bones only — Rung 1+ fills flesh behind these shapes.
- **Conformance harness v0 (mt-006):** `als-conform` drives the jar via `crates/als-conform/shim/OracleShim.java` (`A4Options` API; symmetry/noOverflow/solver always explicit; `noOverflow=true` default per LEDGER-001), per-file JVM with timeout + scratch CWD, typed outcomes (Sat/Unsat/Timeout/Error), Net 0 expect-mining, deterministic text+JSON scorecard. Run it: `cargo build -p als-conform && ./target/debug/conform oracle` (or any `.als` dir; `--symmetry 0 --enumerate exhaustive` = ADR-0002's counting config). 5 jar-integration tests pin the known 87/1129 enumeration facts; they skip cleanly when the jar is absent (CI has no JDK).
- **Corpora vendored locally (mt-007, [reference/corpora.md](reference/corpora.md)):** `corpus/` (git-ignored pending mt-008) holds alloytools-models (94 .als, pinned to the jar's build commit), alloy4fun (Zenodo DOI 10.5281/zenodo.17390557, 186k JSON-Lines records, CC-BY-4.0), portus-63 (63 supported models + deps; licensing hot spot: 2× GPL-3.0, 6× no-license). kodkod: investigated, no `.als` content, not vendored. All retrieval commands recorded verbatim in the manifest — fully reproducible.
- **Pinned syntax contract (mt-010/011 spec, [reference/alloy6-grammar.md](reference/alloy6-grammar.md), [ADR-0007](adr/0007-rung1-lexer-parser-architecture.md)):** token set, filter rewrites F1–F4, 21-level precedence, grammar shapes — derived from the oracle build's grammar sources and jar-verified. AST extended to grammar parity in the same commit.
- **Lexer (mt-010):** `als-syntax::{token,lexer}` — raw spanned tokens, typed `LexError`, 167/167 corpus lex rate (`tests/corpus_lex.rs`, skips without `corpus/`).
- **Parser (mt-011):** `als-syntax::{cook,parser}` — F1–F4 cooking pass + recursive-descent/Pratt parser into the arena AST; typed `ParseError` with the reference's parse-time checks; 167/167 corpus parse rate (`tests/corpus_parse.rs`).
- **Pretty-printer (mt-012):** `als-syntax::print` — minimal-paren precedence-aware `Display` printer + span-free structural `dump` (round-trip witness); `als-syntax::prec` is the single binding-power table shared by parser and printer. 167/167 corpus round-trip (`tests/corpus_roundtrip.rs`: parse→print→reparse→dump-equal + idempotence); insta snapshots (first dev-dep). CLI: `mettle parse <file.als> [--ast]` (canonical source or dump to stdout).
- **Diagnostics + alloy4fun pass (mt-013):** rustc-style caret errors in `crates/mettle/src/diagnostics.rs` (CLI-only per E3); differential parse pass vs. the jar over 150,891 unique alloy4fun codes via batch `crates/als-conform/shim/ParseOnlyShim.java` — zero jar-accepts+mettle-rejects, 99.79% exact error-position match, 2 parser bugs fixed w/ regression tests, 2 narrow divergences documented in LIMITATIONS. Evidence: [reference/alloy4fun-error-pass.md](reference/alloy4fun-error-pass.md).
- **Mutation fuzzer + binder-composition resolution (mt-014):** `als-syntax::tests::fuzz_mutations` — zero-dep hand-rolled SplitMix64 PRNG, byte/token/splice mutation classes + deep-nesting/long-chain stressors, three properties (no panic, sane spans, round-trip). Found + fixed a real printer round-trip bug (post-Part-2 under-parenthesization) and documented a genuine, deliberately-out-of-scope printer/dumper recursion-depth finding (candidate follow-up bead). **Deep-nesting guard shipped** (`MAX_EXPR_DEPTH = 256`, new `ParseError::TooDeep`) — verified safe to 100,000 adversarial levels. **Binder-composition rule resolved** (the mt-013 LIMITATIONS entry): ~220 jar probes mapped the exact rule (one composition hop, refreshed by bare `implies`, hard-blocked by comparisons/set-test prefixes) → `crate::prec::child_binder_budget`, shared by parser (new `ParseError::BinderNeedsParens`) and printer so the two can never drift. 167/167 corpus preserved; full workspace gauntlet green. Evidence: [reference/fuzzing.md](reference/fuzzing.md).
- **Oracle baselines (`baselines/`):** 392 per-command jar verdicts at LEDGER-001 defaults — 234 over alloytools-models (mt-006 era) + 158 over portus-63 (mt-037) — each with triage in `baselines/README.md`; the mt-037 solve gauge's comparison set.
- **Pinned resolution & type contract (mt-016, [reference/alloy6-resolution.md](reference/alloy6-resolution.md), [ADR-0008](adr/0008-rung2-resolver-architecture.md)):** the Rung-2 authority document — `resolveAll` phase pipeline, module system, type system, `ExprChoice` disambiguation, exact reject/warn taxonomy, `util/*` interfaces (§7.1–§7.11 per-member appendix; clean-room, signatures only), 48 jar probes. Key facts: the mt-020 gauge is binary ACCEPT/REJECT (warnings never fatal); resolution mode 1 only; **no `int`↔`Int` coercion** (`1+2` is the set `{1,2}`); funcs/preds overload while sigs/asserts/macros reject dups and facts don't check names; open instance identity = (filename, params); enum ordering auto-aliases to `ordering`.
- **Module graph + `open` layer (mt-017):** `als-types::{file,graph,load,loader,path,stdlib,error}` — `FileTable`/`ModuleGraph` typed-ID arenas, `ModuleLoader` (fs + injected map), §2.1 search order with prefix-cancel `computeModulePath` (jar-verified on mislocated zigbee modules), cycles, instance identity (file, args), aliases + duplicate-alias reject, private-open blocking, all-files `seen_dollar` gate, module-phase `ResolveError`s. **Corpus: 167/167 module graphs load.**
- **Clean-room stdlib (mt-015, [ADR-0006](adr/0006-licensing-posture.md)):** all 11 `util/*.als` at `crates/als-types/stdlib/util/`, written against §7.1–§7.11 only (author attested never seeing upstream text), embedded via `stdlib::MODULES` (disk shadows it, matching the jar). Body-level judgment calls (util/time macro bodies, util/relation `complete`, rank arithmetic in natural/sequence/seqrel) recorded in the mt-015 report for mt-020 differential triage — not resolve-visible, so not Rung-2 risks.
- **Resolver/typechecker core (mt-018, [ADR-0009](adr/0009-fused-resolve-pass-accept-lean.md)):** `als-types::{ty,world,warning,resolve/{mod,sigs,members,expr}}` — `resolve(&ModuleGraph) -> Result<Resolved, ResolveError>`; full §1 pipeline, `Type` union-of-products value type, fused bidirectional §4 checker, complete §5.1 variant coverage, synthetic enum/`seq`/`util/integer` opens in the loader (the jar auto-opens `util/integer` everywhere — jar-probed). **Corpus: 167/167 resolve+typecheck ACCEPT.** 37 probe-table tests. Interim accept-lean divergences → LIMITATIONS; mt-020 decides tightening.
- **Scopes → universe (mt-029):** `als-core::scope::compute_universe` — faithful `ScopeComputer` port (fixpoint, multiplicity forcing, exact atom naming/order); `ScopedUniverse`/`ScopeTable` seam. Corpus 167/167, 564 root commands, byte-identical twice. **Now includes the §1.2 scope-raise** (a sig's scope silently raised to its children's lower sum, probe B19 — found in mt-030 review).
- **Bounds builder (mt-030):** `als-core::bounds_builder::compute_bounds` — faithful `BoundsComputer` + `A4Solution`-builtin-bounds port: relation allocation (leaf/remainder/abstract-union/subset), field product bounds (`one`-sig owner strip), quantified-over-atoms size/mult/disjointness constraint formulas, `Int`/`seq/Int` exact. Emits the per-sig/field **denotation seam** (`RelExprId`) mt-031 consumes. Pinned facts in translation-ref §1.4′ + probe matrix §10.2 (B1–B19); 22 jar-pinned goldens; corpus 167/167 (~14k relations), deterministic. `util/ordering` exact pinning deferred to mt-035 (LEDGER-004, approved).
- **IR lowering + goal assembly (mt-031):** `als_types::choice` (the checker records every name/spine/call/macro resolution, keyed `(ModuleId, ExprId)`; facts/appended-facts/func-bodies/command-targets exposed; **alloy4fun gauge byte-identical** before/after — recording is non-behavioral) + `als_core::lower::lower_command` → `LoweredGoal` (provenance-labeled conjuncts): full §2 mapping tables, quantifier desugars (no skolemization, ADR-0011), pred/fun inlining, synthesized field facts (§2.5 split pinned: mt-030 = sig constraints, lower = field facts; probes L1–L14, translation-ref §10.3), `check` negation. Typed defers, never wrong verdicts: temporal (Rung 6), String (Rung 4), ordering (mt-035), nested arrows/HO macros/misc (mt-038). Corpus: 564 commands → **124 lower, 440 typed defers, 0 panics**, deterministic.
- **CDCL SAT solver (mt-032, ADR-0011 owner decision):** `als_solve::solver::CdclSolver` — zero-dep, deterministic by construction (integer `u64` VSIDS, no floats/wall-clock/hashing; Luby restarts; 1-UIP + minimization; keep-all DB). **Incremental enumeration seam for mt-033**: `add_clause` (watch-repair for already-falsified/unit blocking clauses) + `block()`; learned clauses retained soundly. Verified against a brute-force reference: verdicts, models, **enumeration counts** (the SB-0 foundation), determinism — 30k-instance release fuzz sweep green. PHP(8,7) 30ms.
- **Differential solve gauge (mt-037):** `als-conform::solve_gauge` (`solve-gauge` bin) — Stage 1 runs the full pipeline over every root command of both corpora and buckets every verdict against `baselines/*-verdict.json` (partition asserted, deterministic text+JSON report); Stage 2 (`--count`) checks SB-0 count parity with typed skips for the FO/HO-skolem, ordered-abstract-partition, and cap/timeout divergence families. Fresh jar baseline `baselines/portus-63-verdict.json` (158 commands/63 files) brings the comparison set to **392 cached jar verdicts**. Found and fixed three wrong-verdict bugs along the way (see "Current rung" above). **Gauge: 564 commands → agree_sat 104, agree_unsat 83, DISAGREE 0, jar_nonverdict 2**, 375 typed defers by bucket, **0 panics, 0 self-check failures**; **baseline overlap 187 agree / 0 disagree over 392 jar verdicts.** One count-only divergence (root-module `util/ordering`, verdict-neutral) filed as **mt-041**. Run it: `cargo build --release -p als-conform && ./target/release/solve-gauge [--count]`.
- **Pinned Rung-4 semantics contract (mt-043, [reference/alloy6-translation.md](reference/alloy6-translation.md) §11–§16 + probes §10.7–§10.12, [ADR-0012](adr/0012-rung4-integers-strings-counting.md) Accepted):** integer arithmetic at bitwidth incl. the Milicevic/Jackson forbid-mode polarity rule, `Int/next`/`Int/zero` the only translation-referenced builtin Int relations (`min`/`max` = int constants), String `"String%d"` padding + `max(N,#referenced)` expansion + non-exact-scope reject, `seq` = `seq/Int -> lone X` + one contiguity fact, FO skolemization (depth-0, `$<cmdLabel>_<var>`, count effect pinned), SB=20 = lex-leader cap (verdict-neutral). Ledger entries LEDGER-005 through LEDGER-011 all `approved` and shipped (see [SEMANTICS_LEDGER.md](../SEMANTICS_LEDGER.md)).
- **Integer fidelity end-to-end (mt-044):** `als_core::overflow_guard` (shared encoder/evaluator classifier — universal-rescue iff the binder's own domain is literally bare `Int`/`seq/Int`) + full arithmetic circuits in `encode/int.rs` (structural ports of Kodkod's non-restoring division and `shl`'s junk-bit overflow loop) with the evaluator as a differential-tested matched pair; `Int/next`/`Int/zero` exact bounds; `fun/min|max|next|prev` lowering; LEDGER-009 own-module-first run/check targets. Conformance suite `crates/als-core/tests/int_conformance.rs` (jar-pinned constants, CI-safe). Contract archaeology in translation-ref **§10.7b–d**.
- **String support (mt-045, LEDGER-007, translation-ref §13):** `als-core::strings` (referenced-literal collection); `scope.rs` mints the quoted literal atoms + `"String%d"` padding last in the universe (exact-scope fill, `max(N,#referenced)` expansion, non-exact reject); bounds bind `String` exactly + per-literal `s2k` singletons; encoder/evaluator carry an explicit `int_end`. Deterministic lexicographic literal order (symmetric, so verdict/count-neutral). `StringUnsupported` deleted. Conformance: `crates/als-core/tests/string_conformance.rs` (probes S1–S7 + solve-level, jar-free).
- **seq support (mt-046, LEDGER-008, translation-ref §14 + §10.10a):** `seq X` fields lower as `seq/Int -> lone X` + the per-owner contiguity fact; expression-position `seq` defers typed like a marked arrow. Clean-room `util/sequniv`/`seqrel`/`sequence` bodies differentially corrected (`idxOf`/`lastIdxOf` closure swap; `afterLastIdx` = smallest **unused** index). Counting-net enumeration is budget-bounded: `SolveOptions::enum_conflict_budget` → `InstanceEnumerator::exhausted()` → typed `skip_enum_budget`. Conformance: `crates/als-core/tests/seq_conformance.rs`.
- **FO skolemization (mt-047, translation-ref §15 + probes §10.11):** top-level effective-existential FO decls (incl. run-pred receivers/params and a `check`'s negated `all`) mint `$<label>_<var>` skolem constant relations through the shared `alloc_skolem` core; label = innermost inlined func's tail name, else the command label; `abstract_upper` reads `skolem_bounds` too, so skolem-dependent decls skolemize. Instances display witnesses; SB-0 counts match the jar. Conformance: `crates/als-core/tests/fo_skolem_conformance.rs` (12 tests).
- **Int-equality typing & cast overflow semantics (mt-051, LEDGER-010, translation-ref §10.7e):** one-sided `Int[·]`-cast `=`/`in`/mult-tests solve with the jar's pinned semantics — set equality always; forbid mode = overflowed-cast emptiness + the rules-0–3 polarity guard + the constant escape, all shared between encoder and evaluator via `overflow_guard`. `eq_typing_conformance.rs` (14 jar-pinned test fns). Residual honest corners in LIMITATIONS: the `0-(max+1)` MINUS peephole (**mt-052**, live divergence, zero corpus incidence), nested-inside-`Card` cast flags, quant-bound casts, `&`/`-` nesting (unprobed).
- **Symmetry breaking (mt-048, translation-ref §16.1–§16.4):** `als-core::encode::symmetry` — coarsest-partition detection by exact-signature grouping (per-int + per-string-atom singleton classes; skolems excluded, detection pre-skolem) + the bit-exact lex-leader SBP in the encoder (soft cap per `(class, adjacent pair)` at relation boundaries, `(arity asc, name asc)` relation order with `$`-skolems first, mirror/identity filters, Tseitin-only — verdict-neutral by construction); `SolveOptions::symmetry` (default 20 = jar drop-in; 0 = the ADR-0002 counting regime), `expect 1 ⇒ 0` forcing in exec + both gauge stages; gauge `--symmetry N` / `--count-symmetry N`. Conformance: `sb_conformance.rs`, 9 jar-pinned probes.
- **Runtime observability + deadlines (mt-047, owner-directed):** solver carries deterministic `total_conflicts/decisions/props` counters; `SolveOptions::enum_effort_budget` bounds a whole enumeration ending in a typed `skip_enum_budget`; `solve-gauge` emits a stderr liveness heartbeat (`[k/N] file`, per-command `…`/`took X.Xs`, slowest-10 table) while stdout stays the deterministic report — capture as `> report 2> progress`, never `2>&1`. Standing rule: every long run gets a hard `timeout`; budget changes validate on correctChord/ptcris/addressBook first.
- **Throughput & feedback-loop tooling (mt-054/mt-057/mt-058/mt-059):** `solve-gauge --jobs N` (deterministic command-level parallel fan-out via `solve_gauge/parallel.rs` and the two-phase `execute.rs` executor — byte-identical reports at any job count); cached count baselines (`solve_gauge/count_baseline.rs` format + loader, `refresh.rs` `--refresh-counts`/`--resume` capture, stage 2 JVM-free by default, `--live-jar` opt-in); `--fail-fast`/`--only`/`--from-report --from-buckets` delta modes; `status.rs` owner-facing monitor (`status/<tool>.txt`, `scripts/watch-status.sh`); the count baseline is now consulted **before** enumerating (`count_baseline.rs::CountLookup::resolution`), skipping enumeration wherever no possible count changes the bucket. **Verification battery: ~37 min → ~12 min, zero coverage traded** (ADR-0013). `solve_gauge/mod.rs` split into `execute.rs`/`report.rs`/`count.rs`/`mod.rs` (mt-058). **Standing practice: prefer `--only`/delta/fail-fast iterations; full sweeps for chunk-level verification; refresh count baselines only when new coverage needs fresh jar facts.**
- **Temporal foundation (mt-065, [ADR-0015](adr/0015-rung6-temporal-architecture.md) §1):** `als_core::ir::Mutability` (closed enum on `Relation`, set at every bounds-builder allocation site from the resolved `var` flags — remainder follows the parent's own flag per `BoundsComputer.java:194`, fields their own per `:448`, builtins/ordering/strings/skolems static); `als_core::temporal` = `unroll(ir, bounds, k)` → `UnrolledBounds` (relation-major per-state RelId copies named `name@s`, originals dropped, bridge map `states` is the only handle) + `LassoSelector` (exactly-one loop index, ALO + pairwise AMO); `als_types::is_temporal_model` = the pinned §(a) discriminator (whole-world `var` half, per-command operator half, explicit-worklist walk). **Nothing wired into dispatch (mt-067) — stage-1 sweep byte-identical before/after.** Six unprobed corners → [reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(k), owed to mt-069. Tests: `temporal_conformance.rs` (24) + in-module (13).
- **Temporal lowering (mt-066, [ADR-0015](adr/0015-rung6-temporal-architecture.md) §2 as amended):** `als_core::temporal_lower::lower_temporal_command` — the LTL-on-lasso translation at a fixed trace length k: logical timeline `[0..k) ++ [l..k) × d` (`d` = past-nesting depth; past operators follow the **true lasso history** per probe P-D2, which refuted the ADR's original "honest prefix"), loop index split once above the goal via `FormulaKind::LoopIs` (encoder maps it to the mt-065 `LassoSelector`), temporal-free subtrees memoised per physical state, per-conjunct `always` seam + `var`-hierarchy facts (union rigidity, migration ban) probe-confirmed, FO-only output asserted. Reuses the whole static walk (`lower_command_keeping_temporal`, one flag). `solve_temporal_goal` = one fixed-length encode+solve (selector minted after primaries; self-check skipped — needs the solved loop state, mt-067's shape). Full cell set: [reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(l); probe harness `scratchpad/probe/mt066/` (git-ignored, like mt-063's). Tests: `temporal_lower_conformance.rs` (31) + in-module (5).
- **Temporal solve driver (mt-067, [ADR-0015](adr/0015-rung6-temporal-architecture.md) §3):** `als_core::temporal_solve::solve_temporal_command` — the single shared driver (exec + gauge): ascending-k sweep over `ResolvedCommand::steps_range()` (widened `StepsScope` = the jar's `minprefix`/`maxprefix`, `resolved()` = `ScopeComputer.java:474-493` literally), first SAT = minimal lasso (`TemporalTrace`: per-state `Instance`s keyed by original RelIds + `loop_state`), exhausted = UNSAT-within-bound, budget/capacity at any length = the command's outcome. Typed defers: `1..` (jar text), `check … for 1 steps` (jar NPE, owner fork open). Per-state SB (skolems excluded under temporal; relation-major lex order = disclosed verdict-neutral divergence). Temporal self-check via `Evaluator::with_loop_state`. Also fixed en route: **subset-sig multiplicity constraints were never emitted** (static wrong-verdict class, probe-pinned, `emit_subset_multiplicities` off the denotation). Probe evidence: `scratchpad/probe/mt067/` (git-ignored). Tests: `temporal_solve_conformance.rs` (17) + symmetry (4) + solve regressions (5) + exec (4).
- **Temporal surface (mt-068, [ADR-0015](adr/0015-rung6-temporal-architecture.md) §4):** exec's `render_trace` = the jar's `toString(state<0)` shape exactly (§(f)), block interiors = the unchanged mt-036 renderer (static output byte-identical; LEDGER-012 posture on labels/order, LIMITATIONS-recorded). REPL per-state: `als_core::eliminate_fragment_at_state` (the mt-066 timeline machinery at the concrete solved loop target — one LTL semantics in the codebase), `TraceArtifacts` on `TemporalTrace` (unrolled view + flat instance + goal; irreproducible post-solve), `normalize_state` = the jar's clamp+wrap written in its own form. **P-068-1 pinned eval-at-logical-time** (pasts accumulate across passes; GUI arrows pre-normalize, the API does not) — §(h) extended in place. exec deliberately unbudgeted (jar drop-in). Probe notes `scratchpad/probe/mt068/` (git-ignored). Tests: repl (12) + exec (4) + core (8).
- **Temporal conformance arm (mt-069, [ADR-0015](adr/0015-rung6-temporal-architecture.md) §5):** `buffer.als` banked per-command against the jar (`scratchpad/probe/mt069/PerCommandProbe.java` — a small one-off `OracleShim.runOne` variant taking a command index, so a slow command can never blot out the ones before it in the same file); merged into `baselines/alloytools-models-verdict.json` byte-shape-preserving. All nine outstanding §(k)/§(l)/mt-068 probe cells live-probed (`scratchpad/probe/mt069/NOTES.md`): K1/K2/K3 (non-descent, appended-fact exclusion, `;`-is-temporal) and L7/L8 (static-disj-on-var-sig, `BoundsComputer.size` var-sig witness) all **CONFIRM**; M068 (temporal eval at a static prompt) pins a conservative capability gap, not a divergence. **Two REFUTE mettle's shipped discriminator/steps-scope resolution — filed as open, undecided (LIMITATIONS + SEMANTICS_LEDGER "Corners that NEED entries" + §(m)), not fixed by this bead:** macro-body visibility (K4a/K4b — the jar expands macros before the `isTemporalModel` scan, mettle's surface-AST scan does not) and `for exactly N..M steps` (L6/L6b — the jar collapses to `N..N`, mettle keeps the full range). Temporal counting posture verified (`skip_temporal_trace` unconditional even for `leader.als`'s real count baseline). Deep-budget spot-run (L1 100k/32M/200k, L2 400k/128M/500k, matching mt-050's levels) on the 7 temporal `over_budget` rows converted 4 to `agree_unsat` (3 remain over_budget even at L2 — `buffer.als`'s three heaviest commands), `DISAGREE` 0 at every depth. 2 new tests (`expect` on a temporal command; `leader.als`'s counting posture) + 3 test-comment updates turning "flagged for mt-069" into probe-confirmed citations. Full stage-1 sweep before/after banking: `agree_sat` 201→203, `jar_nonverdict` 18→16, `DISAGREE` 0 both sides.
- **Temporal trace enumeration (mt-076, [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Resolution 3, [LEDGER-014](../SEMANTICS_LEDGER.md)):** `als_core::temporal_enum::TraceEnumerator` — `advance(TraceStep::{NextPath, NextConfig, Fork{hold}}) -> TraceAdvance`, jar-parity semantics from the P-076 probe wave ([reference/alloy6-temporal.md](reference/alloy6-temporal.md) §(g) mt-076 wave + §(i) Correction): per-length raw `(states, loop)` exhaustion → advance through `[min,max]`, across-length infinite-trace dedup, config lock at the first solution (static primaries unit-fixed, skolems excluded), `fork(p)` prefix-hold + state-`p` forcing, `fork(-1)` restart-sweep + the pinned no-alternate rule. Typed budget/cap exhaustion. Counting: temporal SAT commands in both nets, `skip_temporal_config` for the LEDGER-014 by-construction cases. Serve's five temporal verbs ride it.
- **Serve backend (mt-072, [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Decision 2 + Amendments):** `mettle serve <file.als> [--command <sel>] [--port N]` — one `127.0.0.1` process/port (default 4030): hand-rolled HTTP GET (embedded mt-075 stub page) + WS provider routed on the `Upgrade` header, pinned Sterling protocol + the ADR-amended `error` type. `als_sterling::ServeSession` trait (protocol tested solver-free) ← `mettle::serve::session::{StaticSession, TemporalSession}`; `data` = mt-071 XML, `eval` = REPL `eval_line` (stale `datumId` refused), static `next` enumeration, temporal verbs typed-deferred to mt-076. Deps: `tungstenite` no-default-features (accept-echo hand-rolled in `handshake.rs`, vector-pinned) + serde/serde_json; thread-per-connection, no async runtime. 39 tests incl. real-socket + spawn-the-binary integration.
- **Instance-XML writer (mt-071, [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Decision 1):** `als_instance::xml::write_instance_xml(&mut Ir, &XmlRequest) -> Result<String, TranslateError>` (`XmlSolution::{Static, Trace}`) — the jar-shape-exact `A4SolutionWriter` port: lazy touch-order IDs rebuilt per `<instance>` block, fields interleaved after their owning `</sig>`, the always-on `macros` mechanism (`m<i>` fun-skolems + past-depth extra blocks, count `tracelength + extra·(tracelength − loopState)`), pinned escaping, `looplength`. Surfaced as `mettle exec <file> --xml <PATH>` (single-command, exclusive with `--repl`/`--eval`, no-instance ⇒ typed failure without a file) and as the library seam mt-072's serve consumes. Verified 18/18 against the jar's own `A4SolutionReader.read` (evidence `scratchpad/probe/mt071/NOTES.md`); LEDGER-013 (skolem `<types>` over-approximation, approved); cosmetic divergences in LIMITATIONS §mt-071.
- **Rung-5 remainder contracts (mt-070, [ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Proposed):** [reference/alloy6-instance-xml.md](reference/alloy6-instance-xml.md) = the complete jar `writeXML` schema (bytecode-branch-enumerated then live-probed, X-01..X-09b; the `macros`/extra-instance mechanism and lazy touch-order IDs are the two divergence risks a Rust writer must model; tech-lead re-runs byte-identical) and [reference/sterling.md](reference/sterling.md) = the Sterling provider protocol (external contract, not jar authority; `sidprasad/sterling-ts` lineage; the `backloop` XML dialect mismatch and the missing-license fork are the two load-bearing finds). Packaging memo (cargo-dist + flake output) folded into the ADR. Probe evidence: `scratchpad/probe/mt070/` (git-ignored). Implementation beads mt-071–074 cut in [TASKS.md](TASKS.md).
- **Public remote (mt-042(a)):** `origin = git@github.com:chaychoong/mettle.git`; standing workflow — work on `main`, push after each chunk.
- **Machine:** MacBook Pro (10-core M-series, 64 GB, macOS 26, aarch64-darwin) is the primary and only machine — migrated 2026-07-24 from the exe.dev VM per [MIGRATION.md](MIGRATION.md) (as-executed log appended there: cross-arch determinism verified byte-for-byte, `flake.lock` committed, bootstrap fixed for macOS bash 3.2, fixtures moved into `crates/als-conform/fixtures/`). Rust stable + OpenJDK 21 via the nix flake.

## In flight (delegated, background)
- _None._

## Not yet started
**The ADR-0016 cut is fully implemented (mt-071–076, 073, 074 all ✔) and v0.1.0 is released.** What remains: **the combined feature-complete review** ([REVIEW-v0.1.0.md](REVIEW-v0.1.0.md) — all batched human tests: Rung-6 gate, Rung-5 exit; the `check … for 1 steps` fork was decided early — mt-077/LEDGER-015; §0 chores all closed); backlog mt-056, mt-052 — see [TASKS.md](TASKS.md). Toward the proposed exit gate: the counting criterion is **met outright** (SB-0 COUNT_MISMATCH 0; `skip_fo_skolem` and the mt-041 family both retired) and no forbid-mode comparison defers remain; what's left is the honest capacity remainder (mt-050 throughput; the heartbeat's slowest-command table is its worklist). Still-unscheduled backlog: **mt-052** (MINUS peephole), **mt-055** wetdry/tso both closed — no residual, **mt-021** (printer/dumper recursion depth; needs a small ADR), **mt-027** (residual diagnostic parity; standing rule — every Rung-4 typing bead notes its effect on the 314 over-accepts; mt-041 is bounds/scope-phase only, no effect on the 314). None rung-gating.

## Next chunk (planned)
**The census order resumes (a bare "proceed" starts it): evaluate (1b) IR hash-consing — the ADR-0018 deferral — against the established sweep history.** ADR-0018 landed the encoder-level structural value cache (1a) and deferred IR-level hash-consing (1b) until 1a's sweep was green; it has been green through mt-082/083/084/085. The chunk is an evaluation first, not an implementation: measure whether hash-consing still buys conversions on the remaining defer mass (capacity 28 + over_budget 75) now that the value cache absorbs most structural duplication — mt-080's profile suggests the win may have collapsed; if the measured headroom is small, record the rejection in ADR-0018 and move on. Family D (search-bound, solver-quality work) stays last. Also open, low priority: the evaluator can't resolve module-qualified atom literals it prints (mt-084); receiver-call permissiveness on 0-ary preds (mt-085 → mt-027 territory); correctChord[30]'s 25k→100k conflict need is a data point for the next ADR-0017 paired grid, not a standalone change.

Also available, none gating: **mt-056**, **mt-052**, the 7-row recording tail, **mt-027**, **mt-021**. Pacing contract: one chunk per "proceed", report, stop.

## Open questions for the human
_Standing cadence (owner, 2026-07-27): **all human tests batch into ONE combined feature-complete review** after mt-071/072/075/076/073/074 — per-rung gating is superseded until then. The two items below are parked there, not awaiting an answer now._

**1. The Rung-6 exit gate — batched to the combined feature-complete review.** The temporal rung is implementation-complete, measured, and clean: temporal models solve/render/debug end-to-end; all 22 former temporal defers are real gauge rows (13 agreements, 2 mettle-solves-where-the-jar-times-out, 7 honest budget defers — 4 of which convert to agreements at deep budgets with DISAGREE 0 — 2 jar-matched typed rejections); the contract has three probe waves plus the mt-069 closing wave behind it; both late-found divergences are fixed and regression-tested (see below). **The human test:** `./target/release/mettle exec corpus/alloytools-models/models/examples/temporal/trash.als` (full traces, loop marked), then the same file with `--repl --command 0` — step with `:state 1`, `:state 5` (watch it wrap through the loop), and evaluate temporal expressions like `always some Trash` live. The blessing (ADR-0015 → Accepted) lands at that review; the human test above stays the script for it.

**2. ~~The `check … for 1 steps` fork~~ — RESOLVED (owner: Option B, 2026-07-27, mid-review).** Decided, implemented, and ledgered same day as **mt-077** ([LEDGER-015](../SEMANTICS_LEDGER.md)): mettle answers every one-state temporal bound; the mt-077 probe wave corrected the jar bug's scope (constant-folding trigger, `run` and `check` alike — the old refusal was itself a divergence on the majority subset). Nothing owner-facing remains here.

_(The two mt-069 STOP-THE-LINE finds — macro-body visibility and `exactly N..M steps` — were no-fork conformance bugs, decided fix-now by the tech lead under the standing Option-B delegation and closed 2026-07-27: the discriminator follows used macros through the recorded ChoiceTable, and `exactly N..M` collapses to `N..N`; both probe-pinned, regression-tested, sweep byte-identical.)_

Everything below is resolved; kept as one-liners for the trail.

- _Resolved 2026-07-27:_ Sterling frontend/license fork — **option C, browser-first**: own minimal frontend served by `mettle serve` (not Tauri; desktop shell possible later), Sterling wire protocol kept for interop; nothing upstream embedded, license question moot ([ADR-0016](adr/0016-rung5-remainder-serve-xml-packaging.md) Resolutions).
- _Resolved 2026-07-27:_ crates.io name — **skip crates.io outright** (binary product, internal crates; no rename/dispute; revisit only if libraries ever ship).
- _Resolved 2026-07-25:_ Rung-4 exit gate — **blessed** (ADR-0012 §7 → Accepted, 7-command recording-tail disclosure named).
- _Resolved 2026-07-25:_ Rung 5 vs Rung 6 sequencing — owner delegated ("do what you think is best") → tech-lead recommendation adopted: **REPL slice → Rung 6 → rest of Rung 5** ([ADR-0014](adr/0014-rung5-repl-slice-before-rung6.md)).
- _Resolved (mt-042 shipped):_ machine-portability scheduling — bootstrap script + nix flake + migration checklist, executed 2026-07-24 ([MIGRATION.md](MIGRATION.md)).
- _Resolved (mt-048 shipped):_ symmetry-breaking posture — SB-20 perf + dedicated verdict/count net, SB-0 stays the counting yardstick ([ADR-0012](adr/0012-rung4-integers-strings-counting.md) decision 6).
- _Resolved 2026-07-18:_ Ledger approvals — the owner delegated **no-fork entry approval to the tech lead ("Option B")**; genuine forks (jar ambiguity, deliberate divergence, resource tradeoffs) still come to the owner framed as decisions.
- _Resolved 2026-07-19:_ Hardware/scale plan — stayed on the VM through mt-044/045/046, migrated to the Mac after mt-042(b–d) (executed 2026-07-24).

_(The `check … for 1 steps` fork came due at mt-067, was the open decision above until 2026-07-27, and is now resolved — owner Option B → mt-077, [LEDGER-015](../SEMANTICS_LEDGER.md).)_

## Standing constraints (not documented elsewhere)
- **Diff a fresh run, never trust prose numbers** — scorecard/bucket figures in this file go stale silently (mt-055 caught a stale `enum_budget 52` that was actually 79); always re-measure before relying on a figure here for a decision.
- Syntax facts (grammar tokens, literal rules, token-stream rewrites) are pinned in [reference/alloy6-grammar.md](reference/alloy6-grammar.md) — that document, not this one, is authoritative.
- ADR index + rationale: [adr/](adr/README.md). Ledger entries (semantics decisions requiring owner/tech-lead approval): [SEMANTICS_LEDGER.md](../SEMANTICS_LEDGER.md).

# Current state

> The live "where are we" doc. Update this at the end of every work chunk. On pickup, read this first.

**Last updated:** 2026-07-25 (**mt-055 tso half DONE — root-caused, fixed, and the SB-20 counting net is now CLEAN (77 matches / 0 mismatches). Both counting nets are mismatch-free at both symmetry settings.** The divergence was never in the model's memory-model relations at all: **mettle was skolemizing existentials that Kodkod refuses to skolemize.** Kodkod's `Skolemizer` sets `skolemDepth = -1` for the **entire subtree** under a positive `or`/`implies`, a negated `and`, and `iff` (`visit(BinaryFormula)` `Skolemizer.java:471`: `IFF || (negated && AND) || (!negated && (OR || IMPLIES))`; `visit(NaryFormula)` `:541-542` — tech lead re-read both before accepting). tso's trigger is one fact, `tso_transistency_perturbed.als:526-530`, a positive `or` whose right disjunct is `some a: pte.map.VirtualAddress | …`: mettle minted `$sc_per_loc_a`, the jar mints nothing. **The skolem is doubly observable** — an extra free relation multiplies enumerated instances, *and* because `$`-names sort first in the §16.3 SBP relation order it ate the entire lex-leader budget for the `VirtualAddress` symmetry class, leaving that symmetry unbroken. That is what reconciles mt-048's old puzzle ("121 instances, zero of them symmetric duplicates") with an over-count — the SBP was correctly excluded as the *cause*, but was the *amplifier*. Skolemizing there is **sound**; the jar is merely conservative, so conformance requires copying the conservatism. **The prior leading hypothesis (`rf_p` used directly at the top of the acyclic union, correlating exactly with the two divergent commands) was a coincidence — dead lead, recorded as such.** **Review was the story again:** the adversarial pass found the fix left **a live hole of its own class** — formula-valued `if/else` branches bypassed the rule, because the jar's `visit(ExprITE)` emits `c.implies(t) and c.not().implies(e)` (`TranslateAlloyToKodkod.java:776-783`), i.e. two positive `IMPLIES` at positive polarity and a negated `AND` at negative, blocking *both branches* — and mettle's own `(c∧t)∨(¬c∧e)` IR shape is not what the jar sees. Jar-pinned and fixed (positive ITE 16 → 4, negative 24 → 6). It also caught that the `exprs.len() >= 2` block guard was **right in substance but wrong in its stated reason** (the carve-out is `getSingleFormula`'s binary heap of `BinaryFormula` ANDs short-circuiting at `n == 1`, `.java:1035-1058` — Alloy never builds a Kodkod `NaryFormula` for a block), and that §10.6's probe table mis-spelled two rows with `;` (Alloy 6 **sequencing**, jar count 1) instead of the newline spelling actually measured. All corrected. A second hole — a **formula-valued `let` freezes skolemization polarity at the binding site** while the jar decides at the *use* site — was jar-pinned but **deliberately NOT fixed**: the conservative one-line block trades over- for **under**-skolemization, and the faithful fix (lazy per-use lowering) is a design change, not a chunk-tail edit. Filed as **mt-056** with both candidate fixes, an unprobed first-visit-wins corner to settle first, and a LIMITATIONS entry; pre-existing, zero corpus incidence. **Verification — before/after measured on the identical tree, binary and box** (the tech lead reverted `lower.rs` to HEAD and re-ran both nets rather than trusting STATE.md's prose, which was the right call: the recorded SB-0 `enum_budget 52` proved **stale**, the true pre-fix value was 79, and the post-fix 78 had briefly looked like a regression): **SB-0 count_match 52 → 53, COUNT_MISMATCH 0 → 0; SB-20 count_match 74 → 77, COUNT_MISMATCH 2 → 0** (the 2 were exactly `tso[0] mettle=121 jar=11` and `tso[4] mettle=96 jar=8`). **Stage-1 unchanged and byte-identical ×2: agree 343 (195 SAT / 148 UNSAT), DISAGREE 0, jar_nonverdict 16, 0 panics, 0 self-check failures**; defers a wash (one command moved `capacity` 116 → 117 vs `over_budget` 56 → 55), so the sweep hash is re-recorded in MIGRATION (`e7a6c1a8…` → **`c77ef8ce…`**) even though no verdict moved. 11 jar-pinned tests in the new `crates/als-core/tests/skolem_polarity_conformance.rs` (SB-20 **and** SB-0 for 9; the 2 ITE rows SB-20 only, which is all the review pinned); translation-ref §10.6 rewritten. Gauntlet + fmt + clippy green. **Owner directive (2026-07-25): the very next chunk is mt-057** — the feedback-loop speed bead, ahead of any Rung 5/6 content work; ≈85 min of verification wall time on this one-file fix is the motivating measurement.**) Earlier same day: (**mt-050 done — the Rung-4 exit sweeps are complete and the ADR-0012 gate criteria are MEASURED MET: DISAGREE 0 at default AND deep budgets, counting nets clean, every defer bucket typed and accounted for. Owner touchpoint pending.** The chunk had three arms. **(1) Jar verdict campaign:** the 10 portus files that were 60s file-level timeouts at baseline capture re-ran at 1800s/7200s on this box → **152 new command verdicts** banked in `baselines/portus-63-slow-verdict.json` (drop-in supplement, command entries override the base file's timeouts; triage in `baselines/README.md` incl. elevator `I3a` = a stale upstream expect, jar-UNSAT in both overflow modes). Two files stay honestly non-comparable: **correctChord** (the reference jar cannot sweep its 39 commands even in 7200s — while mettle at deep budgets answers 23 of them, arguably the strongest drop-in datapoint of the rung) and nothing else (ertms_1A converted at 7200s, 14/14 expect-matches). **(2) The `lowering 43` bucket was decomposed and mostly killed:** a classification pass proved 36 of 43 were ONE Rung-3 bug — a `let` binding a **formula** value was forced down the relation-lowering path (`elevator_spl_events.als`, all 36 commands via one shared fact) — NOT the temporal-adjacent family the bucket label suggested. Fixed (opus): sort-aware `push_let_bindings` + `Binding::Formula` + the `sort_of` formula-ITE fix; elevator command 0 now answers **VALID matching its `expect 0`**; 10 new tests. The residual bucket is exactly the known typed recording tail: **7** (2 `$`-meta, 3 nested-macro, 1 unconfirmed ertms[5], 1 run-on-fun). **(3) Deep-budget exit sweeps** (budgets validated on the pathological files first, per the standing rule; L1 = 100k conflicts / 32M encode / 200k var-cap for chord+ertms, L2 = 400k / 128M / 500k for the other 30 worklist files): **DISAGREE 0 everywhere, 0 panics, 0 self-check failures** — deep-ertms +8 agreements vs its fresh verdicts, deep-B **180 agreements (95/85)** incl. the first-ever verdict comparisons on the TransForm minimality scripts (the mt-055-adjacent risk family — all agree), residual capacity/over_budget shrinking honestly at depth (~76 genuinely huge commands remain, plus chord's 23 non-comparable solves). **Default battery on the final tree: agree 301 → 343 (195 SAT / 148 UNSAT), DISAGREE 0, jar_nonverdict 35 → 16 (= correctChord only), lowering 43 → 7; SB-0 net 52 / 0 byte-identical ×2; SB-20 74 / 2 (tso pair, mt-055); new stage-1 hash `e7a6c1a8…` re-recorded in MIGRATION (the let-fix legitimately changed the sweep). Gauntlet + fmt + clippy green.** Exit-gate state vs the ADR-0012 proposal: 0 disagreements ✓; counting criterion ✓ (52/0, both retired families); remaining defers = temporal 22 (Rung 6) ✓ + HO 4 (jar-error parity) ✓ + honest capacity/budget remainder ✓ + **one disclosure the proposal text didn't name: the 7-command typed recording tail** (mt-040 family, zero wrong verdicts, in LIMITATIONS) — the owner blesses the gate with that tail named, at the touchpoint this report constitutes.) Earlier: (**mt-041 done — the counting net is CLEAN: SB-0 count_match 52 / COUNT_MISMATCH 0; the ADR-0012 exit gate's counting criterion is met outright.** The filed mystery ("how does the jar detect the ordering module — path? sig label? field names?") had a decisive answer: **none of them** — the trigger is the reserved `pred/totalOrder` keyword itself (context-sensitive grammar token → Kodkod's native TotalOrdering predicate; probe matrix §10.1b rows 1–7: renaming module/file/sig/fields keeps count 1, the keywordless equivalent order counts n!), plus the separately-pinned root-module path where a parametric header's `[exactly X]` forces the materialized param sig exact at header level (`CompModule.addModelName`). translation-ref §5 had the mechanism right all along; mt-035's implementation had substituted a module-identity proxy — LESSONS entry filed (proxy-of-a-pinned-mechanism). Implementation (probe → sonnet, impl → opus ×3 review rounds, codex adversarial review, tech-lead adjudication): `resolve_ordering` re-keyed structurally on the shared `BuiltinCall::TotalOrder` choice (top-level conjunct of a `one` sig's own appended fact over its own fields), `root_exact_sigs` → `additional_exact` (general root-`exactly` propagation), `pin_ordering` `lower == upper` gate + release-safe bounds-shape guard; LEDGER-004 amended (trigger corrected, Option-B tech-lead approval, no fork); documented conservative corners in §10.1b/LIMITATIONS (module-fact qualified spelling under-matched; non-exact elem unprobed; subset-typed fields pin on both sides — jar row 8 count 1, which adjudicated codex's unsound-rebind HIGH as a false positive after the impl delegate's evidence-driven pushback). 7 `mt041_*` tests. **Full battery (final tree): stage-1 sweep hash UNCHANGED `72ad3b33…` (verdict-invisible byte-for-byte — MIGRATION reference stays valid), agree 301 (166/135) / DISAGREE 0, SB-0 net count_match 49 → 52 / COUNT_MISMATCH 3 → 0 (byte-identical ×2), SB-20 net 71/5 → 74 / 2 = the mt-055 tso pair only, 0 panics / 0 self-check failures, 50 suites + fmt + clippy green.** Next: mt-050 deep-budget exit sweeps → ADR-0012 gate → owner touchpoint.) Earlier same day: (**MIGRATION EXECUTED — the MacBook Pro is a verified drop-in and is now the primary machine.** Repo: `/Users/choong/repos/chaychoong/mettle` (10-core M-series, 64 GB, macOS 26; agent memory re-keyed to the new path — future sessions run THERE; the exe.dev VM remains a synced secondary until the owner decommissions it). Executed per [MIGRATION.md](MIGRATION.md) with the as-executed log appended there: flake worked first try on `aarch64-darwin` (`flake.lock` committed `d997803`; rustc 1.95 shell + rustup 1.97 for builds, JDK 21 hermetic), bootstrap fixed for macOS bash 3.2 (`99444be`), the five first-party `.als` fixtures moved out of git-ignored `oracle/` into `crates/als-conform/fixtures/` (`26ccd1d` — the mt-006 lesson, fixture edition; found because the clean clone failed exactly where it should). **Acceptance: full gauntlet 50 suites green on ARM (incl. the 5 jar-integration facts at JDK 21); the stage-1 sweep reproduced `72ad3b33…` BYTE-FOR-BYTE across architectures — the determinism contract's first cross-arch validation; cached SB-0 net bucket-identical (49 / mt-041 ×3). Wall time: release build 11s, sweep + net well under 30 min at `--jobs 10` (was ~90 min on the 2-core VM).**) Earlier: 2026-07-22 night (**mt-055 wetdry half done; tso half re-scoped** — two parallel worktree investigations + one impl wave: **wetdry (888 vs 784 at SB-20) root-caused to the `this/` relation-name divergence** — the jar prefixes root-module relations `this/X` (pinned via `A4Solution.debugExtractKInput` dumps), mettle's were bare, so §16.3's `(arity, name)` SBP sort interleaved root-vs-opened-module relations backwards and the soft cap burned lex bits on the wrong relations (mettle's 888→784 transition at N=32→33 vs the jar's 8→9). **Fix shipped crate-wide** (tech-lead call over a local sort-key shim): root-module `qualified_name`s now carry `this/` (`compute_qualified_names`), atom labels strip it (`scope.rs` — `A$0` naming pinned unchanged), ~70 mechanical test-string migrations, skolem `$<label>_<var>` naming untouched, new jar-pinned two-module probe in `sb_conformance.rs` (1750@sb5/1652@sb20; probe files `scratchpad/probe/mt055-wetdry/`). **tso (121/96 vs 11/8): the SBP is conclusively EXCLUDED** — the jar's own `SymmetryBreaker` bytecode replayed over tuple-verified production bounds is bit-identical to mettle's; isomorphism check finds zero symmetric duplicates among mettle's 121; counts N-invariant. So mettle's raw solution space genuinely exceeds the jar's: an **upstream bounds/facts/encoding divergence, wrong-verdict-adjacent**, next step = shrink-and-diff (bead + `scratchpad/probe/mt055-tso/` harnesses). **Record correction (tech-lead error, LESSONS ×2):** the "SB-0 matches" constraint fed to both investigations was never verified — tso's SB-0 baseline is a `timeout`, wetdry's the cap sentinel; mis-inferred from mismatch-list silence, caught by the delegate's evidence-driven pushback. **Final battery: sweeps byte-identical ×2, hash UNCHANGED (`72ad3b33…` — MIGRATION stays valid), agree 301 / DISAGREE 0, SB-0 49/mt-041×3, SB-20 71 matches / 5 mismatches (wetdry joined; remainder = mt-041×3 + tso×2), 50 suites green.**) Earlier: (**mt-053 done — `univ`/`iden` are the live universe** (probe → sonnet, impl → opus, tech-lead review): the jar's `univ` = `Int ∪ String(padding) ∪ ⋃(top-level sig live populations)`, **dynamic per candidate instance** (probe matrix rows 1–9 in `scratchpad/probe/mt053/NOTES.md`; tech lead re-ran the dead-atom UNSAT + 22→68 skolem-scaling cells); `lower.rs::live_univ/live_iden` at the single `Const::Univ|Iden` seam (denote union; `iden ∩ (live→live)`; internal all-atoms uses untouched — liveness rides the membership constraints, the layering the jar itself uses); evaluator unchanged (upstream fix, self-check nets prove it); **LEDGER-011 approved** (tech lead, no-fork); new `univ_conformance.rs` (15 jar-pinned tests; the divergence probe now matches the jar at 65549/32902). **Full battery: agree 300 → 301 (166/135), DISAGREE 0, byte-identical ×2**; SB-0 net unchanged **49 / mt-041 ×3**; SB-20 **70 / 6** (one count → typed enum_budget skip; tso/wetdry mismatches survived exactly as the probes predicted — **the suspected mt-055 mechanism is disproved**, new lead = the plain-product HO decl reading, see the sharpened mt-055 bead). MIGRATION hash re-recorded `72ad3b33…`. Earlier: (**mt-042 done — machine portability complete** (parts (b–d) this chunk, sonnet delegation + tech-lead review): `scripts/bootstrap.sh` (SHA-verified jar + corpora, idempotent, `--verify`, refuses corrupt jars — fresh-clone-tested incl. the mismatch path); `rust-toolchain.toml` = 1.97.0 (CI aligned); `flake.nix` (JDK 21 + rustc 1.95 via nixpkgs `nixos-26.05` @ `fd146203` rev-pinned — **authored-but-unverified, no nix in this VM; first `nix develop` on the new box generates + commits `flake.lock`**, and MIGRATION carries a toolchain-delta caveat since no stable nixpkgs ships 1.97.x yet); `docs/MIGRATION.md` = the three-phase checklist with the cross-machine determinism gate (recorded stage-1 sweep hash `c4f7f8ca…` must reproduce byte-identically on arrival, then the cached SB-0 net's 49/mt-041×3). **The campaign (mt-054) is fully delivered; migration itself is next and needs the owner-provisioned box (8–16 cores, 32–64 GB, nix).**) Earlier: 2026-07-21 evening (**mt-054 (a)–(d) done** — the owner-directed throughput & feedback-loop campaign's tooling shipped in one opus delegation wave + tech-lead verification: **(a)** `solve-gauge --jobs N` deterministic parallel fan-out (position-ordered fold-in; full 564-command sweep sha256-identical at `-j1`/`-j2`, mt-048 numbers reproduced exactly — agree 300, DISAGREE 0; 12:09 vs 18:47 wall on the 2-core VM, tail-dominated); **(b)** cached jar count baselines — `baselines/*-count-sb{0,20}.json` (config-header-pinned, mismatch = hard error, miss = typed `skip_no_count_baseline`; captured once, ~2h40m; 4 boundary files re-captured at 900s into `*-slow-count-sb0.json` supplements, merge-by-sorted-name); the counting nets now run JVM-free: **SB-0 27:39 (was ~78 min) reproducing count_match 49 / mt-041 ×3 / jar_timeout 3 exactly; SB-20 26:20 (was ~56 min) reproducing 71 / 6 exactly**; `--live-jar` keeps the live path; `--refresh-counts` (parallel, atomic-rewrite-per-file, `--resume`) maintains the cache; **(c)** `--fail-fast` (exit 1, PARTIAL-marked), `--only` substring filters, `--from-report`/`--from-buckets` delta mode over the new `per_command` JSON; **(d)** owner-facing status monitor — long runs atomically rewrite `status/<tool>.txt` (phase, k/N, current file, recent heartbeats; `scripts/watch-status.sh`), git-ignored. Review fixes: restored the measured budget-rationale comments the delegate dropped; delegate's 9-item divergence list adjudicated (all sound; its `--resume` config guard is a strengthening). Part **(e)** — mt-042(b–d) + bigger-box migration — is the next chunk; tech-lead recommendation to the owner (2026-07-21): migrate right after mt-042(b–d), before mt-050's deep-budget exit sweeps.) Earlier same day: **mt-048 done** — symmetry breaking end-to-end: the Kodkod lex-leader SBP ported bit-exactly (translation-ref §16.1–§16.4; `encode/symmetry.rs` signature-grouping partition + soft-capped per-`(class, adjacent-pair)` lex-leq, `(arity, name)` relation order, mirror/identity filters, trivial-circuit escape), `SolveOptions::symmetry` default **20** (jar drop-in), `expect 1 ⇒ SB 0` at every jar-comparing boundary, gauge `--symmetry`/`--count-symmetry`. **Review was the story a third time:** the static source's `retainAll`/`usesInts` gating is a **dead letter on the jar's solve path** — probes Y6/uf1-SB20/fmrun pin that nothing is mention-gated and every int AND string atom (padding included) is a permanent singleton class (per-atom `s2k` singletons, A4Solution:391–400); the missing string singletons were the fm2cfs SB-20 family root cause (fixed, `usesInts` walker deleted), and a review probe surfaced the pre-existing **mt-053** (`univ`/`iden` = LIVE universe in the jar, not all-atoms — wrong-verdict candidate, zero corpus incidence, filed with full root cause). **Final gauge:** verdict net at SB-20 default **agree 300 (166 SAT / 134 UNSAT), DISAGREE 0**, byte-identical ×2, 0 panics/self-check; SB-0 counting net unchanged (49 matches, mt-041 ×3 only); new **SB-20 count net: 71 matches / 6 mismatches** = mt-041 ×3 + **mt-055** (tso ×2 — `univ`-typed HO params, probe after mt-053; wetdry ×1 — unrooted). `sb_conformance.rs` = 9 jar-pinned probes. **Owner directive (2026-07-21): next chunk = mt-054 throughput & feedback-loop campaign** — parallel gauge fan-out, cached jar count baselines, fail-fast/delta modes, an owner-facing progress monitor, mt-042(b–d) then bigger-box migration if needed. Earlier: **mt-051 done** **mt-051 done** — the int-equality typing rule pinned end-to-end and implemented; the 10 eq-typing defers AND LEDGER-005's ITE/`implies`-sliver carve-out both retired, the gauge's `encode` defer bucket eliminated (14 → 0). **The pinned semantics (LEDGER-010, translation-ref §10.7e):** one-sided `Int[·]`-cast `=`/`in` is **set equality always** (never int promotion — peterson's `# priority = # pid + 1` is the union-collapse pitfall, reproduced bug-compatibly); forbid mode = cast-emptiness on overflow (jar's env-less `Int.eq` cells) + the rules-0–3 guard threaded through the compared sides' set structure (`=`/`in`/mult-tests) + the constant escape (translation-constant casts contribute no comparison guard); rule 4 pinned (ITE/`implies`-antecedent = correctly-classified rescue). **Review was the story:** the probe agent's proposed rule was refuted by the existing mt-044 pinned suite + one discriminating cell; the tech lead's own confounded `(0-2)` cells surfaced that mettle lacks the jar's `0-(max+1)` MINUS peephole → **mt-052 filed** (live divergence, zero corpus incidence); the residual mystery bottomed out via `javap` + `Int.java`'s env-less `eq`. Implementation delegated to opus (matched pair, shared `overflow_guard` predicates), six respelled test cells re-pinned against the jar at review. Gauge: agree 276 → **289, DISAGREE 0**, byte-identical ×2; workspace gauntlet green; two LESSONS filed. Earlier: **mt-047 done** — FO skolemization per §15: top-level effective-existentials mint `$<label>_<var>` skolem constants through mt-038's machinery (jar-exact naming incl. inlined-func prefixes); two review fixes — the `abstract_upper`/`skolem_bounds` seam (dependent decls under-counted, addressBook2e/3b) and the inlined-func label. Solve-gauge agree **258 → 276 (163 SAT / 113 UNSAT), DISAGREE 0**; counting net: `skip_fo_skolem` retired, count_match 27 → **48**, mismatches = the 3 filed mt-041 rows only. **Runtime-observability campaign (owner-directed):** enumeration budget re-denominated twice to what costs time — conflicts + decisions + **propagation clause-visits** (new deterministic solver counters), default 250M ⇒ slowest counting command ≈80s, 51 typed `skip_enum_budget`; gauge emits a stderr liveness heartbeat + slowest-10 table; standing rule: every long run carries a hard `timeout`, budget fixes validate on the pathological files first. Full `--count` ≈85 min end-to-end. Earlier: **mt-046 done, same day as mt-045** — `seq` end-to-end per LEDGER-008: `seq X` fields = `seq/Int -> lone X` + the **per-owner** contiguity fact (probe mt046-contig), clean-room seq-stdlib bodies differentially corrected twice (`idxOf`/`lastIdxOf` closure swap; `afterLastIdx` = smallest **unused** index — tech-lead probe round, §10.10a), counting net hardened with a deterministic cumulative enumeration budget (`--enum-budget`, typed `skip_enum_budget`) after `correctChord.als`'s unbudgeted instance solves ground it for an hour. Solve-gauge agree **257 → 258, DISAGREE 0**, `lowering` defers 83 → 43; counting net: count_match 15 → 27, mismatches still exactly the 3 filed mt-041 rows. Sweep wall-time now ~25 min, chord-dominated → mt-050. Earlier same day, **mt-045** — String support end-to-end per LEDGER-007: new `als-core::strings` referenced-literal collection (called-funcs-only walk sharing the lowerer's choice table), `"String%d"` padding + `max(N,#referenced)` expansion minted in scope/universe, `String` bound exactly + per-literal `s2k` singletons, `Str` lowers relationally, `StringUnsupported` deleted; encoder/evaluator `int_end` fix (string atoms trail the int run); `string_conformance.rs` pins probes S1–S7. Solve-gauge agreements **248 → 257, DISAGREE 0**, `scope` defer family gone (9 → agreements incl. fm2cfs's 6 formerly-wrong-UNSATs, 1 → over_budget), byte-identical ×2, HEAD-worktree before/after diff clean. Earlier same day: **mt-044** — integer fidelity end-to-end: arithmetic/shifts/`sum`/int-ITE in encoder+evaluator matched pair, jar's actual forbid-mode guard (per-variable bare-`Int` classification), `Int/next`/`Int/zero` bounds, `fun/min|max|next|prev` lowering, **LEDGER-005 approved** + **LEDGER-009 shipped**; solve-gauge agreements **243 → 248, DISAGREE 0**. The semantics campaign was the story: `div[MIN,−1]` corrected to −8 (mt-043 probe-spelling artifact), a "second guard defect" manufactured by `negate[8]`'s hidden overflow flag was found and **retracted**, shift semantics fully pinned incl. the jar's junk-bit `<<` overflow defect — three probe rounds + two mid-flight implementation corrections, all in [reference/alloy6-translation.md](reference/alloy6-translation.md) §10.7b–d. New honest defers: 10 eq-typing commands → **mt-051**, the jar-unpinned ITE/`implies` sliver.)
**Current rung:** **Rung 4 (integers, coverage & counting — "it agrees with Alloy across everything I have") — COMPLETE pending owner blessing: mt-044/045/046/047/051/048/041/050 + mt-054 done. The ADR-0012 exit-gate criteria are measured met (see header); the owner touchpoint is the current gate.** Proposed exit gate (ADR-0012 at mt-043 formalizes; owner blesses): solve-gauge 0 disagreements with the only remaining typed defers being temporal (Rung 6), jar-error parity, and an honest small capacity remainder; counting net loses its `skip_fo_skolem` and mt-041 families. Rung-3 final state, for reference: mt-037's differential solve gauge (`solve-gauge` bin, `als-conform`): **0 verdict disagreements over 392 cached jar verdicts** (187 agree / 0 disagree — baseline overlap). Full corpus sweep at the gauge's current defaults (5,000 conflicts / 4,000,000 encode-budget units / 20,000 primary-var cap): 564 commands → **agree_sat 104, agree_unsat 83, DISAGREE 0, jar_nonverdict 2**; typed defers **capacity 71, encode 14, lower:higher_order 4, lower:lowering 217, lower:temporal 22, over_budget 37, scope 10** (the new String-scope defers); **0 panics, 0 self-check failures**, deterministic. SB-0 counting net (`--count`): count_match 15, COUNT_MISMATCH 3 (all one filed family — root-module `util/ordering`, verdict-neutral, **mt-041**), skip_fo_skolem 55, skip_ho_skolem 7, skip_jar_timeout 1, skip_mettle_cap 23. **mt-037 found and fixed three wrong-verdict bugs along the way**: a non-zero `String` scope silently solved as if empty (`fm2cfs.als`, 6 wrong UNSATs), a multiplicity-marked arrow on the RHS of `in` silently stripped to a plain product (`hotel2.als[0]` wrong SAT), and swapped `min`/`max` bodies in the clean-room `util/ordering` stdlib (root-caused via a `hotel1.als` SAT-agreement-by-coincidence that the arrow fix exposed) — all three fixed with jar-pinned regression tests, and the ordering fix re-verified alloy4fun byte-identical over all 150,891 codes. Remaining defers root-caused and filed: **mt-040** (resolver recording gaps), **mt-041** (root-module ordering count divergence, verdict-neutral), Rung 4 (integer-ordering builtins, seq/String), Rung 6 (temporal). Rung 2 + ADR-0010 extension complete; owner touchpoint passed 2026-07-16 with the residual diagnostic gap filed as backlog bead **mt-027**. **We are now at the Rung-3 exit / owner touchpoint** (mt-036's human-testable build plus mt-037's clean gauge).
**Scorecard:** verdicts — corpus 167/167 = 100% both stages; alloy4fun 150,891 codes → **0 jar-accepts/mettle-rejects**, **99.79% agreement**, 314 over-accepts. **Solve (post-mt-050, gauge defaults 10k conflicts / 4M encode / 20k var cap / 250M enum-effort, stage-1 symmetry 20, baselines now 544 cached jar verdicts incl. the slow-file supplement):** 564 commands → **agree 343 (195 SAT / 148 UNSAT), DISAGREE 0**, jar_nonverdict 16 (= correctChord, the file the jar itself cannot sweep in 7200s), 0 panics, 0 self-check failures, byte-identical ×2; **deep-budget exit sweeps (L1/L2 up to 400k conflicts / 128M encode) add DISAGREE 0 over every convertible defer** — deep coverage confirms no hidden disagreements at 32× the default budgets; remaining typed defers at defaults: capacity 116 + over_budget 56 (genuinely huge models; ~76 stay capped even at L2 depth), **lowering 7 (= the mt-040 recording tail exactly: 2 `$`-meta, 3 nested-macro, 1 unconfirmed ertms[5], 1 run-on-fun)**, temporal 22 (Rung 6), HO 4 (= jar-error parity, exactly) — the seq/String/int/eq-typing/encode/let-formula defer families are all gone. **SB-0 counting net (--count; JVM-free from cached baselines since mt-054), post-mt-055:** count_match **53**, **COUNT_MISMATCH 0**, all skips typed (enum_budget 78, mettle_cap 51, ho_skolem 9, jar_timeout 3, ordered_abstract 1); **ADR-0012 exit-gate counting criterion met outright.** **SB-20 count net (`--count-symmetry 20`), post-mt-055:** count_match **77**, **COUNT_MISMATCH 0** (mt-055 retired the tso pair, the last mismatch family at either symmetry setting); skips typed (enum_budget 63, mettle_cap 40, ho_skolem 9, jar_timeout 5, ordered_abstract 1). **Both nets are now mismatch-free at both symmetry settings.** _Caution learned at mt-055: these bucket figures go stale silently — the previously-recorded SB-0 `enum_budget 52` was wrong by 27 against a same-tree re-measure, so **diff a fresh before/after run, never this prose** (that is exactly what mt-057 item (3) exists to fix)._ Warnings (mt-023) — all 20 classes; corpus 0 missing; alloy4fun **99.80% files identical** (192 missing / 20 extra, root-caused in [reference/warning-parity.md](reference/warning-parity.md)). Speed (mt-024 `conform bench`) — mettle resolve ~4× the jar's batch total (caveat printed by the tool).
**Conformance scorecard:** Rung-1 gauge: **corpus lex, parse, AND round-trip rate 167/167** (alloytools-models + portus-63), plus mt-014's mutation fuzzer (default 4,248 mutants/~5s in CI, verified to 88,500 mutants offline) — zero panics, sane spans, round-trip holds. Rung-3 solve gauge (mt-037): **187 agree / 0 disagree over 392 cached jar verdicts** (`baselines/`, alloytools-models + the refreshed portus-63 baseline, triaged); SB-0 counting net: 15 exact count matches, 3 count-only divergences (mt-041, verdict-neutral), typed skips for the FO/HO-skolem and cap/timeout families.
**Builds:** `cargo build/fmt/clippy/test` all green workspace-wide. **Human-testable now (mt-036):** `cargo build -p mettle && ./target/debug/mettle exec <file.als>` executes every command in a model — verdicts, instances/counterexamples, `expect` checks, typed `CANNOT EXECUTE` for the honest defers (temporal/String/mt-040 gaps). Try `mettle exec corpus/alloytools-models/models/examples/tutorial/farmer.als` (solves the farmer's puzzle, 8-state instance, both expects ok). `mettle parse`/`mettle check` unchanged.

## What exists
- Repo initialized at `~/repos/mettle` (git, not yet published to a remote).
- Documentation spine: `CLAUDE.md`, `docs/` (index, STATE, ROADMAP, TASKS, ADRs 0001–0005), `LIMITATIONS.md`, `SEMANTICS_LEDGER.md`. Session routines: pickup in `CLAUDE.md` → "Start here"; close via `docs/SESSION_WRAP.md`; lessons in `docs/LESSONS.md`.
- **Binding steering rubrics:** `STYLE.md` + `PORTING_RULES.md` (numbered rules D#/I#/E#/R# citable in review).
- **Pinned conformance oracle:** Alloy **6.2.0** (ADR-0002, [reference/alloy6-reference.md](reference/alloy6-reference.md)); jar in git-ignored `oracle/`.
- **Cargo workspace (mt-004):** 8 crates on the hand-designed DAG; CI at `.github/workflows/ci.yml`.
- **Core type skeleton (mt-005, [ADR-0005](adr/0005-core-ir-type-skeleton.md)):** `als-syntax` = `Arena<I,T>`/`ArenaId`/`define_id!` + `Span`/`FileId` + the full Alloy 6 surface AST (unified `Expr`, temporal first-class, spans required); `als-core` = three-sorted relational IR (`Formula`/`RelExpr`/`IntExpr`) + `Universe`/`TupleSet`/`Bounds` (BTree-ordered, invariants asserted); `als-solve` = dependency-free `Var`/`Lit`/`Cnf`/`Assignment`/`Outcome` + `Solver` trait. Bones only — Rung 1+ fills flesh behind these shapes.
- **Conformance harness v0 (mt-006):** `als-conform` drives the jar via `crates/als-conform/shim/OracleShim.java` (`A4Options` API; symmetry/noOverflow/solver always explicit; `noOverflow=true` default per LEDGER-001), per-file JVM with timeout + scratch CWD, typed outcomes (Sat/Unsat/Timeout/Error), Net 0 expect-mining, deterministic text+JSON scorecard. Run it: `cargo build -p als-conform && ./target/debug/conform oracle` (or any `.als` dir; `--symmetry 0 --enumerate exhaustive` = ADR-0002's counting config). 5 jar-integration tests pin the known 87/1129 enumeration facts; they skip cleanly when the jar is absent (CI has no JDK).
- **Corpora vendored locally (mt-007, [reference/corpora.md](reference/corpora.md)):** `corpus/` (git-ignored pending mt-008) holds alloytools-models (94 .als, pinned to the jar's build commit), alloy4fun (Zenodo DOI 10.5281/zenodo.17390557, 186k JSON-Lines records, CC-BY-4.0), portus-63 (63 supported models + deps; licensing hot spot: 2× GPL-3.0, 6× no-license). kodkod: investigated, no `.als` content, not vendored. All retrieval commands recorded verbatim in the manifest — fully reproducible.
- **Pinned syntax contract (mt-010/011 spec, [reference/alloy6-grammar.md](reference/alloy6-grammar.md), [ADR-0007](adr/0007-rung1-lexer-parser-architecture.md)):** token set, filter rewrites F1–F4, 21-level precedence, grammar shapes — derived from the oracle build's grammar sources and jar-verified. AST extended to grammar parity in the same commit.
- **Lexer (mt-010):** `als-syntax::{token,lexer}` — raw spanned tokens, typed `LexError`, 167/167 corpus lex rate (`tests/corpus_lex.rs`, skips without `corpus/`).
- **Parser (mt-011):** `als-syntax::{cook,parser}` — F1–F4 cooking pass + recursive-descent/Pratt parser into the arena AST; typed `ParseError` with the reference's parse-time checks; 167/167 corpus parse rate (`tests/corpus_parse.rs`).
- **Pretty-printer (mt-012):** `als-syntax::print` — minimal-paren precedence-aware `Display` printer + span-free structural `dump` (round-trip witness); `als-syntax::prec` is the single binding-power table shared by parser and printer. 167/167 corpus round-trip (`tests/corpus_roundtrip.rs`: parse→print→reparse→dump-equal + idempotence); insta snapshots (first dev-dep). CLI: `mettle parse <file.als> [--ast]` (canonical source or dump to stdout).
- **Diagnostics + alloy4fun pass (mt-013):** rustc-style caret errors in `crates/mettle/src/diagnostics.rs` (CLI-only per E3); differential parse pass vs. the jar over 150,891 unique alloy4fun codes via batch `crates/als-conform/shim/ParseOnlyShim.java` — zero jar-accepts+mettle-rejects, 99.79% exact error-position match, 2 parser bugs fixed w/ regression tests, 2 narrow divergences documented in LIMITATIONS. Evidence: [reference/alloy4fun-error-pass.md](reference/alloy4fun-error-pass.md).
- **Mutation fuzzer + binder-composition resolution (mt-014):** `als-syntax::tests::fuzz_mutations` — zero-dep hand-rolled SplitMix64 PRNG, byte/token/splice mutation classes + deep-nesting/long-chain stressors, three properties (no panic, sane spans, round-trip). Found + fixed a real printer round-trip bug (post-Part-2 under-parenthesization) and documented a genuine, deliberately-out-of-scope printer/dumper recursion-depth finding (candidate follow-up bead). **Deep-nesting guard shipped** (`MAX_EXPR_DEPTH = 256`, new `ParseError::TooDeep`) — required, not optional: unguarded pathological `(`/`{` nesting SIGABRTs a debug build well within fuzz reach; verified safe to 100,000 adversarial levels. **Binder-composition rule resolved** (the mt-013 LIMITATIONS entry): ~220 jar probes mapped the exact rule (one composition hop, refreshed by bare `implies`, hard-blocked by comparisons/set-test prefixes) → `crate::prec::child_binder_budget`, shared by parser (new `ParseError::BinderNeedsParens`) and printer so the two can never drift. 167/167 corpus preserved; full workspace gauntlet green. Evidence: [reference/fuzzing.md](reference/fuzzing.md).
- **Oracle baselines (`baselines/`):** 392 per-command jar verdicts at LEDGER-001 defaults — 234 over alloytools-models (mt-006 era) + 158 over portus-63 (mt-037) — each with triage in `baselines/README.md`; the mt-037 solve gauge's comparison set.
- **Pinned resolution & type contract (mt-016, [reference/alloy6-resolution.md](reference/alloy6-resolution.md), [ADR-0008](adr/0008-rung2-resolver-architecture.md)):** the Rung-2 authority document — `resolveAll` phase pipeline, module system, type system, `ExprChoice` disambiguation, exact reject/warn taxonomy, `util/*` interfaces (§7.1–§7.11 per-member appendix; clean-room, signatures only), 48 jar probes. Key facts: the mt-020 gauge is binary ACCEPT/REJECT (warnings never fatal); resolution mode 1 only; **no `int`↔`Int` coercion** (`1+2` is the set `{1,2}`); funcs/preds overload while sigs/asserts/macros reject dups and facts don't check names; open instance identity = (filename, params); enum ordering auto-aliases to `ordering`.
- **Module graph + `open` layer (mt-017):** `als-types::{file,graph,load,loader,path,stdlib,error}` — `FileTable`/`ModuleGraph` typed-ID arenas, `ModuleLoader` (fs + injected map), §2.1 search order with prefix-cancel `computeModulePath` (jar-verified on mislocated zigbee modules), cycles, instance identity (file, args), aliases + duplicate-alias reject, private-open blocking, all-files `seen_dollar` gate, module-phase `ResolveError`s. **Corpus: 167/167 module graphs load.**
- **Clean-room stdlib (mt-015, [ADR-0006](adr/0006-licensing-posture.md)):** all 11 `util/*.als` at `crates/als-types/stdlib/util/`, written against §7.1–§7.11 only (author attested never seeing upstream text), embedded via `stdlib::MODULES` (disk shadows it, matching the jar). Body-level judgment calls (util/time macro bodies, util/relation `complete`, rank arithmetic in natural/sequence/seqrel) recorded in the mt-015 report for mt-020 differential triage — not resolve-visible, so not Rung-2 risks.
- **Resolver/typechecker core (mt-018, [ADR-0009](adr/0009-fused-resolve-pass-accept-lean.md)):** `als-types::{ty,world,warning,resolve/{mod,sigs,members,expr}}` — `resolve(&ModuleGraph) -> Result<Resolved, ResolveError>`; full §1 pipeline, `Type` union-of-products value type, fused bidirectional §4 checker, complete §5.1 variant coverage, synthetic enum/`seq`/`util/integer` opens in the loader (the jar auto-opens `util/integer` everywhere — jar-probed). **Corpus: 167/167 resolve+typecheck ACCEPT.** 37 probe-table tests. Interim accept-lean divergences → LIMITATIONS; mt-020 decides tightening.
- **Scopes → universe (mt-029):** `als-core::scope::compute_universe` — faithful `ScopeComputer` port (fixpoint, multiplicity forcing, exact atom naming/order); `ScopedUniverse`/`ScopeTable` seam. Corpus 167/167, 564 root commands, byte-identical twice. **Now includes the §1.2 scope-raise** (a sig's scope silently raised to its children's lower sum, probe B19 — found in mt-030 review).
- **Bounds builder (mt-030):** `als-core::bounds_builder::compute_bounds` — faithful `BoundsComputer` + `A4Solution`-builtin-bounds port: relation allocation (leaf/remainder/abstract-union/subset), field product bounds (`one`-sig owner strip), quantified-over-atoms size/mult/disjointness constraint formulas, `Int`/`seq/Int` exact. Emits the per-sig/field **denotation seam** (`RelExprId`) mt-031 consumes. Pinned facts in translation-ref §1.4′ + probe matrix §10.2 (B1–B19); 22 jar-pinned goldens; corpus 167/167 (~14k relations), deterministic. `util/ordering` exact pinning deferred to mt-035 (LEDGER-004, approved).
- **IR lowering + goal assembly (mt-031):** `als_types::choice` (the checker records every name/spine/call/macro resolution, keyed `(ModuleId, ExprId)`; facts/appended-facts/func-bodies/command-targets exposed; **alloy4fun gauge byte-identical** before/after — recording is non-behavioral) + `als_core::lower::lower_command` → `LoweredGoal` (provenance-labeled conjuncts): full §2 mapping tables, quantifier desugars (no skolemization, ADR-0011), pred/fun inlining, synthesized field facts (§2.5 split pinned: mt-030 = sig constraints, lower = field facts; probes L1–L14, translation-ref §10.3), `check` negation. Typed defers, never wrong verdicts: temporal (Rung 6), String (Rung 4), ordering (mt-035), nested arrows/HO macros/misc (mt-038). Corpus: 564 commands → **124 lower, 440 typed defers, 0 panics**, deterministic.
- **CDCL SAT solver (mt-032, ADR-0011 owner decision):** `als_solve::solver::CdclSolver` — zero-dep, deterministic by construction (integer `u64` VSIDS, no floats/wall-clock/hashing; Luby restarts; 1-UIP + minimization; keep-all DB). **Incremental enumeration seam for mt-033**: `add_clause` (watch-repair for already-falsified/unit blocking clauses) + `block()`; learned clauses retained soundly. Verified against a brute-force reference: verdicts, models, **enumeration counts** (the SB-0 foundation), determinism — 30k-instance release fuzz sweep green. PHP(8,7) 30ms.
- **Differential solve gauge (mt-037):** `als-conform::solve_gauge` (`solve-gauge` bin) — Stage 1 runs the full pipeline over every root command of both corpora and buckets every verdict against `baselines/*-verdict.json` (partition asserted, deterministic text+JSON report); Stage 2 (`--count`) checks SB-0 count parity with typed skips for the FO/HO-skolem, ordered-abstract-partition, and cap/timeout divergence families. Fresh jar baseline `baselines/portus-63-verdict.json` (158 commands/63 files) brings the comparison set to **392 cached jar verdicts**. Found and fixed three wrong-verdict bugs along the way: non-zero `String` scope silently solved as if empty (now a typed `StringUnsupported` defer), a multiplicity-marked arrow on the RHS of `in` silently stripped to a plain product (now routed through `arrow_value_constraint`, any other position a typed defer), and swapped `min`/`max` bodies in the clean-room `util/ordering` stdlib (two-line fix, jar-pinned regression, alloy4fun byte-identity re-verified). **Gauge: 564 commands → agree_sat 104, agree_unsat 83, DISAGREE 0, jar_nonverdict 2**, 375 typed defers by bucket, **0 panics, 0 self-check failures**; **baseline overlap 187 agree / 0 disagree over 392 jar verdicts.** One count-only divergence (root-module `util/ordering`, verdict-neutral) filed as **mt-041**. Run it: `cargo build --release -p als-conform && ./target/release/solve-gauge [--count]`.
- **Pinned Rung-4 semantics contract (mt-043, [reference/alloy6-translation.md](reference/alloy6-translation.md) §11–§16 + probes §10.7–§10.12, [ADR-0012](adr/0012-rung4-integers-strings-counting.md) Proposed):** integer arithmetic at bitwidth incl. the Milicevic/Jackson forbid-mode polarity rule (NOT flat `∧ ¬overflow` — probe I11), `Int/next`/`Int/zero` the only translation-referenced builtin Int relations (`min`/`max` = int constants), String `"String%d"` padding + `max(N,#referenced)` expansion + non-exact-scope reject, `seq` = `seq/Int -> lone X` + one contiguity fact, FO skolemization (depth-0, `$<cmdLabel>_<var>`, count effect pinned), SB=20 = lex-leader cap (verdict-neutral). Ledger: LEDGER-006/007/008 `verified` (awaiting owner approval), LEDGER-005 `proposed` (two named residuals → close at mt-044 start). Tech-lead review re-ran probes I11/S2/K4 independently — confirmed.
- **Integer fidelity end-to-end (mt-044):** `als_core::overflow_guard` (shared encoder/evaluator classifier — the jar's actual per-variable rule: universal-rescue iff the binder's own domain is literally bare `Int`/`seq/Int`; sig/comprehension-bound ∀ excludes, matching the jar's `DefCond.isInt` defect) + full arithmetic circuits in `encode/int.rs` (structural ports of Kodkod's non-restoring division and `shl`'s junk-bit overflow loop — their artifact values ARE the semantics: `div[x,0]=−sign(x)`, `div[MIN,−1]=MIN`, spurious `<<` overflow on masked-away amount bits) with the evaluator as a differential-tested matched pair; `Int/next`/`Int/zero` exact bounds; `fun/min|max|next|prev` lowering; LEDGER-009 own-module-first run/check targets. Two standing typed defers (forbid mode): the one-sided `Int[·]`-cast relational eq (mt-051) and the jar-unpinned ITE/`implies` sliver. Conformance suite `crates/als-core/tests/int_conformance.rs` (jar-pinned constants, CI-safe). Contract archaeology in translation-ref **§10.7b–d** (16×16 div/rem sweep, the Defect-B retraction, shift matrix).
- **String support (mt-045, LEDGER-007, translation-ref §13):** `als-core::strings` (referenced-literal collection — goal + facts + sig appended facts + field bounds, recursing into *called* funcs/preds only via the lowerer's own `als_types::choice` table); `scope.rs` mints the quoted literal atoms + `"String%d"` padding last in the universe (exact-scope fill, `max(N,#referenced)` expansion, non-exact reject); bounds bind `String` exactly + per-literal `s2k` singletons (`BoundsResult::string_denote`); `Str` lowers to its singleton; encoder/evaluator carry an explicit `int_end` so the int-atom run no longer assumes it ends the universe. Deterministic lexicographic literal order (jar's is HashSet-nondeterministic; symmetric, so verdict/count-neutral). `StringUnsupported` deleted. Conformance: `crates/als-core/tests/string_conformance.rs` (probes S1–S7 + solve-level, jar-free).
- **seq support (mt-046, LEDGER-008, translation-ref §14 + §10.10a):** `seq X` fields lower as `seq/Int -> lone X` (`lower.rs::seq_arrow_constraints`) + the per-owner contiguity fact (`seq_contiguity`, inside the `all this: owner` wrapper — jar-pinned per-owner); expression-position `seq` defers typed like a marked arrow. Clean-room `util/sequniv`/`seqrel`/`sequence` bodies differentially corrected (`idxOf` = first occurrence via `−.^next`; `afterLastIdx` = smallest unused index — NOT `lastIdx.next`). Counting-net enumeration is budget-bounded: `SolveOptions::enum_conflict_budget` (cumulative conflicts, deterministic) → `InstanceEnumerator::exhausted()` → typed `skip_enum_budget` in `solve-gauge --count` (`--enum-budget`, default 2M). Conformance: `crates/als-core/tests/seq_conformance.rs`.
- **FO skolemization (mt-047, translation-ref §15 + probes §10.11):** top-level effective-existential FO decls (incl. run-pred receivers/params and a `check`'s negated `all`) mint `$<label>_<var>` skolem constant relations through the shared `alloc_skolem` core; label = innermost inlined func's tail name, else the command label (jar-exact, K1/K2 + review probe); `abstract_upper` reads `skolem_bounds` too, so skolem-dependent decls skolemize (the addressBook under-count fix). Instances display witnesses; SB-0 counts match the jar (K4 = 12, NoEmpty = 561). Conformance: `crates/als-core/tests/fo_skolem_conformance.rs` (12 tests).
- **Int-equality typing & cast overflow semantics (mt-051, LEDGER-010, translation-ref §10.7e):** one-sided `Int[·]`-cast `=`/`in`/mult-tests solve with the jar's pinned semantics — set equality always; forbid mode = (A) overflowed casts denote the empty set (`empty_on_overflow` in the encoder, mirrored in the evaluator), (B) the rules-0–3 polarity guard threaded through the compared sides' set structure via shared `overflow_guard::collect_capable_casts`, (C) the constant escape via shared `overflow_guard::translation_constant` (both back ends use the same structural predicates — drift impossible by construction); rule 4 (ITE/`implies` antecedent) pinned to correctly-classified rescue, `GuardDecision::Defer` deleted. `eq_typing_conformance.rs` (14 jar-pinned test fns). Residual honest corners in LIMITATIONS: the `0-(max+1)` MINUS peephole (**mt-052**, live divergence, zero corpus incidence), nested-inside-`Card` cast flags, quant-bound casts (value semantics only), `&`/`-` nesting (source-read, unprobed).
- **Symmetry breaking (mt-048, translation-ref §16.1–§16.4):** `als-core::encode::symmetry` — coarsest-partition detection by exact-signature grouping (unconditional per-int + per-string-atom singleton classes — the jar's never-cleared `s2k`/int exact bounds; skolems excluded, detection pre-skolem) + the bit-exact lex-leader SBP in the encoder (soft cap per `(class, adjacent pair)` at relation boundaries, `(arity asc, name asc)` relation order with `$`-skolems first, mirror/identity filters, Tseitin-only — primary vars untouched, verdict-neutral by construction); `SolveOptions::symmetry` (default 20 = jar drop-in; 0 = the ADR-0002 counting regime), `expect 1 ⇒ 0` forcing in exec + both gauge stages; gauge `--symmetry N` (stage 1, default 20) / `--count-symmetry N` (stage 2 both sides, default 0). Conformance: `sb_conformance.rs`, 9 jar-pinned probes (§10.12 Y1–Y7 + string/int inertness). Known SB-20 count residual: mt-055.
- **Runtime observability + deadlines (mt-047, owner-directed):** solver carries deterministic `total_conflicts/decisions/props` counters; `SolveOptions::enum_effort_budget` bounds a whole enumeration in the denomination that tracks wall time (conflicts + decisions + propagation clause-visits) ending in a typed `skip_enum_budget`; `solve-gauge` emits a stderr liveness heartbeat (`[k/N] file`, per-command `…`/`took X.Xs`, slowest-10 table) while stdout stays the deterministic report — capture as `> report 2> progress`, never `2>&1`. Standing rule: every long run gets a hard `timeout`; budget changes validate on correctChord/ptcris/addressBook first.
- **Throughput & feedback-loop tooling (mt-054 a–d):** `solve-gauge --jobs N` (deterministic parallel fan-out via `solve_gauge/parallel.rs::parallel_fold` — position-ordered fold-in, workers never abandon fetched items, byte-identical reports at any job count); cached count baselines (`solve_gauge/count_baseline.rs` format + loader, `refresh.rs` `--refresh-counts`/`--resume` capture, stage 2 JVM-free by default, `--live-jar` opt-in, config-mismatch hard error, `skip_no_count_baseline` typed); `--fail-fast`/`--only`/`--from-report --from-buckets` delta modes over the report's new `per_command` array; `status.rs` owner-facing monitor (`status/<tool>.txt`, `scripts/watch-status.sh`). **Standing practice: prefer `--only`/delta/fail-fast iterations; full sweeps for chunk-level verification; refresh count baselines only when new coverage needs fresh jar facts.**
- **Public remote (mt-042(a)):** `origin = git@github.com:chaychoong/mettle.git`; standing workflow — work on `main`, push after each chunk.
- Toolchains in this VM: Rust stable (`~/.cargo/bin`) and OpenJDK 21.

## In flight (delegated, background)
- _None._

## Not yet started
- **Next beads** **mt-057** (feedback-loop speed — owner-directed as the immediate next chunk) then mt-056, mt-052 — see [TASKS.md](TASKS.md). _(mt-050 and mt-055 are both done; the tso half of mt-055 closed 2026-07-25 and its wetdry half closed at the `this/` naming migration.)_ **LEDGER-004 amended + LEDGER-005/006/007/008/009/010/011 all `approved` and shipped.** Toward the proposed exit gate: the counting criterion is **met outright** (SB-0 COUNT_MISMATCH 0; `skip_fo_skolem` and the mt-041 family both retired) and no forbid-mode comparison defers remain; what's left is the honest capacity remainder (mt-050 throughput; the heartbeat's slowest-command table is its worklist). Still-unscheduled backlog: **mt-052** (MINUS peephole), **mt-055** (SB-20 count residual, tso ×2 — upstream divergence, wrong-verdict-adjacent; shrink-and-diff harnesses ready), **mt-021** (printer/dumper recursion depth; needs a small ADR), **mt-027** (residual diagnostic parity; standing rule — every Rung-4 typing bead notes its effect on the 314 over-accepts; mt-041 is bounds/scope-phase only, no effect on the 314). None rung-gating.

## Next chunk (planned)
**mt-057 — the feedback-loop speed bead. This is an explicit owner directive (2026-07-25): "let's do it in the very next step after [mt-055] is done", scheduled ahead of any Rung 5/6 content work.** The full spec is in [TASKS.md](TASKS.md); in one line: items **(1) committed known-capped list**, **(2) longest-processing-time-first scheduling**, **(3) the sweep report committed as a baseline artifact so deltas are free** are the bead — with the anti-rot constraint that every artifact carries its config header + tree hash and hard-errors on mismatch, and rung gates always run unabridged. Item **(4)** (named tiers T0–T4; stop charging determinism ×2 per chunk) is free discipline to adopt immediately; item **(5)** (a first-class one-shot `.als` → mettle-count + jar-count probe tool) is opportunistic. Motivating measurement, from mt-055: stage-1 10 min, SB-0 net 14 min, SB-20 net 13 min, full battery 47 min, plus a 28 min baseline re-measure ≈ **85 min of verification for a one-file lowering fix**, with 183s + 175s of a 600s sweep spent on two `correctChord` commands that terminate in a *typed defer*.

**Still open below this:** the Rung-4 exit gate remains at the owner (see the mt-050 note below — the criteria are measured met, and mt-055 has since retired the last SB-20 count mismatches, so the gate is now strictly stronger than when it was proposed). **The Rung 5 (evaluator REPL) vs Rung 6 (temporal) sequencing is still a genuine owner scope call ([ROADMAP.md](ROADMAP.md)) — do NOT infer it from a bare "proceed".** Also available after mt-057: **mt-056** (formula-`let` skolem polarity), **mt-052** (MINUS peephole), the 7-row recording tail. Pacing contract: one chunk per "proceed", report, stop.

### Superseded plan (kept for the rationale)
**mt-050 done (2026-07-25, see header) — the Rung-4 exit gate is at the owner.** The 2026-07-25 session-close report IS the rung touchpoint: the ADR-0012 gate criteria are measured met, with one disclosure (the 7-command typed recording tail) named for the blessing; per the standing rule, owner silence = the gate stands. **The Rung 5 (evaluator REPL) vs Rung 6 (temporal) sequencing is a genuine owner scope call ([ROADMAP.md](ROADMAP.md)) — do NOT infer it from a bare "proceed".** Concretely: a bare **"proceed" = start mt-055 (tso shrink-and-diff)** — the highest-value non-gating bead (upstream divergence, wrong-verdict-adjacent; harnesses in `scratchpad/probe/mt055-tso/`; the deep sweeps compared the TransForm minimality scripts against fresh jar verdicts and ALL AGREE, so the divergence stays count-only on current evidence) — while the rung call waits. Also available: **mt-052** (MINUS peephole), the 7-row recording tail. An explicit "Rung 5" / "Rung 6" from the owner starts that rung's contract-first planning instead. Pacing contract: one chunk per "proceed", report, stop.

## Key syntax facts pinned this session (details in [reference/alloy6-grammar.md](reference/alloy6-grammar.md))
- The public grammar appendix is NOT the truth; the reference's `Alloy.lex`/`Alloy.cup`/`CompFilter` at the jar's build commit are, plus jar probes for anything ambiguous.
- `Version.experimental = true` is compiled into the pinned jar → string literals and range scopes (`for 1..4 steps`, `3..:2`) are live syntax.
- Number literals: maximal-run rule; `1_000`, `0x123`, `0b12` are syntax errors; `0x_12`, `0b1_0` are fine.
- Five token-stream rewrites sit between lexer and parser (label reorder, not/comparison + fun-op + arrow merges, minus folding, quantifier disambiguation) — spec §2.

## Recent decisions
- **ADR-0007 — Rung-1 front end (2026-07-15):** hand-written lexer + recursive-descent/Pratt parser, zero new runtime deps; F1–F4 token rewrites as a separate cooking pass; fail-fast precise errors for Rung 1 (recovery = mt-013); authority chain grammar-doc → jar → never memory. The public grammar appendix is NOT authoritative — the reference's `Alloy.lex`/`Alloy.cup`/`CompFilter` at the jar's build commit are.
- **AST parity extensions (tech-lead, with mt-011):** `SigParent::Eq`, `Para::Macro`, `ParaName` string names, `is_bound_disj`, scope ranges (`ScopeEnd`, increments, Steps/Str targets), `Expect::Other(i32)`, `UnOp::{ExactlyOf,IntOf,SumOf}`, integer `fun/` BinOps; builtin keyword-names (`Int`, `disj`, `fun/min`…) are synthesized `Name`s resolution keys on.
- **ADR-0006 — licensing (mt-008, owner-decided 2026-07-15):** mettle = **MPL-2.0** (root LICENSE, workspace manifest); `util/*.als` stdlib = **clean-room rewrite** (bead mt-015 — never copy upstream text; corpus copies are test inputs only); corpora local-only forever; PORTING_RULES legal-hygiene updated accordingly.
- **ADR-0005** — core IR type skeleton: shared arena/ID infra in `als-syntax`; unified surface `Expr` vs three-sorted IR; BTree-ordered bounds; dependency-free `als-solve`; overflow semantics live in the translator, not the types.
- **LEDGER-001 — overflow default = FORBID** (approved 2026-07-15). Harness already sets the oracle to match (`no_overflow=true` default, `--allow-overflow` to flip).
- Shim source lives **inside the crate** (`crates/als-conform/shim/`), not git-ignored `oracle/` — our own code must survive a fresh clone; only the re-downloadable jar stays ignored.

## Recent owner decisions (2026-07-16)
- **LEDGER-002 approved with an owner requirement:** warnings never flip the default verdict (matches the jar), **but mettle must catch the same issues the jar warns about** — warning parity is a conformance target (bead mt-023, incl. `mettle check --strict`); mechanism delegated to tech lead.
- **LEDGER-003 approved** conditional on a gap-closing plan — the plan is mt-022 (precise types), after which the entry is re-proposed with fresh gauge numbers.

## Open questions for the human (non-blocking; answerable with any future "proceed")
1. **Rung-4 exit gate** (genuine gate decision, per the operating contract): proposed — solve-gauge 0 disagreements, only temporal/jar-error/honest-capacity defers remaining, counting net free of the FO-skolem and mt-041 skip families. ADR-0012 (mt-043) will formalize whatever you bless; silence = the proposal stands.
2. **mt-042(b–d) (machine portability) scheduling**: bootstrap script + nix flake + migration checklist — say when.
3. **Symmetry-breaking posture** (mt-048): proposed as perf + a dedicated SB-20 verdict/count net, with SB-0 staying the counting yardstick (ADR-0002 unchanged). Written up as [ADR-0012](adr/0012-rung4-integers-strings-counting.md) decision 6; silence = it stands.

_Resolved 2026-07-18:_ Ledger approvals — the owner delegated **no-fork entry approval to the tech lead ("Option B", now a Ledger process rule)**; LEDGER-006/007/008/009 approved under it same day. Genuine forks (jar ambiguity, deliberate divergence, resource tradeoffs) still come to the owner framed as decisions.
_Resolved 2026-07-19:_ Hardware/scale plan (owner-acked): stay on the current VM through mt-044/045/046; gauge-sweep parallelization folded into mt-050's scope; a bigger machine (8–16 cores, 32–64GB) targeted for the verification-heavy exit phase; mt-042(b–d) jumps the queue before any migration. Details in the mt-050 bead.

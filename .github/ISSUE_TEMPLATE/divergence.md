---
name: Divergence from Alloy
about: mettle and the reference Alloy Analyzer disagree on a model
title: "divergence: <one line — e.g. `check` verdict differs on <model>`"
labels: divergence
---

<!-- The most valuable bug report this project gets. Before filing, skim
     LIMITATIONS.md — every *known* gap is listed there, and a typed
     `CANNOT EXECUTE` is mettle declining honestly, not disagreeing. -->

**The model** (smallest you managed — unshrunk is fine too):

```alloy
// paste the .als here, or attach it
```

**The command you ran:**

```sh
mettle exec model.als            # e.g.; include flags if any
```

**What Alloy says** (reference = Alloy 6.2.0; paste verdict/output/error):

```
```

**What mettle says:**

```
```

**Versions:** output of `mettle --version`, and your Alloy version if not 6.2.0.
